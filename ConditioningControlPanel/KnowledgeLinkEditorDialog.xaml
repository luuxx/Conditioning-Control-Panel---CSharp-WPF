<Window x:Class="ConditioningControlPanel.KnowledgeLinkEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Knowledge Link"
        Width="450" Height="280"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource DarkerBgBrush}"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- URL -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="URL" Foreground="{StaticResource PinkBrush}" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtUrl" Background="{StaticResource PanelBgBrush}" Foreground="White"
                     BorderBrush="{StaticResource PanelAccentBrush}" Padding="8" FontSize="12"/>
        </StackPanel>

        <!-- Title -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Title" Foreground="{StaticResource PinkBrush}" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtTitle" Background="{StaticResource PanelBgBrush}" Foreground="White"
                     BorderBrush="{StaticResource PanelAccentBrush}" Padding="8" FontSize="12"/>
        </StackPanel>

        <!-- Description -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Description (optional)" Foreground="{StaticResource PinkBrush}" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtDescription" Background="{StaticResource PanelBgBrush}" Foreground="White"
                     BorderBrush="{StaticResource PanelAccentBrush}" Padding="8" FontSize="12"
                     Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,10,0"
                    Background="{StaticResource PanelAccentBrush}" Foreground="White" BorderThickness="0"
                    Padding="8" Cursor="Hand" Click="BtnCancel_Click"/>
            <Button Content="Add" Width="80"
                    Background="{StaticResource PinkBrush}" Foreground="White" BorderThickness="0"
                    Padding="8" Cursor="Hand" Click="BtnAdd_Click"/>
        </StackPanel>
    </Grid>
</Window>
