<Window x:Class="ConditioningControlPanel.SessionCompleteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Session Complete"
        Width="500" Height="520"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False">

    <Border Background="{StaticResource DarkerBgBrush}" BorderBrush="{StaticResource PinkBrush}" BorderThickness="3" CornerRadius="20" Padding="30">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Card Image -->
            <Border Grid.Row="0" CornerRadius="12" Margin="0,0,0,15" HorizontalAlignment="Center"
                    Background="{StaticResource CornerGifOptionPanelBackgroundBrush}" Padding="5">
                <Image x:Name="ImgCard" Width="200" Height="140" Stretch="UniformToFill"
                       RenderOptions.BitmapScalingMode="HighQuality">
                    <Image.Clip>
                        <RectangleGeometry Rect="0,0,200,140" RadiusX="8" RadiusY="8"/>
                    </Image.Clip>
                </Image>
            </Border>
            
            <!-- Main Message -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center">
                <TextBlock x:Name="TxtMainMessage" Text="Good Girl!" 
                           Foreground="{StaticResource PinkBrush}" FontSize="36" FontWeight="Bold" 
                           HorizontalAlignment="Center"/>
                <TextBlock x:Name="TxtSubMessage" Text="Session Complete" 
                           Foreground="{StaticResource TextMutedBrush}" FontSize="16" 
                           HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
            
            <!-- Stats -->
            <Border Grid.Row="2" Background="{StaticResource CornerGifOptionPanelBackgroundBrush}" CornerRadius="12" Padding="20" Margin="0,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Session Name -->
                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock Text="Session" Foreground="{StaticResource TextMutedBrush}" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtSessionName" Text="Morning Drift" 
                                   Foreground="White" FontSize="14" FontWeight="Bold" 
                                   HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                    
                    <!-- Duration -->
                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                        <TextBlock Text="Duration" Foreground="{StaticResource TextMutedBrush}" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtDuration" Text="30:00" 
                                   Foreground="White" FontSize="14" FontWeight="Bold" 
                                   HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                    
                    <!-- XP Earned -->
                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <TextBlock Text="XP Earned" Foreground="{StaticResource TextMutedBrush}" FontSize="12" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtXP" Text="+50" 
                                   Foreground="{StaticResource SuccessBrush}" FontSize="18" FontWeight="Bold" 
                                   HorizontalAlignment="Center" Margin="0,4,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- Close Button -->
            <Button Grid.Row="3" x:Name="BtnClose" Content="💗 Continue" 
                    Background="{StaticResource PinkBrush}" Foreground="White" 
                    BorderThickness="0" Padding="30,12" FontSize="16" FontWeight="Bold"
                    Cursor="Hand" Click="BtnClose_Click"
                    HorizontalAlignment="Center">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
