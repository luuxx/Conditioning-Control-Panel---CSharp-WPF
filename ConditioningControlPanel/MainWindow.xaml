<Window x:Class="ConditioningControlPanel.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        xmlns:helpers="clr-namespace:ConditioningControlPanel.Helpers"
        xmlns:features="clr-namespace:ConditioningControlPanel.Features"
        xmlns:loc="clr-namespace:ConditioningControlPanel.Localization"
        Title="Conditioning Control Panel"
        Height="943" Width="1385"
        MinHeight="620" MinWidth="978"
        Background="{DynamicResource DarkerBgBrush}"
        WindowStartupLocation="Manual"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="CanResizeWithGrip"
        AllowDrop="True"
        Drop="Window_Drop"
        DragEnter="Window_DragEnter"
        DragLeave="Window_DragLeave"
        DragOver="Window_DragOver"
        FontFamily="Segoe UI">
    
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" 
                      ResizeBorderThickness="5"
                      GlassFrameThickness="0"
                      CornerRadius="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Note: BoolToVisibility and InverseBoolToVisibility converters are defined in
             Application.Resources (Converters.xaml) to ensure availability in all DataTemplates -->

        <!-- Custom Tooltip Style -->
        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="{StaticResource DarkerBgBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Placement" Value="Mouse"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="8" Opacity="0.5" ShadowDepth="2"/>
                            </Border.Effect>
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tab Button Style (for header navigation) — outlined with bottom indicator on active -->
        <Style x:Key="TabButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="Transparent"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource TransparentPink20Brush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Exclusives Submenu Button Style — used inside the Exclusives popup -->
        <Style x:Key="ExclusivesSubmenuButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#33FF69B4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tab Button Active Style — filled accent with bottom indicator -->
        <Style x:Key="TabButtonActive" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="{DynamicResource PinkButtonHoveredBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Small Pink Button Style (for tutorial guide buttons) -->
        <Style x:Key="SmallPinkButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="borderScale" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource PinkButtonHoveredBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentPressedBrush}"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="borderScale" Storyboard.TargetProperty="ScaleX"
                                                             To="0.97" Duration="0:0:0.08"/>
                                            <DoubleAnimation Storyboard.TargetName="borderScale" Storyboard.TargetProperty="ScaleY"
                                                             To="0.97" Duration="0:0:0.08"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="borderScale" Storyboard.TargetProperty="ScaleX"
                                                             To="1.0" Duration="0:0:0.15">
                                                <DoubleAnimation.EasingFunction>
                                                    <BackEase Amplitude="0.3" EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="borderScale" Storyboard.TargetProperty="ScaleY"
                                                             To="1.0" Duration="0:0:0.15">
                                                <DoubleAnimation.EasingFunction>
                                                    <BackEase Amplitude="0.3" EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- OutlineButton moved to Resources/Theme/Controls.xaml so feature UserControls can reach it. -->

        <!-- Shared template for expandable icon buttons -->
        <ControlTemplate x:Key="ExpandableIconTemplate" TargetType="Button">
            <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="6" Padding="8,6" MinWidth="42">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock x:Name="IconText" Text="{TemplateBinding Content}"
                               FontSize="{TemplateBinding FontSize}" VerticalAlignment="Center"/>
                    <TextBlock x:Name="LabelText" Text="{TemplateBinding Tag}"
                               FontSize="12" FontWeight="Bold" VerticalAlignment="Center"
                               Margin="0" Visibility="Collapsed" Opacity="1"/>
                </StackPanel>
            </Border>
        </ControlTemplate>

        <!-- Base style with shared behavior (animated Width handled in code-behind) -->
        <Style x:Key="ExpandableIconButtonBase" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="MinWidth" Value="38"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Template" Value="{StaticResource ExpandableIconTemplate}"/>
            <EventSetter Event="MouseEnter" Handler="ExpandableIcon_MouseEnter"/>
            <EventSetter Event="MouseLeave" Handler="ExpandableIcon_MouseLeave"/>
        </Style>

        <!-- Default (pink-outline) variant -->
        <Style x:Key="ExpandableIconButton" TargetType="Button" BasedOn="{StaticResource ExpandableIconButtonBase}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource TransparentPinkBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Active (pink filled) variant -->
        <Style x:Key="ExpandableIconButtonActive" TargetType="Button" BasedOn="{StaticResource ExpandableIconButtonBase}">
            <Setter Property="Background" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PinkButtonHoveredBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Patreon (purple) variant -->
        <Style x:Key="ExpandableIconButtonPatreon" TargetType="Button" BasedOn="{StaticResource ExpandableIconButtonBase}">
            <Setter Property="Background" Value="{DynamicResource SecondaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PatreonLightPurpleBrush}"/>
            <Setter Property="FontSize" Value="18"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource PatreonDarkPurpleBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Window Control Button Style -->
        <Style x:Key="WindowControlButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource TransparentWhiteBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Window Close Button Style -->
        <Style x:Key="WindowCloseButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Session Action Button Style (small icon buttons for Edit/Export/Delete) -->
        <Style x:Key="SessionActionButton" TargetType="Button">
            <Setter Property="Width" Value="26"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Background" Value="{DynamicResource PanelAccentBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="2,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Padding="0"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource PinkBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Session Delete Button Style (red hover) -->
        <Style x:Key="SessionDeleteButton" TargetType="Button" BasedOn="{StaticResource SessionActionButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Slider Style -->
        <Style x:Key="PinkSlider" TargetType="Slider">
            <Setter Property="Minimum" Value="0"/>
            <Setter Property="Maximum" Value="100"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border Height="3" Background="{DynamicResource PanelAccentBrush}" CornerRadius="1.5" VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="3" Background="{DynamicResource PinkBrush}" CornerRadius="1.5"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate>
                                                <Border Width="14" Height="14" CornerRadius="7"
                                                        Background="White" Cursor="Hand"
                                                        BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Slider" BasedOn="{StaticResource PinkSlider}" />

        <!-- Simple, minimal girly ScrollBar + Utilities -->
        <!-- Invisible RepeatButton for ScrollBar -->
        <Style x:Key="InvisibleRepeatButton" TargetType="RepeatButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
        </Style>

        <!-- Thumb for controlling ScrollBar -->
        <Style x:Key="SimpleCuteThumb" TargetType="Thumb">
            <Setter Property="MinWidth" Value="6"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="Bd"
                                CornerRadius="4"
                                Background="{DynamicResource PinkBrush}"
                                Opacity="0.6"
                                HorizontalAlignment="Stretch"
                                VerticalAlignment="Stretch"
                                SnapsToDevicePixels="True"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent" SnapsToDevicePixels="True">
                            <Border x:Name="TrackBg"
                                    Background="{DynamicResource PanelAccentBrush}"
                                    CornerRadius="4"
                                    Opacity="0.2"
                                    Margin="1"/>
                            <Track x:Name="PART_Track"
                                   Orientation="{TemplateBinding Orientation}"
                                   IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource InvisibleRepeatButton}" Command="ScrollBar.LineUpCommand"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource InvisibleRepeatButton}" Command="ScrollBar.LineDownCommand"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource SimpleCuteThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Height" Value="8"/>
                                <Setter TargetName="TrackBg" Property="CornerRadius" Value="4"/>
                            </Trigger>
                            <Trigger Property="Orientation" Value="Vertical">
                                <Setter Property="Width" Value="8"/>
                                <Setter TargetName="TrackBg" Property="CornerRadius" Value="4"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="TrackBg" Property="Opacity" Value="0.1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom TreeView style with bigger scrollbars -->
        <Style TargetType="TreeView">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeView">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Focusable="False"
                                          CanContentScroll="False"
                                          Padding="{TemplateBinding Padding}"
                                          HorizontalScrollBarVisibility="Auto"
                                          VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Section Panel Style -->
        <Style x:Key="SectionPanel" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource ElevatedSurfaceBrush}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}"/>
        </Style>

        <!-- TextBox Focus Glow Style -->
        <Style x:Key="FocusGlowTextBox" TargetType="TextBox">
            <Setter Property="CaretBrush" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="8" ShadowDepth="0" Opacity="0"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <EventTrigger RoutedEvent="GotKeyboardFocus">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity"
                                             To="0.5" Duration="0:0:0.25"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="LostKeyboardFocus">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Effect.Opacity"
                                             To="0" Duration="0:0:0.3"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Style.Triggers>
        </Style>

        <!-- Section Header Style -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PinkSoftBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- Label Style -->
        <Style x:Key="SettingLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Value Label Style -->
        <Style x:Key="ValueLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        
        <!-- Achievement Tile Style -->
        <Style x:Key="AchievementTile" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceBgBrush}"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="MinHeight" Value="100"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>
        
        <!-- Feature Image Style -->
        <Style x:Key="FeatureImageBorder" TargetType="Border">
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="180"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Background" Value="{StaticResource DarkerBgBrush}"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Rounded Image Rectangle Style -->
        <Style x:Key="RoundedFeatureImage" TargetType="Rectangle">
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="180"/>
            <Setter Property="RadiusX" Value="15"/>
            <Setter Property="RadiusY" Value="15"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
        </Style>

        <!-- Help Button Style - Small circular (?) icon for section help -->
        <Style x:Key="HelpButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="18"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PanelAccentBrush}"/>
            <Setter Property="Cursor" Value="Help"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="ToolTipService.InitialShowDelay" Value="100"/>
            <Setter Property="ToolTipService.ShowDuration" Value="60000"/>
            <Setter Property="ToolTipService.BetweenShowDelay" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="9"
                                Width="18" Height="18">
                            <TextBlock Text="?"
                                       FontSize="11"
                                       FontWeight="SemiBold"
                                       Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Margin="0,-1,0,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource PinkBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource TransparentPink20Brush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Enhanced Help Tooltip Style -->
        <Style x:Key="HelpTooltipStyle" TargetType="ToolTip">
            <Setter Property="Background" Value="#F0252542"/>
            <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="MaxWidth" Value="380"/>
            <Setter Property="Placement" Value="Right"/>
            <Setter Property="HorizontalOffset" Value="10"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <Border.Effect>
                                <DropShadowEffect Color="#FFFF69B4" BlurRadius="10" ShadowDepth="0" Opacity="0.15"/>
                            </Border.Effect>
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Animated Gradient Panel Style with Particles (using ContentControl) -->
        <Style x:Key="AnimatedGradientPanel" TargetType="ContentControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <Border x:Name="OuterBorder" CornerRadius="15"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                ClipToBounds="True">
                            <Grid>
                                <!-- Animated Gradient Background -->
                                <Rectangle RadiusX="15" RadiusY="15">
                                    <Rectangle.Fill>
                                        <LinearGradientBrush x:Name="GradientFill" StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop x:Name="Stop1" Color="{DynamicResource SurfaceBg}" Offset="0"/>
                                            <GradientStop x:Name="Stop2" Color="{DynamicResource AccentMidGradient}" Offset="0.5"/>
                                            <GradientStop x:Name="Stop3" Color="{DynamicResource DarkerBg}" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Rectangle.Fill>
                                </Rectangle>

                                <!-- Radial Glow Effect (pulsing) -->
                                <Ellipse x:Name="GlowPink" Width="200" Height="200" Opacity="0.15"
                                         HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-50,-50,0,0">
                                    <Ellipse.Fill>
                                        <RadialGradientBrush>
                                            <GradientStop Color="{DynamicResource PinkColor}" Offset="0"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </RadialGradientBrush>
                                    </Ellipse.Fill>
                                </Ellipse>
                                <Ellipse x:Name="GlowPurple" Width="180" Height="180" Opacity="0.12"
                                         HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-40,-40">
                                    <Ellipse.Fill>
                                        <RadialGradientBrush>
                                            <GradientStop Color="{DynamicResource PatreonPurple}" Offset="0"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </RadialGradientBrush>
                                    </Ellipse.Fill>
                                </Ellipse>

                                <!-- Floating Particles -->
                                <Canvas IsHitTestVisible="False">
                                    <!-- Particle 1 -->
                                    <Ellipse Width="4" Height="4" Fill="{DynamicResource PinkBrush}" Opacity="0.4" Canvas.Left="20" Canvas.Top="80">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="Particle1Transform"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <!-- Particle 2 -->
                                    <Ellipse Width="3" Height="3" Fill="{DynamicResource SecondaryBrush}" Opacity="0.35" Canvas.Left="80" Canvas.Top="120">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="Particle2Transform"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <!-- Particle 3 -->
                                    <Ellipse Width="5" Height="5" Fill="{DynamicResource PinkBrush}" Opacity="0.3" Canvas.Left="150" Canvas.Top="60">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="Particle3Transform"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <!-- Particle 4 -->
                                    <Ellipse Width="3" Height="3" Fill="{DynamicResource SecondaryBrush}" Opacity="0.35" Canvas.Left="220" Canvas.Top="100">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="Particle4Transform"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <!-- Particle 5 -->
                                    <Ellipse Width="4" Height="4" Fill="{DynamicResource SecondaryBrush}" Opacity="0.3" Canvas.Left="280" Canvas.Top="40">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="Particle5Transform"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <!-- Particle 6 -->
                                    <Ellipse Width="3" Height="3" Fill="{DynamicResource PinkBrush}" Opacity="0.4" Canvas.Left="50" Canvas.Top="150">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="Particle6Transform"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                </Canvas>

                                <!-- Content -->
                                <Border Padding="20">
                                    <ContentPresenter/>
                                </Border>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsVisible" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard x:Name="PanelAnimationStoryboard">
                                        <Storyboard RepeatBehavior="Forever">
                                            <!-- Particle floating animations -->
                                            <DoubleAnimation Storyboard.TargetName="Particle1Transform" Storyboard.TargetProperty="Y"
                                                             From="0" To="-60" Duration="0:0:8" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle1Transform" Storyboard.TargetProperty="X"
                                                             From="0" To="15" Duration="0:0:6" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle2Transform" Storyboard.TargetProperty="Y"
                                                             From="0" To="-50" Duration="0:0:10" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle2Transform" Storyboard.TargetProperty="X"
                                                             From="0" To="-10" Duration="0:0:7" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle3Transform" Storyboard.TargetProperty="Y"
                                                             From="0" To="-70" Duration="0:0:12" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle3Transform" Storyboard.TargetProperty="X"
                                                             From="0" To="20" Duration="0:0:9" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle4Transform" Storyboard.TargetProperty="Y"
                                                             From="0" To="-55" Duration="0:0:9" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle4Transform" Storyboard.TargetProperty="X"
                                                             From="0" To="-15" Duration="0:0:11" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle5Transform" Storyboard.TargetProperty="Y"
                                                             From="0" To="-65" Duration="0:0:11" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle5Transform" Storyboard.TargetProperty="X"
                                                             From="0" To="12" Duration="0:0:8" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle6Transform" Storyboard.TargetProperty="Y"
                                                             From="0" To="-45" Duration="0:0:7" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle6Transform" Storyboard.TargetProperty="X"
                                                             From="0" To="-8" Duration="0:0:10" AutoReverse="True"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <StopStoryboard BeginStoryboardName="PanelAnimationStoryboard"/>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Animated Gradient Panel for Roadmap (wider, less padding) -->
        <Style x:Key="AnimatedGradientPanelWide" TargetType="ContentControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <Border x:Name="OuterBorder" CornerRadius="10"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                ClipToBounds="True">
                            <Grid>
                                <!-- Animated Gradient Background -->
                                <Rectangle RadiusX="10" RadiusY="10">
                                    <Rectangle.Fill>
                                        <LinearGradientBrush x:Name="GradientFill" StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop x:Name="Stop1" Color="{DynamicResource SurfaceBg}" Offset="0"/>
                                            <GradientStop x:Name="Stop2" Color="{DynamicResource AccentMidGradient}" Offset="0.5"/>
                                            <GradientStop x:Name="Stop3" Color="{DynamicResource DarkerBg}" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Rectangle.Fill>
                                </Rectangle>

                                <!-- Radial Glow Effect (pulsing) -->
                                <Ellipse x:Name="GlowPink" Width="250" Height="150" Opacity="0.12"
                                         HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-80,-40,0,0">
                                    <Ellipse.Fill>
                                        <RadialGradientBrush>
                                            <GradientStop Color="{DynamicResource PinkColor}" Offset="0"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </RadialGradientBrush>
                                    </Ellipse.Fill>
                                </Ellipse>
                                <Ellipse x:Name="GlowPurple" Width="200" Height="120" Opacity="0.1"
                                         HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-60,-30">
                                    <Ellipse.Fill>
                                        <RadialGradientBrush>
                                            <GradientStop Color="{DynamicResource PatreonPurple}" Offset="0"/>
                                            <GradientStop Color="Transparent" Offset="1"/>
                                        </RadialGradientBrush>
                                    </Ellipse.Fill>
                                </Ellipse>

                                <!-- Floating Particles (more spread out for wider panel) -->
                                <Canvas IsHitTestVisible="False">
                                    <Ellipse Width="4" Height="4" Fill="{DynamicResource PinkBrush}" Opacity="0.4" Canvas.Left="30" Canvas.Top="60">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="Particle1Transform"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <Ellipse Width="3" Height="3" Fill="{DynamicResource SecondaryBrush}" Opacity="0.35" Canvas.Left="120" Canvas.Top="100">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="Particle2Transform"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <Ellipse Width="5" Height="5" Fill="{DynamicResource PinkBrush}" Opacity="0.3" Canvas.Left="250" Canvas.Top="40">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="Particle3Transform"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <Ellipse Width="3" Height="3" Fill="{DynamicResource SecondaryBrush}" Opacity="0.35" Canvas.Left="380" Canvas.Top="80">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="Particle4Transform"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <Ellipse Width="4" Height="4" Fill="{DynamicResource SecondaryBrush}" Opacity="0.3" Canvas.Left="500" Canvas.Top="50">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="Particle5Transform"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <Ellipse Width="3" Height="3" Fill="{DynamicResource PinkBrush}" Opacity="0.4" Canvas.Left="180" Canvas.Top="120">
                                        <Ellipse.RenderTransform>
                                            <TranslateTransform x:Name="Particle6Transform"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                </Canvas>

                                <!-- Content -->
                                <Border Padding="10">
                                    <ContentPresenter/>
                                </Border>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsVisible" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard x:Name="WideAnimationStoryboard">
                                        <Storyboard RepeatBehavior="Forever">
                                            <!-- Particle animations -->
                                            <DoubleAnimation Storyboard.TargetName="Particle1Transform" Storyboard.TargetProperty="Y"
                                                             From="0" To="-40" Duration="0:0:8" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle1Transform" Storyboard.TargetProperty="X"
                                                             From="0" To="15" Duration="0:0:6" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle2Transform" Storyboard.TargetProperty="Y"
                                                             From="0" To="-35" Duration="0:0:10" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle2Transform" Storyboard.TargetProperty="X"
                                                             From="0" To="-10" Duration="0:0:7" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle3Transform" Storyboard.TargetProperty="Y"
                                                             From="0" To="-50" Duration="0:0:12" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle3Transform" Storyboard.TargetProperty="X"
                                                             From="0" To="20" Duration="0:0:9" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle4Transform" Storyboard.TargetProperty="Y"
                                                             From="0" To="-45" Duration="0:0:9" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle4Transform" Storyboard.TargetProperty="X"
                                                             From="0" To="-15" Duration="0:0:11" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle5Transform" Storyboard.TargetProperty="Y"
                                                             From="0" To="-55" Duration="0:0:11" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle5Transform" Storyboard.TargetProperty="X"
                                                             From="0" To="12" Duration="0:0:8" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle6Transform" Storyboard.TargetProperty="Y"
                                                             From="0" To="-30" Duration="0:0:7" AutoReverse="True"/>
                                            <DoubleAnimation Storyboard.TargetName="Particle6Transform" Storyboard.TargetProperty="X"
                                                             From="0" To="-8" Duration="0:0:10" AutoReverse="True"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <StopStoryboard BeginStoryboardName="WideAnimationStoryboard"/>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Full-Tab Animated Background (no padding, covers entire tab area) -->
        <ControlTemplate x:Key="AnimatedTabBackground" TargetType="ContentControl">
            <Border CornerRadius="12" ClipToBounds="True">
                <Grid>
                    <!-- Animated Gradient Background -->
                    <Rectangle RadiusX="12" RadiusY="12">
                        <Rectangle.Fill>
                            <LinearGradientBrush x:Name="GradientFill" StartPoint="0,0" EndPoint="1,1">
                                <GradientStop x:Name="Stop1" Color="{DynamicResource SurfaceBg}" Offset="0"/>
                                <GradientStop x:Name="Stop2" Color="{DynamicResource AccentMidGradient}" Offset="0.5"/>
                                <GradientStop x:Name="Stop3" Color="{DynamicResource DarkerBg}" Offset="1"/>
                            </LinearGradientBrush>
                        </Rectangle.Fill>
                    </Rectangle>

                    <!-- Multiple Radial Glow Effects spread across tab -->
                    <Ellipse x:Name="GlowPink1" Width="400" Height="300" Opacity="0.1"
                             HorizontalAlignment="Left" VerticalAlignment="Top" Margin="-100,-80,0,0">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="{DynamicResource PinkColor}" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>
                    <Ellipse x:Name="GlowPurple1" Width="350" Height="250" Opacity="0.08"
                             HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-80,-60">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="{DynamicResource PatreonPurple}" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>
                    <Ellipse x:Name="GlowPink2" Width="300" Height="200" Opacity="0.06"
                             HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,-50,-50,0">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="{DynamicResource PinkColor}" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>
                    <Ellipse x:Name="GlowPurple2" Width="280" Height="180" Opacity="0.07"
                             HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-60,0,0,-40">
                        <Ellipse.Fill>
                            <RadialGradientBrush>
                                <GradientStop Color="{DynamicResource PatreonPurple}" Offset="0"/>
                                <GradientStop Color="Transparent" Offset="1"/>
                            </RadialGradientBrush>
                        </Ellipse.Fill>
                    </Ellipse>

                    <!-- Floating Particles spread across entire tab -->
                    <Canvas IsHitTestVisible="False">
                        <!-- Row 1 - Top -->
                        <Ellipse Width="4" Height="4" Fill="{DynamicResource PinkBrush}" Opacity="0.4" Canvas.Left="50" Canvas.Top="40">
                            <Ellipse.RenderTransform><TranslateTransform x:Name="P1"/></Ellipse.RenderTransform>
                        </Ellipse>
                        <Ellipse Width="3" Height="3" Fill="{DynamicResource SecondaryBrush}" Opacity="0.35" Canvas.Left="200" Canvas.Top="60">
                            <Ellipse.RenderTransform><TranslateTransform x:Name="P2"/></Ellipse.RenderTransform>
                        </Ellipse>
                        <Ellipse Width="5" Height="5" Fill="{DynamicResource SecondaryBrush}" Opacity="0.3" Canvas.Left="400" Canvas.Top="30">
                            <Ellipse.RenderTransform><TranslateTransform x:Name="P3"/></Ellipse.RenderTransform>
                        </Ellipse>
                        <Ellipse Width="3" Height="3" Fill="{DynamicResource PinkBrush}" Opacity="0.35" Canvas.Left="600" Canvas.Top="50">
                            <Ellipse.RenderTransform><TranslateTransform x:Name="P4"/></Ellipse.RenderTransform>
                        </Ellipse>
                        <Ellipse Width="4" Height="4" Fill="{DynamicResource SecondaryBrush}" Opacity="0.3" Canvas.Left="800" Canvas.Top="40">
                            <Ellipse.RenderTransform><TranslateTransform x:Name="P5"/></Ellipse.RenderTransform>
                        </Ellipse>
                        <!-- Row 2 - Middle -->
                        <Ellipse Width="3" Height="3" Fill="{DynamicResource PinkBrush}" Opacity="0.4" Canvas.Left="100" Canvas.Top="150">
                            <Ellipse.RenderTransform><TranslateTransform x:Name="P6"/></Ellipse.RenderTransform>
                        </Ellipse>
                        <Ellipse Width="4" Height="4" Fill="{DynamicResource SecondaryBrush}" Opacity="0.35" Canvas.Left="300" Canvas.Top="180">
                            <Ellipse.RenderTransform><TranslateTransform x:Name="P7"/></Ellipse.RenderTransform>
                        </Ellipse>
                        <Ellipse Width="3" Height="3" Fill="{DynamicResource SecondaryBrush}" Opacity="0.3" Canvas.Left="500" Canvas.Top="160">
                            <Ellipse.RenderTransform><TranslateTransform x:Name="P8"/></Ellipse.RenderTransform>
                        </Ellipse>
                        <Ellipse Width="5" Height="5" Fill="{DynamicResource PinkBrush}" Opacity="0.35" Canvas.Left="700" Canvas.Top="140">
                            <Ellipse.RenderTransform><TranslateTransform x:Name="P9"/></Ellipse.RenderTransform>
                        </Ellipse>
                        <!-- Row 3 - Lower -->
                        <Ellipse Width="4" Height="4" Fill="{DynamicResource SecondaryBrush}" Opacity="0.4" Canvas.Left="150" Canvas.Top="280">
                            <Ellipse.RenderTransform><TranslateTransform x:Name="P10"/></Ellipse.RenderTransform>
                        </Ellipse>
                        <Ellipse Width="3" Height="3" Fill="{DynamicResource PinkBrush}" Opacity="0.3" Canvas.Left="350" Canvas.Top="300">
                            <Ellipse.RenderTransform><TranslateTransform x:Name="P11"/></Ellipse.RenderTransform>
                        </Ellipse>
                        <Ellipse Width="4" Height="4" Fill="{DynamicResource SecondaryBrush}" Opacity="0.35" Canvas.Left="550" Canvas.Top="260">
                            <Ellipse.RenderTransform><TranslateTransform x:Name="P12"/></Ellipse.RenderTransform>
                        </Ellipse>
                    </Canvas>

                    <!-- Content -->
                    <ContentPresenter/>
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <!-- Start animation when visible, stop when hidden (saves resources) -->
                <Trigger Property="IsVisible" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard x:Name="TabAnimationStoryboard">
                            <Storyboard RepeatBehavior="Forever">
                                <!-- Glow pulses -->
                                <DoubleAnimation Storyboard.TargetName="GlowPink1" Storyboard.TargetProperty="Opacity"
                                                 From="0.1" To="0.18" Duration="0:0:4" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="GlowPurple1" Storyboard.TargetProperty="Opacity"
                                                 From="0.08" To="0.15" Duration="0:0:5" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="GlowPink2" Storyboard.TargetProperty="Opacity"
                                                 From="0.06" To="0.12" Duration="0:0:6" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="GlowPurple2" Storyboard.TargetProperty="Opacity"
                                                 From="0.07" To="0.14" Duration="0:0:4.5" AutoReverse="True"/>
                                <!-- Particle animations - varied speeds and directions -->
                                <DoubleAnimation Storyboard.TargetName="P1" Storyboard.TargetProperty="Y" From="0" To="-50" Duration="0:0:9" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P1" Storyboard.TargetProperty="X" From="0" To="20" Duration="0:0:7" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P2" Storyboard.TargetProperty="Y" From="0" To="-40" Duration="0:0:11" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P2" Storyboard.TargetProperty="X" From="0" To="-15" Duration="0:0:8" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P3" Storyboard.TargetProperty="Y" From="0" To="-60" Duration="0:0:13" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P3" Storyboard.TargetProperty="X" From="0" To="25" Duration="0:0:10" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P4" Storyboard.TargetProperty="Y" From="0" To="-45" Duration="0:0:10" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P4" Storyboard.TargetProperty="X" From="0" To="-18" Duration="0:0:12" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P5" Storyboard.TargetProperty="Y" From="0" To="-55" Duration="0:0:12" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P5" Storyboard.TargetProperty="X" From="0" To="15" Duration="0:0:9" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P6" Storyboard.TargetProperty="Y" From="0" To="-35" Duration="0:0:8" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P6" Storyboard.TargetProperty="X" From="0" To="-12" Duration="0:0:11" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P7" Storyboard.TargetProperty="Y" From="0" To="-50" Duration="0:0:14" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P7" Storyboard.TargetProperty="X" From="0" To="22" Duration="0:0:10" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P8" Storyboard.TargetProperty="Y" From="0" To="-40" Duration="0:0:10" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P8" Storyboard.TargetProperty="X" From="0" To="-20" Duration="0:0:13" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P9" Storyboard.TargetProperty="Y" From="0" To="-55" Duration="0:0:11" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P9" Storyboard.TargetProperty="X" From="0" To="18" Duration="0:0:8" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P10" Storyboard.TargetProperty="Y" From="0" To="-45" Duration="0:0:9" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P10" Storyboard.TargetProperty="X" From="0" To="-16" Duration="0:0:12" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P11" Storyboard.TargetProperty="Y" From="0" To="-35" Duration="0:0:12" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P11" Storyboard.TargetProperty="X" From="0" To="14" Duration="0:0:9" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P12" Storyboard.TargetProperty="Y" From="0" To="-50" Duration="0:0:10" AutoReverse="True"/>
                                <DoubleAnimation Storyboard.TargetName="P12" Storyboard.TargetProperty="X" From="0" To="-22" Duration="0:0:14" AutoReverse="True"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <StopStoryboard BeginStoryboardName="TabAnimationStoryboard"/>
                    </Trigger.ExitActions>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- Custom ScrollBar Style for Skill Tree (Larger horizontal scrollbar) -->
        <Style x:Key="SkillTreeScrollBarStyle" TargetType="ScrollBar">
            <Setter Property="Background" Value="{DynamicResource DarkerBgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource PinkBrush}"/>
            <Style.Triggers>
                <!-- Horizontal scrollbar -->
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="20"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid>
                                    <Track x:Name="PART_Track" IsDirectionReversed="False">
                                        <Track.Thumb>
                                            <Thumb>
                                                <Thumb.Style>
                                                    <Style TargetType="Thumb">
                                                        <Setter Property="Background" Value="{DynamicResource PinkBrush}"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Thumb">
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            CornerRadius="8"
                                                                            Margin="2"/>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#FF85C4"/>
                                                            </Trigger>
                                                            <Trigger Property="IsDragging" Value="True">
                                                                <Setter Property="Background" Value="#FF4D9E"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Thumb.Style>
                                            </Thumb>
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- ScrollViewer Style for Skill Tree -->
        <Style x:Key="SkillTreeScrollViewerStyle" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Grid.Row="0"/>
                            <ScrollBar x:Name="PART_HorizontalScrollBar" Grid.Row="1"
                                      Orientation="Horizontal"
                                      Style="{StaticResource SkillTreeScrollBarStyle}"
                                      Value="{TemplateBinding HorizontalOffset}"
                                      Maximum="{TemplateBinding ScrollableWidth}"
                                      ViewportSize="{TemplateBinding ViewportWidth}"
                                      Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Default ComboBoxItem style: black text on dropdown so items are readable -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="Black"/>
        </Style>

        <!-- Dark theme ComboBox style for code-behind usage -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Focusable="False" ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{DynamicResource PanelBgBrush}" BorderBrush="#3A3A5C" BorderThickness="1" CornerRadius="3" Cursor="Hand">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="16"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" Fill="#B0B0B0" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Data="M 0 0 L 3.5 3.5 L 7 0 Z"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              Margin="6,3,18,3" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <ContentPresenter.Resources>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="White"/>
                                    </Style>
                                </ContentPresenter.Resources>
                            </ContentPresenter>
                            <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom" AllowsTransparency="True" Focusable="False"
                                   PopupAnimation="Slide">
                                <Border Background="{DynamicResource PanelBgBrush}" BorderBrush="#3A3A5C" BorderThickness="1" CornerRadius="3" Margin="0,1,0,0"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Background" Value="{DynamicResource PanelBgBrush}"/>
                        <Setter Property="Padding" Value="6,4"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource PinkBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Exclusives Section Expander Style -->
        <Style x:Key="ExclusivesSectionExpander" TargetType="Expander">
            <Setter Property="IsExpanded" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Border CornerRadius="10" Margin="0,0,0,10" BorderThickness="1" ClipToBounds="True">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{DynamicResource SurfaceBg}" Offset="0"/>
                                    <GradientStop Color="#252548" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{DynamicResource PinkColor}" Offset="0"/>
                                    <GradientStop Color="{DynamicResource PatreonPurple}" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <Border.Effect>
                                <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.15"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Header (clickable) -->
                                <ToggleButton Grid.Row="0" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                              Cursor="Hand">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="Transparent" Padding="12,10">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="3"/>
                                                        <ColumnDefinition Width="8"/>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <!-- Left accent strip -->
                                                    <Rectangle Grid.Column="0" RadiusX="1.5" RadiusY="1.5">
                                                        <Rectangle.Fill>
                                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                                <GradientStop Color="{DynamicResource PinkColor}" Offset="0"/>
                                                                <GradientStop Color="{DynamicResource PatreonPurple}" Offset="1"/>
                                                            </LinearGradientBrush>
                                                        </Rectangle.Fill>
                                                    </Rectangle>
                                                    <!-- Header content -->
                                                    <ContentPresenter Grid.Column="2" Content="{Binding Header, RelativeSource={RelativeSource AncestorType=Expander}}"
                                                                      VerticalAlignment="Center"/>
                                                    <!-- Chevron -->
                                                    <TextBlock x:Name="Chevron" Grid.Column="3" Text="▸" Foreground="{DynamicResource PinkBrush}" FontSize="14"
                                                               FontWeight="Bold" VerticalAlignment="Center" Margin="8,0,4,0"
                                                               RenderTransformOrigin="0.5,0.5">
                                                        <TextBlock.RenderTransform>
                                                            <RotateTransform x:Name="ChevronRotate" Angle="0"/>
                                                        </TextBlock.RenderTransform>
                                                    </TextBlock>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Trigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetName="ChevronRotate" Storyboard.TargetProperty="Angle"
                                                                                 To="90" Duration="0:0:0.2"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.EnterActions>
                                                    <Trigger.ExitActions>
                                                        <BeginStoryboard>
                                                            <Storyboard>
                                                                <DoubleAnimation Storyboard.TargetName="ChevronRotate" Storyboard.TargetProperty="Angle"
                                                                                 To="0" Duration="0:0:0.2"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </Trigger.ExitActions>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" TargetName="Chevron" Value="Transparent"/>
                                                    <Setter Property="Foreground" TargetName="Chevron" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>

                                <!-- Content -->
                                <ContentPresenter x:Name="ExpanderContent" Grid.Row="1" Margin="15,0,15,15"
                                                  Visibility="Collapsed"/>
                            </Grid>
                            </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpanderContent" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <!-- Root grid allows browser fullscreen overlay without reparenting WebView across HWNDs -->
    <Grid x:Name="RootGrid">
    <!-- Viewbox scales all content proportionally with window size -->
    <Viewbox Stretch="Fill">
        <!-- Design canvas at reference size - wider to fit all header buttons -->
        <Grid Width="1405" Height="901">
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Border x:Name="TitleBarBorder" Grid.Row="0" Background="{DynamicResource PinkBrush}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Icon and Title -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                    <TextBlock Text="💗" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TxtTitleBarVersion" Text="{loc:Str app_title}" Foreground="White" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Window Control Buttons -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button x:Name="BtnTitleBarBugReport" Content="🐛" Width="46" Height="36"
                            Click="BtnReportBug_Click" Style="{StaticResource WindowControlButton}"
                            ToolTip="{loc:Str btn_report_bug}" FontSize="14"/>
                    <Button x:Name="BtnMinimize" Content="─" Width="46" Height="36"
                            Click="BtnMinimize_Click" Style="{StaticResource WindowControlButton}"/>
                    <Button x:Name="BtnMaximize" Content="☐" Width="46" Height="36" 
                            Click="BtnMaximize_Click" Style="{StaticResource WindowControlButton}"/>
                    <Button x:Name="BtnClose" Content="✕" Width="46" Height="36" 
                            Click="BtnClose_Click" Style="{StaticResource WindowCloseButton}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Header Bar - Row 1 -->
        <Border Grid.Row="1" Background="{StaticResource PanelBgBrush}" Padding="15,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Title + Player Title + App Name + Version -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Mod Selector Button -->
                    <Border x:Name="ModSelectorBorder" Background="{DynamicResource PanelAccentBrush}" CornerRadius="8" Padding="8,4" Margin="0,0,10,0"
                            VerticalAlignment="Center" Cursor="Hand" MouseLeftButtonDown="ModSelector_Click"
                            ToolTip="{loc:Str tooltip_click_to_open_mod_manager}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#2A2A4A"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="ModColorDot" Width="8" Height="8" CornerRadius="4" Margin="0,0,6,0"
                                    VerticalAlignment="Center"/>
                            <TextBlock x:Name="TxtModName" Text="{loc:Str label_bambi_sleep}" FontSize="11" FontWeight="SemiBold"
                                       Foreground="White" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock Text="&#xE70D;" FontFamily="Segoe MDL2 Assets" FontSize="8"
                                       Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <TextBlock x:Name="TxtPlayerTitle" Text="{loc:Str label_basic_bimbo}" Foreground="{DynamicResource PinkBrush}"
                               FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,12,0">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <Border Background="{DynamicResource PinkBrush}" CornerRadius="12" Padding="10,4" Margin="0,0,20,0">
                        <TextBlock x:Name="TxtLevel" Text="{loc:Str label_lvl_1}" Foreground="White" FontWeight="Bold" FontSize="12"/>
                    </Border>
                    <TextBlock x:Name="TxtHeaderVersion" Text="" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Normal"
                               VerticalAlignment="Bottom" Margin="6,0,0,2"/>
                    <!-- Streak Fire Icon (unlocked by Good Girl Streak skill) -->
                    <Border x:Name="StreakFirePill" VerticalAlignment="Center"
                            Margin="10,0,0,0" Visibility="Collapsed" CornerRadius="11" Padding="7,4"
                            Background="#1E1232" BorderBrush="#5C3D6E" BorderThickness="1"
                            ToolTip="{loc:Str tooltip_daily_login_streak_good_girl_streak_skill}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <!-- Flame vector icon -->
                            <Viewbox Width="13" Height="13" VerticalAlignment="Center">
                                <Path Fill="#C47DAA"
                                      Data="M8,1 C8,1 2.5,6.5 2.5,10 C2.5,13 5,15.5 8,15.5 C11,15.5 13.5,13 13.5,10 C13.5,6.5 8,1 8,1 Z M8,13.5 C6,13.5 4.5,12 4.5,10 C4.5,8 7,5 8,3.8 C9,5 11.5,8 11.5,10 C11.5,12 10,13.5 8,13.5 Z"/>
                            </Viewbox>
                            <TextBlock x:Name="TxtStreakFireCount" Text="0" Foreground="#C9A0D0" FontSize="13"
                                       FontWeight="Bold" VerticalAlignment="Center" Margin="4,0,0,0"/>
                            <!-- Shield vector icon -->
                            <Viewbox x:Name="TxtStreakShieldIcon" Width="12" Height="12"
                                     VerticalAlignment="Center" Margin="5,0,0,0" Visibility="Collapsed"
                                     ToolTip="{loc:Str tooltip_streak_shield_available_protects_your_streak}">
                                <Path Fill="#8B7BA0"
                                      Data="M8,1 L2,4 V9.5 C2,13 5,15.5 8,17 C11,15.5 14,13 14,9.5 V4 Z M8,15 C5.8,13.8 4,11.8 4,9.5 V5.2 L8,3 L12,5.2 V9.5 C12,11.8 10.2,13.8 8,15 Z"/>
                            </Viewbox>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Row 1 Tab Buttons: Settings, Presets, Progression, Assets -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnSettings" Tag="Settings" Style="{StaticResource TabButtonActive}"
                            Margin="3,0" Click="BtnSettings_Click" ToolTip="{loc:Str tooltip_dashboard}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" FontSize="12" Margin="0,0,4,0"/>
                            <TextBlock Text="{loc:Str tab_dashboard}" FontSize="11"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnPresets" Tag="Presets" Style="{StaticResource TabButton}"
                            Margin="3,0" Click="BtnPresets_Click" ToolTip="{loc:Str tooltip_presets_sessions}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📋" FontSize="12" Margin="0,0,4,0"/>
                            <TextBlock Text="{loc:Str tab_presets}" FontSize="11"/>
                        </StackPanel>
                    </Button>
                    <!-- BtnProgression removed in velvet-mosaic phase 6 — progression features
                         are now cards on the main Dashboard instead of a separate tab. -->
                    <Button x:Name="BtnQuests" Tag="Quests" Style="{StaticResource TabButton}"
                            Margin="3,0" Click="BtnQuests_Click" ToolTip="{loc:Str tooltip_daily_weekly_quests}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📜" FontSize="12" Margin="0,0,4,0"/>
                            <TextBlock Text="{loc:Str tab_quests}" FontSize="11"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnEnhancements" Tag="Enhancements" Style="{StaticResource TabButton}"
                            Margin="3,0" Click="BtnEnhancements_Click" ToolTip="{loc:Str tooltip_enhancement_tree}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="✨" FontSize="12" Margin="0,0,4,0"/>
                            <TextBlock Text="{loc:Str tab_enhancements}" FontSize="11"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnOpenAssetsTop" Tag="Assets" Style="{StaticResource TabButton}"
                            Margin="3,0" Click="BtnAssets_Click" ToolTip="{loc:Str tooltip_assets_packs}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📁" FontSize="12" Margin="0,0,4,0"/>
                            <TextBlock Text="{loc:Str tab_assets}" FontSize="11"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Right side: Latest Version Button -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <ComboBox x:Name="CmbPresets" Width="160" Margin="0,0,10,0" Visibility="Collapsed"
                              SelectionChanged="CmbPresets_SelectionChanged"
                              ToolTip="{loc:Str tooltip_select_a_preset_configuration}">
                        <ComboBox.Style>
                            <Style TargetType="ComboBox">
                                <Setter Property="Background" Value="{DynamicResource SurfaceBgBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Padding" Value="8,6"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ComboBox">
                                            <Grid>
                                                <ToggleButton x:Name="ToggleButton"
                                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                              Focusable="False" ClickMode="Press">
                                                    <ToggleButton.Template>
                                                        <ControlTemplate TargetType="ToggleButton">
                                                            <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="{DynamicResource PinkBrush}"
                                                                    BorderThickness="1" CornerRadius="4" Cursor="Hand">
                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="20"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <Path Grid.Column="1" Fill="#E0E0E0" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                          Data="M 0 0 L 4 4 L 8 0 Z"/>
                                                                </Grid>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </ToggleButton.Template>
                                                </ToggleButton>
                                                <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                  Margin="8,6,25,6" VerticalAlignment="Center" HorizontalAlignment="Left">
                                                    <ContentPresenter.Resources>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
                                                        </Style>
                                                    </ContentPresenter.Resources>
                                                </ContentPresenter>
                                                <TextBlock x:Name="Watermark" Text="{loc:Str label_preset}" Foreground="{DynamicResource TextMutedBrush}"
                                                           Margin="8,6,25,6" VerticalAlignment="Center" IsHitTestVisible="False"
                                                           Visibility="Collapsed"/>
                                                <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                                                       Placement="Bottom" AllowsTransparency="True" Focusable="False"
                                                       PopupAnimation="Slide">
                                                    <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="{DynamicResource PinkBrush}"
                                                            BorderThickness="1" CornerRadius="4" Margin="0,2,0,0"
                                                            MinWidth="{TemplateBinding ActualWidth}">
                                                        <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                                                            <ItemsPresenter/>
                                                        </ScrollViewer>
                                                    </Border>
                                                </Popup>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="SelectedItem" Value="{x:Null}">
                                                    <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ComboBox.Style>
                        <ComboBox.ItemContainerStyle>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
                                <Setter Property="Background" Value="{DynamicResource SurfaceBgBrush}"/>
                                <Setter Property="Padding" Value="8,6"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ComboBoxItem">
                                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource TransparentPinkBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource PinkBrush}"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ComboBox.ItemContainerStyle>
                    </ComboBox>
                    <!-- Language Pill -->
                    <ComboBox x:Name="CmbLanguagePill" Width="90" Margin="0,0,10,0"
                              SelectionChanged="CmbLanguagePill_SelectionChanged"
                              ToolTip="{loc:Str tooltip_change_language}">
                        <ComboBox.Style>
                            <Style TargetType="ComboBox">
                                <Setter Property="Background" Value="{DynamicResource SurfaceBgBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
                                <Setter Property="BorderBrush" Value="#3D3D60"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="Padding" Value="8,4"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ComboBox">
                                            <Grid>
                                                <ToggleButton x:Name="ToggleButton"
                                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                              Focusable="False" ClickMode="Press">
                                                    <ToggleButton.Template>
                                                        <ControlTemplate TargetType="ToggleButton">
                                                            <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#3D3D60"
                                                                    BorderThickness="1" CornerRadius="4" Cursor="Hand">
                                                                <Grid>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*"/>
                                                                        <ColumnDefinition Width="16"/>
                                                                    </Grid.ColumnDefinitions>
                                                                    <Path Grid.Column="1" Fill="#808080" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                          Data="M 0 0 L 3 3 L 6 0 Z"/>
                                                                </Grid>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </ToggleButton.Template>
                                                </ToggleButton>
                                                <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                  Margin="6,4,18,4" VerticalAlignment="Center" HorizontalAlignment="Left">
                                                    <ContentPresenter.Resources>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
                                                        </Style>
                                                    </ContentPresenter.Resources>
                                                </ContentPresenter>
                                                <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                                                       Placement="Bottom" AllowsTransparency="True" Focusable="False"
                                                       PopupAnimation="Slide">
                                                    <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#3D3D60"
                                                            BorderThickness="1" CornerRadius="4" Margin="0,2,0,0"
                                                            MinWidth="{TemplateBinding ActualWidth}">
                                                        <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                                                            <ItemsPresenter/>
                                                        </ScrollViewer>
                                                    </Border>
                                                </Popup>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ComboBox.Style>
                        <ComboBox.ItemContainerStyle>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
                                <Setter Property="Background" Value="{DynamicResource SurfaceBgBrush}"/>
                                <Setter Property="Padding" Value="8,4"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ComboBoxItem">
                                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource TransparentPinkBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource PinkBrush}"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ComboBox.ItemContainerStyle>
                    </ComboBox>
                    <Button x:Name="BtnUpdateAvailable" Content="{loc:Str btn_v5_8_3_is_out}"
                            Click="BtnUpdateAvailable_Click" ToolTip="{loc:Str tooltip_v5_8_3_signed_yours_truly}"
                            FontSize="11" FontWeight="Bold" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#3D1A3D"/>
                                <Setter Property="Foreground" Value="{DynamicResource PinkBrush}"/>
                                <Setter Property="MinWidth" Value="110"/>
                                <Setter Property="Height" Value="32"/>
                                <Setter Property="Padding" Value="12,6"/>
                                <Setter Property="Tag" Value="NoUpdate"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                                    CornerRadius="6" BorderThickness="1" BorderBrush="#3D3D60"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#2D2D50"/>
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#5D5D80"/>
                                                </Trigger>
                                                <Trigger Property="Tag" Value="UpdateAvailable">
                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource DarkPinkBrush}"/>
                                                    <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                                    <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                    <Setter Property="FontSize" Value="13"/>
                                                </Trigger>
                                                <!-- Server-controlled urgent update - bright red -->
                                                <Trigger Property="Tag" Value="UrgentUpdate">
                                                    <Setter TargetName="border" Property="Background" Value="#DC143C"/>
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#FF4444"/>
                                                    <Setter TargetName="border" Property="BorderThickness" Value="3"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                    <Setter Property="FontSize" Value="12"/>
                                                    <Setter Property="FontWeight" Value="ExtraBold"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Row 2: Banner + Second row buttons + Patreon Login -->
        <Border Grid.Row="2" Background="{StaticResource PanelBgBrush}" Padding="10,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.85*"/>
                    <ColumnDefinition Width="1.15*"/>
                </Grid.ColumnDefinitions>

                <!-- Banner -->
                <Border Grid.Column="0" Background="{DynamicResource SurfaceBgBrush}"
                        BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1"
                        CornerRadius="8" Padding="12,8" Margin="0,0,6,0">
                    <Grid>
                        <TextBlock x:Name="TxtBannerPrimary" Foreground="{DynamicResource PinkBrush}"
                                   FontSize="14" FontStyle="Italic"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   TextAlignment="Center" Opacity="1">
                            <Run Text="{loc:Str label_if_you_love_the_project_please}"/>
                            <Run Text="{loc:Str label_consider_supporting_it}" FontWeight="Bold"/>
                            <Run Text=" "/>
                            <Hyperlink NavigateUri="https://linktr.ee/CodeBambi" RequestNavigate="HandleHyperlinkClick">
                                <Run Text="https://linktr.ee/CodeBambi"/>
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock x:Name="TxtBannerSecondary" Foreground="{DynamicResource PinkBrush}"
                                   FontSize="14" FontStyle="Italic"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   TextAlignment="Center" Opacity="0" IsHitTestVisible="False"
                                   Text="{loc:Str login_welcome_back}"/>
                        <TextBlock x:Name="TxtBannerTertiary" Foreground="{DynamicResource PinkBrush}"
                                   FontSize="14" FontStyle="Italic" FontWeight="Bold"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   TextAlignment="Center" Opacity="0" IsHitTestVisible="False">
                            <Hyperlink NavigateUri="https://www.patreon.com/c/PlatinumPuppets" RequestNavigate="HandleHyperlinkClick">
                                <Run Text="{loc:Str label_a_special_thanks_to_platinum_puppets_for_prov}"/>
                            </Hyperlink>
                        </TextBlock>
                    </Grid>
                </Border>

                <!-- Right side: Achievements, Leaderboard, Companion, Assets buttons + Patreon Login + Help -->
                <Grid Grid.Column="1" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1.1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="0.7*"/>
                        <ColumnDefinition Width="1.2*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" x:Name="BtnAchievements" Tag="Achievements" Style="{StaticResource TabButton}"
                            Margin="2,0" Click="BtnAchievements_Click" ToolTip="{loc:Str tab_achievements}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🏆" FontSize="10" Margin="0,0,3,0"/>
                            <TextBlock Text="{loc:Str tab_achievements}" FontSize="10"/>
                        </StackPanel>
                    </Button>
                    <Button Grid.Column="1" x:Name="BtnLeaderboard" Tag="Leaderboard" Style="{StaticResource TabButton}"
                            Margin="2,0" Click="BtnLeaderboard_Click" ToolTip="{loc:Str tab_leaderboard}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" FontSize="10" Margin="0,0,3,0"/>
                            <TextBlock Text="{loc:Str tab_leaderboard}" FontSize="10"/>
                        </StackPanel>
                    </Button>
                    <Button Grid.Column="2" x:Name="BtnCompanion" Tag="Companion" Style="{StaticResource TabButton}"
                            Margin="2,0" Click="BtnCompanion_Click" ToolTip="{loc:Str tooltip_companion_settings}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🤖" FontSize="10" Margin="0,0,3,0"/>
                            <TextBlock Text="{loc:Str tab_companion}" FontSize="10"/>
                        </StackPanel>
                    </Button>

                    <Button Grid.Column="3" x:Name="BtnDiscordTab" Tag="Profile" Click="BtnDiscordTab_Click"
                            ToolTip="{loc:Str tab_profile}" Cursor="Hand" Height="32" Margin="2,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#5865F2"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FontSize" Value="10"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                                    CornerRadius="6" BorderThickness="2" BorderBrush="#4752C4"
                                                    Padding="7,3">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#4752C4"/>
                                                    <Setter TargetName="border" Property="BorderBrush" Value="#5865F2"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👤" FontSize="10" Margin="0,0,3,0"/>
                            <TextBlock Text="{loc:Str tab_profile}" FontSize="10"/>
                        </StackPanel>
                    </Button>
                    <Button Grid.Column="4" x:Name="BtnLab" Tag="Lab" Style="{StaticResource TabButton}"
                            Margin="2,0" Click="BtnLab_Click" ToolTip="{loc:Str tooltip_lab}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🧪" FontSize="10" Margin="0,0,3,0"/>
                            <TextBlock Text="{loc:Str tab_lab}" FontSize="10"/>
                        </StackPanel>
                    </Button>
                    <Grid Grid.Column="5">
                        <Button x:Name="BtnPatreonExclusives" Tag="Patreon" Click="BtnPatreonExclusives_Click"
                                MouseEnter="BtnPatreonExclusives_MouseEnter"
                                MouseLeave="ExclusivesMenu_MouseLeave"
                                ToolTip="{loc:Str tooltip_patreon_exclusives}" Cursor="Hand" Height="32" Margin="2,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#FF424D"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="FontSize" Value="10"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="border" Background="{TemplateBinding Background}"
                                                        CornerRadius="6" BorderThickness="2" BorderBrush="#E8363F"
                                                        Padding="7,3">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="#E8363F"/>
                                                        <Setter TargetName="border" Property="BorderBrush" Value="#FF424D"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⭐" FontSize="10" Margin="0,0,3,0"/>
                                <TextBlock Text="{loc:Str tab_exclusives}" FontSize="10"/>
                            </StackPanel>
                        </Button>
                        <Popup x:Name="ExclusivesSubmenuPopup"
                               PlacementTarget="{Binding ElementName=BtnPatreonExclusives}"
                               Placement="Bottom" StaysOpen="False" AllowsTransparency="True"
                               PopupAnimation="Slide"
                               MouseLeave="ExclusivesMenu_MouseLeave"
                               MouseEnter="ExclusivesSubmenuPopup_MouseEnter">
                            <Border Background="#1A1A2E" BorderBrush="#FF69B4" BorderThickness="2"
                                    CornerRadius="6" Padding="6" Margin="0,4,0,0" MinWidth="220">
                                <StackPanel Orientation="Vertical">
                                    <Button x:Name="BtnSubRemoteControl" Style="{StaticResource ExclusivesSubmenuButton}"
                                            Click="BtnSubRemoteControl_Click">
                                        <DockPanel LastChildFill="True">
                                            <Border DockPanel.Dock="Right" x:Name="SubBadgeRemoteControl"
                                                    Background="#FF69B4" CornerRadius="8" Padding="6,1" Margin="8,0,0,0"
                                                    VerticalAlignment="Center">
                                                <TextBlock Text="✨" FontSize="9" Foreground="White"/>
                                            </Border>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🎮" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{loc:Str tab_remote_control}" Foreground="White" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DockPanel>
                                    </Button>
                                    <Button x:Name="BtnSubBambiTakeover" Style="{StaticResource ExclusivesSubmenuButton}"
                                            Click="BtnSubBambiTakeover_Click">
                                        <DockPanel LastChildFill="True">
                                            <Border DockPanel.Dock="Right" x:Name="SubBadgeBambiTakeover"
                                                    Background="#FF69B4" CornerRadius="8" Padding="6,1" Margin="8,0,0,0"
                                                    VerticalAlignment="Center">
                                                <TextBlock Text="✨" FontSize="9" Foreground="White"/>
                                            </Border>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🤖" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                                <TextBlock x:Name="TxtSubBambiTakeover" Text="{loc:Str tab_bambi_takeover}" Foreground="White" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DockPanel>
                                    </Button>
                                    <Button x:Name="BtnSubHaptics" Style="{StaticResource ExclusivesSubmenuButton}"
                                            Click="BtnSubHaptics_Click">
                                        <DockPanel LastChildFill="True">
                                            <Border DockPanel.Dock="Right" x:Name="SubBadgeHaptics"
                                                    Background="#FF69B4" CornerRadius="8" Padding="6,1" Margin="8,0,0,0"
                                                    VerticalAlignment="Center">
                                                <TextBlock Text="✨" FontSize="9" Foreground="White"/>
                                            </Border>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="💜" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{loc:Str tab_haptics}" Foreground="White" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DockPanel>
                                    </Button>
                                    <Button x:Name="BtnSubAwareness" Style="{StaticResource ExclusivesSubmenuButton}"
                                            Click="BtnSubAwareness_Click">
                                        <DockPanel LastChildFill="True">
                                            <Border DockPanel.Dock="Right" x:Name="SubBadgeAwareness"
                                                    Background="#FF69B4" CornerRadius="8" Padding="6,1" Margin="8,0,0,0"
                                                    VerticalAlignment="Center">
                                                <TextBlock Text="✨" FontSize="9" Foreground="White"/>
                                            </Border>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="👁" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{loc:Str tab_awareness}" Foreground="White" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </DockPanel>
                                    </Button>
                                </StackPanel>
                            </Border>
                        </Popup>
                    </Grid>
                    <Button x:Name="BtnMainHelp" Grid.Column="6" Content="?" Click="BtnMainHelp_Click"
                            Width="26" Height="26" ToolTip="{loc:Str tooltip_how_to_use}" Margin="3,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{DynamicResource PinkBrush}" CornerRadius="13" Width="26" Height="26" Cursor="Hand">
                                    <TextBlock Text="?" Foreground="White" FontWeight="Bold" FontSize="16"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.85"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button x:Name="BtnMainBugReport" Grid.Column="7" Click="BtnReportBug_Click"
                            Width="26" Height="26" ToolTip="{loc:Str btn_report_bug}" Margin="3,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="#3D3D60" CornerRadius="13" Width="26" Height="26" Cursor="Hand">
                                    <TextBlock Text="🐛" FontSize="13" FontFamily="Segoe UI Emoji"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Opacity" Value="0.85"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <!-- XP Progress Bar + Stat Pills - Row 3 -->
        <Grid Grid.Row="3" Margin="15,10,15,5">
            <!-- Main content with XP bar and stat pills -->
            <Grid x:Name="XPBarContent">
                <Grid.ColumnDefinitions>
                    <!-- XP Bar section (fills remaining space) -->
                    <ColumnDefinition Width="*"/>
                    <!-- Bonuses + Stat Pills (shrinks to fit content) -->
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- LEFT SIDE: XP Bar -->
                <Grid Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="TxtLevelLabel" Grid.Column="0" Text="{loc:Str label_lvl_1_2}" Foreground="{DynamicResource PinkBrush}"
                               FontWeight="Bold" FontSize="12" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <Border Grid.Column="1" Background="{DynamicResource PanelAccentBrush}" CornerRadius="4" Height="8" Margin="0,0,10,0">
                        <Border x:Name="XPBar" Background="{DynamicResource PinkBrush}" CornerRadius="4"
                                HorizontalAlignment="Left" Width="100"/>
                    </Border>
                    <TextBlock x:Name="TxtXP" Grid.Column="2" Text="{loc:Str label_0_70_xp}" Foreground="{StaticResource TextMutedBrush}"
                               FontSize="11" VerticalAlignment="Center" Margin="0,0,15,0"/>
                </Grid>

                <!-- RIGHT SIDE: Active Bonuses + Stat Pills -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <!-- Active XP Bonus Chips (dynamically populated) -->
                    <StackPanel x:Name="XPBarBonusList" Orientation="Horizontal"/>

                    <!-- Pink Hours Stat: Total Conditioning Time (Unlocks at 5 points) -->
                    <Border x:Name="PillConditioningTime" Background="#2A2A4A" CornerRadius="10" Padding="10,4" Margin="0,0,8,0"
                            Visibility="Collapsed" ToolTip="{loc:Str tooltip_total_conditioning_time_pink_hours_skill}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⏱️" FontSize="12" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock x:Name="TxtPillConditioningTime" Text="{loc:Str label_0h_0m}" Foreground="#B0B0B0" FontSize="11" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Hive Mind Stat: Online Users (Unlocks at 60 points total) -->
                    <Border x:Name="PillOnlineUsers" Background="#2A2A4A" CornerRadius="10" Padding="10,4" Margin="0,0,8,0"
                            Visibility="Collapsed" ToolTip="{loc:Str tooltip_bimbos_online_now_hive_mind_skill}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👯‍♀️" FontSize="12" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock x:Name="TxtPillOnlineUsers" Text="0" Foreground="#00FF88" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Popular Girl Stat: Rank Percentile (Unlocks at 130 points total) -->
                    <Border x:Name="PillRankPercentile" Background="#2A2A4A" CornerRadius="10" Padding="10,4"
                            Visibility="Collapsed" ToolTip="{loc:Str tooltip_your_rank_percentile_popular_girl_skill}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="👑" FontSize="12" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <TextBlock x:Name="TxtPillRankPercentile" Text="{loc:Str label_top_0}" Foreground="#FFD700" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>

            <!-- Login overlay - shown when not logged in -->
            <Border x:Name="XPBarLoginOverlay" Background="#CC1A1A2E" CornerRadius="4" Visibility="Collapsed">
                <TextBlock Text="{loc:Str label_login_with_discord_or_patreon_to_track_your_x}"
                           Foreground="{DynamicResource PinkBrush}" FontSize="11" FontWeight="SemiBold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Opacity="0.9"/>
            </Border>
        </Grid>

        <!-- Main Content - Settings Tab -->
        <ContentControl x:Name="SettingsTab" Grid.Row="4" Margin="10,5,10,2" Template="{StaticResource AnimatedTabBackground}">
            <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="640"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ============================================================ -->
            <!-- VELVET MOSAIC DASHBOARD - 4x4 feature card grid (col 0)       -->
            <!-- ============================================================ -->
            <Grid Grid.Column="0" Grid.Row="0" x:Name="VelvetFeatureGrid" Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Row 1: Dashboard features -->
                <features:FeatureCard Grid.Row="0" Grid.Column="0" x:Name="CardFlash"
                                      Title="Flash Images"
                                      Icon="pack://application:,,,/Resources/features/flash.png"
                                      HelpSectionId="FlashImages"
                                      Click="CardFlash_Click"/>
                <features:FeatureCard Grid.Row="0" Grid.Column="1" x:Name="CardVisuals"
                                      Title="Visuals"
                                      Glyph="👁"
                                      HelpSectionId="Visuals"
                                      Click="CardVisuals_Click"/>
                <features:FeatureCard Grid.Row="0" Grid.Column="2" x:Name="CardVideo"
                                      Title="Mandatory Video"
                                      Icon="pack://application:,,,/Resources/features/mandatory_videos.png"
                                      HelpSectionId="Video"
                                      Click="CardVideo_Click"/>
                <features:FeatureCard Grid.Row="0" Grid.Column="3" x:Name="CardSubliminal"
                                      Title="Subliminals"
                                      Icon="pack://application:,,,/Resources/features/subliminal.png"
                                      HelpSectionId="Subliminals"
                                      Click="CardSubliminal_Click"/>

                <!-- Row 2: Left edge + center logo start + right edge -->
                <features:FeatureCard Grid.Row="1" Grid.Column="0" x:Name="CardSpiral"
                                      Title="Spiral Overlay"
                                      Icon="pack://application:,,,/Resources/features/spiral_overlay.png"
                                      HelpSectionId="SpiralOverlay"
                                      Click="CardSpiral_Click"/>

                <!-- Center logo (2x2) spanning rows 1-2, cols 1-2 -->
                <Border Grid.Row="1" Grid.RowSpan="2" Grid.Column="1" Grid.ColumnSpan="2"
                        Background="#01000000" Margin="6"
                        Cursor="Hand" MouseLeftButtonDown="ImgLogo_MouseLeftButtonDown"
                        ToolTip="{loc:Str tooltip_click_me_rapidly}">
                    <Border CornerRadius="12" ClipToBounds="True">
                        <Image x:Name="ImgLogo" Stretch="Uniform"
                               HorizontalAlignment="Stretch" VerticalAlignment="Center"
                               RenderOptions.BitmapScalingMode="HighQuality"/>
                    </Border>
                </Border>

                <features:FeatureCard Grid.Row="1" Grid.Column="3" x:Name="CardLockCard"
                                      Title="Lock Card"
                                      Icon="pack://application:,,,/Resources/features/Phrase_Lock.png"
                                      HelpSectionId="LockCard"
                                      Click="CardLockCard_Click"/>

                <!-- Row 3: Left edge + center logo continued + right edge -->
                <features:FeatureCard Grid.Row="2" Grid.Column="0" x:Name="CardPinkFilter"
                                      Title="Pink Filter"
                                      Icon="pack://application:,,,/Resources/features/Pink_filter.png"
                                      HelpSectionId="PinkFilter"
                                      Click="CardPinkFilter_Click"/>
                <features:FeatureCard Grid.Row="2" Grid.Column="3" x:Name="CardMindWipe"
                                      Title="Mind Wipe"
                                      Icon="pack://application:,,,/Resources/features/Mind_Wipers.png"
                                      HelpSectionId="MindWipe"
                                      Click="CardMindWipe_Click"/>

                <!-- Row 4: Bottom row -->
                <features:FeatureCard Grid.Row="3" Grid.Column="0" x:Name="CardBubblePop"
                                      Title="Bubble Pop"
                                      Icon="pack://application:,,,/Resources/features/Bubble_pop.png"
                                      HelpSectionId="BubblePop"
                                      Click="CardBubblePop_Click"/>
                <features:FeatureCard Grid.Row="3" Grid.Column="1" x:Name="CardBouncingText"
                                      Title="Bouncing Text"
                                      Icon="pack://application:,,,/Resources/features/bouncing_text.png"
                                      HelpSectionId="BouncingText"
                                      Click="CardBouncingText_Click"/>
                <features:FeatureCard Grid.Row="3" Grid.Column="2" x:Name="CardSystem"
                                      Title="System"
                                      Glyph="⚙"
                                      HelpSectionId="System"
                                      Click="CardSystem_Click"/>
                <features:FeatureCard Grid.Row="3" Grid.Column="3" x:Name="CardBubbleCount"
                                      Title="Bubble Count"
                                      Icon="pack://application:,,,/Resources/features/Bubble_count.png"
                                      HelpSectionId="BubbleCount"
                                      Click="CardBubbleCount_Click"/>
            </Grid>

            <!-- ============================================================ -->
            <!-- LEGACY DASHBOARD HOST - kept collapsed to preserve x:Names   -->
            <!-- referenced by MainWindow.xaml.cs until the full rewire is    -->
            <!-- complete. Phase 6+ will delete this block.                   -->
            <!-- ============================================================ -->
            <Grid x:Name="LegacyDashboardHost" Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="330"/>
                <ColumnDefinition Width="360"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT COLUMN -->
            <ScrollViewer Grid.Column="0" Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="5">
                <StackPanel>

                    <!-- Flash Images Section -->
                    <Border x:Name="FlashSection" CornerRadius="12" Padding="10,6" Margin="0,0,0,6"
                            Background="{DynamicResource ElevatedSurfaceBrush}"
                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <Grid Margin="0,0,0,2">
                                <TextBlock x:Name="TxtFeatureFlash" Style="{StaticResource SectionHeader}" Text="{loc:Str section_flash_images}" Margin="0"/>
                                <Button x:Name="HelpBtnFlash" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="FlashImages"/>
                            </Grid>
                            
                            <!-- Enable / Clickable / Hydra / Linked in one row -->
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,10,0"
                                            ToolTip="{loc:Str tooltip_flash_enable}">
                                    <CheckBox x:Name="ChkFlashEnabled" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                              Checked="ChkFlashEnabled_Changed" Unchecked="ChkFlashEnabled_Changed"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str btn_enable}" Margin="5,0,0,0" FontSize="11"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,10,0"
                                            ToolTip="{loc:Str tooltip_when_enabled_clicking_an_image_dismisses_it_w}">
                                    <CheckBox x:Name="ChkClickable" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                              Checked="ChkClickable_Changed" Unchecked="ChkClickable_Changed"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_click}" Margin="5,0,0,0" FontSize="11"/>
                                </StackPanel>
                            </Grid>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,6,0"
                                            ToolTip="{loc:Str tooltip_hydra_mode_clicking_an_image_spawns_two_more}">
                                    <CheckBox x:Name="ChkCorruption" Style="{StaticResource ToggleStyle}"
                                              Checked="ChkCorruption_Changed" Unchecked="ChkCorruption_Changed"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_hydra}" Margin="5,0,0,0" FontSize="11"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Orientation="Horizontal"
                                            ToolTip="{loc:Str tooltip_linked_hydra_spawns_share_the_parent_flash_ti}">
                                    <CheckBox x:Name="ChkHydraLinked" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                              Checked="ChkHydraLinked_Changed" Unchecked="ChkHydraLinked_Changed"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_linked}" Margin="5,0,0,0" FontSize="11"/>
                                </StackPanel>
                                <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="6,0,0,0"
                                            ToolTip="{loc:Str tooltip_toggle_the_pink_sparkle_glow_effect_on_flash}">
                                    <CheckBox x:Name="ChkFlashGlow" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                              Checked="ChkFlashGlow_Changed" Unchecked="ChkFlashGlow_Changed"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_glow}" Margin="5,0,0,0" FontSize="11"/>
                                </StackPanel>
                            </Grid>
                            <!-- Per Hour / Images in two columns -->
                            <Grid Margin="0,2,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" ToolTip="{loc:Str tooltip_how_many_flash_events_occur_per_hour_higher_v}">
                                    <Grid><TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_per_hour}" FontSize="11"/><TextBlock x:Name="TxtPerMin" Style="{StaticResource ValueLabel}" Text="10" HorizontalAlignment="Right"/></Grid>
                                    <Slider x:Name="SliderPerMin" Minimum="1" Maximum="180" Value="10" Margin="0,1" ValueChanged="SliderPerMin_Changed"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" ToolTip="{loc:Str tooltip_number_of_images_shown_per_flash_event_multip}">
                                    <Grid><TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_images}" FontSize="11"/><TextBlock x:Name="TxtImages" Style="{StaticResource ValueLabel}" Text="5" HorizontalAlignment="Right"/></Grid>
                                    <Slider x:Name="SliderImages" Minimum="1" Maximum="15" Value="5" Margin="0,1" ValueChanged="SliderImages_Changed"/>
                                </StackPanel>
                            </Grid>
                            
                            <!-- Max On Screen -->
                            <Grid Margin="0,2,0,0" ToolTip="{loc:Str tooltip_maximum_images_displayed_at_once_limits_scree}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_max_on_screen}" FontSize="11"/>
                                <TextBlock x:Name="TxtMaxOnScreen" Grid.Column="1" Style="{StaticResource ValueLabel}" Text="20"/>
                            </Grid>
                            <Slider x:Name="SliderMaxOnScreen" Minimum="2" Maximum="20" Value="20" Margin="0,1" ValueChanged="SliderMaxOnScreen_Changed"/>
                        </StackPanel>
                    </Border>

                    <!-- Visuals Section -->
                    <Border x:Name="VisualsSection" CornerRadius="12" Padding="10,6" Margin="0,0,0,6"
                            Background="{DynamicResource ElevatedSurfaceBrush}"
                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <Grid Margin="0,0,0,2">
                                <TextBlock Style="{StaticResource SectionHeader}" Text="{loc:Str section_visuals}" Margin="0"/>
                                <Button x:Name="HelpBtnVisuals" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="Visuals"/>
                            </Grid>
                            
                            <!-- Size / Opacity in two columns -->
                            <Grid Margin="0,2,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" ToolTip="{loc:Str tooltip_scale_of_flash_images_100_original_size_200_d}">
                                    <Grid><TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_size}" FontSize="11"/><TextBlock x:Name="TxtSize" Style="{StaticResource ValueLabel}" Text="100%" HorizontalAlignment="Right"/></Grid>
                                    <Slider x:Name="SliderSize" Minimum="50" Maximum="250" Value="100" Margin="0,1" ValueChanged="SliderSize_Changed"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" ToolTip="{loc:Str tooltip_transparency_of_flash_images_lower_more_see_t}">
                                    <Grid><TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_opacity}" FontSize="11"/><TextBlock x:Name="TxtOpacity" Style="{StaticResource ValueLabel}" Text="100%" HorizontalAlignment="Right"/></Grid>
                                    <Slider x:Name="SliderOpacity" Minimum="10" Maximum="100" Value="100" Margin="0,1" ValueChanged="SliderOpacity_Changed"/>
                                </StackPanel>
                            </Grid>
                            
                            <!-- Fade / Duration in two columns -->
                            <Grid Margin="0,2,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" ToolTip="{loc:Str tooltip_fade_in_out_animation_speed_0_instant_appear}">
                                    <Grid><TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_fade}" FontSize="11"/><TextBlock x:Name="TxtFade" Style="{StaticResource ValueLabel}" Text="50%" HorizontalAlignment="Right"/></Grid>
                                    <Slider x:Name="SliderFade" Minimum="0" Maximum="100" Value="50" Margin="0,1" ValueChanged="SliderFade_Changed"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" ToolTip="{loc:Str tooltip_how_long_images_stay_on_screen_only_used_when}">
                                    <Grid><TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_duration}" FontSize="11"/><TextBlock x:Name="TxtFlashDuration" Style="{StaticResource ValueLabel}" Text="{loc:Str setting_5s}" HorizontalAlignment="Right"/></Grid>
                                    <Slider x:Name="SliderFlashDuration" Minimum="1" Maximum="30" Value="5" Margin="0,1" ValueChanged="SliderFlashDuration_Changed"/>
                                </StackPanel>
                            </Grid>
                            
                            <!-- Link to Audio -->
                            <Border Background="#3D2D4D" CornerRadius="5" Padding="8,6" Margin="0,5,0,0"
                                    ToolTip="{loc:Str tooltip_when_enabled_flash_duration_matches_audio_len}">
                                <Grid>
                                    <CheckBox x:Name="ChkFlashAudio" IsChecked="True" Checked="ChkFlashAudio_Changed" Unchecked="ChkFlashAudio_Changed">
                                        <TextBlock Text="{loc:Str label_link_to_audio_trigger}" FontSize="12" FontWeight="SemiBold"/>
                                    </CheckBox>
                                    <TextBlock x:Name="TxtAudioWarning" Text="{loc:Str label_max_30_h}" Foreground="#FFB347" FontSize="10" 
                                               HorizontalAlignment="Right" VerticalAlignment="Center" Visibility="Collapsed"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Legacy Logo removed — velvet-mosaic grid owns the live ImgLogo. -->

                </StackPanel>
            </ScrollViewer>

            <!-- MIDDLE COLUMN -->
            <ScrollViewer Grid.Column="1" Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="5">
                <StackPanel>
                    
                    <!-- Mandatory Video Section -->
                    <Border x:Name="VideoSection" CornerRadius="12" Padding="10,7" Margin="0,0,0,6"
                            Background="{DynamicResource ElevatedSurfaceBrush}"
                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <Grid>
                                <TextBlock x:Name="TxtFeatureVideo" Style="{StaticResource SectionHeader}" Text="{loc:Str section_mandatory_video}"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button x:Name="HelpBtnVideo" Style="{StaticResource HelpButtonStyle}" Tag="Video" Margin="0,0,8,0"/>
                                    <Button x:Name="BtnTestVideo" Content="{loc:Str btn_test_2}" Style="{StaticResource OutlineButton}"
                                            Padding="10,4" Click="BtnTestVideo_Click"
                                            ToolTip="{loc:Str tooltip_play_a_random_video_now_to_test}"/>
                                </StackPanel>
                            </Grid>

                            <!-- Enable + Strict + Per Hour -->
                            <Grid Margin="0,5,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="30"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Orientation="Horizontal" ToolTip="{loc:Str tooltip_turn_mandatory_videos_on_or_off}">
                                    <CheckBox x:Name="ChkVideoEnabled" Style="{StaticResource ToggleStyle}"
                                              Checked="ChkVideoEnabled_Changed" Unchecked="ChkVideoEnabled_Changed"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str btn_enable}" Margin="6,0,15,0"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" ToolTip="{loc:Str tooltip_strict_mode_videos_cannot_be_skipped_must_wat}">
                                    <CheckBox x:Name="ChkStrictLock" Style="{StaticResource ToggleStyle}" Checked="ChkStrictLock_Changed" Unchecked="ChkStrictLock_Changed"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_strict}" Foreground="#FFB347" Margin="6,0,15,0"/>
                                </StackPanel>
                                <Slider Grid.Column="2" x:Name="SliderPerHour" Minimum="1" Maximum="20" Value="2" ValueChanged="SliderPerHour_Changed"
                                        ToolTip="{loc:Str tooltip_videos_per_hour}"/>
                                <TextBlock x:Name="TxtPerHour" Grid.Column="3" Style="{StaticResource ValueLabel}" Text="2" HorizontalAlignment="Right"/>
                            </Grid>

                            <!-- Mini-Game Sub-Section -->
                            <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="6" Padding="10,7" Margin="0,7,0,0">
                                <StackPanel>
                                    <Grid>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{loc:Str label_mini_game}" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold"/>
                                            <Button x:Name="HelpBtnMiniGame" Style="{StaticResource HelpButtonStyle}" Tag="MiniGame"/>
                                        </StackPanel>
                                        <CheckBox x:Name="ChkMiniGameEnabled" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right"
                                                  Checked="ChkMiniGameEnabled_Changed" Unchecked="ChkMiniGameEnabled_Changed"
                                                  ToolTip="{loc:Str tooltip_enable_attention_check_targets}"/>
                                    </Grid>

                                    <!-- Targets / Duration / Size -->
                                    <Grid Margin="0,5,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="0,0,6,0" ToolTip="{loc:Str tooltip_number_of_targets_to_click_per_video}">
                                            <Grid>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{loc:Str label_targets}" FontSize="11" Foreground="#A0A0B0"/>
                                                    <CheckBox x:Name="ChkRandomizeTargets" Margin="6,0,0,0" VerticalAlignment="Center"
                                                              ToolTip="{loc:Str tooltip_randomize_target_count_1_to_max}"
                                                              Checked="ChkRandomizeTargets_Changed" Unchecked="ChkRandomizeTargets_Changed">
                                                        <TextBlock Text="🎲" FontSize="9" ToolTip="{loc:Str tooltip_randomize}"/>
                                                    </CheckBox>
                                                </StackPanel>
                                                <TextBlock x:Name="TxtTargets" Text="3" FontSize="11" Foreground="{DynamicResource TextLightBrush}" HorizontalAlignment="Right"/>
                                            </Grid>
                                            <Slider x:Name="SliderTargets" Minimum="1" Maximum="10" Value="3" Margin="0,3,0,0" ValueChanged="SliderTargets_Changed"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Margin="3,0" ToolTip="{loc:Str tooltip_seconds_each_target_stays_visible}">
                                            <Grid><TextBlock Text="{loc:Str setting_duration}" FontSize="11" Foreground="#A0A0B0"/><TextBlock x:Name="TxtDuration" Text="5" FontSize="11" Foreground="{DynamicResource TextLightBrush}" HorizontalAlignment="Right"/></Grid>
                                            <Slider x:Name="SliderDuration" Minimum="1" Maximum="15" Value="5" Margin="0,3,0,0" ValueChanged="SliderDuration_Changed"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" Margin="6,0,0,0" ToolTip="{loc:Str tooltip_target_button_size_in_pixels}">
                                            <Grid><TextBlock Text="{loc:Str setting_size}" FontSize="11" Foreground="#A0A0B0"/><TextBlock x:Name="TxtTargetSize" Text="70" FontSize="11" Foreground="{DynamicResource TextLightBrush}" HorizontalAlignment="Right"/></Grid>
                                            <Slider x:Name="SliderTargetSize" Minimum="30" Maximum="150" Value="70" Margin="0,3,0,0" ValueChanged="SliderTargetSize_Changed"/>
                                        </StackPanel>
                                    </Grid>

                                    <StackPanel Orientation="Horizontal" Margin="0,7,0,0">
                                        <Button Content="{loc:Str btn_phrases}" Style="{StaticResource PinkButton}" Padding="8,5" Click="BtnManageAttention_Click"
                                                ToolTip="{loc:Str tooltip_edit_attention_check_phrases}"/>
                                        <Button Content="{loc:Str btn_style}" Style="{StaticResource OutlineButton}" Margin="8,0,0,0" Padding="8,5" Click="BtnAttentionStyle_Click"
                                                ToolTip="{loc:Str tooltip_customize_target_appearance_colors_border_fon}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Subliminals Section -->
                    <Border x:Name="SubliminalSection" CornerRadius="12" Padding="10,7" Margin="0,0,0,6"
                            Background="{DynamicResource ElevatedSurfaceBrush}"
                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <Grid>
                                <TextBlock x:Name="TxtFeatureSubliminal" Style="{StaticResource SectionHeader}" Text="{loc:Str section_subliminals_2}"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button x:Name="HelpBtnSubliminals" Style="{StaticResource HelpButtonStyle}" Tag="Subliminals" Margin="0,0,8,0"/>
                                    <Button Content="📝" Style="{StaticResource OutlineButton}" Padding="6,3" Click="BtnManageMessages_Click" Margin="0,0,4,0"
                                            ToolTip="{loc:Str tooltip_edit_subliminal_messages}"/>
                                    <Button Content="⚙" Style="{StaticResource OutlineButton}" Padding="6,3" Click="BtnSubliminalSettings_Click" Margin="0,0,8,0"
                                            ToolTip="{loc:Str tooltip_advanced_subliminal_visual_settings}"/>
                                    <CheckBox x:Name="ChkSubliminalEnabled" Style="{StaticResource ToggleStyle}"
                                              Checked="ChkSubliminalEnabled_Changed" Unchecked="ChkSubliminalEnabled_Changed"
                                              ToolTip="{loc:Str tooltip_enable_disable_subliminal_messages}"/>
                                </StackPanel>
                            </Grid>

                            <!-- Per Min / Frames / Opacity in 3 columns -->
                            <Grid Margin="0,6,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" Margin="0,0,6,0" ToolTip="{loc:Str tooltip_messages_shown_per_minute}">
                                    <Grid><TextBlock Text="{loc:Str label_per_min}" FontSize="11" Foreground="#A0A0B0"/><TextBlock x:Name="TxtSubPerMin" Text="5" FontSize="11" Foreground="{DynamicResource TextLightBrush}" HorizontalAlignment="Right"/></Grid>
                                    <Slider x:Name="SliderSubPerMin" Minimum="1" Maximum="30" Value="5" Margin="0,3,0,0" ValueChanged="SliderSubPerMin_Changed"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Margin="3,0" ToolTip="{loc:Str tooltip_display_duration_in_frames_lower_faster_flash}">
                                    <Grid><TextBlock Text="{loc:Str label_frames}" FontSize="11" Foreground="#A0A0B0"/><TextBlock x:Name="TxtFrames" Text="2" FontSize="11" Foreground="{DynamicResource TextLightBrush}" HorizontalAlignment="Right"/></Grid>
                                    <Slider x:Name="SliderFrames" Minimum="1" Maximum="10" Value="2" Margin="0,3,0,0" ValueChanged="SliderFrames_Changed"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" Margin="6,0,0,0" ToolTip="{loc:Str tooltip_text_visibility_lower_more_subtle}">
                                    <Grid><TextBlock Text="{loc:Str setting_opacity}" FontSize="11" Foreground="#A0A0B0"/><TextBlock x:Name="TxtSubOpacity" Text="80%" FontSize="11" Foreground="{DynamicResource TextLightBrush}" HorizontalAlignment="Right"/></Grid>
                                    <Slider x:Name="SliderSubOpacity" Minimum="10" Maximum="100" Value="80" Margin="0,3,0,0" ValueChanged="SliderSubOpacity_Changed"/>
                                </StackPanel>
                            </Grid>

                            <!-- Audio Whispers -->
                            <Grid Margin="0,6,0,0" ToolTip="{loc:Str tooltip_play_whispered_audio_when_subliminal_messages}">
                                <StackPanel Orientation="Horizontal">
                                    <CheckBox x:Name="ChkAudioWhispers" Style="{StaticResource ToggleStyle}"
                                              Checked="ChkAudioWhispers_Changed" Unchecked="ChkAudioWhispers_Changed"/>
                                    <TextBlock x:Name="TxtFeatureWhispers" Style="{StaticResource SettingLabel}" Text="{loc:Str setting_audio_whispers}" Margin="6,0,0,0"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Slider x:Name="SliderWhisperVol" Minimum="0" Maximum="100" Value="50" Width="100" ValueChanged="SliderWhisperVol_Changed"/>
                                    <TextBlock x:Name="TxtWhisperVol" Style="{StaticResource ValueLabel}" Text="50%" Width="40"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- System Section -->
                    <Border x:Name="SystemSection" CornerRadius="12" Padding="10,7" Margin="0,0,0,6"
                            Background="{DynamicResource ElevatedSurfaceBrush}"
                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <Grid>
                                <TextBlock Style="{StaticResource SectionHeader}" Text="{loc:Str section_system}"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button x:Name="HelpBtnSystem" Style="{StaticResource HelpButtonStyle}" Tag="System" Margin="0,0,8,0"/>
                                    <Button Content="📁" Style="{StaticResource OutlineButton}"
                                            Padding="6,3" Click="BtnPickAssetsFolder_Click" Margin="0,0,4,0"
                                            ToolTip="{loc:Str tooltip_set_custom_assets_folder}"/>
                                    <Button Content="{loc:Str btn_assets}" Style="{StaticResource OutlineButton}"
                                            Padding="8,3" Click="BtnOpenAssetsFolder_Click"
                                            ToolTip="{loc:Str tooltip_open_assets_folder_images_videos_sounds}"/>
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,3"
                                            ToolTip="{loc:Str tooltip_show_content_on_all_connected_monitors_2_3_or}">
                                    <CheckBox x:Name="ChkDualMon" Style="{StaticResource ToggleStyle}"
                                              Checked="ChkDualMon_Changed" Unchecked="ChkDualMon_Changed"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_multi_mon}" Margin="6,0,0,0"/>
                                </StackPanel>
                                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,3"
                                            ToolTip="{loc:Str tooltip_launch_automatically_when_windows_starts}">
                                    <CheckBox x:Name="ChkWinStart" Style="{StaticResource ToggleStyle}" Click="ChkWinStart_Click"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_win_start}" Margin="6,0,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Margin="0,3"
                                            ToolTip="{loc:Str tooltip_play_a_mandatory_video_when_app_opens}">
                                    <CheckBox x:Name="ChkVidLaunch" Style="{StaticResource ToggleStyle}"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_vid_launch}" Margin="6,0,0,0"/>
                                </StackPanel>
                                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" Margin="0,3"
                                            ToolTip="{loc:Str tooltip_start_the_engine_automatically_on_launch}">
                                    <CheckBox x:Name="ChkAutoRun" Style="{StaticResource ToggleStyle}"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_auto_run}" Margin="6,0,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal" Margin="0,3"
                                            ToolTip="{loc:Str tooltip_start_minimized_to_system_tray}">
                                    <CheckBox x:Name="ChkStartHidden" Style="{StaticResource ToggleStyle}" Click="ChkStartHidden_Click"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_start_hidden}" Margin="6,0,0,0"/>
                                </StackPanel>
                                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,3"
                                            ToolTip="{loc:Str tooltip_danger_completely_disables_panic_key_you_won}">
                                    <CheckBox x:Name="ChkNoPanic" Style="{StaticResource ToggleStyle}" Checked="ChkNoPanic_Changed" Unchecked="ChkNoPanic_Changed"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_no_panic}" Margin="6,0,0,0" Foreground="#FF6B6B"/>
                                </StackPanel>

                                <StackPanel Grid.Row="3" Grid.Column="0" Orientation="Horizontal" Margin="0,3"
                                            ToolTip="{loc:Str tooltip_disable_all_network_features_updates_ai_leade}">
                                    <CheckBox x:Name="ChkOfflineMode" Style="{StaticResource ToggleStyle}"
                                              Checked="ChkOfflineMode_Changed" Unchecked="ChkOfflineMode_Changed"/>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_offline_mode}" Margin="6,0,0,0"/>
                                </StackPanel>
                            </Grid>

                            <!-- Startup Video Selection -->
                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0"
                                        ToolTip="{loc:Str tooltip_select_a_specific_video_to_play_on_startup_wh}">
                                <TextBlock Text="{loc:Str label_startup_video}" Foreground="#B0B0B0" FontSize="10" VerticalAlignment="Center" Width="75"/>
                                <TextBlock x:Name="TxtStartupVideo" Text="{loc:Str label_random}" Foreground="{DynamicResource TextLightBrush}" FontSize="10"
                                           VerticalAlignment="Center" Width="90" TextTrimming="CharacterEllipsis"/>
                                <Button Content="📁" Background="#40FFFFFF" Foreground="White" BorderThickness="0"
                                        Padding="5,1" Margin="4,0,0,0" Cursor="Hand" Click="BtnSelectStartupVideo_Click"
                                        ToolTip="{loc:Str tooltip_browse_for_startup_video}"/>
                                <Button Content="✕" Background="#40FF6B6B" Foreground="White" BorderThickness="0"
                                        Padding="5,1" Margin="4,0,0,0" Cursor="Hand" Click="BtnClearStartupVideo_Click"
                                        ToolTip="{loc:Str tooltip_clear_selection_use_random}"/>
                            </StackPanel>

                            <!-- Gaming Tip -->
                            <Grid Margin="0,5,0,0">
                                <TextBlock Text="{loc:Str label_use_borderless_windowed_for_overlays}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10" VerticalAlignment="Center"
                                           ToolTip="{loc:Str tooltip_exclusive_fullscreen_games_block_overlays_use}"/>
                                <Button x:Name="BtnPanicKey" Content="{loc:Str btn_escape}" Style="{StaticResource OutlineButton}"
                                        HorizontalAlignment="Right" Padding="10,4" Click="BtnPanicKey_Click" ToolTip="{loc:Str tooltip_click_to_change_panic_key}"/>
                            </Grid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
            </Grid>
            <!-- /LegacyDashboardHost -->

            <!-- RIGHT HALF (was col 2, now col 1 — Browser / Audio / Quick Links) -->
            <Grid Grid.Column="1" Grid.Row="0" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Browser Section -->
                <Border Grid.Row="0" CornerRadius="12" Padding="9" Margin="0,0,0,5"
                        Background="{DynamicResource ElevatedSurfaceBrush}"
                        BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                    <StackPanel>
                        <!-- Browser Header - single row with title, tabs, status, pop out -->
                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <TextBlock Style="{StaticResource SectionHeader}" Text="{loc:Str section_browser}" Margin="0"/>
                                <Button x:Name="HelpBtnBrowser" Style="{StaticResource HelpButtonStyle}" Tag="Browser"/>
                            </StackPanel>
                            <!-- Site Toggle - inline -->
                            <Border Grid.Column="1" Background="{DynamicResource PanelAccentBrush}" CornerRadius="10" Padding="2" Margin="10,0,0,0" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <RadioButton x:Name="RbBambiCloud" Content="{loc:Str btn_bambicloud}" GroupName="BrowserSite"
                                                 IsChecked="True" Checked="BrowserSiteToggle_Changed"
                                                 Foreground="{DynamicResource TextLightBrush}" FontSize="10" Cursor="Hand" Margin="2,1">
                                        <RadioButton.Template>
                                            <ControlTemplate TargetType="RadioButton">
                                                <Border x:Name="border" Background="Transparent" CornerRadius="8" Padding="8,3">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="{DynamicResource PinkBrush}"/>
                                                        <Setter Property="Foreground" Value="White"/>
                                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                                    </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#353565"/>
                                                </Trigger>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition Property="IsChecked" Value="True"/>
                                                        <Condition Property="IsMouseOver" Value="True"/>
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource PinkButtonHoveredBrush}"/>
                                                </MultiTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </RadioButton.Template>
                                </RadioButton>
                                <RadioButton x:Name="RbHypnoTube" Content="{loc:Str btn_hypnotube}" GroupName="BrowserSite"
                                             Checked="BrowserSiteToggle_Changed"
                                             Foreground="{DynamicResource TextLightBrush}" FontSize="10" Cursor="Hand" Margin="2,1">
                                    <RadioButton.Template>
                                        <ControlTemplate TargetType="RadioButton">
                                            <Border x:Name="border" Background="Transparent" CornerRadius="8" Padding="8,3">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource PinkBrush}"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#353565"/>
                                                </Trigger>
                                                <MultiTrigger>
                                                    <MultiTrigger.Conditions>
                                                        <Condition Property="IsChecked" Value="True"/>
                                                        <Condition Property="IsMouseOver" Value="True"/>
                                                    </MultiTrigger.Conditions>
                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource PinkButtonHoveredBrush}"/>
                                                </MultiTrigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </RadioButton.Template>
                                </RadioButton>
                                </StackPanel>
                            </Border>
                            <!-- Status indicator -->
                            <TextBlock Grid.Column="3" x:Name="TxtBrowserStatus" Text="{loc:Str label_loading}"
                                       Foreground="{DynamicResource PinkBrush}" FontSize="10"
                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <!-- Pop Out button -->
                            <Button Grid.Column="4" x:Name="BtnPopOutBrowser" Content="{loc:Str btn_pop_out}"
                                    Click="BtnPopOutBrowser_Click"
                                    ToolTip="{loc:Str tooltip_pop_out_browser_to_resizable_window}"
                                    FontSize="9" Padding="6,3" VerticalAlignment="Center">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="{DynamicResource PinkBrush}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{DynamicResource PinkButtonHoveredBrush}"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter Property="Background" Value="#E05A9E"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>

                        <!-- Browser Container -->
                        <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="6" ClipToBounds="True" Height="365">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Audio Sync Controls (shown when audio sync enabled) -->
                                <Border x:Name="AudioSyncLatencyPanel" Grid.Row="0" Background="{DynamicResource PanelBgBrush}" Padding="6,3" Visibility="Collapsed">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="8"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Delay Slider (left half) -->
                                        <Grid Grid.Column="0">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="40"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{loc:Str label_delay}" Foreground="#AAAAAA" FontSize="9" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <Slider x:Name="SliderAudioSyncLatency" Grid.Column="1" Minimum="-600" Maximum="600" Value="0"
                                                    VerticalAlignment="Center" ValueChanged="SliderAudioSyncLatency_Changed"
                                                    ToolTip="{loc:Str tooltip_adjust_haptic_timing_positive_earlier_negativ}"/>
                                            <TextBlock x:Name="TxtAudioSyncLatency" Grid.Column="2" Text="{loc:Str label_0ms}" Foreground="{DynamicResource PinkBrush}" FontSize="9"
                                                       VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                        </Grid>

                                        <!-- Intensity Slider (right half) -->
                                        <Grid Grid.Column="2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="32"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{loc:Str label_power}" Foreground="#AAAAAA" FontSize="9" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <Slider x:Name="SliderAudioSyncIntensity" Grid.Column="1" Minimum="0" Maximum="100" Value="100"
                                                    VerticalAlignment="Center" ValueChanged="SliderAudioSyncIntensity_Changed"
                                                    ToolTip="{loc:Str tooltip_live_intensity_control_reduce_if_vibration_is}"/>
                                            <TextBlock x:Name="TxtAudioSyncIntensity" Grid.Column="2" Text="100%" Foreground="{DynamicResource PinkBrush}" FontSize="9"
                                                       VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                        </Grid>
                                    </Grid>
                                </Border>

                                <Grid x:Name="BrowserContainer" Grid.Row="1">
                                    <!-- WebView2 will be added here programmatically -->
                                    <TextBlock x:Name="BrowserLoadingText"
                                               Text="{loc:Str label_click_to_load_browser}"
                                               Foreground="{DynamicResource TextMutedBrush}" FontSize="12"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"
                                               Cursor="Hand"
                                               MouseLeftButtonDown="BrowserLoadingText_Click"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Audio Section + Quick Actions -->
                <Grid Grid.Row="1" Margin="0,5,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="5"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Audio Section (left half) -->
                    <Border x:Name="AudioSection" Grid.Column="0" CornerRadius="12" Padding="9" Margin="0,0,0,6"
                            Background="{DynamicResource ElevatedSurfaceBrush}"
                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" VerticalAlignment="Stretch">
                        <StackPanel>
                            <Grid>
                                <TextBlock Style="{StaticResource SectionHeader}" Text="{loc:Str section_audio}" FontSize="12"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <Button x:Name="HelpBtnAudio" Style="{StaticResource HelpButtonStyle}" Tag="Audio" Margin="0,0,8,0"/>
                                    <TextBlock Text="{loc:Str label_duck}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <CheckBox x:Name="ChkAudioDuck" Style="{StaticResource ToggleStyle}"
                                              IsChecked="True"
                                              Checked="ChkAudioDuck_Changed" Unchecked="ChkAudioDuck_Changed"
                                              ToolTip="{loc:Str tooltip_automatically_reduce_other_app_volumes_during}"/>
                                </StackPanel>
                            </Grid>

                            <!-- Master Volume -->
                            <Grid Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="45"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="32"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_master}" FontSize="10"
                                           ToolTip="{loc:Str tooltip_master_volume_for_all_audio}"/>
                                <Slider Grid.Column="1" x:Name="SliderMaster" Minimum="0" Maximum="100" Value="50"
                                        Margin="4,0" ValueChanged="SliderMaster_Changed"
                                        ToolTip="{loc:Str tooltip_overall_audio_volume_level}"/>
                                <TextBlock x:Name="TxtMaster" Grid.Column="2" Style="{StaticResource ValueLabel}" Text="50%" FontSize="10"/>
                            </Grid>

                            <!-- Video Volume -->
                            <Grid Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="45"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="32"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_video}" FontSize="10"
                                           ToolTip="{loc:Str tooltip_volume_for_mandatory_videos}"/>
                                <Slider Grid.Column="1" x:Name="SliderVideoVolume" Minimum="0" Maximum="100" Value="50"
                                        Margin="4,0" ValueChanged="SliderVideoVolume_Changed"
                                        ToolTip="{loc:Str tooltip_separate_volume_control_for_videos}"/>
                                <TextBlock x:Name="TxtVideoVolume" Grid.Column="2" Style="{StaticResource ValueLabel}" Text="50%" FontSize="10"/>
                            </Grid>

                            <!-- Duck % -->
                            <Grid Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="45"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="32"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str label_duck}" FontSize="10"
                                           ToolTip="{loc:Str tooltip_how_much_to_reduce_other_audio}"/>
                                <Slider Grid.Column="1" x:Name="SliderDuck" Minimum="0" Maximum="100" Value="80"
                                        Margin="4,0" ValueChanged="SliderDuck_Changed"
                                        ToolTip="{loc:Str tooltip_ducking_percentage_80_reduce_other_audio_to_2}"/>
                                <TextBlock x:Name="TxtDuck" Grid.Column="2" Style="{StaticResource ValueLabel}" Text="80%" FontSize="10"/>
                            </Grid>

                            <!-- Don't-duck-browser toggle + Audio Test on the same row -->
                            <Grid Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <CheckBox Grid.Column="0"
                                          x:Name="ChkExcludeBambiCloudDucking"
                                          VerticalAlignment="Center"
                                          Content="{loc:Str setting_don_t_duck_browser}"
                                          IsChecked="True"
                                          Checked="ChkExcludeBambiCloudDucking_Changed"
                                          Unchecked="ChkExcludeBambiCloudDucking_Changed"
                                          ToolTip="{loc:Str tooltip_when_enabled_audio_from_the_integrated_bambic}"
                                          Foreground="{DynamicResource PinkBrush}" FontSize="9"/>
                                <Button Grid.Column="1"
                                        x:Name="BtnTestAudio" Content="Test Audio"
                                        Click="BtnTestAudio_Click"
                                        ToolTip="Play a test sound and show audio diagnostics. Use this if audio isn't working."
                                        Background="#33FF69B4" Foreground="White" FontSize="9" Padding="8,3"
                                        BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1" Cursor="Hand"
                                        VerticalAlignment="Center"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Quick Actions (right half) -->
                    <Border Grid.Column="2" CornerRadius="12" Padding="9" Margin="0,0,0,6"
                            Background="{DynamicResource ElevatedSurfaceBrush}"
                            BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1" VerticalAlignment="Stretch">
                        <StackPanel>
                            <Grid Margin="0,0,0,6">
                                <TextBlock Style="{StaticResource SectionHeader}" Text="{loc:Str section_quick_links}" FontSize="12" Margin="0"/>
                                <Button x:Name="HelpBtnQuickLinks" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="QuickLinks"/>
                            </Grid>

                            <!-- Unified Login Button -->
                            <Button x:Name="BtnUnifiedLogin" Content="{loc:Str btn_login_2}"
                                    Click="BtnUnifiedLogin_Click"
                                    Padding="8,12" Margin="0,0,0,6"
                                    ToolTip="{loc:Str tooltip_login_with_discord_or_patreon}">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="{DynamicResource PinkBrush}"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="12"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Cursor" Value="Hand"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                            CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#E055A0"/>
                                                        </Trigger>
                                                        <Trigger Property="IsPressed" Value="True">
                                                            <Setter Property="Background" Value="#C04888"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!-- Logged In Status (shown when logged in) -->
                            <Border x:Name="LoggedInStatusPanel" Visibility="Collapsed"
                                    Background="{DynamicResource PanelBgBrush}" CornerRadius="6" Padding="10,8" Margin="0,0,0,6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock x:Name="TxtLoggedInAs" Text="{loc:Str label_logged_in_as}"
                                                   Foreground="{DynamicResource TextMutedBrush}" FontSize="10"/>
                                        <TextBlock x:Name="TxtLoggedInName" Text="{loc:Str label_username}"
                                                   Foreground="White" FontWeight="Bold" FontSize="12"/>
                                    </StackPanel>
                                    <Button x:Name="BtnQuickLogout" Grid.Column="1" Content="{loc:Str btn_logout}"
                                            Click="BtnQuickLogout_Click"
                                            Padding="10,5" Background="{DynamicResource AccentTintedBgBrush}" Foreground="{DynamicResource PinkBrush}"
                                            BorderThickness="0" FontSize="10" Cursor="Hand"/>
                                </Grid>
                            </Border>

                            <!-- Discord Join Button + Rich Presence -->
                            <Grid Margin="0,4,0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0" Click="BtnDiscord_Click"
                                        ToolTip="{loc:Str tooltip_join_the_discord_community}" Cursor="Hand"
                                        HorizontalAlignment="Stretch">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="BorderThickness" Value="0"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="border" Background="#5865F2" CornerRadius="5"
                                                                Padding="0,4" HorizontalAlignment="Stretch">
                                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                                <TextBlock Text="💬" FontSize="11" VerticalAlignment="Center"/>
                                                                <TextBlock Text="{loc:Str label_join_discord}" Foreground="White" FontSize="10" FontWeight="Bold"
                                                                           VerticalAlignment="Center" Margin="5,0,0,0"/>
                                                            </StackPanel>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="border" Property="Background" Value="#6C75F2"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="6,0,0,0">
                                    <StackPanel.ToolTip>
                                        <ToolTip Background="{DynamicResource SurfaceBgBrush}" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1" Padding="10">
                                            <StackPanel MaxWidth="280">
                                                <TextBlock Text="{loc:Str label_discord_rich_presence}" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,6"/>
                                                <TextBlock Foreground="#CCCCCC" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,8">
                                                    Shows "Playing Conditioning Control Panel" in your Discord status, along with your current level and session time.
                                                </TextBlock>
                                                <TextBlock Text="{loc:Str label_who_can_see_this}" Foreground="{DynamicResource SecondaryBrush}" FontSize="11" FontWeight="Bold" Margin="0,0,0,4"/>
                                                <TextBlock Foreground="#AAAAAA" FontSize="10" TextWrapping="Wrap">
                                                    Only people who can already see your Discord activity status (friends, server members if enabled in your Discord privacy settings). This does NOT send notifications - it simply updates your "Currently Playing" status like any other game.
                                                </TextBlock>
                                            </StackPanel>
                                        </ToolTip>
                                    </StackPanel.ToolTip>
                                    <CheckBox x:Name="ChkQuickDiscordRichPresence" Style="{StaticResource ToggleStyle}"
                                              Checked="ChkDiscordRichPresence_Changed" Unchecked="ChkDiscordRichPresence_Changed">
                                        <CheckBox.LayoutTransform>
                                            <ScaleTransform ScaleX="0.85" ScaleY="0.85"/>
                                        </CheckBox.LayoutTransform>
                                    </CheckBox>
                                    <TextBlock Text="{loc:Str label_rp}" Foreground="{DynamicResource TextMutedBrush}" FontSize="9"
                                               VerticalAlignment="Center" Margin="4,0,0,0"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>

            <!-- ============================================================ -->
            <!-- VELVET MOSAIC HELPER BUTTON ROW - full width, under both     -->
            <!-- the feature grid AND the right half.                         -->
            <!-- ============================================================ -->
            <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" x:Name="VelvetHelperButtonRow"
                  Height="30" Margin="5,4,5,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" x:Name="VelvetBtnScheduler"
                        Click="VelvetBtnScheduler_Click"
                        Cursor="Hand" FontSize="12" FontWeight="Bold">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b"
                                    Background="{DynamicResource ElevatedSurfaceBrush}"
                                    BorderBrush="{DynamicResource GlassBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="14">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="📅" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="{loc:Str section_scheduler}" Foreground="White" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource TransparentPinkBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Grid.Column="2" x:Name="VelvetBtnAppInfo"
                        Click="VelvetBtnAppInfo_Click"
                        Cursor="Hand" FontSize="12" FontWeight="Bold">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b"
                                    Background="{DynamicResource ElevatedSurfaceBrush}"
                                    BorderBrush="{DynamicResource GlassBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="14">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="ℹ" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,8,0"
                                               Foreground="{DynamicResource PinkBrush}"/>
                                    <TextBlock Text="{loc:Str label_app_info}" Foreground="White" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource TransparentPinkBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Grid.Column="4" x:Name="VelvetBtnRamp"
                        Click="VelvetBtnRamp_Click"
                        Cursor="Hand" FontSize="12" FontWeight="Bold">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b"
                                    Background="{DynamicResource ElevatedSurfaceBrush}"
                                    BorderBrush="{DynamicResource GlassBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="14">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="📈" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <TextBlock Text="{loc:Str section_intensity_ramp}" Foreground="White" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource TransparentPinkBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <!-- Scrolling Marquee Banner - row 2, spans both columns -->
            <Border x:Name="MarqueeBanner" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                    Background="#1A0A1A" BorderBrush="{DynamicResource DarkPinkBrush}" BorderThickness="2"
                    CornerRadius="8" Height="48" Margin="5,5,5,0" ClipToBounds="True">
                <Border.Effect>
                    <DropShadowEffect Color="{DynamicResource DarkPink}" BlurRadius="15" ShadowDepth="0" Opacity="0.5"/>
                </Border.Effect>
                <Canvas x:Name="MarqueeCanvas" ClipToBounds="True">
                    <TextBlock x:Name="MarqueeText"
                               Text="{loc:Str label_v5_2_1_is_out_thanks_to_all_the_100_patreons}"
                               FontFamily="Impact" FontSize="28" FontWeight="Bold"
                               Foreground="{DynamicResource PinkBrush}" Canvas.Top="8">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="{DynamicResource DarkPink}" BlurRadius="8" ShadowDepth="0" Opacity="0.8"/>
                        </TextBlock.Effect>
                        <TextBlock.RenderTransform>
                            <TranslateTransform x:Name="MarqueeTransform"/>
                        </TextBlock.RenderTransform>
                    </TextBlock>
                </Canvas>
            </Border>
            </Grid>
        </ContentControl>

        <!-- Presets Tab (Hidden by default) -->
        <ContentControl x:Name="PresetsTab" Grid.Row="4" Margin="10,5,10,10" Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
            <!-- Main Content -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="430" MinWidth="300" MaxWidth="700"/>
                </Grid.ColumnDefinitions>

                <!-- Left Side: Presets and Sessions -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Top Section: Preset Cards Row -->
                    <Border Grid.Row="0" Style="{StaticResource SectionPanel}" Margin="5,5,5,0" Padding="10">
                        <StackPanel>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Style="{StaticResource SectionHeader}" Text="{loc:Str section_presets}" Margin="0"/>
                                <Button x:Name="HelpBtnPresets" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="Presets"/>
                            </Grid>
                            
                            <!-- Horizontal Scrolling Preset Cards -->
                            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"
                                          PreviewMouseWheel="HorizontalScrollViewer_PreviewMouseWheel">
                                <StackPanel Orientation="Horizontal">
                                    <!-- Preset Cards Container -->
                                    <StackPanel x:Name="PresetCardsPanel" Orientation="Horizontal">
                                        <!-- Preset cards will be generated here dynamically -->
                                </StackPanel>
                                
                                <!-- Fixed "New Preset" Card -->
                                <Border Background="#2A2A4A" CornerRadius="6" Padding="8" Margin="4,0,0,0" 
                                        Width="100" Height="70" Cursor="Hand"
                                        MouseLeftButtonUp="BtnNewPreset_Click"
                                        ToolTip="{loc:Str tooltip_create_a_new_preset_from_current_settings}">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="{StaticResource PanelAccentBrush}"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                                    <Setter Property="Background" Value="#353550"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock Text="➕" FontSize="20" HorizontalAlignment="Center" Foreground="{DynamicResource PinkBrush}"/>
                                        <TextBlock Text="{loc:Str label_new}" FontSize="10" FontWeight="Bold" 
                                                   Foreground="{DynamicResource PinkBrush}" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </StackPanel>
                </Border>

                <!-- Bottom Section: Sessions -->
                <Border Grid.Row="1" Style="{StaticResource SectionPanel}" Margin="5,8,5,5" Padding="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="2*"/>
                            <RowDefinition Height="*" MinHeight="120"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,10">
                            <TextBlock Style="{StaticResource SectionHeader}" Text="{loc:Str section_sessions}"/>
                            <Button x:Name="HelpBtnSessions" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="Sessions"/>
                        </Grid>

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <StackPanel x:Name="SessionsPanel">
                                <!-- Morning Drift - Available -->
                                <Border Background="#2A2A4A" CornerRadius="8" Padding="16,14" Margin="0,0,0,8"
                                        Cursor="Hand" x:Name="SessionMorningDrift" MouseLeftButtonUp="SessionCard_Click" Tag="morning_drift">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="{StaticResource PanelAccentBrush}"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{loc:Str label_morning_drift}" Foreground="White" FontWeight="SemiBold" FontSize="15"/>
                                                <Border Background="{DynamicResource PinkBrush}" CornerRadius="3" Padding="8,3" Margin="10,0,0,0">
                                                    <TextBlock Text="{loc:Str label_30_min}" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                                <Border Background="#2A3A2A" CornerRadius="3" Padding="8,3" Margin="6,0,0,0">
                                                    <TextBlock Text="{loc:Str label_easy}" Foreground="#90EE90" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                                <Border Background="#2A4A2A" CornerRadius="3" Padding="8,3" Margin="6,0,0,0">
                                                    <TextBlock Text="{loc:Str label_400_xp}" Foreground="#90EE90" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                            </StackPanel>
                                            <TextBlock Text="{loc:Str label_gentle_subliminals_bouncing_text_with_gradual}"
                                                       Foreground="#A0A0A0" FontSize="13" Margin="0,6,0,0"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Style="{StaticResource SessionActionButton}" Content="✏" ToolTip="{loc:Str tooltip_edit_session}"
                                                    Tag="morning_drift" Click="SessionBtn_Edit"/>
                                            <Button Style="{StaticResource SessionActionButton}" Content="📤" ToolTip="{loc:Str tooltip_export_session}"
                                                    Tag="morning_drift" Click="SessionBtn_Export"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Gamer Girl - Available -->
                                <Border Background="#2A2A4A" CornerRadius="8" Padding="16,14" Margin="0,0,0,8"
                                        Cursor="Hand" x:Name="SessionGamerGirl" MouseLeftButtonUp="SessionCard_Click" Tag="gamer_girl">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="{StaticResource PanelAccentBrush}"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{loc:Str label_gamer_girl}" Foreground="White" FontWeight="SemiBold" FontSize="15"/>
                                                <Border Background="{DynamicResource PinkBrush}" CornerRadius="3" Padding="8,3" Margin="10,0,0,0">
                                                    <TextBlock Text="{loc:Str label_45_min}" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                                <Border Background="#3A3A2A" CornerRadius="3" Padding="8,3" Margin="6,0,0,0">
                                                    <TextBlock Text="{loc:Str label_medium}" Foreground="#FFD700" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                                <Border Background="#3A4A2A" CornerRadius="3" Padding="8,3" Margin="6,0,0,0">
                                                    <TextBlock Text="{loc:Str label_800_xp}" Foreground="#FFD700" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                            </StackPanel>
                                            <TextBlock Text="{loc:Str label_non_intrusive_conditioning_with_pink_filter_s}"
                                                       Foreground="#A0A0A0" FontSize="13" Margin="0,6,0,0"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Style="{StaticResource SessionActionButton}" Content="✏" ToolTip="{loc:Str tooltip_edit_session}"
                                                    Tag="gamer_girl" Click="SessionBtn_Edit"/>
                                            <Button Style="{StaticResource SessionActionButton}" Content="📤" ToolTip="{loc:Str tooltip_export_session}"
                                                    Tag="gamer_girl" Click="SessionBtn_Export"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- The Distant Doll - Available -->
                                <Border Background="#2A2A4A" CornerRadius="8" Padding="16,14" Margin="0,0,0,8"
                                        Cursor="Hand" x:Name="SessionDistantDoll" MouseLeftButtonUp="SessionCard_Click" Tag="distant_doll">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="{StaticResource PanelAccentBrush}"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{loc:Str label_the_distant_doll}" Foreground="White" FontWeight="SemiBold" FontSize="15"/>
                                                <Border Background="{DynamicResource PinkBrush}" CornerRadius="3" Padding="8,3" Margin="10,0,0,0">
                                                    <TextBlock Text="{loc:Str label_45_min}" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                                <Border Background="#2A3A2A" CornerRadius="3" Padding="8,3" Margin="6,0,0,0">
                                                    <TextBlock Text="{loc:Str label_easy}" Foreground="#90EE90" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                                <Border Background="#2A4A2A" CornerRadius="3" Padding="8,3" Margin="6,0,0,0">
                                                    <TextBlock Text="{loc:Str label_400_xp}" Foreground="#90EE90" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                            </StackPanel>
                                            <TextBlock Text="{loc:Str label_large_visuals_for_couch_viewing_no_audio_duck}"
                                                       Foreground="#A0A0A0" FontSize="13" Margin="0,6,0,0"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Style="{StaticResource SessionActionButton}" Content="✏" ToolTip="{loc:Str tooltip_edit_session}"
                                                    Tag="distant_doll" Click="SessionBtn_Edit"/>
                                            <Button Style="{StaticResource SessionActionButton}" Content="📤" ToolTip="{loc:Str tooltip_export_session}"
                                                    Tag="distant_doll" Click="SessionBtn_Export"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Good Girls Don't Cum - Available -->
                                <Border Background="#2A2A4A" CornerRadius="8" Padding="16,14" Margin="0,0,0,8"
                                        Cursor="Hand" x:Name="SessionGoodGirls" MouseLeftButtonUp="SessionCard_Click" Tag="good_girls_dont_cum">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="{StaticResource PanelAccentBrush}"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{loc:Str label_good_girls_don_t_cum}" Foreground="White" FontWeight="SemiBold" FontSize="15"/>
                                                <Border Background="{DynamicResource PinkBrush}" CornerRadius="3" Padding="8,3" Margin="10,0,0,0">
                                                    <TextBlock Text="{loc:Str label_60_min}" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                                <Border Background="#4A3A2A" CornerRadius="3" Padding="8,3" Margin="6,0,0,0">
                                                    <TextBlock Text="{loc:Str label_hard}" Foreground="#FFA500" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                                <Border Background="#4A4A2A" CornerRadius="3" Padding="8,3" Margin="6,0,0,0">
                                                    <TextBlock Text="{loc:Str label_1200_xp}" Foreground="#FFA500" FontSize="10" FontWeight="Bold"/>
                                                </Border>
                                            </StackPanel>
                                            <TextBlock Text="{loc:Str label_denial_session_with_heavy_conditioning_and_es}"
                                                       Foreground="#A0A0A0" FontSize="13" Margin="0,6,0,0"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                            <Button Style="{StaticResource SessionActionButton}" Content="✏" ToolTip="{loc:Str tooltip_edit_session}"
                                                    Tag="good_girls_dont_cum" Click="SessionBtn_Edit"/>
                                            <Button Style="{StaticResource SessionActionButton}" Content="📤" ToolTip="{loc:Str tooltip_export_session}"
                                                    Tag="good_girls_dont_cum" Click="SessionBtn_Export"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Custom/Imported Sessions Section -->
                                <TextBlock x:Name="TxtCustomSessionsHeader" Text="{loc:Str label_your_sessions}" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="13" FontWeight="SemiBold" Margin="0,16,0,10" Visibility="Collapsed"/>
                                <StackPanel x:Name="CustomSessionsPanel"/>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Drop Zone and Editor Panel -->
                        <Grid Grid.Row="2" Margin="0,8,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Left: Drop Zone -->
                            <Border Grid.Column="0"
                                    x:Name="SessionDropZone"
                                    Background="{DynamicResource SurfaceBgBrush}"
                                    BorderBrush="{DynamicResource PanelAccentBrush}"
                                    BorderThickness="2"
                                    CornerRadius="8"
                                    Margin="0,0,4,0"
                                    AllowDrop="True"
                                    Drop="SessionDropZone_Drop"
                                    DragEnter="SessionDropZone_DragEnter"
                                    DragLeave="SessionDropZone_DragLeave"
                                    DragOver="SessionDropZone_DragOver">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="BorderBrush" Value="#606080"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock x:Name="DropZoneIcon" Text="📂" FontSize="28" HorizontalAlignment="Center" Foreground="#707090"/>
                                    <TextBlock Text="{loc:Str label_drag_drop}" FontSize="13" FontWeight="SemiBold"
                                               Foreground="#909090" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                                    <TextBlock Text="{loc:Str label_session_json}" FontSize="11"
                                               Foreground="#606070" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                    <TextBlock x:Name="DropZoneStatus" Text="" FontSize="10"
                                               Foreground="{DynamicResource PinkBrush}" HorizontalAlignment="Center" Margin="0,6,0,0"
                                               Visibility="Collapsed"/>
                                </StackPanel>
                            </Border>

                            <!-- Right: Session Editor Panel -->
                            <Border Grid.Column="1"
                                    Background="{DynamicResource SurfaceBgBrush}"
                                    BorderBrush="{DynamicResource PanelAccentBrush}"
                                    BorderThickness="2"
                                    CornerRadius="8"
                                    Margin="4,0,0,0">
                                <Grid>
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock Text="{loc:Str label_session_editor}" FontSize="13" FontWeight="SemiBold"
                                                   Foreground="{DynamicResource PinkBrush}" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{loc:Str label_create_custom_sessions}" FontSize="11"
                                                   Foreground="#707090" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                                            <Button x:Name="BtnCreateSession" Content="{loc:Str btn_create_new}"
                                                    Click="BtnCreateSession_Click"
                                                    Background="{DynamicResource PinkBrush}" Foreground="White"
                                                    BorderThickness="0"
                                                    Padding="12,6" FontSize="11" FontWeight="SemiBold" Cursor="Hand"
                                                    Margin="0,0,8,0">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource PinkBrush}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                            <Button x:Name="BtnExportSession" Content="{loc:Str btn_export}"
                                                    Click="BtnExportSession_Click"
                                                    IsEnabled="False"
                                                    Background="#2A2A4A" Foreground="#909090"
                                                    BorderBrush="{DynamicResource PanelAccentBrush}" BorderThickness="1"
                                                    Padding="12,6" FontSize="11" Cursor="Hand">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                                            BorderThickness="{TemplateBinding BorderThickness}"
                                                                            CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsEnabled" Value="True">
                                                                <Setter Property="Foreground" Value="White"/>
                                                                <Setter Property="Background" Value="#3A3A5A"/>
                                                            </Trigger>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource PinkBrush}"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

                <!-- Splitter -->
                <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                              Background="{DynamicResource PanelAccentBrush}" Cursor="SizeWE"/>

            <!-- Right Side: Details Panel -->
            <Border Grid.Column="2" Style="{StaticResource SectionPanel}" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Header -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,12">
                        <Grid>
                            <TextBlock x:Name="TxtDetailTitle" Text="{loc:Str label_select_a_preset}"
                                       Foreground="{DynamicResource PinkBrush}" FontSize="18" FontWeight="Bold"/>
                            <Button x:Name="HelpBtnSessionDetails" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="SessionDetails"/>
                        </Grid>
                        <TextBlock x:Name="TxtDetailSubtitle" Text="{loc:Str label_click_on_a_preset_or_session_to_see_details}"
                                   Foreground="#909090" FontSize="12" TextWrapping="Wrap" Margin="0,5,0,0"/>
                    </StackPanel>
                    
                    <!-- Details Content - Preset View -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" x:Name="PresetDetailScroller">
                        <StackPanel x:Name="DetailContentPanel">
                            <!-- Preset details will show here -->
                            <TextBlock x:Name="TxtDetailFlashLabel" Text="{loc:Str section_flash_images}" Foreground="White" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock x:Name="TxtDetailFlash" Text="—" Foreground="#909090" FontSize="11" Margin="0,0,0,12"/>

                            <TextBlock x:Name="TxtDetailVideoLabel" Text="{loc:Str label_mandatory_videos}" Foreground="White" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock x:Name="TxtDetailVideo" Text="—" Foreground="#909090" FontSize="11" Margin="0,0,0,12"/>

                            <TextBlock x:Name="TxtDetailSubLabel" Text="{loc:Str section_subliminals_2}" Foreground="White" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock x:Name="TxtDetailSubliminal" Text="—" Foreground="#909090" FontSize="11" Margin="0,0,0,12"/>
                            
                            <TextBlock Text="{loc:Str section_audio}" Foreground="White" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock x:Name="TxtDetailAudio" Text="—" Foreground="#909090" FontSize="11" Margin="0,0,0,12"/>
                            
                            <TextBlock Text="{loc:Str label_overlays}" Foreground="White" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock x:Name="TxtDetailOverlays" Text="—" Foreground="#909090" FontSize="11" Margin="0,0,0,12"/>
                            
                            <TextBlock Text="{loc:Str label_advanced}" Foreground="White" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                            <TextBlock x:Name="TxtDetailAdvanced" Text="—" Foreground="#909090" FontSize="11" Margin="0,0,0,12"/>
                        </StackPanel>
                    </ScrollViewer>
                    
                    <!-- Details Content - Session View (Spoiler-Free) -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" x:Name="SessionDetailScroller" Visibility="Collapsed">
                        <StackPanel x:Name="SessionDetailPanel">
                            <!-- Session Duration & Difficulty -->
                            <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                        <TextBlock Text="{loc:Str label_duration}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="TxtSessionDuration" Text="{loc:Str label_30_minutes}" Foreground="White" 
                                                   FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                        <TextBlock Text="{loc:Str label_reward}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10" HorizontalAlignment="Center"/>
                                        <TextBlock x:Name="TxtSessionXP" Text="{loc:Str label_50_xp}" Foreground="#90EE90" 
                                                   FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            
                            <!-- Difficulty Badge -->
                            <Border x:Name="DifficultyBadge" Background="#2A2A4A" CornerRadius="6" Padding="8,4" 
                                    Margin="0,0,0,12" HorizontalAlignment="Center">
                                <TextBlock x:Name="TxtSessionDifficulty" Text="{loc:Str label_easy_2}" Foreground="#FFD700" 
                                           FontSize="12" FontWeight="Bold"/>
                            </Border>
                            
                            <!-- Spoiler-Free Description -->
                            <TextBlock x:Name="TxtSessionDescription" TextWrapping="Wrap" Foreground="#C0C0C0" FontSize="12" 
                                       LineHeight="20" Margin="0,0,0,16"
                                       Text="{loc:Str label_let_the_morning_carry_you_away}"/>
                            
                            <!-- Corner GIF Option (shown for Gamer Girl session) -->
                            <Border x:Name="CornerGifOptionPanel" Background="#252540" CornerRadius="6" Padding="10" 
                                    Margin="0,0,0,12" Visibility="Collapsed">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock x:Name="TxtFeatureCornerGif" Text="{loc:Str label_corner_gif}" Foreground="{DynamicResource PinkBrush}"
                                                   FontWeight="Bold" FontSize="11"/>
                                        <CheckBox x:Name="ChkCornerGifEnabled" Style="{StaticResource ToggleStyle}" 
                                                  HorizontalAlignment="Right" Checked="ChkCornerGifEnabled_Changed" 
                                                  Unchecked="ChkCornerGifEnabled_Changed"/>
                                    </Grid>
                                    <TextBlock x:Name="TxtCornerGifDesc" Text="{loc:Str label_place_a_subtle_gif_in_a_screen_corner}" 
                                               Foreground="{DynamicResource TextMutedBrush}" FontSize="10" Margin="0,4,0,0" TextWrapping="Wrap"/>
                                    
                                    <StackPanel x:Name="CornerGifSettings" Visibility="Collapsed" Margin="0,8,0,0">
                                        <Button x:Name="BtnSelectCornerGif" Content="{loc:Str btn_select_gif}" 
                                                Style="{StaticResource OutlineButton}" Padding="8,6" FontSize="10"
                                                Margin="0,0,0,8" Click="BtnSelectCornerGif_Click"/>
                                        <TextBlock Text="{loc:Str label_position}" Foreground="#909090" FontSize="10" Margin="0,0,0,4"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <RadioButton x:Name="RbCornerTL" Content="{loc:Str btn_top_left}" Foreground="White"
                                                         GroupName="CornerPos" Margin="0,2" FontSize="10"/>
                                            <RadioButton x:Name="RbCornerTR" Grid.Column="1" Content="{loc:Str btn_top_right}"
                                                         Foreground="White" GroupName="CornerPos" Margin="0,2" FontSize="10"/>
                                            <RadioButton x:Name="RbCornerBL" Grid.Row="1" Content="{loc:Str btn_bottom_left}"
                                                         Foreground="White" GroupName="CornerPos" Margin="0,2" FontSize="10" IsChecked="True"/>
                                            <RadioButton x:Name="RbCornerBR" Grid.Row="1" Grid.Column="1" Content="{loc:Str btn_bottom_right}"
                                                         Foreground="White" GroupName="CornerPos" Margin="0,2" FontSize="10"/>
                                        </Grid>

                                        <!-- Size Slider -->
                                        <TextBlock Text="{loc:Str label_size}" Foreground="#909090" FontSize="10" Margin="0,10,0,4"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="50"/>
                                            </Grid.ColumnDefinitions>
                                            <Slider x:Name="SliderCornerGifSize" Minimum="100" Maximum="600" Value="300"
                                                    VerticalAlignment="Center" ValueChanged="SliderCornerGifSize_ValueChanged"/>
                                            <TextBlock x:Name="TxtCornerGifSize" Grid.Column="1" Text="{loc:Str label_300px}"
                                                       Foreground="#C0C0C0" FontSize="10" VerticalAlignment="Center"
                                                       HorizontalAlignment="Right"/>
                                        </Grid>

                                        <!-- Opacity Slider -->
                                        <TextBlock Text="{loc:Str label_opacity}" Foreground="#909090" FontSize="10" Margin="0,10,0,4"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="50"/>
                                            </Grid.ColumnDefinitions>
                                            <Slider x:Name="SliderCornerGifOpacity" Minimum="5" Maximum="100" Value="20"
                                                    VerticalAlignment="Center" ValueChanged="SliderCornerGifOpacity_ValueChanged"/>
                                            <TextBlock x:Name="TxtCornerGifOpacity" Grid.Column="1" Text="20%"
                                                       Foreground="#C0C0C0" FontSize="10" VerticalAlignment="Center"
                                                       HorizontalAlignment="Right"/>
                                        </Grid>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                            
                            <!-- Spoiler Warning -->
                            <Border Background="#2A2040" CornerRadius="8" Padding="12" Margin="0,0,0,12">
                                <StackPanel>
                                    <TextBlock Text="{loc:Str label_mystery_experience}" Foreground="{DynamicResource PinkBrush}" 
                                               FontWeight="Bold" FontSize="13" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{loc:Str label_the_magic_works_best_when_you_don_t_know_what}" 
                                               Foreground="{DynamicResource TextMutedBrush}" FontSize="11" TextAlignment="Center" 
                                               Margin="0,6,0,0" FontStyle="Italic"/>
                                </StackPanel>
                            </Border>
                            
                            <!-- Reveal Spoilers Button -->
                            <Button x:Name="BtnRevealSpoilers" Content="{loc:Str btn_reveal_details}" 
                                    Style="{StaticResource OutlineButton}" Padding="12,8" FontSize="12"
                                    Click="BtnRevealSpoilers_Click" Margin="0,0,0,12"
                                    ToolTip="{loc:Str tooltip_this_will_show_all_session_settings_spoils_th}"/>
                            
                            <!-- Spoiler Content (Hidden by default) -->
                            <StackPanel x:Name="SessionSpoilerPanel" Visibility="Collapsed">
                                <Border Background="#301520" CornerRadius="6" Padding="10" Margin="0,0,0,12">
                                    <TextBlock Text="{loc:Str label_spoilers_below}" Foreground="#FF6B6B" FontSize="12" 
                                               FontWeight="Bold" HorizontalAlignment="Center"/>
                                </Border>
                                
                                <TextBlock x:Name="TxtSessionFlashLabel" Text="{loc:Str section_flash_images}" Foreground="White" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,3"/>
                                <TextBlock x:Name="TxtSessionFlash" Text="—" Foreground="#909090" FontSize="11" Margin="0,0,0,10" TextWrapping="Wrap"/>

                                <TextBlock x:Name="TxtSessionSubLabel" Text="{loc:Str section_subliminals_2}" Foreground="White" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,3"/>
                                <TextBlock x:Name="TxtSessionSubliminal" Text="—" Foreground="#909090" FontSize="11" Margin="0,0,0,10"/>
                                
                                <TextBlock Text="{loc:Str section_audio}" Foreground="White" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,3"/>
                                <TextBlock x:Name="TxtSessionAudio" Text="—" Foreground="#909090" FontSize="11" Margin="0,0,0,10"/>
                                
                                <TextBlock Text="{loc:Str label_overlays}" Foreground="White" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,3"/>
                                <TextBlock x:Name="TxtSessionOverlays" Text="—" Foreground="#909090" FontSize="11" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                
                                <TextBlock Text="{loc:Str label_extras}" Foreground="White" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,3"/>
                                <TextBlock x:Name="TxtSessionExtras" Text="—" Foreground="#909090" FontSize="11" Margin="0,0,0,10" TextWrapping="Wrap"/>
                                
                                <TextBlock Text="{loc:Str label_timeline}" Foreground="White" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,3"/>
                                <TextBlock x:Name="TxtSessionTimeline" Text="—" Foreground="#909090" FontSize="11" Margin="0,0,0,10" TextWrapping="Wrap"/>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                    
                    <!-- Preset Action Buttons -->
                    <StackPanel Grid.Row="2" x:Name="PresetButtonsPanel" Margin="0,10,0,0">
                        <Button x:Name="BtnLoadPreset" Content="{loc:Str btn_load_preset}" Style="{StaticResource PinkButton}"
                                Margin="0,0,0,6" Padding="12,10" FontSize="13" Click="BtnLoadPreset_Click" IsEnabled="False"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="BtnSaveOverPreset" Grid.Column="0" Content="{loc:Str btn_save_2}" 
                                    Style="{StaticResource OutlineButton}" Margin="0,0,3,0" Padding="10,8"
                                    Click="BtnSaveOverPreset_Click" IsEnabled="False" FontSize="12"/>
                            <Button x:Name="BtnDeletePreset" Grid.Column="1" Content="{loc:Str btn_delete_2}" 
                                    Style="{StaticResource OutlineButton}" Margin="3,0,0,0" Padding="10,8"
                                    Click="BtnDeletePreset_Click" IsEnabled="False" FontSize="12"/>
                        </Grid>
                    </StackPanel>
                    
                    <!-- Session Action Buttons -->
                    <StackPanel Grid.Row="2" x:Name="SessionButtonsPanel" Margin="0,10,0,0" Visibility="Collapsed">
                        <Button x:Name="BtnStartSession" Content="{loc:Str btn_start_session}" Style="{StaticResource PinkButton}"
                                Padding="12,12" FontSize="14" Click="BtnStartSession_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
            </Grid>
        </ContentControl>

        <!-- Progression Tab (Hidden by default) -->
        <ContentControl x:Name="ProgressionTab" Grid.Row="4" Margin="10,5,10,10" Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
            <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <!-- Unlockables -->
            <Border Grid.Column="0" Style="{StaticResource SectionPanel}" Margin="5"
                    ToolTip="{loc:Str tooltip_features_that_unlock_as_you_level_up_keep_con}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Grid>
                        <TextBlock Style="{StaticResource SectionHeader}" Text="{loc:Str section_unlockables}" Margin="0"/>
                        <Button x:Name="HelpBtnUnlockables" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="Unlockables"/>
                    </Grid>
                    
                    <!-- Spiral Overlay - Lvl 10 -->
                    <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,5"
                            ToolTip="{loc:Str tooltip_hypnotic_spiral_animation_overlay_covers_all}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Grid Grid.Column="0">
                            <!-- Locked State -->
                            <StackPanel x:Name="SpiralLocked" Visibility="Visible">
                                <Grid>
                                    <TextBlock x:Name="TxtFeatureSpiralLocked" Text="{loc:Str label_spiral_overlay}" Foreground="#666666" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{loc:Str label_lvl_10}" Foreground="#666666" HorizontalAlignment="Right" FontSize="13"/>
                                </Grid>
                                <TextBlock Text="{loc:Str label_reach_level_10_to_unlock}" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="12" Margin="0,8,0,0"/>
                            </StackPanel>

                            <!-- Unlocked State -->
                            <StackPanel x:Name="SpiralUnlocked" Visibility="Collapsed">
                                <Grid>
                                    <TextBlock x:Name="TxtFeatureSpiral" Text="{loc:Str label_spiral_overlay}" Foreground="White" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{loc:Str label_unlocked}" Foreground="#00E676" HorizontalAlignment="Right" FontSize="13"/>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str btn_enable}"
                                               ToolTip="{loc:Str tooltip_enable_disable_spiral_overlay_on_all_monitors}"/>
                                    <CheckBox x:Name="ChkSpiralEnabled" Style="{StaticResource ToggleStyle}" 
                                              HorizontalAlignment="Right"
                                              Checked="ChkSpiralEnabled_Changed" Unchecked="ChkSpiralEnabled_Changed"
                                              ToolTip="{loc:Str tooltip_show_hypnotic_spiral_overlay}"/>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="40"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_opacity}"
                                               ToolTip="{loc:Str tooltip_spiral_transparency_level}"/>
                                    <Slider Grid.Column="1" x:Name="SliderSpiralOpacity" Minimum="5" Maximum="50" Value="15" 
                                            Margin="10,0" ValueChanged="SliderSpiralOpacity_Changed"
                                            ToolTip="{loc:Str tooltip_spiral_opacity_5_50}"/>
                                    <TextBlock x:Name="TxtSpiralOpacity" Grid.Column="2" Style="{StaticResource ValueLabel}" Text="15%"/>
                                </Grid>
                                <Button Content="{loc:Str btn_select_gif}" Style="{StaticResource OutlineButton}"
                                        Margin="0,10,0,0" Click="BtnSelectSpiral_Click"
                                        ToolTip="{loc:Str tooltip_choose_a_spiral_gif_animation}"/>
                                <TextBlock Text="{loc:Str label_if_you_have_display_problems_try_setting_your}"
                                           Foreground="#FFA500" FontSize="10" FontStyle="Italic" TextWrapping="Wrap"
                                           Margin="0,8,0,0" Opacity="0.8"/>
                            </StackPanel>
                            </Grid>
                            
                            <!-- Feature Image -->
                            <Rectangle x:Name="SpiralFeatureImage" Grid.Column="1" Style="{StaticResource RoundedFeatureImage}">
                                <Rectangle.Fill>
                                    <ImageBrush ImageSource="pack://application:,,,/Resources/features/spiral_overlay.png" Stretch="UniformToFill"/>
                                </Rectangle.Fill>
                            </Rectangle>
                        </Grid>
                    </Border>

                    <!-- Pink Filter - Lvl 10 -->
                    <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,5"
                            ToolTip="{loc:Str tooltip_pink_color_tint_overlay_for_all_screens_subtl}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Grid Grid.Column="0">
                            <!-- Locked State -->
                            <StackPanel x:Name="PinkFilterLocked" Visibility="Visible">
                                <Grid>
                                    <TextBlock x:Name="TxtFeaturePinkFilterLocked" Text="{loc:Str label_pink_filter}" Foreground="#666666" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{loc:Str label_lvl_10}" Foreground="#666666" HorizontalAlignment="Right" FontSize="13"/>
                                </Grid>
                                <TextBlock Text="{loc:Str label_reach_level_10_to_unlock}" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="12" Margin="0,8,0,0"/>
                            </StackPanel>

                            <!-- Unlocked State -->
                            <StackPanel x:Name="PinkFilterUnlocked" Visibility="Collapsed">
                                <Grid>
                                    <TextBlock x:Name="TxtFeaturePinkFilter" Text="{loc:Str label_pink_filter}" Foreground="White" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{loc:Str label_unlocked}" Foreground="#00E676" HorizontalAlignment="Right" FontSize="13"/>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str btn_enable}"
                                               ToolTip="{loc:Str tooltip_enable_disable_pink_tint_overlay}"/>
                                    <CheckBox x:Name="ChkPinkFilterEnabled" Style="{StaticResource ToggleStyle}" 
                                              HorizontalAlignment="Right"
                                              Checked="ChkPinkFilterEnabled_Changed" Unchecked="ChkPinkFilterEnabled_Changed"
                                              ToolTip="{loc:Str tooltip_apply_pink_color_filter_to_all_monitors}"/>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="40"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_opacity}"
                                               ToolTip="{loc:Str tooltip_pink_filter_intensity}"/>
                                    <Slider Grid.Column="1" x:Name="SliderPinkOpacity" Minimum="5" Maximum="50" Value="10" 
                                            Margin="10,0" ValueChanged="SliderPinkOpacity_Changed"
                                            ToolTip="{loc:Str tooltip_filter_opacity_5_50}"/>
                                    <TextBlock x:Name="TxtPinkOpacity" Grid.Column="2" Style="{StaticResource ValueLabel}" Text="10%"/>
                                </Grid>
                            </StackPanel>
                            </Grid>
                            
                            <!-- Feature Image -->
                            <Rectangle x:Name="PinkFilterFeatureImage" Grid.Column="1" Style="{StaticResource RoundedFeatureImage}">
                                <Rectangle.Fill>
                                    <ImageBrush ImageSource="pack://application:,,,/Resources/features/Pink_filter.png" Stretch="UniformToFill"/>
                                </Rectangle.Fill>
                            </Rectangle>
                        </Grid>
                    </Border>

                    <!-- Bubble Pop - Lvl 20 -->
                    <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,5"
                            ToolTip="{loc:Str tooltip_pop_floating_bubbles_to_earn_xp_fun_mini_game}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Grid Grid.Column="0">
                            <!-- Locked State -->
                            <StackPanel x:Name="BubblesLocked" Visibility="Visible">
                                <Grid>
                                    <TextBlock x:Name="TxtFeatureBubblePopLocked" Text="{loc:Str label_bubble_pop}" Foreground="#666666" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{loc:Str label_lvl_20}" Foreground="#666666" HorizontalAlignment="Right" FontSize="13"/>
                                </Grid>
                                <TextBlock Text="{loc:Str label_reach_level_20_to_unlock}" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="12" Margin="0,8,0,0"/>
                            </StackPanel>

                            <!-- Unlocked State -->
                            <StackPanel x:Name="BubblesUnlocked" Visibility="Collapsed">
                                <Grid>
                                    <TextBlock x:Name="TxtFeatureBubblePop" Text="{loc:Str label_bubble_pop}" Foreground="White" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{loc:Str label_unlocked}" Foreground="#00E676" HorizontalAlignment="Right" FontSize="13"/>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str btn_enable}"
                                               ToolTip="{loc:Str tooltip_enable_disable_bubble_pop_mini_game}"/>
                                    <CheckBox x:Name="ChkBubblesEnabled" Style="{StaticResource ToggleStyle}" 
                                              HorizontalAlignment="Right"
                                              Checked="ChkBubblesEnabled_Changed" Unchecked="ChkBubblesEnabled_Changed"
                                              ToolTip="{loc:Str tooltip_pop_bubbles_to_earn_xp}"/>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="40"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str label_per_min}"
                                               ToolTip="{loc:Str tooltip_bubbles_spawned_per_minute}"/>
                                    <Slider Grid.Column="1" x:Name="SliderBubbleFreq" Minimum="1" Maximum="15" Value="5" 
                                            Margin="10,0" ValueChanged="SliderBubbleFreq_Changed"
                                            ToolTip="{loc:Str tooltip_bubble_spawn_rate}"/>
                                    <TextBlock x:Name="TxtBubbleFreq" Grid.Column="2" Style="{StaticResource ValueLabel}" Text="5"/>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="40"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_volume}"
                                               ToolTip="{loc:Str tooltip_volume_of_bubble_pop_sounds}"/>
                                    <Slider Grid.Column="1" x:Name="SliderBubbleVolume" Minimum="0" Maximum="100" Value="50"
                                            Margin="10,0" ValueChanged="SliderBubbleVolume_Changed"
                                            ToolTip="{loc:Str tooltip_bubble_pop_sound_volume}"/>
                                    <TextBlock x:Name="TxtBubbleVolume" Grid.Column="2" Style="{StaticResource ValueLabel}" Text="50%"/>
                                </Grid>
                            </StackPanel>
                            </Grid>
                            
                            <!-- Feature Image -->
                            <Rectangle x:Name="BubblePopFeatureImage" Grid.Column="1" Style="{StaticResource RoundedFeatureImage}">
                                <Rectangle.Fill>
                                    <ImageBrush ImageSource="pack://application:,,,/Resources/features/Bubble_pop.png" Stretch="UniformToFill"/>
                                </Rectangle.Fill>
                            </Rectangle>
                        </Grid>
                    </Border>

                    <!-- Lock Card - Lvl 35 -->
                    <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,5"
                            ToolTip="{loc:Str tooltip_screen_lock_that_requires_typing_phrases_rein}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Grid Grid.Column="0">
                            <!-- Locked State -->
                            <StackPanel x:Name="LockCardLocked" Visibility="Visible">
                                <Grid>
                                    <TextBlock x:Name="TxtFeatureLockCardLocked" Text="{loc:Str label_lock_card}" Foreground="#666666" FontWeight="Bold"/>
                                    <TextBlock Text="{loc:Str label_lvl_35}" Foreground="#666666" HorizontalAlignment="Right" FontSize="13"/>
                                </Grid>
                                <TextBlock Text="{loc:Str label_reach_level_35_to_unlock}" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="12" Margin="0,8,0,0"/>
                            </StackPanel>

                            <!-- Unlocked State -->
                            <StackPanel x:Name="LockCardUnlocked" Visibility="Collapsed">
                                <Grid>
                                    <TextBlock x:Name="TxtFeatureLockCard" Text="{loc:Str label_lock_card}" Foreground="White" FontWeight="Bold"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Button Content="{loc:Str btn_test_2}" Style="{StaticResource OutlineButton}" 
                                                Padding="10,4" FontSize="11" Margin="0,0,8,0"
                                                Click="BtnTestLockCard_Click"
                                                ToolTip="{loc:Str tooltip_test_a_lock_card_now}"/>
                                        <TextBlock Text="{loc:Str label_unlocked}" Foreground="#00E676" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str btn_enable}"
                                               ToolTip="{loc:Str tooltip_enable_disable_lock_card_popups}"/>
                                    <CheckBox x:Name="ChkLockCardEnabled" Style="{StaticResource ToggleStyle}" 
                                              HorizontalAlignment="Right"
                                              Checked="ChkLockCardEnabled_Changed" Unchecked="ChkLockCardEnabled_Changed"
                                              ToolTip="{loc:Str tooltip_show_lock_cards_that_require_typing_to_dismis}"/>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="40"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_per_hour}"
                                               ToolTip="{loc:Str tooltip_lock_cards_shown_per_hour}"/>
                                    <Slider Grid.Column="1" x:Name="SliderLockCardFreq" Minimum="1" Maximum="10" Value="2" 
                                            Margin="10,0" ValueChanged="SliderLockCardFreq_Changed"
                                            ToolTip="{loc:Str tooltip_lock_card_frequency_1_10_per_hour}"/>
                                    <TextBlock x:Name="TxtLockCardFreq" Grid.Column="2" Style="{StaticResource ValueLabel}" Text="2"/>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="40"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_repeats}"
                                               ToolTip="{loc:Str tooltip_times_the_phrase_must_be_typed}"/>
                                    <Slider Grid.Column="1" x:Name="SliderLockCardRepeats" Minimum="1" Maximum="10" Value="3" 
                                            Margin="10,0" ValueChanged="SliderLockCardRepeats_Changed"
                                            ToolTip="{loc:Str tooltip_number_of_times_to_type_the_phrase_1_10}"/>
                                    <TextBlock x:Name="TxtLockCardRepeats" Grid.Column="2" Style="{StaticResource ValueLabel}" Text="3x"/>
                                </Grid>
                                <Grid Margin="0,8,0,0">
                                    <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_strict_lock}"
                                               ToolTip="{loc:Str tooltip_prevent_esc_key_from_closing_the_lock_card}"/>
                                    <CheckBox x:Name="ChkLockCardStrict" Style="{StaticResource ToggleStyle}"
                                              HorizontalAlignment="Right" Checked="ChkLockCardStrict_Changed" Unchecked="ChkLockCardStrict_Changed"
                                              ToolTip="{loc:Str tooltip_cannot_escape_must_complete_typing}"/>
                                </Grid>
                                <Grid Margin="0,10,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Button Grid.Column="0" Content="{loc:Str btn_manage_phrases}" Style="{StaticResource OutlineButton}" 
                                            Margin="0,0,5,0" Click="BtnManageLockCardPhrases_Click"
                                            ToolTip="{loc:Str tooltip_edit_phrases_that_must_be_typed}"/>
                                    <Button Grid.Column="1" Content="⚙" Style="{StaticResource OutlineButton}" 
                                            Width="40" Click="BtnLockCardSettings_Click"
                                            ToolTip="{loc:Str tooltip_customize_lock_card_colors}"/>
                                </Grid>
                            </StackPanel>
                            </Grid>
                            
                            <!-- Feature Image -->
                            <Rectangle x:Name="LockCardFeatureImage" Grid.Column="1" Style="{StaticResource RoundedFeatureImage}">
                                <Rectangle.Fill>
                                    <ImageBrush ImageSource="pack://application:,,,/Resources/features/Phrase_Lock.png" Stretch="UniformToFill"/>
                                </Rectangle.Fill>
                            </Rectangle>
                        </Grid>
                    </Border>

                    <!-- Bubble Count Game - Lvl 50 -->
                    <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,5"
                            ToolTip="{loc:Str tooltip_count_the_bubbles_challenge_memory_and_attent}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Grid Grid.Column="0">
                            <!-- Locked State -->
                            <StackPanel x:Name="Level50Locked" Visibility="Visible">
                                <Grid>
                                    <TextBlock x:Name="TxtFeatureBubbleCountLocked" Text="{loc:Str label_bubble_count}" Foreground="#666666" FontWeight="Bold"/>
                                    <TextBlock Text="{loc:Str label_lvl_50}" Foreground="#666666" HorizontalAlignment="Right" FontSize="13"/>
                                </Grid>
                                <TextBlock Text="{loc:Str label_reach_level_50_to_unlock}" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="12" Margin="0,8,0,0"/>
                            </StackPanel>

                            <!-- Unlocked State -->
                            <StackPanel x:Name="Level50Unlocked" Visibility="Collapsed">
                                <Grid>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock x:Name="TxtFeatureBubbleCount" Text="{loc:Str label_bubble_count}" Foreground="{DynamicResource PinkBrush}" FontWeight="Bold"/>
                                        <TextBlock Text="{loc:Str label_lvl_50_2}" Foreground="{DynamicResource PinkBrush}" Margin="8,0,0,0"/>
                                    </StackPanel>
                                    <CheckBox x:Name="ChkBubbleCountEnabled" Style="{StaticResource ToggleStyle}" 
                                              HorizontalAlignment="Right"
                                              Checked="ChkBubbleCountEnabled_Changed" Unchecked="ChkBubbleCountEnabled_Changed"/>
                                </Grid>
                                
                                <StackPanel x:Name="BubbleCountSettings" Margin="0,10,0,0">
                                    <!-- Per Hour -->
                                    <Grid Margin="0,3,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_per_hour}"
                                                   ToolTip="{loc:Str tooltip_how_often_the_bubble_count_challenge_appears}"/>
                                        <TextBlock x:Name="TxtBubbleCountFreq" Grid.Column="1" Style="{StaticResource ValueLabel}" Text="2"/>
                                    </Grid>
                                    <Slider x:Name="SliderBubbleCountFreq" Minimum="1" Maximum="10" Value="2" 
                                            Margin="0,1" ValueChanged="SliderBubbleCountFreq_Changed"
                                            ToolTip="{loc:Str tooltip_challenges_per_hour_1_10}"/>
                                    
                                    <!-- Difficulty -->
                                    <Grid Margin="0,8,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_difficulty}"/>
                                        <ComboBox x:Name="CmbBubbleCountDifficulty" Grid.Column="1" 
                                                  Width="100" Background="{DynamicResource SurfaceBgBrush}" Foreground="White"
                                                  SelectionChanged="CmbBubbleCountDifficulty_Changed">
                                            <ComboBoxItem Content="{loc:Str btn_easy}" Tag="0"/>
                                            <ComboBoxItem Content="{loc:Str btn_medium}" Tag="1" IsSelected="True"/>
                                            <ComboBoxItem Content="{loc:Str btn_hard}" Tag="2"/>
                                        </ComboBox>
                                    </Grid>
                                    
                                    <!-- Strict Lock -->
                                    <Grid Margin="0,8,0,0">
                                        <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_strict_lock}"
                                                   ToolTip="{loc:Str tooltip_prevent_closing_until_correctly_answered}"/>
                                        <CheckBox x:Name="ChkBubbleCountStrict" Style="{StaticResource ToggleStyle}" 
                                                  HorizontalAlignment="Right"
                                                  Checked="ChkBubbleCountStrict_Changed" Unchecked="ChkBubbleCountStrict_Changed"/>
                                    </Grid>
                                    
                                    <!-- Test Button -->
                                    <Button Content="{loc:Str btn_test_now}" 
                                            Background="#40FFFFFF" Foreground="White" 
                                            BorderThickness="0" Padding="10,5" Margin="0,10,0,0"
                                            Cursor="Hand" Click="BtnTestBubbleCount_Click"
                                            ToolTip="{loc:Str tooltip_trigger_a_bubble_count_challenge_now}"/>
                                </StackPanel>
                            </StackPanel>
                            </Grid>
                            
                            <!-- Feature Image -->
                            <Rectangle x:Name="BubbleCountFeatureImage" Grid.Column="1" Style="{StaticResource RoundedFeatureImage}">
                                <Rectangle.Fill>
                                    <ImageBrush ImageSource="pack://application:,,,/Resources/features/Bubble_count.png" Stretch="UniformToFill"/>
                                </Rectangle.Fill>
                            </Rectangle>
                        </Grid>
                    </Border>

                    <!-- Bouncing Text - Lvl 60 -->
                    <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,5"
                            ToolTip="{loc:Str tooltip_dvd_screensaver_style_bouncing_text_customiza}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <Grid Grid.Column="0">
                            <!-- Locked State -->
                            <StackPanel x:Name="Level60Locked" Visibility="Visible">
                                <Grid>
                                    <TextBlock x:Name="TxtFeatureBouncingLocked" Text="{loc:Str label_bouncing_text}" Foreground="#666666" FontWeight="Bold"/>
                                    <TextBlock Text="{loc:Str label_lvl_60}" Foreground="#666666" HorizontalAlignment="Right" FontSize="13"/>
                                </Grid>
                                <TextBlock Text="{loc:Str label_reach_level_60_to_unlock}" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="12" Margin="0,8,0,0"/>
                            </StackPanel>

                            <!-- Unlocked State -->
                            <StackPanel x:Name="Level60Unlocked" Visibility="Collapsed">
                                <Grid>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock x:Name="TxtFeatureBouncing" Text="{loc:Str label_bouncing_text}" Foreground="{DynamicResource PinkBrush}" FontWeight="Bold"/>
                                        <TextBlock Text="{loc:Str label_lvl_60_2}" Foreground="{DynamicResource PinkBrush}" Margin="8,0,0,0"/>
                                    </StackPanel>
                                    <CheckBox x:Name="ChkBouncingTextEnabled" Style="{StaticResource ToggleStyle}" 
                                              HorizontalAlignment="Right"
                                              Checked="ChkBouncingTextEnabled_Changed" Unchecked="ChkBouncingTextEnabled_Changed"/>
                                </Grid>
                                
                                <StackPanel x:Name="BouncingTextSettings" Margin="0,10,0,0">
                                    <!-- Speed -->
                                    <Grid Margin="0,3,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_speed}"
                                                   ToolTip="{loc:Str tooltip_how_fast_the_text_bounces}"/>
                                        <TextBlock x:Name="TxtBouncingTextSpeed" Grid.Column="1" Style="{StaticResource ValueLabel}" Text="5"/>
                                    </Grid>
                                    <Slider x:Name="SliderBouncingTextSpeed" Minimum="1" Maximum="10" Value="5" 
                                            Margin="0,1" ValueChanged="SliderBouncingTextSpeed_Changed"
                                            ToolTip="{loc:Str tooltip_bounce_speed_1_slow_10_fast}"/>
                                    
                                    <!-- Size -->
                                    <Grid Margin="0,8,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_size}"
                                                   ToolTip="{loc:Str tooltip_text_size_50_to_300}"/>
                                        <TextBlock x:Name="TxtBouncingTextSize" Grid.Column="1" Style="{StaticResource ValueLabel}" Text="100%"/>
                                    </Grid>
                                    <Slider x:Name="SliderBouncingTextSize" Minimum="50" Maximum="300" Value="100" 
                                            Margin="0,1" ValueChanged="SliderBouncingTextSize_Changed"
                                            ToolTip="{loc:Str tooltip_text_size_50_small_100_normal_300_huge}"/>
                                    
                                    <!-- XP Info -->
                                    <TextBlock Text="{loc:Str label_10_xp_per_bounce}" 
                                               Foreground="#50C878" FontSize="12" 
                                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                    
                                    <!-- Edit Phrases -->
                                    <Button Content="{loc:Str btn_edit_phrases}"
                                            Background="#40FFFFFF" Foreground="White"
                                            BorderThickness="0" Padding="10,5" Margin="0,10,0,0"
                                            Cursor="Hand" Click="BtnEditBouncingText_Click"
                                            ToolTip="{loc:Str tooltip_customize_the_bouncing_text_phrases}"/>

                                    <!-- Show Over Videos -->
                                    <Grid Margin="0,10,0,0">
                                        <TextBlock Text="{loc:Str setting_show_over_videos}" Style="{StaticResource SettingLabel}"
                                                   ToolTip="{loc:Str tooltip_keep_bouncing_text_visible_on_top_of_fullscre}"/>
                                        <CheckBox x:Name="ChkBouncingTextAlwaysOnTop" Style="{StaticResource ToggleStyle}"
                                                  HorizontalAlignment="Right"
                                                  Checked="ChkBouncingTextAlwaysOnTop_Changed" Unchecked="ChkBouncingTextAlwaysOnTop_Changed"/>
                                    </Grid>
                                </StackPanel>
                            </StackPanel>
                            </Grid>
                            
                            <!-- Feature Image -->
                            <Rectangle x:Name="BouncingTextFeatureImage" Grid.Column="1" Style="{StaticResource RoundedFeatureImage}">
                                <Rectangle.Fill>
                                    <ImageBrush ImageSource="pack://application:,,,/Resources/features/bouncing_text.png" Stretch="UniformToFill"/>
                                </Rectangle.Fill>
                            </Rectangle>
                        </Grid>
                    </Border>

                    <!-- Brain Drain - DISABLED FOR REWORK -->
                    <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,5" Opacity="0.5"
                            ToolTip="{loc:Str tooltip_this_feature_is_temporarily_disabled_while_we}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0">
                                <StackPanel>
                                    <Grid>
                                        <TextBlock x:Name="TxtFeatureBrainDrain" Text="{loc:Str section_brain_drain}" Foreground="#666666" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock Text="{loc:Str section_rework}" Foreground="#FFA500" HorizontalAlignment="Right" FontSize="13" FontWeight="Bold"/>
                                    </Grid>
                                    <Border Background="#2A2A4A" CornerRadius="4" Padding="8" Margin="0,8,0,0">
                                        <TextBlock Text="{loc:Str section_up_for_rework_this_feature_is_temporarily_dis}"
                                                   Foreground="#FFA500" FontSize="11" TextWrapping="Wrap"/>
                                    </Border>
                                </StackPanel>

                                <!-- Hidden elements to prevent XAML binding errors -->
                                <StackPanel x:Name="BrainDrainLocked" Visibility="Collapsed"/>
                                <StackPanel x:Name="BrainDrainUnlocked" Visibility="Collapsed">
                                    <CheckBox x:Name="ChkBrainDrainEnabled" Visibility="Collapsed"/>
                                    <StackPanel x:Name="BrainDrainSettings" Visibility="Collapsed">
                                        <TextBlock x:Name="TxtBrainDrainIntensity"/>
                                        <Slider x:Name="SliderBrainDrainIntensity"/>
                                        <CheckBox x:Name="ChkBrainDrainHighRefresh"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>

                            <!-- Feature Image (grayed out) -->
                            <Rectangle x:Name="BrainDrainFeatureImage" Grid.Column="1" Style="{StaticResource RoundedFeatureImage}" Opacity="0.3">
                                <Rectangle.Fill>
                                    <ImageBrush ImageSource="pack://application:,,,/Resources/features/brain_drain.png" Stretch="UniformToFill"/>
                                </Rectangle.Fill>
                            </Rectangle>
                        </Grid>
                    </Border>

                    <!-- Mind Wipe - Lvl 75 -->
                    <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,5"
                            ToolTip="{loc:Str tooltip_random_mind_wipe_audio_effects_escalates_in_f}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0">
                            <!-- Locked State -->
                            <StackPanel x:Name="MindWipeLocked" Visibility="Visible">
                                <Grid>
                                    <TextBlock x:Name="TxtFeatureMindWipeLocked" Text="{loc:Str label_mind_wipe}" Foreground="#666666" FontWeight="Bold" FontSize="14"/>
                                    <TextBlock Text="{loc:Str label_lvl_75}" Foreground="#666666" HorizontalAlignment="Right" FontSize="13"/>
                                </Grid>
                                <TextBlock Text="{loc:Str label_reach_level_75_to_unlock}" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="12" Margin="0,8,0,0"/>
                            </StackPanel>

                            <!-- Unlocked State -->
                            <StackPanel x:Name="MindWipeUnlocked" Visibility="Collapsed">
                                <Grid>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock x:Name="TxtFeatureMindWipe" Text="{loc:Str label_mind_wipe}" Foreground="{DynamicResource PinkBrush}" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock Text="{loc:Str label_lvl_75_2}" Foreground="{DynamicResource PinkBrush}" Margin="8,0,0,0"/>
                                    </StackPanel>
                                    <CheckBox x:Name="ChkMindWipeEnabled" Style="{StaticResource ToggleStyle}"
                                              HorizontalAlignment="Right"
                                              Checked="ChkMindWipeEnabled_Changed" Unchecked="ChkMindWipeEnabled_Changed"/>
                                </Grid>

                                <StackPanel x:Name="MindWipeSettings" Margin="0,10,0,0">
                                    <!-- Frequency -->
                                    <Grid Margin="0,3,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_frequency}"
                                                   ToolTip="{loc:Str tooltip_how_many_times_per_hour_outside_sessions}"/>
                                        <TextBlock x:Name="TxtMindWipeFreq" Grid.Column="1" Style="{StaticResource ValueLabel}" Text="{loc:Str label_6_h}"/>
                                    </Grid>
                                    <Slider x:Name="SliderMindWipeFreq" Minimum="1" Maximum="180" Value="6"
                                            Margin="0,1" ValueChanged="SliderMindWipeFreq_Changed"
                                            ToolTip="{loc:Str tooltip_mind_wipe_frequency_1_180_per_hour}"/>

                                    <!-- Volume -->
                                    <Grid Margin="0,8,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_volume}"
                                                   ToolTip="{loc:Str tooltip_mind_wipe_audio_volume_independent_of_other_a}"/>
                                        <TextBlock x:Name="TxtMindWipeVolume" Grid.Column="1" Style="{StaticResource ValueLabel}" Text="50%"/>
                                    </Grid>
                                    <Slider x:Name="SliderMindWipeVolume" Minimum="0" Maximum="100" Value="50"
                                            Margin="0,1" ValueChanged="SliderMindWipeVolume_Changed"
                                            ToolTip="{loc:Str tooltip_volume_level_0_100}"/>

                                    <!-- Loop Toggle -->
                                    <Grid Margin="0,10,0,0">
                                        <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_loop_in_background}"
                                                   ToolTip="{loc:Str tooltip_continuously_loop_a_single_track_instead_of_r}"/>
                                        <CheckBox x:Name="ChkMindWipeLoop" Style="{StaticResource ToggleStyle}"
                                                  HorizontalAlignment="Right"
                                                  Checked="ChkMindWipeLoop_Changed" Unchecked="ChkMindWipeLoop_Changed"/>
                                    </Grid>

                                    <!-- Test Button -->
                                    <Button Content="{loc:Str btn_test_now_2}"
                                            Background="#40FFFFFF" Foreground="White"
                                            BorderThickness="0" Padding="10,5" Margin="0,10,0,0"
                                            Cursor="Hand" Click="BtnTestMindWipe_Click"
                                            ToolTip="{loc:Str tooltip_play_a_mind_wipe_sound_now}"/>
                                </StackPanel>
                            </StackPanel>
                            </Grid>

                            <!-- Feature Image -->
                            <Rectangle x:Name="MindWipeFeatureImage" Grid.Column="1" Style="{StaticResource RoundedFeatureImage}">
                                <Rectangle.Fill>
                                    <ImageBrush ImageSource="pack://application:,,,/Resources/features/Mind_Wipers.png" Stretch="UniformToFill"/>
                                </Rectangle.Fill>
                            </Rectangle>
                        </Grid>
                    </Border>

                </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Right Column: Scheduler + Ramp + Community stacked -->
            <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <!-- Scheduler Box -->
                <Border x:Name="SchedulerPanel" Style="{StaticResource SectionPanel}" Margin="5">
                    <StackPanel>
                        <Grid>
                            <TextBlock Style="{StaticResource SectionHeader}" Text="{loc:Str section_scheduler}" FontSize="14" Margin="0"/>
                            <Button x:Name="HelpBtnScheduler" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="Scheduler"/>
                        </Grid>
                        
                        <!-- Enable -->
                        <Grid Margin="0,3">
                            <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str btn_enable}" FontSize="12"/>
                            <CheckBox x:Name="ChkSchedulerEnabled" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right"/>
                        </Grid>
                        
                        <!-- Time Range -->
                        <Grid Margin="0,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TxtStartTime" Grid.Column="0" Text="16:00" Background="{DynamicResource SurfaceBgBrush}"
                                     Foreground="White" BorderBrush="{DynamicResource PinkBrush}"
                                     Padding="6,4" FontSize="12" TextAlignment="Center"
                                     Style="{StaticResource FocusGlowTextBox}"/>
                            <TextBlock Grid.Column="1" Text="→" Foreground="White" FontSize="12" Margin="5,0" VerticalAlignment="Center"/>
                            <TextBox x:Name="TxtEndTime" Grid.Column="2" Text="22:00" Background="{DynamicResource SurfaceBgBrush}"
                                     Foreground="White" BorderBrush="{DynamicResource PinkBrush}"
                                     Padding="6,4" FontSize="12" TextAlignment="Center"
                                     Style="{StaticResource FocusGlowTextBox}"/>
                        </Grid>
                        
                        <!-- Days -->
                        <WrapPanel Margin="0,3">
                            <CheckBox x:Name="ChkMon" Content="M" Foreground="White" Margin="0,0,6,0" IsChecked="True" FontSize="10"/>
                            <CheckBox x:Name="ChkTue" Content="T" Foreground="White" Margin="0,0,6,0" IsChecked="True" FontSize="10"/>
                            <CheckBox x:Name="ChkWed" Content="W" Foreground="White" Margin="0,0,6,0" IsChecked="True" FontSize="10"/>
                            <CheckBox x:Name="ChkThu" Content="T" Foreground="White" Margin="0,0,6,0" IsChecked="True" FontSize="10"/>
                            <CheckBox x:Name="ChkFri" Content="F" Foreground="White" Margin="0,0,6,0" IsChecked="True" FontSize="10"/>
                            <CheckBox x:Name="ChkSat" Content="S" Foreground="White" Margin="0,0,6,0" IsChecked="True" FontSize="10"/>
                            <CheckBox x:Name="ChkSun" Content="S" Foreground="White" Margin="0,0,0,0" IsChecked="True" FontSize="10"/>
                        </WrapPanel>
                    </StackPanel>
                </Border>
                
                <!-- Intensity Ramp Box -->
                <Border x:Name="RampPanel" Style="{StaticResource SectionPanel}" Margin="5">
                    <StackPanel>
                        <Grid>
                            <TextBlock Style="{StaticResource SectionHeader}" Text="{loc:Str section_intensity_ramp}" FontSize="14" Margin="0"/>
                            <Button x:Name="HelpBtnRamp" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="IntensityRamp"/>
                        </Grid>
                        
                        <!-- Enable Ramp -->
                        <Grid Margin="0,3">
                            <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str btn_enable}" FontSize="12"/>
                            <CheckBox x:Name="ChkRampEnabled" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right"/>
                        </Grid>
                        
                        <!-- Duration -->
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_duration}" FontSize="11"/>
                            <Slider Grid.Column="1" x:Name="SliderRampDuration" Minimum="10" Maximum="180" Value="60" 
                                    Margin="5,0" ValueChanged="SliderRampDuration_Changed"/>
                            <TextBlock x:Name="TxtRampDuration" Grid.Column="2" Style="{StaticResource ValueLabel}" Text="{loc:Str label_60_min_2}" FontSize="11"/>
                        </Grid>
                        
                        <!-- Multiplier -->
                        <Grid Margin="0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_multiplier}" FontSize="11"/>
                            <Slider Grid.Column="1" x:Name="SliderMultiplier" Minimum="1" Maximum="3" Value="1.5" 
                                    Margin="5,0" ValueChanged="SliderMultiplier_Changed"/>
                            <TextBlock x:Name="TxtMultiplier" Grid.Column="2" Style="{StaticResource ValueLabel}" Text="1.5x" FontSize="11"/>
                        </Grid>
                        
                        <!-- End at Ramp -->
                        <Grid Margin="0,2">
                            <TextBlock Style="{StaticResource SettingLabel}" Text="{loc:Str setting_end_at_complete}" FontSize="11"/>
                            <CheckBox x:Name="ChkEndAtRamp" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right"/>
                        </Grid>
                        
                        <!-- Ramp Links -->
                        <TextBlock Text="{loc:Str label_link_to_ramp}" Foreground="{DynamicResource PinkBrush}" FontSize="11" Margin="0,5,0,2"/>
                        <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="4" Padding="6,4">
                            <WrapPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,8,2">
                                    <CheckBox x:Name="ChkRampLinkFlash" Style="{StaticResource ToggleStyle}"/>
                                    <TextBlock Text="{loc:Str label_flash}" Foreground="#AAA" FontSize="10" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,8,2">
                                    <CheckBox x:Name="ChkRampLinkSpiral" Style="{StaticResource ToggleStyle}"/>
                                    <TextBlock Text="{loc:Str label_spiral}" Foreground="#AAA" FontSize="10" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,8,2">
                                    <CheckBox x:Name="ChkRampLinkPink" Style="{StaticResource ToggleStyle}"/>
                                    <TextBlock Text="{loc:Str label_pink}" Foreground="#AAA" FontSize="10" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,8,2">
                                    <CheckBox x:Name="ChkRampLinkMaster" Style="{StaticResource ToggleStyle}"/>
                                    <TextBlock Text="{loc:Str setting_master}" Foreground="#AAA" FontSize="10" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                    <CheckBox x:Name="ChkRampLinkSubAudio" Style="{StaticResource ToggleStyle}"/>
                                    <TextBlock Text="{loc:Str label_sub}" Foreground="#AAA" FontSize="10" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                </StackPanel>
                            </WrapPanel>
                        </Border>
                    </StackPanel>
                </Border>
                
                <!-- Community Discord -->
                <Border Style="{StaticResource SectionPanel}" Margin="5" Padding="10,8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,8">
                            <TextBlock Text="{loc:Str label_community}" Foreground="{DynamicResource PinkBrush}"
                                       FontSize="13" FontWeight="Bold" VerticalAlignment="Center"/>
                            <Button x:Name="HelpBtnCommunity" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Tag="Community"/>
                        </Grid>

                        <!-- Discord Join Button - rectangular -->
                        <Button x:Name="BtnDiscord" Grid.Row="1" Click="BtnDiscord_Click" Cursor="Hand"
                                ToolTip="{loc:Str tooltip_click_to_join_our_discord_server}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="discordBorder" Background="#5865F2" CornerRadius="8" Padding="12,8">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <Image Source="pack://application:,,,/Resources/discord.png"
                                                   Width="28" Height="28" Stretch="Uniform" VerticalAlignment="Center"/>
                                            <TextBlock Text="{loc:Str label_join_discord}" Foreground="White" FontSize="13" FontWeight="Bold"
                                                       VerticalAlignment="Center" Margin="10,0,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="discordBorder" Property="Background" Value="#6C75F2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <!-- Discord Rich Presence Toggle -->
                        <CheckBox x:Name="ChkDiscordRichPresence" Grid.Row="2"
                                  HorizontalAlignment="Center"
                                  Margin="0,8,0,2"
                                  Checked="ChkDiscordRichPresence_Changed"
                                  Unchecked="ChkDiscordRichPresence_Changed">
                            <CheckBox.ToolTip>
                                <ToolTip Background="{DynamicResource SurfaceBgBrush}" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1" Padding="10">
                                    <StackPanel MaxWidth="280">
                                        <TextBlock Text="{loc:Str label_discord_rich_presence}" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,6"/>
                                        <TextBlock Foreground="#CCCCCC" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,8">
                                            Shows "Playing Conditioning Control Panel" in your Discord status, along with your current level and session time.
                                        </TextBlock>
                                        <TextBlock Text="{loc:Str label_who_can_see_this}" Foreground="{DynamicResource SecondaryBrush}" FontSize="11" FontWeight="Bold" Margin="0,0,0,4"/>
                                        <TextBlock Foreground="#AAAAAA" FontSize="10" TextWrapping="Wrap">
                                            Only people who can already see your Discord activity status (friends, server members if enabled in your Discord privacy settings). This does NOT send notifications - it simply updates your "Currently Playing" status like any other game.
                                        </TextBlock>
                                    </StackPanel>
                                </ToolTip>
                            </CheckBox.ToolTip>
                            <CheckBox.Template>
                                <ControlTemplate TargetType="CheckBox">
                                    <StackPanel Orientation="Horizontal" Cursor="Hand">
                                        <Border x:Name="checkBorder" Width="14" Height="14"
                                                Background="#353550" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1.5"
                                                CornerRadius="3" VerticalAlignment="Center">
                                            <TextBlock x:Name="checkMark" Text="✓" Foreground="{DynamicResource PinkBrush}"
                                                       FontSize="10" FontWeight="Bold"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       Visibility="Collapsed" Margin="0,-1,0,0"/>
                                        </Border>
                                        <TextBlock Text="{loc:Str label_rich_presence}" Foreground="#A0A0A0" FontSize="10"
                                                   VerticalAlignment="Center" Margin="5,0,0,0"/>
                                    </StackPanel>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                            <Setter TargetName="checkBorder" Property="Background" Value="#452545"/>
                                        </Trigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="checkBorder" Property="BorderBrush" Value="{DynamicResource PinkButtonHoveredBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </CheckBox.Template>
                        </CheckBox>
                    </Grid>
                </Border>

                <!-- Language selection moved to header bar pill -->

                <!-- App Info / Updates -->
                <Border Style="{StaticResource SectionPanel}" Margin="5" Padding="10,8">
                    <Grid>
                        <Button x:Name="HelpBtnAppInfo" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,0,0" Tag="AppInfo"/>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="{loc:Str label_app_info}" Foreground="{DynamicResource PinkBrush}"
                                       FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <TextBlock x:Name="TxtVersion" Text="{loc:Str label_version}" Foreground="#888888"
                                   FontSize="12" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <Button x:Name="BtnCheckUpdates" Content="{loc:Str btn_check_updates}"
                                Click="BtnCheckUpdates_Click" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="{DynamicResource PanelBgBrush}"/>
                                    <Setter Property="Foreground" Value="#E0E0E0"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="Padding" Value="16,8"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="border" Background="{TemplateBinding Background}"
                                                        CornerRadius="6" Padding="{TemplateBinding Padding}"
                                                        BorderBrush="{DynamicResource PanelAccentBrush}" BorderThickness="1">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="#353560"/>
                                                        <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button x:Name="BtnReportBug" Content="{loc:Str btn_report_bug}"
                                Click="BtnReportBug_Click" Cursor="Hand" Margin="0,8,0,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="{DynamicResource PanelBgBrush}"/>
                                    <Setter Property="Foreground" Value="#E0E0E0"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="Padding" Value="16,8"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="border" Background="{TemplateBinding Background}"
                                                        CornerRadius="6" Padding="{TemplateBinding Padding}"
                                                        BorderBrush="{DynamicResource PanelAccentBrush}" BorderThickness="1">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="border" Property="Background" Value="#353560"/>
                                                        <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
            </ScrollViewer>
            </Grid>
        </ContentControl>

        <!-- Quests Tab -->
        <ContentControl x:Name="QuestsTab" Grid.Row="4" Margin="10,5,10,10" Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
            <Grid>
            <Border Style="{StaticResource SectionPanel}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="15">
                        <!-- Sub-tab Navigation -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                            <Button x:Name="BtnQuestSubDaily" Content="{loc:Str btn_daily_weekly}"
                                    Style="{StaticResource TabButtonActive}"
                                    Click="BtnQuestSubDaily_Click" Width="120"/>
                            <Button x:Name="BtnQuestSubRoadmap" Content="{loc:Str btn_bimbo_journal}"
                                    Style="{StaticResource TabButton}"
                                    Click="BtnQuestSubRoadmap_Click" Margin="10,0,0,0" Width="150"/>
                        </StackPanel>

                        <!-- Daily/Weekly Quests Panel -->
                        <StackPanel x:Name="DailyWeeklyPanel">
                            <!-- Season Title Banner -->
                            <Border Background="{DynamicResource DarkerBgBrush}" CornerRadius="12" Padding="20,14" Margin="0,0,0,18"
                                    BorderThickness="1" BorderBrush="#3D3D60">
                                <TextBlock x:Name="TxtSeasonTitle" Text="{loc:Str label_airhead_april}"
                                           FontSize="28" FontWeight="ExtraBold" FontStyle="Italic"
                                           HorizontalAlignment="Center" TextAlignment="Center">
                                    <TextBlock.Foreground>
                                        <LinearGradientBrush x:Name="SeasonTitleBrush" StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop x:Name="SeasonGrad1" Color="{DynamicResource PinkColor}" Offset="0.0"/>
                                            <GradientStop x:Name="SeasonGrad2" Color="#DA70D6" Offset="0.25"/>
                                            <GradientStop x:Name="SeasonGrad3" Color="{DynamicResource DarkPink}" Offset="0.5"/>
                                            <GradientStop x:Name="SeasonGrad4" Color="#DA70D6" Offset="0.75"/>
                                            <GradientStop x:Name="SeasonGrad5" Color="{DynamicResource PinkColor}" Offset="1.0"/>
                                        </LinearGradientBrush>
                                    </TextBlock.Foreground>
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
                                    </TextBlock.Effect>
                                    <!-- Season title shimmer managed in code-behind (StartSeasonTitleShimmer/StopSeasonTitleShimmer) -->
                                </TextBlock>
                            </Border>

                            <!-- Header -->
                            <Grid Margin="0,0,0,20">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{loc:Str label_quests}" Foreground="{DynamicResource PinkBrush}" FontSize="18" FontWeight="Bold"/>
                                    <Button x:Name="HelpBtnQuests" Style="{StaticResource HelpButtonStyle}" Tag="Quests"/>
                                </StackPanel>
                                <TextBlock x:Name="TxtQuestStats" Text="{loc:Str label_0_completed_this_week}"
                                           Foreground="#888888" FontSize="12" HorizontalAlignment="Right"
                                           VerticalAlignment="Center"/>
                            </Grid>

                            <!-- Two-column layout for Daily and Weekly -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- DAILY QUEST CARD -->
                            <ContentControl Grid.Column="0" Style="{StaticResource AnimatedGradientPanel}"
                                    BorderThickness="2" BorderBrush="#FFD700">
                                <StackPanel>
                                    <!-- Card Header -->
                                    <Grid Margin="0,0,0,15">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="☀️" FontSize="20"/>
                                            <TextBlock Text="{loc:Str label_daily_quest}" Foreground="#FFD700"
                                                       FontSize="16" FontWeight="Bold" Margin="8,0,0,0"
                                                       VerticalAlignment="Center"/>
                                            <!-- Daily quest counter badge -->
                                            <Border x:Name="DailyQuestCounterBadge" Background="#3D3D60" CornerRadius="8"
                                                    Padding="6,2" Margin="8,0,0,0" VerticalAlignment="Center">
                                                <TextBlock x:Name="TxtDailyQuestCounter" Text="0/3"
                                                           Foreground="#FFD700" FontSize="11" FontWeight="Bold"/>
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{loc:Str label_resets_at_midnight}" Foreground="#666666"
                                                   FontSize="10" HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"/>
                                    </Grid>

                                    <!-- Daily Progress Segments -->
                                    <Grid Margin="0,8,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="4"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border x:Name="DailySegment1" Grid.Column="0" Background="#3D3D60"
                                                CornerRadius="3" Height="6"/>
                                        <Border x:Name="DailySegment2" Grid.Column="2" Background="#3D3D60"
                                                CornerRadius="3" Height="6"/>
                                        <Border x:Name="DailySegment3" Grid.Column="4" Background="#3D3D60"
                                                CornerRadius="3" Height="6"/>
                                    </Grid>
                                    <TextBlock Text="{loc:Str label_complete_3_daily_quests_1_weekly_quest}"
                                               Foreground="#666666" FontSize="10"
                                               HorizontalAlignment="Center" Margin="0,0,0,8"/>

                                    <!-- Quest Content -->
                                    <Border x:Name="DailyQuestCard" Background="{DynamicResource PanelBgBrush}"
                                            CornerRadius="10" Padding="15" MinHeight="180">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <TextBlock x:Name="TxtDailyQuestIcon" Text=""
                                                           FontSize="24" HorizontalAlignment="Left" Visibility="Collapsed"/>
                                                <TextBlock x:Name="TxtDailyQuestName" Text="{loc:Str label_loading_2}"
                                                           Foreground="White" FontSize="16" FontWeight="Bold"
                                                           HorizontalAlignment="Left" Margin="0,0,0,5"/>
                                                <TextBlock x:Name="TxtDailyQuestDesc" Text=""
                                                           Foreground="#AAAAAA" FontSize="12"
                                                           HorizontalAlignment="Left" TextWrapping="Wrap"
                                                           MaxWidth="180"/>

                                                <!-- Progress Bar -->
                                                <Grid Margin="0,12,0,0">
                                                    <Border x:Name="DailyProgressTrack" Background="{DynamicResource DarkerBgBrush}" CornerRadius="4" Height="10">
                                                        <Border x:Name="DailyProgressFill" Background="#FFD700"
                                                                CornerRadius="4" HorizontalAlignment="Left" Width="0"/>
                                                    </Border>
                                                </Grid>
                                                <TextBlock x:Name="TxtDailyProgress" Text="0 / 0"
                                                           Foreground="#FFD700" FontSize="12" FontWeight="Bold"
                                                           HorizontalAlignment="Left" Margin="0,5,0,0"/>

                                                <!-- XP Reward -->
                                                <Border Background="#3D3D60" CornerRadius="5"
                                                        Padding="10,5" Margin="0,10,0,0"
                                                        HorizontalAlignment="Left">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock x:Name="TxtDailyXP" Text="{loc:Str label_0_xp}"
                                                                   Foreground="{DynamicResource PinkBrush}" FontSize="14"
                                                                   FontWeight="Bold"/>
                                                        <TextBlock x:Name="TxtDailyStreakBonus" Text=""
                                                                   Foreground="#FFB347" FontSize="12"
                                                                   FontWeight="Bold" Margin="6,1,0,0"
                                                                   VerticalAlignment="Center"
                                                                   FontFamily="Segoe UI Emoji"
                                                                   Visibility="Collapsed"/>
                                                        <TextBlock x:Name="TxtDailyRerollBonus" Text=""
                                                                   Foreground="#7BC8F6" FontSize="12"
                                                                   FontWeight="Bold" Margin="6,1,0,0"
                                                                   VerticalAlignment="Center"
                                                                   Visibility="Collapsed"/>
                                                    </StackPanel>
                                                </Border>
                                            </StackPanel>
                                            <!-- Quest Image -->
                                            <Border Grid.Column="1" CornerRadius="12" Margin="10,0,0,0"
                                                    Width="150" Height="150" Background="{DynamicResource DarkerBgBrush}">
                                                <Border.Clip>
                                                    <RectangleGeometry RadiusX="12" RadiusY="12" Rect="0,0,150,150"/>
                                                </Border.Clip>
                                                <Grid>
                                                    <Image x:Name="ImgDailyQuest" Stretch="UniformToFill"/>
                                                    <!-- Checkmark overlay for completed state -->
                                                    <Border x:Name="DailyCompletedOverlay" Background="#802D4A2D"
                                                            Visibility="Collapsed">
                                                        <TextBlock Text="✓" Foreground="#00E676" FontSize="64"
                                                                   FontWeight="Bold" HorizontalAlignment="Center"
                                                                   VerticalAlignment="Center">
                                                            <TextBlock.Effect>
                                                                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2"/>
                                                            </TextBlock.Effect>
                                                        </TextBlock>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <!-- All Daily Quests Completed Message -->
                                    <Border x:Name="DailyAllCompletedMessage" Background="#2D4A2D" CornerRadius="10"
                                            Padding="15" MinHeight="180" Visibility="Collapsed">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <TextBlock Text="✅" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                            <TextBlock Text="{loc:Str label_all_daily_quests_completed}" Foreground="#00E676"
                                                       FontSize="16" FontWeight="Bold" HorizontalAlignment="Center"
                                                       Margin="0,0,0,5"/>
                                            <TextBlock Text="{loc:Str label_check_back_tomorrow_for_new_quests}" Foreground="#888888"
                                                       FontSize="12" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Reroll Button -->
                                    <Button x:Name="BtnRerollDaily" Content="{loc:Str btn_reroll}"
                                            Style="{StaticResource OutlineButton}"
                                            Margin="0,15,0,0" Click="BtnRerollDaily_Click"
                                            ToolTip="{loc:Str tooltip_reroll_for_a_different_quest_once_per_day}"/>
                                </StackPanel>
                            </ContentControl>

                            <!-- WEEKLY QUEST CARD -->
                            <ContentControl Grid.Column="2" Style="{StaticResource AnimatedGradientPanel}"
                                    BorderThickness="2" BorderBrush="#9370DB">
                                <StackPanel>
                                    <!-- Card Header -->
                                    <Grid Margin="0,0,0,15">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="🌙" FontSize="20"/>
                                            <TextBlock Text="{loc:Str label_weekly_quest}" Foreground="#9370DB"
                                                       FontSize="16" FontWeight="Bold" Margin="8,0,0,0"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{loc:Str label_resets_sunday}" Foreground="#666666"
                                                   FontSize="10" HorizontalAlignment="Right"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                    <TextBlock Text="{loc:Str label_1_weekly_quest_to_complete}"
                                               Foreground="#666666" FontSize="10"
                                               HorizontalAlignment="Center" Margin="0,0,0,8"/>

                                    <!-- Quest Content -->
                                    <Border x:Name="WeeklyQuestCard" Background="{DynamicResource PanelBgBrush}"
                                            CornerRadius="10" Padding="15" MinHeight="180">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <TextBlock x:Name="TxtWeeklyQuestIcon" Text=""
                                                           FontSize="24" HorizontalAlignment="Left" Visibility="Collapsed"/>
                                                <TextBlock x:Name="TxtWeeklyQuestName" Text="{loc:Str label_loading_2}"
                                                           Foreground="White" FontSize="16" FontWeight="Bold"
                                                           HorizontalAlignment="Left" Margin="0,0,0,5"/>
                                                <TextBlock x:Name="TxtWeeklyQuestDesc" Text=""
                                                           Foreground="#AAAAAA" FontSize="12"
                                                           HorizontalAlignment="Left" TextWrapping="Wrap"
                                                           MaxWidth="180"/>

                                                <!-- Progress Bar -->
                                                <Grid Margin="0,12,0,0">
                                                    <Border x:Name="WeeklyProgressTrack" Background="{DynamicResource DarkerBgBrush}" CornerRadius="4" Height="10">
                                                        <Border x:Name="WeeklyProgressFill" Background="#9370DB"
                                                                CornerRadius="4" HorizontalAlignment="Left" Width="0"/>
                                                    </Border>
                                                </Grid>
                                                <TextBlock x:Name="TxtWeeklyProgress" Text="0 / 0"
                                                           Foreground="#9370DB" FontSize="12" FontWeight="Bold"
                                                           HorizontalAlignment="Left" Margin="0,5,0,0"/>

                                                <!-- XP Reward -->
                                                <Border Background="#3D3D60" CornerRadius="5"
                                                        Padding="10,5" Margin="0,10,0,0"
                                                        HorizontalAlignment="Left">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock x:Name="TxtWeeklyXP" Text="{loc:Str label_0_xp}"
                                                                   Foreground="{DynamicResource PinkBrush}" FontSize="14"
                                                                   FontWeight="Bold"/>
                                                        <TextBlock x:Name="TxtWeeklyStreakBonus" Text=""
                                                                   Foreground="#FFB347" FontSize="12"
                                                                   FontWeight="Bold" Margin="6,1,0,0"
                                                                   VerticalAlignment="Center"
                                                                   FontFamily="Segoe UI Emoji"
                                                                   Visibility="Collapsed"/>
                                                        <TextBlock x:Name="TxtWeeklyRerollBonus" Text=""
                                                                   Foreground="#7BC8F6" FontSize="12"
                                                                   FontWeight="Bold" Margin="6,1,0,0"
                                                                   VerticalAlignment="Center"
                                                                   Visibility="Collapsed"/>
                                                    </StackPanel>
                                                </Border>
                                            </StackPanel>
                                            <!-- Quest Image -->
                                            <Border Grid.Column="1" CornerRadius="12" Margin="10,0,0,0"
                                                    Width="150" Height="150" Background="{DynamicResource DarkerBgBrush}">
                                                <Border.Clip>
                                                    <RectangleGeometry RadiusX="12" RadiusY="12" Rect="0,0,150,150"/>
                                                </Border.Clip>
                                                <Grid>
                                                    <Image x:Name="ImgWeeklyQuest" Stretch="UniformToFill"/>
                                                    <!-- Checkmark overlay for completed state -->
                                                    <Border x:Name="WeeklyCompletedOverlay" Background="#802D4A2D"
                                                            Visibility="Collapsed">
                                                        <TextBlock Text="✓" Foreground="#00E676" FontSize="64"
                                                                   FontWeight="Bold" HorizontalAlignment="Center"
                                                                   VerticalAlignment="Center">
                                                            <TextBlock.Effect>
                                                                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="2"/>
                                                            </TextBlock.Effect>
                                                        </TextBlock>
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <!-- Reroll Button -->
                                    <Button x:Name="BtnRerollWeekly" Content="{loc:Str btn_reroll}"
                                            Style="{StaticResource OutlineButton}"
                                            Margin="0,15,0,0" Click="BtnRerollWeekly_Click"
                                            ToolTip="{loc:Str tooltip_reroll_for_a_different_quest_once_per_week}"/>
                                </StackPanel>
                            </ContentControl>
                        </Grid>

                            <!-- Quest Complete Banner (shown briefly when quest completes) -->
                            <Border x:Name="QuestCompleteBanner" Background="#2D4A2D" CornerRadius="10"
                                    Padding="15" Margin="0,20,0,0" Visibility="Collapsed">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="✨" FontSize="24"/>
                                    <TextBlock x:Name="TxtQuestComplete" Text="{loc:Str label_quest_complete}"
                                               Foreground="#00E676" FontSize="18" FontWeight="Bold"
                                               Margin="10,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="✨" FontSize="24"/>
                                </StackPanel>
                            </Border>

                            <!-- Quest Streak Calendar -->
                            <Border Background="{DynamicResource DarkerBgBrush}" CornerRadius="10" Padding="15,12" Margin="0,20,0,0">
                                <StackPanel>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{loc:Str label_quest_streak}" Foreground="White" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="TxtQuestStreakCount" Grid.Column="1" Text="" Foreground="#FFD700"
                                                   FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <Button x:Name="BtnFixStreak" Grid.Column="2" Content="{loc:Str btn_fix_day}"
                                                Style="{StaticResource OutlineButton}" Padding="8,3" FontSize="11"
                                                Click="BtnFixStreak_Click" Visibility="Collapsed" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Canvas x:Name="StreakCalendarCanvas" Height="50" ClipToBounds="True"
                                           SizeChanged="StreakCalendarCanvas_SizeChanged"/>
                                    <TextBlock x:Name="TxtFixStreakStatus" Text="" Foreground="{DynamicResource PinkBrush}" FontSize="11"
                                               TextAlignment="Center" Margin="0,5,0,0" Visibility="Collapsed"/>
                                </StackPanel>
                            </Border>

                            <!-- Quest Statistics Section -->
                            <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="10" Padding="15" Margin="0,20,0,0">
                                <StackPanel>
                                    <Grid Margin="0,0,0,15">
                                        <TextBlock Text="{loc:Str label_quest_statistics}" Foreground="White"
                                                   FontSize="14" FontWeight="Bold"/>
                                        <Button x:Name="HelpBtnQuestStats" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="QuestStats"/>
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                            <TextBlock x:Name="TxtTotalDailyCompleted" Text="0"
                                                       Foreground="#FFD700" FontSize="28" FontWeight="Bold"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="{loc:Str label_daily_completed}" Foreground="#888888"
                                                       FontSize="11" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                            <TextBlock x:Name="TxtTotalWeeklyCompleted" Text="0"
                                                       Foreground="#9370DB" FontSize="28" FontWeight="Bold"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="{loc:Str label_weekly_completed}" Foreground="#888888"
                                                       FontSize="11" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                            <TextBlock x:Name="TxtTotalQuestXP" Text="0"
                                                       Foreground="{DynamicResource PinkBrush}" FontSize="28" FontWeight="Bold"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="{loc:Str label_total_xp_earned}" Foreground="#888888"
                                                       FontSize="11" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                        <!-- End Daily/Weekly Panel -->

                        <!-- Bimbo Journal Panel -->
                        <StackPanel x:Name="RoadmapPanel" Visibility="Collapsed">
                            <!-- Journal Header -->
                            <TextBlock Text="{loc:Str label_look_how_far_you_ve_come_sweetie}"
                                       FontSize="26" FontStyle="Italic" FontWeight="Light"
                                       Foreground="#CC88AACC" HorizontalAlignment="Center"
                                       Margin="0,0,0,8" TextAlignment="Center"/>

                            <!-- Journal Info Box -->
                            <Border Background="#30FFD700" BorderBrush="#AAFFD700" BorderThickness="1.5"
                                    CornerRadius="6" Padding="14,10" Margin="20,0,20,14" MaxWidth="700"
                                    HorizontalAlignment="Center">
                                <StackPanel>
                                    <TextBlock Text="{loc:Str label_what_is_the_bimbo_journal}" Foreground="#FFD700" FontSize="14" FontWeight="Bold" Margin="0,0,0,6"/>
                                    <TextBlock TextWrapping="Wrap" Foreground="#DDFFFFFF" FontSize="12" LineHeight="18">
                                        <Run Text="{loc:Str label_each_track_has_steps_that_ask_you_to_take_a_p}"/>
                                        <LineBreak/>
                                        <Run Text="{loc:Str label_click_a_step_take_the_pic_and_upload_it_to_ma}"/>
                                        <LineBreak/>
                                        <Run Text="{loc:Str label_complete_all_steps_in_a_track_to_unlock_the_n}"/>
                                    </TextBlock>
                                    <TextBlock Foreground="#AAFFD700" FontSize="11" FontStyle="Italic" Margin="0,8,0,0"
                                               Text="{loc:Str label_all_photos_are_stored_locally_on_your_pc_only}"/>
                                </StackPanel>
                            </Border>

                            <!-- Track Selector Tabs -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <Button x:Name="BtnTrack1" Content="{loc:Str btn_the_empty_doll}"
                                        Style="{StaticResource TabButtonActive}"
                                        Click="BtnTrack_Click" Tag="EmptyDoll" Width="130"/>
                                <Button x:Name="BtnTrack2" Content="{loc:Str btn_the_obedient_puppet}"
                                        Style="{StaticResource TabButton}"
                                        Click="BtnTrack_Click" Tag="ObedientPuppet" Margin="10,0,0,0" Width="150"/>
                                <Button x:Name="BtnTrack3" Content="{loc:Str btn_the_slutty_blowdoll}"
                                        Style="{StaticResource TabButton}"
                                        Click="BtnTrack_Click" Tag="SluttyBlowdoll" Margin="10,0,0,0" Width="150"/>
                            </StackPanel>

                            <!-- Track Info Header -->
                            <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="10" Padding="15" Margin="0,0,0,15">
                                <Grid>
                                    <Button x:Name="HelpBtnRoadmap" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Top" Tag="Roadmap"/>
                                    <StackPanel>
                                        <TextBlock x:Name="TxtRoadmapTrackName" Text="{loc:Str btn_the_empty_doll}"
                                                   Foreground="{DynamicResource PinkBrush}" FontSize="20" FontWeight="Bold"/>
                                        <TextBlock x:Name="TxtRoadmapTrackSubtitle" Text="{loc:Str label_gateway_phase}"
                                                   Foreground="#888888" FontSize="14"/>
                                        <TextBlock x:Name="TxtRoadmapTrackProgress" Text="{loc:Str label_0_7_steps_completed}"
                                                   Foreground="#AAAAAA" FontSize="12" Margin="0,5,0,0"/>
                                    </StackPanel>
                                    <!-- Badge indicator for final track -->
                                    <StackPanel x:Name="BadgeIndicator" Orientation="Horizontal"
                                                HorizontalAlignment="Right" VerticalAlignment="Center"
                                                Visibility="Collapsed">
                                        <TextBlock Text="🏆" FontSize="24"/>
                                        <TextBlock Text="{loc:Str label_badge_earned}" Foreground="#FFD700" FontSize="14"
                                                   FontWeight="Bold" VerticalAlignment="Center" Margin="5,0,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Locked Track Overlay -->
                            <Border x:Name="TrackLockedOverlay" Background="#CC1A1A2E" CornerRadius="10"
                                    Padding="40" Visibility="Collapsed">
                                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="🔒" FontSize="48" HorizontalAlignment="Center"/>
                                    <TextBlock x:Name="TxtLockReason" Text="{loc:Str label_complete_the_previous_track_to_unlock}"
                                               Foreground="White" FontSize="16" Margin="0,15,0,0"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <!-- Horizontal Scrolling Roadmap -->
                            <ContentControl x:Name="RoadmapScrollContainer" Style="{StaticResource AnimatedGradientPanelWide}"
                                    MinHeight="220">
                                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled"
                                              PreviewMouseWheel="HorizontalScrollViewer_PreviewMouseWheel">
                                    <StackPanel x:Name="RoadmapNodesPanel" Orientation="Horizontal" Margin="10,15"/>
                                </ScrollViewer>
                            </ContentControl>

                            <!-- Roadmap Statistics -->
                            <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="10" Padding="15" Margin="0,15,0,0">
                                <StackPanel>
                                    <Grid Margin="0,0,0,15">
                                        <TextBlock Text="{loc:Str label_transformation_progress}" Foreground="White"
                                                   FontSize="14" FontWeight="Bold"/>
                                        <Button x:Name="HelpBtnRoadmapStats" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="RoadmapStats"/>
                                    </Grid>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                            <TextBlock x:Name="TxtRoadmapTotalSteps" Text="0 / 21"
                                                       Foreground="{DynamicResource PinkBrush}" FontSize="24" FontWeight="Bold"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="{loc:Str label_steps_completed}" Foreground="#888888"
                                                       FontSize="11" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                            <TextBlock x:Name="TxtRoadmapPhotos" Text="0"
                                                       Foreground="#9370DB" FontSize="24" FontWeight="Bold"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="{loc:Str label_photos_submitted}" Foreground="#888888"
                                                       FontSize="11" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                            <TextBlock x:Name="TxtRoadmapJourneyDays" Text="--"
                                                       Foreground="#FFD700" FontSize="24" FontWeight="Bold"
                                                       HorizontalAlignment="Center"/>
                                            <TextBlock Text="{loc:Str label_days_on_journey}" Foreground="#888888"
                                                       FontSize="11" HorizontalAlignment="Center"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                        <!-- End Roadmap Panel -->
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Login Required Overlay -->
            <Border x:Name="QuestsLoginOverlay" Background="#E6151528" CornerRadius="12" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="40">
                    <TextBlock Text="🔒" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                    <TextBlock Text="{loc:Str label_login_required}" Foreground="{DynamicResource PinkBrush}" FontSize="22" FontWeight="Bold"
                               HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="{loc:Str label_log_in_with_discord_or_patreon_to_access_ques}"
                               Foreground="#AA99AA" FontSize="14" HorizontalAlignment="Center"
                               TextAlignment="Center" TextWrapping="Wrap" MaxWidth="350"/>
                </StackPanel>
            </Border>
            </Grid>
        </ContentControl>

        <!-- Achievements Tab -->
        <ContentControl x:Name="AchievementsTab" Grid.Row="4" Margin="10,5,10,10" Visibility="Collapsed"
                Template="{StaticResource AnimatedTabBackground}">
            <Border Style="{StaticResource SectionPanel}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <Grid Margin="0,0,0,10">
                        <TextBlock Style="{StaticResource SectionHeader}" Text="{loc:Str section_achievements}" Margin="0"/>
                        <Button x:Name="HelpBtnAchievements" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="Achievements"/>
                    </Grid>
                    <TextBlock Text="{loc:Str label_complete_challenges_to_unlock_achievements_lo}" 
                               Foreground="#888888" FontSize="12" Margin="0,0,0,15"/>
                    
                    <!-- Achievement Grid (populated in code-behind) -->
                    <UniformGrid x:Name="AchievementGrid" Columns="7" Margin="0,5">
                    </UniformGrid>
                    
                    <TextBlock x:Name="TxtAchievementCount" Text="{loc:Str label_0_25_achievements_unlocked}" Foreground="#888888" FontSize="14"
                               HorizontalAlignment="Center" Margin="0,15,0,0"/>
                </StackPanel>
            </ScrollViewer>
            </Border>
        </ContentControl>

        <!-- Companion Tab (v5.5 - Revamped with outlined sections) -->
        <ContentControl x:Name="CompanionTab" Grid.Row="4" Margin="10,5,10,10" Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
            <Border Style="{StaticResource SectionPanel}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="15">

                        <!-- Tab Header -->
                        <Grid Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="{loc:Str tab_companion}" Foreground="White" FontSize="22" FontWeight="Bold">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="#9370DB" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text="{loc:Str label_manage_your_ai_companion_personalities_and_co}" Foreground="{DynamicResource TextDimBrush}" FontSize="11" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Grid>

                        <!-- AI Features Section (free for all users, requires login) -->
                        <Grid Margin="0,0,0,15">
                        <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="12" Padding="15">
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{DynamicResource PinkColor}" Offset="0"/>
                                    <GradientStop Color="{DynamicResource PatreonPurple}" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <Border.BorderThickness>
                                <Thickness>1</Thickness>
                            </Border.BorderThickness>
                            <Border.Effect>
                                <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.2"/>
                            </Border.Effect>
                            <StackPanel>
                                <Grid Margin="0,0,0,10">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{loc:Str label_ai_features}" Foreground="{DynamicResource PinkBrush}" FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                                        <Border Background="#2A1A3A" CornerRadius="4" Padding="6,2" Margin="8,0,0,0" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1">
                                            <TextBlock Text="{loc:Str label_free_for_all_users}" Foreground="{DynamicResource PinkBrush}" FontSize="9"/>
                                        </Border>
                                    </StackPanel>
                                    <Button x:Name="HelpBtnAiChat" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Tag="AiChat" Panel.ZIndex="1"/>
                                </Grid>

                                <!-- AI Status -->
                                <TextBlock x:Name="TxtAiStatus" Text="{loc:Str label_ai_initializing}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,0,0,10"/>

                                <!-- AI Chat Toggle -->
                                <Grid Margin="0,0,0,8">
                                    <StackPanel>
                                        <TextBlock Text="{loc:Str label_enable_ai_chat}" Foreground="White" FontSize="12"/>
                                        <TextBlock Text="{loc:Str label_double_click_avatar_to_chat}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10"/>
                                    </StackPanel>
                                    <CheckBox x:Name="ChkAiChat" Style="{StaticResource ToggleStyle}"
                                              HorizontalAlignment="Right" VerticalAlignment="Center"
                                              IsChecked="True"
                                              Checked="ChkAiChat_Changed" Unchecked="ChkAiChat_Changed"/>
                                </Grid>

                                <!-- Window Awareness -->
                                <Border BorderBrush="#353550" BorderThickness="0,1,0,0" Margin="0,4,0,0" Padding="0,10,0,0">
                                    <StackPanel>
                                        <Grid>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{loc:Str label_window_awareness}" Foreground="White" FontWeight="Bold" FontSize="13"/>
                                                <Button x:Name="HelpBtnAwareness" Style="{StaticResource HelpButtonStyle}" VerticalAlignment="Center" Margin="8,0,0,0" Tag="WindowAwareness" Panel.ZIndex="1"/>
                                            </StackPanel>
                                        </Grid>

                                        <!-- Awareness Mode Toggle -->
                                        <Grid Margin="0,8,0,0">
                                            <StackPanel>
                                                <TextBlock Text="{loc:Str label_enable_awareness}" Foreground="White" FontSize="12"/>
                                                <TextBlock Text="{loc:Str label_reacts_to_your_activity}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10"/>
                                            </StackPanel>
                                            <CheckBox x:Name="ChkAwarenessMode" Style="{StaticResource ToggleStyle}"
                                                      HorizontalAlignment="Right" VerticalAlignment="Center"
                                                      IsChecked="False"
                                                      Checked="ChkAwarenessMode_Changed" Unchecked="ChkAwarenessMode_Changed"/>
                                        </Grid>

                                        <!-- Privacy Warning -->
                                        <Border Background="#2A1A1A" CornerRadius="6" Padding="8" Margin="0,8,0,0" BorderBrush="#FF6B6B" BorderThickness="1">
                                            <StackPanel>
                                                <Grid>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="⚠️" FontSize="12" VerticalAlignment="Center"/>
                                                        <TextBlock Text="{loc:Str label_privacy_notice}" Foreground="#FF6B6B" FontSize="11" FontWeight="Bold" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                    <Button x:Name="BtnPrivacySpoiler" Content="{loc:Str btn_click_to_reveal}"
                                                            Background="Transparent" Foreground="{DynamicResource TextMutedBrush}" BorderThickness="0"
                                                            HorizontalAlignment="Right" FontSize="10" Cursor="Hand"
                                                            Click="BtnPrivacySpoiler_Click"/>
                                                </Grid>
                                                <TextBlock x:Name="TxtPrivacyDetails" Visibility="Collapsed"
                                                           TextWrapping="Wrap" Foreground="#CCCCCC" FontSize="10" Margin="0,8,0,0"
                                                           Text="{loc:Str label_this_feature_reads_the_name_of_the_active_win}"/>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </Border>

                                <!-- Awareness Settings (shown when enabled) -->
                                <Border x:Name="AwarenessSettingsPanel" Background="{DynamicResource SurfaceBgBrush}" CornerRadius="6" Padding="10" Margin="0,8,0,0" Visibility="Collapsed">
                                    <StackPanel>
                                        <TextBlock Text="{loc:Str label_awareness_settings}" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold" Margin="0,0,0,8"/>
                                        <Grid>
                                            <TextBlock Text="{loc:Str label_reaction_cooldown}" Foreground="White" FontSize="11"/>
                                            <TextBlock x:Name="TxtAwarenessCooldown" Text="{loc:Str label_90s}" Foreground="{DynamicResource PinkBrush}"
                                                       HorizontalAlignment="Right" FontSize="11" FontWeight="Bold"/>
                                        </Grid>
                                        <Slider x:Name="SliderAwarenessCooldown"
                                                Style="{StaticResource PinkSlider}"
                                                Minimum="10" Maximum="300" Value="90"
                                                Margin="0,6,0,0"
                                                ValueChanged="SliderAwarenessCooldown_ValueChanged"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- AI Features Lock Overlay (shown when not logged in) -->
                        <Border x:Name="AiFeaturesLockOverlay" Background="#CC1A1A2E" CornerRadius="12" Panel.ZIndex="100">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,15">
                                <TextBlock Text="🔒" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock Text="{loc:Str label_login_required}" Foreground="{DynamicResource PinkBrush}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="15" ShadowDepth="0" Opacity="0.5"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text="{loc:Str label_login_with_discord_or_patreon_to_use_ai_featu}"
                                           Foreground="#AAAAAA" FontSize="12" HorizontalAlignment="Center" Margin="0,6,0,0" TextWrapping="Wrap" TextAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        </Grid>

                        <!-- ROW 1: Companions + Community Prompts -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- COMPANIONS BOX -->
                            <Border Grid.Column="0" CornerRadius="12" Padding="15"
                                    Background="{DynamicResource ElevatedSurfaceBrush}"
                                    BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <Grid Margin="0,0,0,12">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{loc:Str label_companions}" Foreground="{DynamicResource PinkBrush}" FontSize="14" FontWeight="Bold"/>
                                            <Border Background="#FF6B6B" CornerRadius="3" Padding="5,1" Margin="8,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="{loc:Str label_beta}" Foreground="White" FontSize="9" FontWeight="Bold"/>
                                            </Border>
                                        </StackPanel>
                                        <Button x:Name="HelpBtnCompanions" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="Companions"/>
                                    </Grid>

                                    <!-- Companion Cards 3x2 -->
                                    <UniformGrid Rows="3" Columns="2" Margin="0,0,0,12">
                                        <Border x:Name="CompanionCard0" CornerRadius="8" Margin="0,0,5,5"
                                                Padding="12,10" Cursor="Hand" MouseLeftButtonDown="CompanionCard_Click" Tag="0"
                                                BorderThickness="2" BorderBrush="Transparent">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#1A1A32" Offset="0"/>
                                                    <GradientStop Color="#201A38" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <StackPanel>
                                                <Grid>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock x:Name="TxtCompanion0Name" Text="{loc:Str label_synthetic_blowdoll}" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="SemiBold"/>
                                                        <TextBlock x:Name="TxtCompanion0Lock" Text=" [50]" Foreground="#FFD700" FontSize="9"
                                                                   VerticalAlignment="Center" Visibility="Collapsed"/>
                                                    </StackPanel>
                                                    <TextBlock x:Name="TxtCompanion0Level" Text="{loc:Str label_lv_1}" Foreground="#AAAAAA" FontSize="12"
                                                               HorizontalAlignment="Right"/>
                                                </Grid>
                                                <TextBlock Text="{loc:Str label_pink_filter_xp_bonus}" Foreground="{DynamicResource TextDimBrush}" FontSize="10" Margin="0,4,0,0"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <Button x:Name="BtnCompanion0Personality" Content="🎭" FontSize="10" Padding="4,1"
                                                            Background="{DynamicResource DarkerBgBrush}" Foreground="#888" BorderThickness="0" Cursor="Hand"
                                                            PreviewMouseLeftButtonDown="BtnCompanionPersonality_Click" Tag="0" ToolTip="{loc:Str tooltip_assign_ai_personality}"/>
                                                    <TextBlock x:Name="TxtCompanion0Prompt" Text="" Foreground="#555" FontSize="9"
                                                               VerticalAlignment="Center" Margin="4,0,0,0" TextTrimming="CharacterEllipsis"
                                                               MaxWidth="80"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="CompanionCard1" CornerRadius="8" Margin="5,0,0,5"
                                                Padding="12,10" Cursor="Hand" MouseLeftButtonDown="CompanionCard_Click" Tag="1"
                                                BorderThickness="2" BorderBrush="Transparent">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#1A1A32" Offset="0"/>
                                                    <GradientStop Color="#201A38" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <StackPanel>
                                                <Grid>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock x:Name="TxtCompanion1Name" Text="{loc:Str label_perfect_fuckpuppet}" Foreground="#9370DB" FontSize="12" FontWeight="SemiBold"/>
                                                        <TextBlock x:Name="TxtCompanion1Lock" Text=" [100]" Foreground="#FFD700" FontSize="9"
                                                                   VerticalAlignment="Center" Visibility="Collapsed"/>
                                                    </StackPanel>
                                                    <TextBlock x:Name="TxtCompanion1Level" Text="{loc:Str label_lv_1}" Foreground="#AAAAAA" FontSize="12"
                                                               HorizontalAlignment="Right"/>
                                                </Grid>
                                                <TextBlock Text="{loc:Str label_autonomy_xp_bonus}" Foreground="{DynamicResource TextDimBrush}" FontSize="10" Margin="0,4,0,0"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <Button x:Name="BtnCompanion1Personality" Content="🎭" FontSize="10" Padding="4,1"
                                                            Background="{DynamicResource DarkerBgBrush}" Foreground="#888" BorderThickness="0" Cursor="Hand"
                                                            PreviewMouseLeftButtonDown="BtnCompanionPersonality_Click" Tag="1" ToolTip="{loc:Str tooltip_assign_ai_personality}"/>
                                                    <TextBlock x:Name="TxtCompanion1Prompt" Text="" Foreground="#555" FontSize="9"
                                                               VerticalAlignment="Center" Margin="4,0,0,0" TextTrimming="CharacterEllipsis"
                                                               MaxWidth="80"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="CompanionCard2" CornerRadius="8" Margin="0,5,5,0"
                                                Padding="12,10" Cursor="Hand" MouseLeftButtonDown="CompanionCard_Click" Tag="2"
                                                BorderThickness="2" BorderBrush="Transparent">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#1A1A32" Offset="0"/>
                                                    <GradientStop Color="#201A38" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <StackPanel>
                                                <Grid>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock x:Name="TxtCompanion2Name" Text="{loc:Str label_brainwashed_slavedoll}" Foreground="#50C878" FontSize="12" FontWeight="SemiBold"/>
                                                        <TextBlock x:Name="TxtCompanion2Lock" Text=" [125]" Foreground="#FFD700" FontSize="9"
                                                                   VerticalAlignment="Center" Visibility="Collapsed"/>
                                                    </StackPanel>
                                                    <TextBlock x:Name="TxtCompanion2Level" Text="{loc:Str label_lv_1}" Foreground="#AAAAAA" FontSize="12"
                                                               HorizontalAlignment="Right"/>
                                                </Grid>
                                                <TextBlock Text="{loc:Str label_constant_xp_drain}" Foreground="{DynamicResource TextDimBrush}" FontSize="10" Margin="0,4,0,0"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <Button x:Name="BtnCompanion2Personality" Content="🎭" FontSize="10" Padding="4,1"
                                                            Background="{DynamicResource DarkerBgBrush}" Foreground="#888" BorderThickness="0" Cursor="Hand"
                                                            PreviewMouseLeftButtonDown="BtnCompanionPersonality_Click" Tag="2" ToolTip="{loc:Str tooltip_assign_ai_personality}"/>
                                                    <TextBlock x:Name="TxtCompanion2Prompt" Text="" Foreground="#555" FontSize="9"
                                                               VerticalAlignment="Center" Margin="4,0,0,0" TextTrimming="CharacterEllipsis"
                                                               MaxWidth="80"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="CompanionCard3" CornerRadius="8" Margin="5,5,0,0"
                                                Padding="12,10" Cursor="Hand" MouseLeftButtonDown="CompanionCard_Click" Tag="3"
                                                BorderThickness="2" BorderBrush="Transparent">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#1A1A32" Offset="0"/>
                                                    <GradientStop Color="#201A38" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <StackPanel>
                                                <Grid>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock x:Name="TxtCompanion3Name" Text="{loc:Str label_platinum_puppet}" Foreground="#FF6B6B" FontSize="12" FontWeight="SemiBold"/>
                                                        <TextBlock x:Name="TxtCompanion3Lock" Text=" [150]" Foreground="#FFD700" FontSize="9"
                                                                   VerticalAlignment="Center" Visibility="Collapsed"/>
                                                    </StackPanel>
                                                    <TextBlock x:Name="TxtCompanion3Level" Text="{loc:Str label_lv_1}" Foreground="#AAAAAA" FontSize="12"
                                                               HorizontalAlignment="Right"/>
                                                </Grid>
                                                <TextBlock Text="{loc:Str label_strict_mode_bonuses}" Foreground="{DynamicResource TextDimBrush}" FontSize="10" Margin="0,4,0,0"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <Button x:Name="BtnCompanion3Personality" Content="🎭" FontSize="10" Padding="4,1"
                                                            Background="{DynamicResource DarkerBgBrush}" Foreground="#888" BorderThickness="0" Cursor="Hand"
                                                            PreviewMouseLeftButtonDown="BtnCompanionPersonality_Click" Tag="3" ToolTip="{loc:Str tooltip_assign_ai_personality}"/>
                                                    <TextBlock x:Name="TxtCompanion3Prompt" Text="" Foreground="#555" FontSize="9"
                                                               VerticalAlignment="Center" Margin="4,0,0,0" TextTrimming="CharacterEllipsis"
                                                               MaxWidth="80"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                        <Border x:Name="CompanionCard4" CornerRadius="8" Margin="0,5,5,0"
                                                Padding="12,10" Cursor="Hand" MouseLeftButtonDown="CompanionCard_Click" Tag="4"
                                                BorderThickness="2" BorderBrush="Transparent">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                                    <GradientStop Color="#1A1A32" Offset="0"/>
                                                    <GradientStop Color="#201A38" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <StackPanel>
                                                <Grid>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock x:Name="TxtCompanion4Name" Text="{loc:Str label_bambi_cow}" Foreground="#F5DEB3" FontSize="12" FontWeight="SemiBold"/>
                                                        <TextBlock x:Name="TxtCompanion4Lock" Text=" [75]" Foreground="#FFD700" FontSize="9"
                                                                   VerticalAlignment="Center" Visibility="Collapsed"/>
                                                    </StackPanel>
                                                    <TextBlock x:Name="TxtCompanion4Level" Text="{loc:Str label_lv_1}" Foreground="#AAAAAA" FontSize="12"
                                                               HorizontalAlignment="Right"/>
                                                </Grid>
                                                <TextBlock Text="{loc:Str label_session_completion_bonus}" Foreground="{DynamicResource TextDimBrush}" FontSize="10" Margin="0,4,0,0"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <Button x:Name="BtnCompanion4Personality" Content="🎭" FontSize="10" Padding="4,1"
                                                            Background="{DynamicResource DarkerBgBrush}" Foreground="#888" BorderThickness="0" Cursor="Hand"
                                                            PreviewMouseLeftButtonDown="BtnCompanionPersonality_Click" Tag="4" ToolTip="{loc:Str tooltip_assign_ai_personality}"/>
                                                    <TextBlock x:Name="TxtCompanion4Prompt" Text="" Foreground="#555" FontSize="9"
                                                               VerticalAlignment="Center" Margin="4,0,0,0" TextTrimming="CharacterEllipsis"
                                                               MaxWidth="80"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </UniformGrid>

                                    <!-- Active Companion Info -->
                                    <Border CornerRadius="8" Padding="12" BorderBrush="{DynamicResource TransparentPink20Brush}" BorderThickness="1">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                <GradientStop Color="#16162B" Offset="0"/>
                                                <GradientStop Color="#1C1635" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <StackPanel>
                                            <Grid>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock x:Name="TxtActiveCompanionName" Text="{loc:Str label_bambi_sprite}" Foreground="White"
                                                               FontSize="14" FontWeight="Bold"/>
                                                    <TextBlock x:Name="TxtActiveCompanionLevel" Text="{loc:Str label_level_1}" Foreground="#888"
                                                               FontSize="14"/>
                                                </StackPanel>
                                                <TextBlock x:Name="TxtActiveCompanionXP" Text="{loc:Str label_0_550_xp}" Foreground="{DynamicResource PinkBrush}"
                                                           FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                            </Grid>
                                            <ProgressBar x:Name="PrgCompanion0" Height="6" Margin="0,8,0,0" Value="0"
                                                         Background="{DynamicResource SurfaceBgBrush}" Foreground="{DynamicResource PinkBrush}"/>
                                            <TextBlock x:Name="TxtActiveCompanionDesc" Text="{loc:Str label_gains_bonus_xp_based_on_pink_filter_intensity}"
                                                       Foreground="{DynamicResource TextDimBrush}" FontSize="11" Margin="0,8,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <TextBlock x:Name="TxtCompanionStatus" Text="" Visibility="Collapsed"/>
                                </StackPanel>
                            </Border>

                            <!-- COMMUNITY PROMPTS BOX -->
                            <Border Grid.Column="2" CornerRadius="12" Padding="15"
                                    Background="{DynamicResource ElevatedSurfaceBrush}"
                                    BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <Grid Margin="0,0,0,12">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{loc:Str label_community_prompts}" Foreground="#9370DB" FontSize="14" FontWeight="Bold"/>
                                            <Border Background="#FF6B6B" CornerRadius="3" Padding="5,1" Margin="8,0,0,0" VerticalAlignment="Center">
                                                <TextBlock Text="{loc:Str label_beta}" Foreground="White" FontSize="9" FontWeight="Bold"/>
                                            </Border>
                                            <Button x:Name="HelpBtnPrompts" Style="{StaticResource HelpButtonStyle}" VerticalAlignment="Center" Margin="8,0,0,0" Tag="CommunityPrompts"/>
                                        </StackPanel>
                                        <Button x:Name="BtnRefreshPrompts" Content="{loc:Str btn_refresh}" HorizontalAlignment="Right"
                                                Background="Transparent" Foreground="{DynamicResource TextMutedBrush}" BorderThickness="0"
                                                FontSize="11" Padding="8,2" Cursor="Hand" Click="BtnRefreshPrompts_Click"/>
                                    </Grid>

                                    <!-- Active Prompt -->
                                    <Border CornerRadius="8" Padding="12" Margin="0,0,0,10" BorderBrush="#339370DB" BorderThickness="1">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                <GradientStop Color="#16162B" Offset="0"/>
                                                <GradientStop Color="#1C1635" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <Grid>
                                            <StackPanel>
                                                <TextBlock Text="{loc:Str label_active_personality}" Foreground="{DynamicResource TextDimBrush}" FontSize="11"/>
                                                <TextBlock x:Name="TxtActivePromptName" Text="{loc:Str label_default_built_in}" Foreground="White"
                                                           FontSize="14" FontWeight="SemiBold" Margin="0,4,0,0"/>
                                            </StackPanel>
                                            <Button x:Name="BtnDeactivatePrompt" Content="{loc:Str btn_reset}" HorizontalAlignment="Right"
                                                    VerticalAlignment="Center"
                                                    Background="{DynamicResource SurfaceBgBrush}" Foreground="{DynamicResource TextMutedBrush}" BorderThickness="0"
                                                    FontSize="11" Padding="10,4" Cursor="Hand" Visibility="Collapsed"
                                                    Click="BtnDeactivatePrompt_Click"/>
                                        </Grid>
                                    </Border>

                                    <!-- Installed List -->
                                    <TextBlock Text="{loc:Str label_installed_prompts}" Foreground="{DynamicResource TextDimBrush}" FontSize="11" Margin="0,0,0,6"/>
                                    <Border CornerRadius="8" Padding="10" MinHeight="80" MaxHeight="120"
                                            Margin="0,0,0,12" BorderBrush="#22808080" BorderThickness="1">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                <GradientStop Color="#16162B" Offset="0"/>
                                                <GradientStop Color="#1A1832" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                                            <StackPanel x:Name="InstalledPromptsPanel">
                                                <TextBlock x:Name="TxtNoInstalledPrompts" Text="{loc:Str label_no_prompts_installed_yet}"
                                                           Foreground="#505050" FontSize="12" FontStyle="Italic"
                                                           HorizontalAlignment="Center" Margin="0,20,0,0"/>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </Border>

                                    <!-- Buttons -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Button x:Name="BtnBrowsePrompts" Grid.Column="0" Content="{loc:Str btn_browse}"
                                                Background="{DynamicResource SurfaceBgBrush}" Foreground="White" BorderBrush="#9370DB" BorderThickness="1"
                                                FontSize="12" Padding="0,8" Margin="0,0,4,0" Cursor="Hand" Click="BtnBrowsePrompts_Click"/>
                                        <Button x:Name="BtnImportPrompt" Grid.Column="1" Content="{loc:Str btn_import}"
                                                Background="{DynamicResource SurfaceBgBrush}" Foreground="White" BorderBrush="#505050" BorderThickness="1"
                                                FontSize="12" Padding="0,8" Margin="4,0,4,0" Cursor="Hand" Click="BtnImportPrompt_Click"/>
                                        <Button x:Name="BtnExportPrompt" Grid.Column="2" Content="{loc:Str btn_export}"
                                                Background="{DynamicResource SurfaceBgBrush}" Foreground="White" BorderBrush="#505050" BorderThickness="1"
                                                FontSize="12" Padding="0,8" Margin="4,0,0,0" Cursor="Hand" Click="BtnExportPrompt_Click"/>
                                    </Grid>

                                    <!-- Hypnotube Links Section -->
                                    <TextBlock Text="{loc:Str label_hypnotube_links}" FontWeight="Bold" FontSize="14"
                                               Foreground="{DynamicResource PinkBrush}" Margin="0,20,0,5"/>
                                    <TextBlock Text="{loc:Str label_video_links_for_ai_to_suggest_comma_separated}"
                                               FontSize="11" Foreground="#888" Margin="0,0,0,5" TextWrapping="Wrap"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                        <TextBlock Text="{loc:Str label_current_mode}" FontSize="11" Foreground="#AAA"/>
                                        <TextBlock x:Name="TxtHypnotubeModeLabel" Text="{loc:Str label_bambi_sleep}"
                                                   FontSize="11" Foreground="{DynamicResource PinkBrush}"/>
                                    </StackPanel>
                                    <TextBox x:Name="TxtHypnotubeLinks"
                                             MaxLength="2000"
                                             TextWrapping="Wrap"
                                             AcceptsReturn="False"
                                             Height="60"
                                             VerticalScrollBarVisibility="Auto"
                                             Background="{DynamicResource SurfaceBgBrush}"
                                             Foreground="White"
                                             BorderBrush="#505050"
                                             BorderThickness="1"
                                             Padding="8,6"
                                             Margin="0,0,0,10"
                                             Style="{StaticResource FocusGlowTextBox}"
                                             TextChanged="TxtHypnotubeLinks_TextChanged"/>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!-- Divider -->
                        <Border Height="1" Margin="20,5,20,10" CornerRadius="1">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="Transparent" Offset="0"/>
                                    <GradientStop Color="#9370DB" Offset="0.5"/>
                                    <GradientStop Color="Transparent" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>

                        <!-- ROW 2: Settings + Quick Controls -->
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- SETTINGS BOX -->
                            <Border Grid.Column="0" CornerRadius="12" Padding="15"
                                    Background="{DynamicResource ElevatedSurfaceBrush}"
                                    BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <Grid Margin="0,0,0,15">
                                        <TextBlock Text="{loc:Str label_companion_settings}" Foreground="{DynamicResource PinkBrush}" FontSize="14" FontWeight="Bold"/>
                                        <Button x:Name="HelpBtnCompanionSettings" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="CompanionSettings"/>
                                    </Grid>

                                    <!-- Show Companion -->
                                    <Grid Margin="0,0,0,12">
                                        <TextBlock Text="{loc:Str label_show_companion}" Foreground="White" FontSize="13" VerticalAlignment="Center"/>
                                        <CheckBox x:Name="ChkAvatarEnabledCompanion" Style="{StaticResource ToggleStyle}"
                                                  HorizontalAlignment="Right"
                                                  Checked="ChkAvatarEnabled_Changed" Unchecked="ChkAvatarEnabled_Changed"/>
                                    </Grid>

                                    <!-- Detach -->
                                    <Grid Margin="0,0,0,12">
                                        <StackPanel>
                                            <TextBlock Text="{loc:Str label_window_mode}" Foreground="White" FontSize="13"/>
                                            <TextBlock x:Name="TxtDetachStatusCompanion" Text="{loc:Str label_anchored_to_main_window}"
                                                       Foreground="{DynamicResource TextDimBrush}" FontSize="11" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <Button x:Name="BtnDetachCompanionTab" Content="{loc:Str btn_detach}" HorizontalAlignment="Right"
                                                VerticalAlignment="Center"
                                                Background="{DynamicResource SurfaceBgBrush}" Foreground="White" BorderBrush="#505050" BorderThickness="1"
                                                Padding="15,6" FontSize="12" Cursor="Hand" Click="BtnDetachCompanion_Click"/>
                                    </Grid>

                                    <!-- Idle Interval -->
                                    <Grid Margin="0,0,0,15">
                                        <StackPanel>
                                            <TextBlock Text="{loc:Str label_idle_giggle_interval}" Foreground="White" FontSize="13"/>
                                            <TextBlock Text="{loc:Str label_how_often_companion_speaks_when_idle}" Foreground="{DynamicResource TextDimBrush}" FontSize="11" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <TextBlock x:Name="TxtIdleIntervalCompanion" Text="{loc:Str label_120s}" Foreground="{DynamicResource PinkBrush}"
                                                   FontSize="14" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Slider x:Name="SliderIdleIntervalCompanion" Style="{StaticResource PinkSlider}"
                                            Minimum="5" Maximum="300" Value="120" Margin="0,0,0,15"
                                            ValueChanged="SliderIdleInterval_ValueChanged"/>

                                    <!-- Customize Button -->
                                    <Button x:Name="BtnCustomizeCompanion"
                                            Background="{DynamicResource SurfaceBgBrush}" Foreground="White" BorderBrush="#9370DB" BorderThickness="1"
                                            Padding="10,10" FontSize="13" Cursor="Hand" Click="BtnCustomizeCompanion_Click">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <TextBlock Text="{loc:Str label_customize_ai}" Foreground="White"/>
                                            <TextBlock x:Name="TxtCustomizePromptName" Text="{loc:Str label_default}" Foreground="#9370DB" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Button>

                                    <!-- Manage Phrases Button -->
                                    <Button x:Name="BtnManagePhrases"
                                            Background="{DynamicResource SurfaceBgBrush}" Foreground="White" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1"
                                            Padding="10,10" FontSize="13" Cursor="Hand" Margin="0,8,0,0"
                                            Click="BtnManagePhrases_Click">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                            <TextBlock Text="{loc:Str label_manage_phrases}" Foreground="White"/>
                                            <TextBlock x:Name="TxtPhraseCount" Text="{loc:Str label_0_active}" Foreground="{DynamicResource PinkBrush}" FontWeight="SemiBold"/>
                                        </StackPanel>
                                    </Button>

                                    <!-- Phrase Presets -->
                                    <Grid Margin="0,8,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ComboBox x:Name="CmbPhrasePresets" Grid.Column="0" FontSize="11"
                                                  Style="{StaticResource DarkComboBoxStyle}"
                                                  DisplayMemberPath="DisplayText"
                                                  SelectedValuePath="Id"
                                                  SelectionChanged="CmbPhrasePresets_SelectionChanged"/>
                                        <Button Content="{loc:Str btn_save}" Grid.Column="1"
                                                Style="{StaticResource SmallPinkButton}"
                                                Padding="10,4" FontSize="11" Margin="6,0,0,0"
                                                ToolTip="{loc:Str tooltip_save_current_phrase_config_as_a_preset}"
                                                Click="BtnSavePhrasePreset_Click"/>
                                        <Button Content="{loc:Str btn_delete}" Grid.Column="2"
                                                Style="{StaticResource OutlineButton}"
                                                Padding="10,4" FontSize="11" Margin="4,0,0,0"
                                                ToolTip="{loc:Str tooltip_delete_selected_phrase_preset}"
                                                Click="BtnDeletePhrasePreset_Click"/>
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- QUICK CONTROLS BOX -->
                            <Border Grid.Column="2" CornerRadius="12" Padding="15"
                                    Background="{DynamicResource ElevatedSurfaceBrush}"
                                    BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                                <StackPanel>
                                    <Grid Margin="0,0,0,15">
                                        <TextBlock Text="{loc:Str label_quick_controls}" Foreground="{DynamicResource PinkBrush}" FontSize="14" FontWeight="Bold"/>
                                        <Button x:Name="HelpBtnQuickControls" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="QuickControls"/>
                                    </Grid>

                                    <!-- Mute Avatar -->
                                    <Grid Margin="0,0,0,12">
                                        <StackPanel>
                                            <TextBlock Text="{loc:Str label_mute_avatar}" Foreground="White" FontSize="13"/>
                                            <TextBlock Text="{loc:Str label_hide_speech_bubbles_and_sounds}" Foreground="{DynamicResource TextDimBrush}" FontSize="11" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <CheckBox x:Name="ChkMuteAvatarCompanion" Style="{StaticResource ToggleStyle}"
                                                  HorizontalAlignment="Right" VerticalAlignment="Center"
                                                  Checked="ChkMuteAvatar_Changed" Unchecked="ChkMuteAvatar_Changed"/>
                                    </Grid>

                                    <!-- Mute Whispers -->
                                    <Grid Margin="0,0,0,12">
                                        <StackPanel>
                                            <TextBlock Text="{loc:Str label_mute_whispers}" Foreground="White" FontSize="13"/>
                                            <TextBlock Text="{loc:Str label_mute_subliminal_audio}" Foreground="{DynamicResource TextDimBrush}" FontSize="11" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <CheckBox x:Name="ChkMuteWhispersCompanion" Style="{StaticResource ToggleStyle}"
                                                  HorizontalAlignment="Right" VerticalAlignment="Center"
                                                  Checked="ChkMuteWhispers_Changed" Unchecked="ChkMuteWhispers_Changed"/>
                                    </Grid>

                                    <!-- Pause Browser -->
                                    <Grid Margin="0,0,0,12">
                                        <StackPanel>
                                            <TextBlock Text="{loc:Str label_pause_browser}" Foreground="White" FontSize="13"/>
                                            <TextBlock Text="{loc:Str label_pause_embedded_browser_audio}" Foreground="{DynamicResource TextDimBrush}" FontSize="11" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <CheckBox x:Name="ChkPauseBrowserCompanion" Style="{StaticResource ToggleStyle}"
                                                  HorizontalAlignment="Right" VerticalAlignment="Center"
                                                  Checked="ChkPauseBrowser_Changed" Unchecked="ChkPauseBrowser_Changed"/>
                                    </Grid>

                                    <!-- Tips -->
                                    <Border CornerRadius="6" Padding="10" Margin="0,8,0,0" BorderBrush="#22808080" BorderThickness="1">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                <GradientStop Color="#16162B" Offset="0"/>
                                                <GradientStop Color="#1A1832" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                        <TextBlock Foreground="{DynamicResource TextDimBrush}" FontSize="11" TextWrapping="Wrap" TextAlignment="Center">
                                            <Run Text="{loc:Str label_click_avatar_for_progress_right_click_for_men}"/>
                                        </TextBlock>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </Grid>

                        <!-- ROW 3: Triggers -->
                        <Border CornerRadius="10" Padding="15" BorderThickness="1">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{DynamicResource SurfaceBg}" Offset="0"/>
                                    <GradientStop Color="#252548" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#9370DB" Offset="0"/>
                                    <GradientStop Color="{DynamicResource PinkColor}" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <Border.Effect>
                                <DropShadowEffect Color="#9370DB" BlurRadius="12" ShadowDepth="0" Opacity="0.15"/>
                            </Border.Effect>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{loc:Str label_trigger_mode}" Foreground="#9370DB" FontSize="14" FontWeight="Bold"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Text="{loc:Str label_random_conditioning_phrases_with_audio}" Foreground="{DynamicResource TextDimBrush}" FontSize="12"
                                                   VerticalAlignment="Center" Margin="8,0,0,0"/>
                                    </StackPanel>

                                    <!-- Trigger Settings (shown when enabled) -->
                                    <StackPanel x:Name="TriggerSettingsPanelCompanion" Orientation="Horizontal" Visibility="Collapsed"
                                                Margin="0,12,0,0">
                                        <TextBlock Text="{loc:Str label_interval}" Foreground="White" FontSize="12" VerticalAlignment="Center"/>
                                        <Slider x:Name="SliderTriggerIntervalCompanion" Style="{StaticResource PinkSlider}"
                                                Width="150" Minimum="10" Maximum="600" Value="60"
                                                VerticalAlignment="Center" Margin="10,0,0,0"
                                                ValueChanged="SliderTriggerInterval_ValueChanged"/>
                                        <TextBlock x:Name="TxtTriggerIntervalCompanion" Text="{loc:Str label_60s}" Foreground="#9370DB"
                                                   FontSize="13" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                        <Button x:Name="BtnEditTriggersCompanion" Content="{loc:Str btn_edit_phrases_2}"
                                                Background="{DynamicResource SurfaceBgBrush}" Foreground="White" BorderBrush="#505050" BorderThickness="1"
                                                Padding="12,4" FontSize="11" Margin="20,0,0,0" Cursor="Hand"
                                                Click="BtnEditTriggers_Click"/>
                                    </StackPanel>
                                </StackPanel>

                                <CheckBox x:Name="ChkTriggerModeCompanion" Grid.Column="1" Style="{StaticResource ToggleStyle}"
                                          VerticalAlignment="Top" Margin="0,2,0,0"
                                          Checked="ChkTriggerMode_Changed" Unchecked="ChkTriggerMode_Changed"/>
                            </Grid>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </ContentControl>

        <!-- Patreon Exclusives Tab -->
        <ContentControl x:Name="PatreonTab" Grid.Row="4" Margin="10,5,10,10" Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
            <Border Style="{StaticResource SectionPanel}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="PatreonTabContent" Margin="15">
                        <Grid Margin="0,0,0,15">
                            <StackPanel>
                                <TextBlock Text="{loc:Str label_patreon_exclusives}" Foreground="White" FontSize="22" FontWeight="Bold">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text="{loc:Str label_premium_features_for_supporters}" Foreground="{DynamicResource TextDimBrush}" FontSize="11" Margin="0,2,0,0"/>
                            </StackPanel>
                            <Button x:Name="HelpBtnPatreon" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="PatreonExclusives"/>
                        </Grid>

                        <!-- Patreon Login Status -->
                        <Border x:Name="PatreonLoginCard" CornerRadius="12" Padding="15" Margin="0,0,0,15"
                                Background="{DynamicResource ElevatedSurfaceBrush}"
                                BorderBrush="#FF424D" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <TextBlock x:Name="TxtPatreonStatus" Text="{loc:Str label_not_connected}" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock x:Name="TxtPatreonTier" Text="{loc:Str label_login_to_unlock_exclusive_features}" Foreground="{DynamicResource TextMutedBrush}" FontSize="12"/>
                                </StackPanel>

                                <Button x:Name="BtnPatreonLogin" Grid.Column="1"
                                        Content="{loc:Str btn_login}"
                                        Background="#FF424D" Foreground="White"
                                        BorderThickness="0" Padding="15,8"
                                        FontWeight="Bold" FontSize="12"
                                        Cursor="Hand"
                                        Click="BtnPatreonLogin_Click"/>
                            </Grid>
                        </Border>

                        <!-- Discord Login Status -->
                        <Border x:Name="DiscordLoginCard" CornerRadius="12" Padding="15" Margin="0,0,0,15"
                                Background="{DynamicResource ElevatedSurfaceBrush}"
                                BorderBrush="#5865F2" BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel>
                                    <TextBlock x:Name="TxtDiscordStatus" Text="{loc:Str label_not_connected}" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                    <TextBlock x:Name="TxtDiscordInfo" Text="{loc:Str label_link_discord_for_community_features}" Foreground="{DynamicResource TextMutedBrush}" FontSize="12"/>
                                </StackPanel>

                                <Button x:Name="BtnDiscordLogin" Grid.Column="1"
                                        Content="{loc:Str btn_login}"
                                        Background="#5865F2" Foreground="White"
                                        BorderThickness="0" Padding="15,8"
                                        FontWeight="Bold" FontSize="12"
                                        Cursor="Hand"
                                        Click="BtnDiscordLogin_Click"/>
                            </Grid>
                        </Border>

                        <!-- Account Linking Section (visible when logged in but other provider not linked) -->
                        <Border x:Name="AccountLinkingSection" Background="{DynamicResource DarkerBgBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,15"
                                BorderBrush="#FFD700" BorderThickness="1" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="{loc:Str label_link_accounts}" Foreground="#FFD700" FontSize="14" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBlock Text="{loc:Str label_link_both_providers_to_access_your_account_fr}"
                                           Foreground="#B0B0B0" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,10"/>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button x:Name="BtnLinkPatreon"
                                            Content="{loc:Str btn_link_patreon}"
                                            Background="#FF424D" Foreground="White"
                                            BorderThickness="0" Padding="15,8" Margin="0,0,10,0"
                                            FontWeight="Bold" FontSize="12"
                                            Cursor="Hand" Visibility="Collapsed"
                                            Click="BtnLinkPatreon_Click"/>
                                    <Button x:Name="BtnLinkDiscord"
                                            Content="{loc:Str btn_link_discord}"
                                            Background="#5865F2" Foreground="White"
                                            BorderThickness="0" Padding="15,8"
                                            FontWeight="Bold" FontSize="12"
                                            Cursor="Hand" Visibility="Collapsed"
                                            Click="BtnLinkDiscord_Click"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Cloud Settings Backup Section (visible when user has cloud identity) -->
                        <Border x:Name="CloudSettingsBackupSection" Background="{DynamicResource DarkerBgBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,15"
                                BorderBrush="{DynamicResource SecondaryBrush}" BorderThickness="1" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="{loc:Str label_cloud_settings_backup}" Foreground="{DynamicResource SecondaryBrush}" FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>
                                <TextBlock x:Name="TxtCloudBackupStatus"
                                           Text="{loc:Str label_checking_backup_status}"
                                           Foreground="#B0B0B0" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,10"/>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button x:Name="BtnBackupSettingsNow"
                                            Content="{loc:Str btn_backup_now}"
                                            Background="{DynamicResource SecondaryBrush}" Foreground="White"
                                            BorderThickness="0" Padding="15,8" Margin="0,0,10,0"
                                            FontWeight="Bold" FontSize="12"
                                            Cursor="Hand"
                                            Click="BtnBackupSettingsNow_Click"/>
                                    <Button x:Name="BtnRestoreSettings"
                                            Content="{loc:Str btn_restore_from_cloud}"
                                            Background="Transparent" Foreground="{DynamicResource SecondaryBrush}"
                                            BorderBrush="{DynamicResource SecondaryBrush}" BorderThickness="1" Padding="15,8"
                                            FontWeight="Bold" FontSize="12"
                                            Cursor="Hand"
                                            Click="BtnRestoreSettings_Click"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Data & Privacy Section (visible when user has cloud identity) -->
                        <Border x:Name="DataPrivacySection" Background="{DynamicResource DarkerBgBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,15"
                                BorderBrush="#555" BorderThickness="1" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="{loc:Str label_data_privacy}" Foreground="#B0B0B0" FontSize="14" FontWeight="Bold" Margin="0,0,0,8"/>
                                <TextBlock Text="{loc:Str label_export_your_cloud_data_or_view_our_privacy_po}"
                                           Foreground="#888" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,10"/>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button x:Name="BtnExportData"
                                            Content="{loc:Str btn_export_my_data}"
                                            Background="#555" Foreground="White"
                                            BorderThickness="0" Padding="15,8" Margin="0,0,10,0"
                                            FontWeight="Bold" FontSize="12"
                                            Cursor="Hand"
                                            Click="BtnExportData_Click"/>
                                    <Button x:Name="BtnPrivacyPolicy"
                                            Content="{loc:Str btn_privacy_policy}"
                                            Background="Transparent" Foreground="#888"
                                            BorderBrush="#555" BorderThickness="1" Padding="15,8"
                                            FontWeight="Bold" FontSize="12"
                                            Cursor="Hand"
                                            Click="BtnPrivacyPolicy_Click"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- 2-Column Grid of Feature Expanders (wrapped with Patreon lock overlay) -->
                        <Grid Margin="0,10,0,0">
                        <!-- Patreon-Only Overlay for entire features grid -->
                        <Border x:Name="PatreonFeaturesOverlay" Background="#CC1A1A2E" CornerRadius="12" Panel.ZIndex="100">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="🔒" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                <TextBlock Text="{loc:Str section_patreon_exclusive_features}" Foreground="{DynamicResource PinkBrush}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center">
                                    <TextBlock.Effect>
                                        <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text="{loc:Str label_support_on_patreon_to_unlock_autonomy_remote}"
                                           Foreground="#AAAAAA" FontSize="13" HorizontalAlignment="Center" Margin="0,10,0,0" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="400"/>
                                <Button Content="{loc:Str btn_visit_patreon}" Background="#FF424D" Foreground="White"
                                        BorderThickness="0" Padding="20,10" Margin="0,18,0,0"
                                        FontWeight="Bold" FontSize="13" Cursor="Hand"
                                        Click="BtnVisitPatreon_Click">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#E8363F"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>
                        </Border>
                        <!-- Actual features grid -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Row 0, Col 0: Bambi Takeover — moved to BambiTakeoverTab -->

                            <!-- Row 0, Col 2: Remote Control — moved to RemoteControlTab -->

                            <!-- Row 1, Col 0: Keyword Triggers -->
                            <Expander Grid.Row="1" Grid.Column="0" Style="{StaticResource ExclusivesSectionExpander}">
                                <Expander.Header>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{loc:Str label_keyword_triggers}" Foreground="White" FontSize="14" FontWeight="Bold"/>
                                    </StackPanel>
                                </Expander.Header>
                                <StackPanel>
                                    <!-- Header row -->
                                    <Grid>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{loc:Str label_keyword_triggers_2}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="Bold"/>
                                            <TextBlock x:Name="TxtKeywordTriggersLocked" Text="{loc:Str label_in_beta_testing_for_patreon_supporters}"
                                                       Foreground="#DAA520" FontSize="10" FontStyle="Italic" VerticalAlignment="Center" Margin="8,0,0,0"
                                                       Visibility="Collapsed"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                            <Button x:Name="HelpBtnKeywordTriggers" Style="{StaticResource HelpButtonStyle}"
                                                    VerticalAlignment="Center" Margin="0,0,8,0" Tag="KeywordTriggers" Panel.ZIndex="1"/>
                                            <Button x:Name="BtnKeywordTriggersStartStop" Content="{loc:Str btn_start}"
                                                    Background="{DynamicResource PinkBrush}" Foreground="White" FontWeight="Bold" FontSize="11"
                                                    Padding="14,4" BorderThickness="0" Cursor="Hand"
                                                    Click="BtnKeywordTriggersStartStop_Click"/>
                                        </StackPanel>
                                    </Grid>

                                    <TextBlock Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,4,0,0" TextWrapping="Wrap">
                                            <Run Text="{loc:Str label_your_everyday_typing_becomes_part_of_your_con}"/>
                                        </TextBlock>

                                    <!-- Settings panel -->
                                    <StackPanel x:Name="KeywordTriggersSettingsPanel" Margin="0,10,0,0">

                                        <!-- Global settings row -->
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Buffer timeout -->
                                            <StackPanel Grid.Column="0" Margin="0,0,6,0"
                                                        ToolTip="{loc:Str tooltip_how_long_to_wait_after_a_keystroke_before_che}">
                                                <TextBlock Text="{loc:Str label_buffer_timeout}" Foreground="#A0A0A0" FontSize="10"/>
                                                <Grid>
                                                    <Slider x:Name="SliderKeywordBufferTimeout" Minimum="1000" Maximum="10000"
                                                            TickFrequency="500" IsSnapToTickEnabled="True"
                                                            ValueChanged="SliderKeywordBufferTimeout_ValueChanged"/>
                                                    <TextBlock x:Name="TxtKeywordBufferTimeout" Text="{loc:Str label_3_0s}"
                                                               Foreground="{DynamicResource PinkBrush}" FontSize="10" HorizontalAlignment="Right" IsHitTestVisible="False"/>
                                                </Grid>
                                            </StackPanel>

                                            <!-- Global cooldown -->
                                            <StackPanel Grid.Column="1" Margin="6,0,6,0"
                                                        ToolTip="{loc:Str tooltip_minimum_time_between_any_two_trigger_activati}">
                                                <TextBlock Text="{loc:Str label_global_cooldown}" Foreground="#A0A0A0" FontSize="10"/>
                                                <Grid>
                                                    <Slider x:Name="SliderKeywordGlobalCooldown" Minimum="1" Maximum="120"
                                                            TickFrequency="1" IsSnapToTickEnabled="True"
                                                            ValueChanged="SliderKeywordGlobalCooldown_ValueChanged"/>
                                                    <TextBlock x:Name="TxtKeywordGlobalCooldown" Text="{loc:Str setting_5s}"
                                                               Foreground="{DynamicResource PinkBrush}" FontSize="10" HorizontalAlignment="Right" IsHitTestVisible="False"/>
                                                </Grid>
                                            </StackPanel>

                                            <!-- Session XP boost -->
                                            <StackPanel Grid.Column="2" Margin="6,0,0,0"
                                                        ToolTip="{loc:Str tooltip_xp_multiplier_bonus_when_triggers_fire_during}">
                                                <TextBlock Text="{loc:Str label_session_xp_boost}" Foreground="#A0A0A0" FontSize="10"/>
                                                <Grid>
                                                    <Slider x:Name="SliderKeywordSessionMultiplier" Minimum="1.0" Maximum="3.0"
                                                            TickFrequency="0.1" IsSnapToTickEnabled="True"
                                                            ValueChanged="SliderKeywordSessionMultiplier_ValueChanged"/>
                                                    <TextBlock x:Name="TxtKeywordSessionMultiplier" Text="1.5x"
                                                               Foreground="{DynamicResource PinkBrush}" FontSize="10" HorizontalAlignment="Right" IsHitTestVisible="False"/>
                                                </Grid>
                                            </StackPanel>
                                        </Grid>

                                        <!-- Screen OCR -->
                                        <Border CornerRadius="6" Padding="8" Margin="0,0,0,8"
                                                Background="{DynamicResource DarkerBgBrush}" BorderBrush="#353550" BorderThickness="1">
                                            <StackPanel>
                                                <Grid>
                                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                        <TextBlock Text="{loc:Str label_screen_text_detection_ocr}" Foreground="#C0C0C0" FontSize="12" FontWeight="SemiBold"/>
                                                        <Button x:Name="HelpBtnScreenOcr" Style="{StaticResource HelpButtonStyle}"
                                                                VerticalAlignment="Center" Margin="6,0,0,0" Tag="ScreenOcr" Panel.ZIndex="1"/>
                                                    </StackPanel>
                                                    <CheckBox x:Name="ChkScreenOcrEnabled" Style="{StaticResource ToggleStyle}"
                                                              HorizontalAlignment="Right" VerticalAlignment="Center"
                                                              Checked="ChkScreenOcrEnabled_Changed" Unchecked="ChkScreenOcrEnabled_Changed"/>
                                                </Grid>
                                                <TextBlock Text="{loc:Str label_scans_your_screen_for_trigger_keywords_using}"
                                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="10" Margin="0,2,0,0" TextWrapping="Wrap"/>
                                                <CheckBox x:Name="ChkKeywordHighlightEnabled" Content="{loc:Str setting_highlight_matched_keywords_on_screen}"
                                                          Foreground="#A0A0A0" FontSize="10" Margin="0,4,0,0"
                                                          IsChecked="True"
                                                          Checked="ChkKeywordHighlightEnabled_Changed"
                                                          Unchecked="ChkKeywordHighlightEnabled_Changed"
                                                          ToolTip="{loc:Str tooltip_show_a_glowing_overlay_over_the_word_when_ocr}"/>
                                                <StackPanel x:Name="HighlightDurationPanel" Visibility="Collapsed" Margin="0,4,0,0">
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                        <TextBlock Text="{loc:Str label_highlight_mode}" Foreground="#A0A0A0" FontSize="10" VerticalAlignment="Center"/>
                                                        <ComboBox x:Name="CmbOcrHighlightMode" Margin="6,0,0,0" MinWidth="110"
                                                                  Style="{StaticResource DarkComboBoxStyle}" FontSize="10"
                                                                  SelectionChanged="CmbOcrHighlightMode_SelectionChanged"
                                                                  ToolTip="{loc:Str tooltip_all_matches_highlights_every_keyword_immediat}">
                                                            <ComboBoxItem Content="{loc:Str btn_all_matches}"/>
                                                            <ComboBoxItem Content="{loc:Str btn_random_subset}"/>
                                                        </ComboBox>
                                                    </StackPanel>
                                                    <CheckBox x:Name="ChkHighlightVisibleInCapture" Content="{loc:Str setting_show_highlights_in_screen_capture_streams}"
                                                              Foreground="#A0A0A0" FontSize="10" Margin="0,2,0,4"
                                                              Checked="ChkHighlightVisibleInCapture_Changed"
                                                              Unchecked="ChkHighlightVisibleInCapture_Changed"
                                                              ToolTip="{loc:Str tooltip_when_enabled_keyword_highlights_will_be_visib}"/>
                                                    <TextBlock Text="{loc:Str label_highlight_duration}" Foreground="#A0A0A0" FontSize="10"/>
                                                    <Grid ToolTip="{loc:Str tooltip_how_long_the_pink_highlight_stays_on_screen_0}">
                                                        <Slider x:Name="SliderKeywordHighlightDuration" Minimum="0.3" Maximum="5.0"
                                                                TickFrequency="0.1" IsSnapToTickEnabled="True"
                                                                ValueChanged="SliderKeywordHighlightDuration_ValueChanged"/>
                                                        <TextBlock x:Name="TxtKeywordHighlightDuration" Text="{loc:Str label_0_6s}"
                                                                   Foreground="{DynamicResource PinkBrush}" FontSize="10" HorizontalAlignment="Right" IsHitTestVisible="False"/>
                                                    </Grid>
                                                </StackPanel>
                                                <StackPanel x:Name="ScreenOcrIntervalPanel" Visibility="Collapsed" Margin="0,6,0,0">
                                                    <TextBlock Text="{loc:Str label_scan_interval}" Foreground="#A0A0A0" FontSize="10"/>
                                                    <Grid ToolTip="{loc:Str tooltip_how_often_to_scan_your_screen_for_trigger_key}">
                                                        <Slider x:Name="SliderScreenOcrInterval" Minimum="2" Maximum="10"
                                                                TickFrequency="1" IsSnapToTickEnabled="True"
                                                                ValueChanged="SliderScreenOcrInterval_ValueChanged"/>
                                                        <TextBlock x:Name="TxtScreenOcrInterval" Text="{loc:Str label_3s}"
                                                                   Foreground="{DynamicResource PinkBrush}" FontSize="10" HorizontalAlignment="Right" IsHitTestVisible="False"/>
                                                    </Grid>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>

                                        <!-- Trigger list -->
                                        <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto" Margin="0,4,0,0"
                                                      PreviewMouseWheel="InnerScrollViewer_PreviewMouseWheel">
                                            <StackPanel x:Name="KeywordTriggerListPanel"/>
                                        </ScrollViewer>

                                        <!-- Action buttons -->
                                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                            <Button Content="{loc:Str btn_add_trigger}" Background="{DynamicResource PinkBrush}" Foreground="White" FontWeight="Bold"
                                                    FontSize="11" Padding="12,6" BorderThickness="0" Cursor="Hand"
                                                    Click="BtnAddKeywordTrigger_Click"/>
                                            <Button Content="{loc:Str btn_import_from_trigger_mode}" Background="#353550" Foreground="#A0A0A0"
                                                    FontSize="11" Padding="12,6" Margin="8,0,0,0" BorderThickness="0" Cursor="Hand"
                                                    Click="BtnImportFromCustomTriggers_Click"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>
                            </Expander>

                            <!-- Row 2, Full Width: Haptic Feedback — moved to HapticsTab -->

                        </Grid>
                        </Grid>

                        <!-- Patreon Info Box -->
                        <Border x:Name="SupportDevelopmentCard" CornerRadius="12" Padding="15" Margin="0,10,0,0"
                                Background="{DynamicResource ElevatedSurfaceBrush}"
                                BorderBrush="{DynamicResource GlassBorderBrush}" BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{loc:Str section_support_development}" Foreground="{DynamicResource SecondaryBrush}" FontSize="14" FontWeight="Bold"/>
                                <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,8,0,0">
                                    <Run Text="{loc:Str section_patreon_unlocks}"/> <Run x:Name="RunPatreonFeatures" Text="{loc:Str section_ai_chat_window_awareness_slut_mode_bambi_take}" Foreground="White"/>
                                </TextBlock>
                                <Button x:Name="BtnVisitPatreon" Content="{loc:Str btn_visit_patreon}"
                                        Background="{DynamicResource SecondaryBrush}" Foreground="White"
                                        BorderThickness="0" Padding="15,8" Margin="0,12,0,0"
                                        HorizontalAlignment="Left" FontSize="12"
                                        Cursor="Hand"
                                        Click="BtnVisitPatreon_Click"/>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </ContentControl>

        <!-- Profile Tab -->
        <ContentControl x:Name="DiscordTab" Grid.Row="4" Margin="10,5,10,10" Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
            <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="10"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="10"/><RowDefinition Height="*"/></Grid.RowDefinitions>
            <Border Grid.Column="0" Grid.Row="0" Grid.RowSpan="3" Style="{StaticResource SectionPanel}" BorderBrush="#5865F2" BorderThickness="1">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="15">
                        <Grid Margin="0,0,0,15">
                            <TextBlock Text="{loc:Str label_profile_viewer}" Foreground="#5865F2" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <Button x:Name="HelpBtnDiscordProfile" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="DiscordProfile"/>
                        </Grid>

                        <!-- Search Input -->
                        <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="10" Margin="0,0,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TxtProfileSearch" Background="Transparent" BorderThickness="0" Foreground="White"
                                         FontSize="13" VerticalAlignment="Center" KeyDown="TxtProfileSearch_KeyDown">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Style.Triggers>
                                                <Trigger Property="Text" Value="">
                                                    <Setter Property="Background">
                                                        <Setter.Value>
                                                            <VisualBrush Stretch="None" AlignmentX="Left">
                                                                <VisualBrush.Visual>
                                                                    <TextBlock Text="{loc:Str label_search_by_display_name}" Foreground="#606080" FontSize="13" Margin="2,0,0,0"/>
                                                                </VisualBrush.Visual>
                                                            </VisualBrush>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                                <Button Grid.Column="1" Content="🔍" Background="Transparent" BorderThickness="0"
                                        Foreground="#5865F2" FontSize="16" Cursor="Hand" Click="BtnProfileSearch_Click"
                                        ToolTip="{loc:Str tooltip_search_profile}"/>
                            </Grid>
                        </Border>

                        <!-- Quick buttons -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
                            <Button Content="{loc:Str btn_my_profile}" Background="#5865F2" Foreground="White" BorderThickness="0"
                                    Padding="12,6" Margin="0,0,8,0" FontSize="11" FontWeight="SemiBold" Cursor="Hand"
                                    Click="BtnViewMyProfile_Click"/>
                            <Button Content="{loc:Str btn_clear}" Background="{DynamicResource PanelAccentBrush}" Foreground="White" BorderThickness="0"
                                    Padding="12,6" FontSize="11" Cursor="Hand" Click="BtnClearProfile_Click"/>
                        </StackPanel>

                        <!-- Profile Card with optional OG animated border (layered via Grid) -->
                        <Grid x:Name="ProfileCardWrapper" Visibility="Collapsed">
                            <!-- OG animated border (behind the card) -->
                            <Border x:Name="OgBorderContainer" Visibility="Collapsed" Padding="0" CornerRadius="18">
                                <Border.Resources>
                                    <Storyboard x:Key="OgBorderAnimation" RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetName="OgBorderRotation"
                                                         Storyboard.TargetProperty="Angle"
                                                         From="0" To="360" Duration="0:0:3"/>
                                    </Storyboard>
                                </Border.Resources>
                                <Border.Background>
                                    <LinearGradientBrush x:Name="OgBorderBrush" StartPoint="0,0" EndPoint="1,1">
                                        <LinearGradientBrush.RelativeTransform>
                                            <RotateTransform x:Name="OgBorderRotation" CenterX="0.5" CenterY="0.5" Angle="0"/>
                                        </LinearGradientBrush.RelativeTransform>
                                        <GradientStop Color="#FFD700" Offset="0"/>
                                        <GradientStop Color="#FFA500" Offset="0.25"/>
                                        <GradientStop Color="{DynamicResource PinkColor}" Offset="0.5"/>
                                        <GradientStop Color="#FFA500" Offset="0.75"/>
                                        <GradientStop Color="#FFD700" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                            <!-- Profile card content (always visible when wrapper is visible) -->
                            <Border x:Name="ProfileCardContainer" Margin="8" Background="{DynamicResource DarkerBgBrush}" CornerRadius="12">
                            <StackPanel>
                                <!-- Profile Header (Avatar + Name + Status) -->
                                <Border CornerRadius="12" Padding="15" Margin="0,0,0,10">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#1E1E3F" Offset="0"/>
                                            <GradientStop Color="#2A2A4A" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Avatar with online indicator -->
                                        <Grid Width="100" Height="100">
                                            <Border CornerRadius="50" BorderBrush="#5865F2" BorderThickness="3">
                                                <Border.Background>
                                                    <ImageBrush x:Name="ProfileViewerAvatar" Stretch="UniformToFill"/>
                                                </Border.Background>
                                            </Border>
                                            <!-- Online status dot -->
                                            <Ellipse x:Name="ProfileOnlineIndicator" Width="20" Height="20"
                                                     HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                                     Margin="0,0,5,5" Stroke="#1E1E3F" StrokeThickness="3" Fill="#43B581"/>
                                        </Grid>

                                        <!-- Name and handle -->
                                        <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock x:Name="TxtProfileViewerName" Text="{loc:Str login_display_name}"
                                                           Foreground="White" FontSize="18" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                                <!-- Edit display name button (own profile only) -->
                                                <Button x:Name="BtnChangeDisplayName" Visibility="Collapsed"
                                                        Width="24" Height="24" Margin="6,0,0,0"
                                                        Background="Transparent" BorderThickness="0"
                                                        Cursor="Hand" VerticalAlignment="Center"
                                                        ToolTip="{loc:Str tooltip_change_display_name}"
                                                        Click="BtnChangeDisplayName_Click">
                                                    <TextBlock Text="{loc:Str label_text}" Foreground="#888" FontSize="14" VerticalAlignment="Center"/>
                                                </Button>
                                                <!-- Delete profile button (own profile only) -->
                                                <Button x:Name="BtnDeleteProfile" Visibility="Collapsed"
                                                        Width="24" Height="24" Margin="4,0,0,0"
                                                        Background="Transparent" BorderThickness="0"
                                                        Cursor="Hand" VerticalAlignment="Center"
                                                        ToolTip="{loc:Str tooltip_delete_your_profile}"
                                                        Click="BtnDeleteProfile_Click">
                                                    <TextBlock Text="{loc:Str label_text_2}" Foreground="#FF4444" FontSize="13" VerticalAlignment="Center"/>
                                                </Button>
                                                <!-- OG GOOD GIRL Banner -->
                                                <Border x:Name="OgBannerBadge" Visibility="Collapsed" Margin="10,0,0,0" Padding="8,3" CornerRadius="4"
                                                        VerticalAlignment="Center">
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#FFD700" Offset="0"/>
                                                            <GradientStop Color="#FFA500" Offset="0.5"/>
                                                            <GradientStop Color="{DynamicResource PinkColor}" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                    <TextBlock Text="{loc:Str label_og_good_girl}" Foreground="{DynamicResource DarkerBgBrush}" FontSize="10" FontWeight="ExtraBold"
                                                               FontFamily="Segoe UI" VerticalAlignment="Center"/>
                                                </Border>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                                                <TextBlock x:Name="TxtProfileViewerOnline" Text="{loc:Str label_online}"
                                                           Foreground="#43B581" FontSize="11"/>
                                            </StackPanel>
                                            <!-- Discord button + Patreon badge row -->
                                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                                <Button x:Name="BtnProfileDiscord" Visibility="Collapsed"
                                                        Background="#5865F2" Foreground="White" BorderThickness="0"
                                                        Padding="10,5" Cursor="Hand"
                                                        HorizontalAlignment="Left" VerticalAlignment="Center"
                                                        Click="BtnProfileDiscord_Click"
                                                        ToolTip="{loc:Str tooltip_open_discord_profile}">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="💬" FontSize="12" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                        <TextBlock x:Name="TxtProfileDiscordId" Text="{loc:Str label_message_on_discord}" Foreground="White" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Button>
                                                <!-- Patreon tier badge (same as leaderboard) -->
                                                <Image x:Name="ProfilePatreonTierBadge" Width="80" Height="80" Margin="8,0,0,0"
                                                       VerticalAlignment="Center" Visibility="Collapsed" Stretch="Uniform"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Patreon tier banner -->
                                        <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="10,0,0,0">
                                            <!-- Patreon tier banner (Pink filter / Prime subject) -->
                                            <Border x:Name="ProfilePatreonTierBanner" Visibility="Collapsed"
                                                    Width="140" Height="100" CornerRadius="8">
                                                <Image x:Name="ImgPatreonTierBanner" Stretch="Uniform"/>
                                            </Border>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Level, Rank & Patreon Badge -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="10" Padding="12" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0">
                                            <TextBlock Text="{loc:Str label_level_2}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10" FontWeight="Bold"/>
                                            <TextBlock x:Name="TxtProfileViewerLevel" Text="1" Foreground="#FFD700" FontSize="32" FontWeight="ExtraBold" Margin="0,-5,0,0"/>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{loc:Str label_rank}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10" FontWeight="Bold"/>
                                            <TextBlock x:Name="TxtProfileViewerRank" Text="#-" Foreground="#FFD700" FontSize="32" FontWeight="ExtraBold" Margin="0,-5,0,0"/>
                                        </StackPanel>
                                        <!-- Patreon Badge -->
                                        <Image x:Name="ProfilePatreonBadge" Grid.Column="2" Height="40" VerticalAlignment="Center"
                                               Visibility="Collapsed"/>
                                    </Grid>
                                </Border>

                                <!-- Stats Grid (Trainer Card Style) -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="10" Padding="12" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- XP with image -->
                                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,12" Orientation="Horizontal">
                                            <Border Width="64" Height="64" CornerRadius="8" Background="{DynamicResource PanelAccentBrush}" Margin="0,0,12,0">
                                                <Image x:Name="ImgStatXp" Source="pack://application:,,,/Resources/achievements/lv_10.png" Stretch="Uniform" Margin="3"/>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{loc:Str label_xp}" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold"/>
                                                <TextBlock x:Name="TxtProfileViewerXp" Text="0" Foreground="White" FontSize="18" FontWeight="Bold"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Bubbles with image -->
                                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" Orientation="Horizontal">
                                            <Border Width="64" Height="64" CornerRadius="8" Background="{DynamicResource PanelAccentBrush}" Margin="0,0,12,0">
                                                <Image x:Name="ImgStatBubbles" Source="pack://application:,,,/Resources/achievements/pop_the_Thought.png" Stretch="Uniform" Margin="3"/>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{loc:Str label_bubbles_2}" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold"/>
                                                <TextBlock x:Name="TxtProfileViewerBubbles" Text="0" Foreground="White" FontSize="18" FontWeight="Bold"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Video Time with image -->
                                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,12" Orientation="Horizontal">
                                            <Border Width="64" Height="64" CornerRadius="8" Background="{DynamicResource PanelAccentBrush}" Margin="0,0,12,0">
                                                <Image x:Name="ImgStatVideos" Source="pack://application:,,,/Resources/achievements/10_hours_pink.png" Stretch="Uniform" Margin="3"/>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{loc:Str label_videos}" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold"/>
                                                <TextBlock x:Name="TxtProfileViewerVideos" Text="{loc:Str label_0h}" Foreground="White" FontSize="18" FontWeight="Bold"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- GIFs with image -->
                                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" Orientation="Horizontal">
                                            <Border Width="64" Height="64" CornerRadius="8" Background="{DynamicResource PanelAccentBrush}" Margin="0,0,12,0">
                                                <Image x:Name="ImgStatGifs" Source="pack://application:,,,/Resources/achievements/retinal_burn.png" Stretch="Uniform" Margin="3"/>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{loc:Str label_flashes}" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold"/>
                                                <TextBlock x:Name="TxtProfileViewerGifs" Text="0" Foreground="White" FontSize="18" FontWeight="Bold"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Lock Cards with image -->
                                        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal">
                                            <Border Width="64" Height="64" CornerRadius="8" Background="{DynamicResource PanelAccentBrush}" Margin="0,0,12,0">
                                                <Image x:Name="ImgStatLockCards" Source="pack://application:,,,/Resources/achievements/total_lockdown.png" Stretch="Uniform" Margin="3"/>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{loc:Str label_lock_cards_2}" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold"/>
                                                <TextBlock x:Name="TxtProfileViewerLockCards" Text="0" Foreground="White" FontSize="18" FontWeight="Bold"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Achievements with image -->
                                        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                                            <Border Width="64" Height="64" CornerRadius="8" Background="{DynamicResource PanelAccentBrush}" Margin="0,0,12,0">
                                                <Image x:Name="ImgStatAchievements" Source="pack://application:,,,/Resources/achievements/spiral_eyes.png" Stretch="Uniform" Margin="3"/>
                                            </Border>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{loc:Str label_achievements}" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold"/>
                                                <TextBlock x:Name="TxtProfileViewerAchievements" Text="0" Foreground="White" FontSize="18" FontWeight="Bold"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Achievement Images Grid -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="10" Padding="10" Margin="0,0,0,5">
                                    <StackPanel>
                                        <TextBlock Text="{loc:Str label_achievements}" Foreground="#5865F2" FontSize="11" FontWeight="Bold" Margin="0,0,0,8"/>
                                        <ItemsControl x:Name="ProfileAchievementGrid">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <WrapPanel/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Width="80" Height="80" Margin="4" CornerRadius="8"
                                                            Background="{DynamicResource PanelAccentBrush}" ToolTip="{Binding Name}">
                                                        <Image Source="{Binding Image}" Stretch="Uniform" Margin="4"/>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                        <TextBlock x:Name="TxtNoAchievements" Text="{loc:Str label_no_achievements_yet}"
                                                   Foreground="#606080" FontSize="11" FontStyle="Italic"
                                                   HorizontalAlignment="Center" Visibility="Collapsed"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                        </Grid>

                        <!-- No profile selected message -->
                        <Border x:Name="NoProfileSelected" Background="{DynamicResource SurfaceBgBrush}" CornerRadius="10" Padding="30">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="👤" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                                <TextBlock Text="{loc:Str label_search_for_a_user}" Foreground="{DynamicResource TextMutedBrush}" FontSize="14" HorizontalAlignment="Center"/>
                                <TextBlock Text="{loc:Str label_or_click_my_profile}" Foreground="#606080" FontSize="12" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>            <Border Grid.Column="2" Grid.Row="0" Grid.RowSpan="3" Style="{StaticResource SectionPanel}" BorderBrush="#5865F2" BorderThickness="1"><ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel Margin="10"><TextBlock Text="{loc:Str label_community_settings}" Foreground="#5865F2" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/><Border Background="{DynamicResource DarkerBgBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,12" BorderBrush="#5865F2" BorderThickness="1"><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel VerticalAlignment="Center"><TextBlock x:Name="TxtDiscordTabStatus" Text="{loc:Str label_not_connected}" Foreground="White" FontSize="14" FontWeight="Bold"/><TextBlock x:Name="TxtDiscordTabInfo" Text="{loc:Str label_link_discord_for_community_features}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,2,0,0"/></StackPanel><Button x:Name="BtnDiscordTabLogin" Grid.Column="1" Content="{loc:Str btn_login}" Background="#5865F2" Foreground="White" BorderThickness="0" Padding="15,8" FontWeight="Bold" FontSize="11" Cursor="Hand" Click="BtnDiscordTabLogin_Click"/></Grid></Border><Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,8"><Grid><StackPanel><TextBlock Text="{loc:Str label_discord_rich_presence}" Foreground="White" FontSize="12" FontWeight="SemiBold"/><TextBlock Text="{loc:Str label_show_your_activity_status}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10"/></StackPanel><CheckBox x:Name="ChkDiscordTabRichPresence" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Checked="ChkDiscordRichPresence_Changed" Unchecked="ChkDiscordRichPresence_Changed"/></Grid></Border><Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,8"><Grid><StackPanel><TextBlock Text="{loc:Str label_show_level_in_status}" Foreground="White" FontSize="12" FontWeight="SemiBold"/><TextBlock Text="{loc:Str label_display_your_level}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10"/></StackPanel><CheckBox x:Name="ChkDiscordTabShowLevel" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" IsChecked="True" Checked="ChkShowLevelInPresence_Changed" Unchecked="ChkShowLevelInPresence_Changed"/></Grid></Border><TextBlock Text="{loc:Str label_community_sharing}" Foreground="#5865F2" FontSize="13" FontWeight="Bold" Margin="0,12,0,10"/><Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,8"><Grid><StackPanel><TextBlock Text="{loc:Str label_share_achievements}" Foreground="White" FontSize="12" FontWeight="SemiBold"/><TextBlock Text="{loc:Str label_post_achievements_to_discord}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10"/></StackPanel><CheckBox x:Name="ChkDiscordTabShareAchievements" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Checked="ChkShareAchievements_Changed" Unchecked="ChkShareAchievements_Changed"/></Grid></Border><Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,8"><Grid><StackPanel><TextBlock Text="{loc:Str label_share_level_milestones}" Foreground="White" FontSize="12" FontWeight="SemiBold"/><TextBlock Text="{loc:Str label_post_level_ups_to_discord}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10"/></StackPanel><CheckBox x:Name="ChkDiscordTabShareLevelUps" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Checked="ChkShareLevelUps_Changed" Unchecked="ChkShareLevelUps_Changed"/></Grid></Border><Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,8"><Grid><StackPanel><TextBlock Text="{loc:Str label_allow_dms_from_leaderboard}" Foreground="White" FontSize="12" FontWeight="SemiBold"/><TextBlock Text="{loc:Str label_let_others_message_you}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10"/></StackPanel><CheckBox x:Name="ChkDiscordTabAllowDm" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Checked="ChkAllowDiscordDm_Changed" Unchecked="ChkAllowDiscordDm_Changed"/></Grid></Border><Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,8"><Grid><StackPanel><TextBlock Text="{loc:Str label_share_profile_picture}" Foreground="White" FontSize="12" FontWeight="SemiBold"/><TextBlock Text="{loc:Str label_show_your_avatar_to_others}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10"/></StackPanel><CheckBox x:Name="ChkDiscordTabSharePfp" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" Checked="ChkShareProfilePicture_Changed" Unchecked="ChkShareProfilePicture_Changed"/></Grid></Border><Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,8"><Grid><StackPanel><TextBlock Text="{loc:Str label_show_online_status}" Foreground="White" FontSize="12" FontWeight="SemiBold"/><TextBlock Text="{loc:Str label_appear_offline_when_disabled}" Foreground="{DynamicResource TextMutedBrush}" FontSize="10"/></StackPanel><CheckBox x:Name="ChkDiscordTabShowOnline" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" VerticalAlignment="Center" IsChecked="True" Checked="ChkShowOnlineStatus_Changed" Unchecked="ChkShowOnlineStatus_Changed"/></Grid></Border><TextBlock Text="{loc:Str label_all_sharing_is_opt_in}" Foreground="#5865F2" FontSize="10" FontStyle="Italic" Margin="0,10,0,0"/><Button Content="{loc:Str btn_join_discord_server}" Background="#5865F2" Foreground="White" BorderThickness="0" Padding="15,12" Margin="0,15,0,0" FontWeight="Bold" FontSize="12" Cursor="Hand" Click="BtnDiscord_Click"/></StackPanel></ScrollViewer></Border>
            </Grid>
        </ContentControl>
        <!-- Lab Tab -->
        <ContentControl x:Name="LabTab" Grid.Row="4" Margin="10,5,10,10" Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
            <Grid>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <!-- Header -->
                    <TextBlock Text="{loc:Str label_the_lab}" Foreground="White" FontSize="24" FontWeight="Bold" Margin="0,0,0,4">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#00FF88" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <TextBlock Text="{loc:Str label_experimental_features_and_special_modes}" Foreground="{DynamicResource TextDimBrush}" FontSize="12" Margin="0,0,0,16"/>

                    <!-- 2x2 Modes Grid -->
                    <UniformGrid Rows="2" Columns="2">
                        <!-- Lockdown Mode Card -->
                        <Border x:Name="LockdownCardBorder" CornerRadius="12" Margin="6" Padding="15">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#1A1A32" Offset="0"/>
                                    <GradientStop Color="#201A38" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{DynamicResource PinkColor}" Offset="0"/>
                                    <GradientStop Color="{DynamicResource PatreonPurple}" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <Border.BorderThickness>
                                <Thickness>2</Thickness>
                            </Border.BorderThickness>
                            <StackPanel>
                                <Border x:Name="LockdownImageBorder" CornerRadius="10" Margin="0,0,0,12"
                                        BorderThickness="2" ClipToBounds="True">
                                    <Border.Background>
                                        <SolidColorBrush Color="#12FFFFFF"/>
                                    </Border.Background>
                                    <Border.BorderBrush>
                                        <SolidColorBrush x:Name="LockdownImageBorderBrush" Color="#FF1493"/>
                                    </Border.BorderBrush>
                                    <Border.Effect>
                                        <DropShadowEffect x:Name="LockdownImageGlow" Color="#FF1493" BlurRadius="12" ShadowDepth="0" Opacity="0.7"/>
                                    </Border.Effect>
                                    <!-- Lockdown pulse managed in code-behind (StartLockdownPulse/StopLockdownPulse) -->
                                    <Image Source="Resources/lockdown_icon.png" Stretch="Uniform"
                                           RenderOptions.BitmapScalingMode="HighQuality"/>
                                </Border>
                                <TextBlock Text="{loc:Str label_lockdown_mode}" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,0,0,6"/>
                                <TextBlock Text="{loc:Str label_lock_yourself_into_a_timed_conditioning_sessi}"
                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,12"/>

                                <!-- Setup Panel (visible when lockdown is NOT active) -->
                                <StackPanel x:Name="LockdownSetupPanel">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <TextBlock Text="{loc:Str label_duration_2}" Foreground="{DynamicResource TextMutedBrush}" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <ComboBox x:Name="CmbLockdownDuration" Width="120" SelectedIndex="1"
                                                  Background="{DynamicResource DarkerBgBrush}" Foreground="Black" FontSize="12" BorderBrush="{DynamicResource PanelAccentBrush}">
                                            <ComboBoxItem Content="{loc:Str btn_5_minutes}" Tag="5" Foreground="Black"/>
                                            <ComboBoxItem Content="{loc:Str btn_10_minutes}" Tag="10" Foreground="Black"/>
                                            <ComboBoxItem Content="{loc:Str btn_15_minutes}" Tag="15" Foreground="Black"/>
                                            <ComboBoxItem Content="{loc:Str label_30_minutes}" Tag="30" Foreground="Black"/>
                                            <ComboBoxItem Content="{loc:Str btn_60_minutes}" Tag="60" Foreground="Black"/>
                                            <ComboBoxItem Content="{loc:Str btn_2_hours}" Tag="120" Foreground="Black"/>
                                            <ComboBoxItem Content="{loc:Str btn_4_hours}" Tag="240" Foreground="Black"/>
                                        </ComboBox>
                                    </StackPanel>
                                    <Button x:Name="BtnActivateLockdown" Content="{loc:Str btn_activate}" Cursor="Hand"
                                            Background="{DynamicResource PinkBrush}" Foreground="White" FontWeight="Bold" FontSize="12"
                                            BorderThickness="0" Padding="12,8" HorizontalAlignment="Left"
                                            Click="BtnActivateLockdown_Click">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="8"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </StackPanel>

                                <!-- Active Panel (visible when lockdown IS active) -->
                                <StackPanel x:Name="LockdownActivePanel" Visibility="Collapsed">
                                    <TextBlock Text="{loc:Str label_lockdown_active}" Foreground="#DC143C" FontWeight="Bold" FontSize="14" Margin="0,0,0,6"/>
                                    <TextBlock x:Name="TxtLockdownTimer" Text="00:00" Foreground="#DC143C" FontWeight="Bold" FontSize="28"
                                               Margin="0,0,0,6" Cursor="Hand" MouseLeftButtonDown="TxtLockdownTimer_Click"/>
                                    <TextBox x:Name="TxtLockdownExit" Visibility="Collapsed"
                                             Background="#1A0A0A" Foreground="#DC143C" BorderBrush="#DC143C" BorderThickness="1"
                                             FontSize="12" Padding="6,4" Margin="0,6,0,0"
                                             KeyDown="TxtLockdownExit_KeyDown"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Quiz Training Card -->
                        <Border CornerRadius="12" Margin="6" Padding="15">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#1A1A32" Offset="0"/>
                                    <GradientStop Color="#201A38" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{DynamicResource PatreonPurple}" Offset="0"/>
                                    <GradientStop Color="{DynamicResource PinkColor}" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <Border.BorderThickness>
                                <Thickness>2</Thickness>
                            </Border.BorderThickness>
                            <StackPanel>
                                <Grid Margin="0,0,0,6">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{loc:Str label_quiz_training}" Foreground="White" FontWeight="Bold" FontSize="16"/>
                                        <TextBlock Text="{loc:Str label_beta_2}" Foreground="{DynamicResource SecondaryBrush}" FontWeight="SemiBold" FontSize="11" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                    </StackPanel>
                                    <Button x:Name="HelpBtnQuiz" Style="{StaticResource HelpButtonStyle}" HorizontalAlignment="Right" Tag="Quiz"/>
                                </Grid>
                                <TextBlock Text="{loc:Str label_ai_generated_personality_quiz_10_questions_th}"
                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,12"/>

                                <CheckBox x:Name="ChkQuizFullscreen" Content="{loc:Str btn_fullscreen}" IsChecked="True"
                                          Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,0,0,4"/>
                                <CheckBox x:Name="ChkQuizDrone" Content="{loc:Str setting_play_drone_in_background}" IsChecked="False"
                                          Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,0,0,10"/>

                                <Button x:Name="BtnStartQuiz" Content="{loc:Str btn_start_quiz}" Cursor="Hand"
                                        Background="{DynamicResource SecondaryBrush}" Foreground="White" FontWeight="Bold" FontSize="12"
                                        BorderThickness="0" Padding="12,8" HorizontalAlignment="Left"
                                        Click="BtnStartQuiz_Click">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>

                                <TextBlock Text="{loc:Str label_past_quizzes}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" Margin="0,10,0,4"
                                           x:Name="TxtPastQuizzesHeader" Visibility="Collapsed"/>
                                <Border MaxHeight="130" CornerRadius="6" x:Name="PastQuizzesPanel" Visibility="Collapsed"
                                        Background="#10FFFFFF" BorderBrush="#20FFFFFF" BorderThickness="1">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <StackPanel x:Name="PastQuizzesList"/>
                                    </ScrollViewer>
                                </Border>
                            </StackPanel>
                        </Border>

                        <!-- Pop Quiz Card -->
                        <Border CornerRadius="12" Margin="6" Padding="15">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{DynamicResource TransparentPink20}" Offset="0"/>
                                    <GradientStop Color="{DynamicResource TransparentPink20}" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <SolidColorBrush Color="{DynamicResource TransparentPink40}"/>
                            </Border.BorderBrush>
                            <Border.BorderThickness>
                                <Thickness>1</Thickness>
                            </Border.BorderThickness>
                            <StackPanel>
                                <TextBlock Text="{loc:Str label_pop_quiz}" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,0,0,6"/>
                                <TextBlock Text="{loc:Str label_reinforcement_questions_pop_up_during_session}"
                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,12"/>

                                <CheckBox x:Name="ChkPopQuizEnabled" Content="{loc:Str setting_enable_pop_up_questions}"
                                          Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,0,0,8"
                                          Checked="ChkPopQuizEnabled_Changed" Unchecked="ChkPopQuizEnabled_Changed"/>

                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="{loc:Str label_frequency}" Foreground="{DynamicResource TextMutedBrush}" FontSize="12" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                    <Slider x:Name="SliderPopQuizFrequency" Minimum="1" Maximum="100" Width="120"
                                            VerticalAlignment="Center"
                                            ValueChanged="SliderPopQuizFrequency_ValueChanged"/>
                                    <TextBlock x:Name="TxtPopQuizFrequency" Foreground="#B0B0C8" FontSize="12"
                                               VerticalAlignment="Center" Margin="8,0,0,0" Text="{loc:Str label_2_session_hr}"/>
                                </StackPanel>

                                <Button x:Name="BtnTestPopQuiz" Content="{loc:Str btn_test}" Cursor="Hand"
                                        Background="{DynamicResource SecondaryBrush}" Foreground="White" FontWeight="Bold" FontSize="12"
                                        BorderThickness="0" Padding="12,8" HorizontalAlignment="Left"
                                        Click="BtnTestPopQuiz_Click">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </Border>

                        <!-- Wallpaper Override Card (hidden until feature is ready) -->
                        <Border CornerRadius="12" Margin="6" Padding="15" Visibility="Collapsed">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#1A1A32" Offset="0"/>
                                    <GradientStop Color="#201A38" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="{DynamicResource PinkColor}" Offset="0"/>
                                    <GradientStop Color="{DynamicResource PatreonPurple}" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <Border.BorderThickness>
                                <Thickness>2</Thickness>
                            </Border.BorderThickness>
                            <StackPanel>
                                <TextBlock Text="{loc:Str label_wallpaper_override}" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,0,0,6"/>
                                <TextBlock Text="{loc:Str label_wallpaper_description}"
                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,12"/>

                                <CheckBox x:Name="ChkWallpaperEnabled" Content="{loc:Str setting_enable_wallpaper_override}"
                                          Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,0,0,8"
                                          Checked="ChkWallpaperEnabled_Changed" Unchecked="ChkWallpaperEnabled_Changed"/>

                                <TextBlock x:Name="TxtCurrentWallpaper" Foreground="#B0B0C8" FontSize="11"
                                           Margin="0,0,0,8" Visibility="Collapsed"/>

                                <Button x:Name="BtnShuffleWallpaper" Content="{loc:Str btn_shuffle_wallpaper}" Cursor="Hand"
                                        Background="{DynamicResource SecondaryBrush}" Foreground="White" FontWeight="Bold" FontSize="12"
                                        BorderThickness="0" Padding="12,8" HorizontalAlignment="Left" Visibility="Collapsed"
                                        Click="BtnShuffleWallpaper_Click">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="8"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                </StackPanel>
            </ScrollViewer>

            <!-- Lab Smokescreen Overlay (Patreon T2 / Whitelist gate) -->
            <Border x:Name="LabSmokescreen" Background="#CC1A1A2E" IsHitTestVisible="True" Visibility="Visible">
                <Border Background="#20FFFFFF" CornerRadius="16" Padding="32,28" HorizontalAlignment="Center" VerticalAlignment="Center"
                        MaxWidth="360" BorderBrush="{DynamicResource TransparentPink50Brush}" BorderThickness="1.5">
                    <Border.Effect>
                        <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="30" ShadowDepth="0" Opacity="0.3"/>
                    </Border.Effect>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{loc:Str label_text_3}" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                        <Border Background="{DynamicResource TransparentPinkBrush}" CornerRadius="8" Padding="12,4" HorizontalAlignment="Center" Margin="0,0,0,12">
                            <TextBlock Text="{loc:Str label_beta_testing}" Foreground="{DynamicResource PinkBrush}" FontWeight="Bold" FontSize="14"/>
                        </Border>
                        <TextBlock Text="{loc:Str label_these_experimental_features_are_currently_bei}"
                                   Foreground="#B0B0C8" FontSize="13" TextWrapping="Wrap" TextAlignment="Center" LineHeight="20" Margin="0,0,0,4"/>
                    </StackPanel>
                </Border>
            </Border>
            </Grid>
        </ContentControl>

        <!-- Leaderboard Tab -->
        <ContentControl x:Name="LeaderboardTab" Grid.Row="4" Margin="10,5,10,10" Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header with sort dropdown and refresh -->
                    <Border Grid.Row="0" Background="{DynamicResource TransparentPink20Brush}" CornerRadius="12,12,0,0" Padding="20,15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Help button + left flavour text -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="HelpBtnLeaderboard" Style="{StaticResource HelpButtonStyle}" VerticalAlignment="Center" Tag="Leaderboard"/>
                                <Border Background="#1888AACC" CornerRadius="8" Padding="12,8" Margin="10,0,0,0" BorderBrush="#3388AACC" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="{loc:Str label_who_s_the_prettiest_bimbo}" FontStyle="Italic" FontSize="15" FontWeight="Bold"
                                                   Foreground="#9988AACC"/>
                                        <TextBlock Text="{loc:Str label_every_level_counts_every_quest_matters}" FontStyle="Italic" FontSize="13" FontWeight="Bold"
                                                   Foreground="#6688AACC" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>

                            <!-- Big LEADERBOARD title with stroke effect + mode toggle -->
                            <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Grid HorizontalAlignment="Center">
                                    <!-- Shadow/outline layer -->
                                    <TextBlock Text="LEADERBOARD" FontSize="38" FontWeight="ExtraBold" FontFamily="Segoe UI"
                                               Foreground="#80000000" Margin="2,2,0,0"/>
                                    <!-- Main text -->
                                    <TextBlock Text="LEADERBOARD" FontSize="38" FontWeight="ExtraBold" FontFamily="Segoe UI"
                                               Foreground="{DynamicResource PinkBrush}"/>
                                </Grid>
                                <!-- Monthly / All-Time toggle buttons -->
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,2,0,0">
                                    <Button x:Name="BtnLeaderboardMonthly" Content="{loc:Str btn_monthly}" Padding="16,5" FontSize="12" FontWeight="SemiBold"
                                            Background="{DynamicResource PinkBrush}" Foreground="White" Cursor="Hand" Click="BtnLeaderboardMode_Click" Tag="monthly">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="MonthlyBorder" Background="{TemplateBinding Background}" CornerRadius="10,0,0,10" Padding="{TemplateBinding Padding}" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Opacity" Value="0.85"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                    <Button x:Name="BtnLeaderboardAllTime" Content="{loc:Str btn_all_time}" Padding="16,5" FontSize="12" FontWeight="SemiBold"
                                            Background="{DynamicResource AccentTintedBgBrush}" Foreground="{DynamicResource PinkBrush}" Cursor="Hand" Click="BtnLeaderboardMode_Click" Tag="all-time">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="AllTimeBorder" Background="{TemplateBinding Background}" CornerRadius="0,10,10,0" Padding="{TemplateBinding Padding}" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Opacity" Value="0.85"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Border Background="#1888AACC" CornerRadius="8" Padding="12,8" Margin="0,0,12,0" BorderBrush="#3388AACC" BorderThickness="1" VerticalAlignment="Center">
                                        <StackPanel>
                                            <TextBlock x:Name="TxtLeaderboardSeason" Text="{loc:Str label_season_1_prove_your_devotion}"
                                                       FontStyle="Italic" FontSize="15" FontWeight="Bold" Foreground="#9988AACC"/>
                                            <TextBlock x:Name="TxtLeaderboardSubtitle" Text="{loc:Str label_resets_monthly_your_rank_is_everything}" FontStyle="Italic" FontSize="13" FontWeight="Bold"
                                                       Foreground="#6688AACC" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock x:Name="TxtLeaderboardStatus" Foreground="{DynamicResource PinkBrush}"
                                                   HorizontalAlignment="Center" Margin="0,0,0,4" FontSize="13"/>
                                        <Button x:Name="BtnRefreshLeaderboard" Content="{loc:Str btn_refresh}" Padding="15,8"
                                                Background="{DynamicResource PinkBrush}" Foreground="White" FontSize="13" FontWeight="SemiBold"
                                                Click="BtnRefreshLeaderboard_Click" Cursor="Hand">
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="{DynamicResource PinkButtonHoveredBrush}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Leaderboard list - double-click to view profile -->
                    <ListView x:Name="LstLeaderboard" Grid.Row="1" Background="Transparent"
                              BorderThickness="0" Margin="10,5,10,10" Foreground="{DynamicResource PinkBrush}"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              VirtualizingStackPanel.IsVirtualizing="True"
                              VirtualizingStackPanel.VirtualizationMode="Recycling"
                              ScrollViewer.IsDeferredScrollingEnabled="True"
                              FontSize="18" FontFamily="Segoe UI" FontWeight="ExtraBold"
                              GridViewColumnHeader.Click="LeaderboardColumnHeader_Click"
                              MouseDoubleClick="LstLeaderboard_MouseDoubleClick">
                        <ListView.Resources>
                            <!-- Header style for dark theme - clickable for sorting -->
                            <Style TargetType="GridViewColumnHeader">
                                <Setter Property="Background" Value="{DynamicResource AccentTintedBgBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource PinkBrush}"/>
                                <Setter Property="FontWeight" Value="ExtraBold"/>
                                <Setter Property="FontSize" Value="18"/>
                                <Setter Property="FontFamily" Value="Segoe Print"/>
                                <Setter Property="Padding" Value="12,10"/>
                                <Setter Property="BorderThickness" Value="0,0,1,2"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource PinkBrush}"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource AccentTintedBgHoverBrush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.Resources>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="{loc:Str section_rank}" Width="80" DisplayMemberBinding="{Binding Rank}"/>
                                <!-- OG Column - Glowing pink neon for Season 0 OGs -->
                                <GridViewColumn Header="" Width="50">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{loc:Str label_og}" FontWeight="ExtraBold" FontSize="16"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                                       FontFamily="Segoe UI Black">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSeason0Og}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                <Setter Property="Foreground" Value="{DynamicResource PinkBrush}"/>
                                                                <Setter Property="Effect">
                                                                    <Setter.Value>
                                                                        <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="15" ShadowDepth="0" Opacity="1"/>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="{loc:Str section_name}" Width="280" DisplayMemberBinding="{Binding DisplayNameWithFlair}"/>
                                <GridViewColumn Header="{loc:Str section_patreon}" Width="120">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <!-- Tier 1 Badge (also fallback for IsPatreon with tier 0) -->
                                                <Image Source="/Resources/Patreon tier1.png" Height="42" RenderOptions.BitmapScalingMode="HighQuality">
                                                    <Image.Style>
                                                        <Style TargetType="Image">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding PatreonTier}" Value="1">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                                <!-- Fallback: show tier1 for patrons with tier 0 -->
                                                                <MultiDataTrigger>
                                                                    <MultiDataTrigger.Conditions>
                                                                        <Condition Binding="{Binding IsPatreon}" Value="True"/>
                                                                        <Condition Binding="{Binding PatreonTier}" Value="0"/>
                                                                    </MultiDataTrigger.Conditions>
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </MultiDataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Image.Style>
                                                </Image>
                                                <!-- Tier 2 Badge -->
                                                <Image Source="/Resources/Patreon tier2.png" Height="42" RenderOptions.BitmapScalingMode="HighQuality">
                                                    <Image.Style>
                                                        <Style TargetType="Image">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding PatreonTier}" Value="2">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Image.Style>
                                                </Image>
                                                <!-- Tier 3 Badge -->
                                                <Image Source="/Resources/Patreon tier3.png" Height="42" RenderOptions.BitmapScalingMode="HighQuality">
                                                    <Image.Style>
                                                        <Style TargetType="Image">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding PatreonTier}" Value="3">
                                                                    <Setter Property="Visibility" Value="Visible"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Image.Style>
                                                </Image>
                                            </Grid>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="{loc:Str section_disc}" Width="60">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Click="BtnLeaderboardDiscord_Click" Cursor="Hand"
                                                    Tag="{Binding DiscordId}" Background="Transparent" BorderThickness="0"
                                                    ToolTip="{loc:Str tooltip_send_discord_dm}">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HasDiscord}" Value="True">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                                <Image Source="/Resources/discord.png" Height="36" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                                            </Button>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="{loc:Str label_level}" Width="100" DisplayMemberBinding="{Binding Level}"/>
                                <GridViewColumn Header="{loc:Str label_xp}" Width="100" DisplayMemberBinding="{Binding XpDisplay}"/>
                                <GridViewColumn Header="{loc:Str tab_achievements}" Width="130" DisplayMemberBinding="{Binding AchievementsDisplay}"/>
                                <!-- Seasons column (shown only in All-Time mode) -->
                                <GridViewColumn Header="{loc:Str section_seasons}" Width="0" DisplayMemberBinding="{Binding SeasonsCompleted}"/>
                                <!-- Trophy Case Columns (shown only when skill unlocked) -->
                                <GridViewColumn x:Name="ColLongestSession" Header="{loc:Str section_best_session}" Width="110">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Visibility="{Binding HasTrophyCase, Converter={StaticResource BoolToVisibility}}">
                                                <TextBlock Text="{Binding LongestSessionDisplay}" HorizontalAlignment="Center"/>
                                                <TextBlock Text="{loc:Str label_min}" FontSize="11" Foreground="#B0B0B0" HorizontalAlignment="Center" Margin="0,-3,0,0"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn x:Name="ColHighestStreak" Header="{loc:Str section_best_streak}" Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Visibility="{Binding HasTrophyCase, Converter={StaticResource BoolToVisibility}}">
                                                <TextBlock Text="{Binding HighestStreakDisplay}" HorizontalAlignment="Center"/>
                                                <TextBlock Text="{loc:Str label_days}" FontSize="11" Foreground="#B0B0B0" HorizontalAlignment="Center" Margin="0,-3,0,0"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="{loc:Str label_online}" Width="80">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Ellipse Width="18" Height="18" HorizontalAlignment="Center">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Fill" Value="#353545"/>
                                                        <Setter Property="Stroke" Value="#454560"/>
                                                        <Setter Property="StrokeThickness" Value="2"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsOnline}" Value="True">
                                                                <Setter Property="Fill" Value="{DynamicResource PinkBrush}"/>
                                                                <Setter Property="Stroke" Value="{DynamicResource PinkButtonHoveredBrush}"/>
                                                                <Setter Property="Effect">
                                                                    <Setter.Value>
                                                                        <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="12" ShadowDepth="0"/>
                                                                    </Setter.Value>
                                                                </Setter>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Foreground" Value="{DynamicResource PinkBrush}"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Padding" Value="8"/>
                                <Setter Property="Margin" Value="0,2"/>
                                <Setter Property="FontSize" Value="18"/>
                                <Setter Property="FontWeight" Value="ExtraBold"/>
                                <Setter Property="FontFamily" Value="Segoe Print"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Style.Triggers>
                                    <!-- OG users get gold-tinted row -->
                                    <DataTrigger Binding="{Binding IsSeason0Og}" Value="True">
                                        <Setter Property="Background" Value="#25FFD700"/>
                                        <Setter Property="BorderBrush" Value="#50FFD700"/>
                                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                                    </DataTrigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource TransparentPink40Brush}"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource TransparentPink50Brush}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>
                </Grid>
        </ContentControl>

        <!-- Assets & Packs Tab -->
        <ContentControl x:Name="AssetsTab" Grid.Row="4" Margin="10,5,10,10" Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
            <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto" MaxHeight="340"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header with title and actions -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{loc:Str section_assets_content_packs}" Style="{StaticResource SectionHeader}"
                               FontSize="18" VerticalAlignment="Center"/>
                    <Button x:Name="HelpBtnAssets" Style="{StaticResource HelpButtonStyle}" VerticalAlignment="Center" Margin="8,0,0,0" Tag="Assets"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnRefreshPacks" Content="{loc:Str btn_refresh_2}"
                            Style="{StaticResource OutlineButton}" Margin="0,0,8,0" Padding="12,6"
                            Click="BtnRefreshPacks_Click" ToolTip="{loc:Str tooltip_refresh_available_packs}"/>
                    <Button x:Name="BtnDeleteDownloadedPacks" Content="{loc:Str btn_delete_downloaded_packs}"
                            Style="{StaticResource OutlineButton}" Margin="0,0,8,0" Padding="12,6"
                            Click="BtnDeleteDownloadedPacks_Click"
                            ToolTip="{loc:Str tooltip_delete_all_downloaded_content_packs_your_loca}"/>
                    <Button x:Name="BtnOpenAssetsFolder" Content="{loc:Str btn_open_folder}"
                            Style="{StaticResource OutlineButton}" Padding="12,6"
                            Click="BtnOpenAssetsFolder_Click" ToolTip="{loc:Str tooltip_open_assets_folder_in_explorer}"/>
                </StackPanel>
            </Grid>

            <!-- TOP: Content Packs Section -->
            <Border Grid.Row="1" Style="{StaticResource SectionPanel}" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,6">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="{loc:Str label_community_content_packs}"
                                       Style="{StaticResource SectionHeader}"/>
                            <Button x:Name="HelpBtnPacks" Style="{StaticResource HelpButtonStyle}" VerticalAlignment="Center" Margin="8,0,0,0" Tag="ContentPacks"/>
                        </StackPanel>
                        <TextBlock Text="{loc:Str label_after_downloading_a_pack_just_drag_and_drop_t}"
                                   Foreground="#888" FontSize="11" FontStyle="Italic"
                                   HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Horizontal scrolling pack cards -->
                    <ScrollViewer x:Name="PacksScrollViewer" Grid.Row="1"
                                  HorizontalScrollBarVisibility="Visible"
                                  VerticalScrollBarVisibility="Disabled"
                                  PreviewMouseWheel="PacksScrollViewer_PreviewMouseWheel"
                                  Padding="0,0,0,8">
                        <ScrollViewer.Resources>
                            <!-- Force bigger scrollbar -->
                            <Style TargetType="ScrollBar">
                                <Setter Property="Height" Value="14"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ScrollBar">
                                            <Grid Background="Transparent">
                                                <Border Background="#403949" CornerRadius="7" Opacity="0.5" Margin="2"/>
                                                <Track x:Name="PART_Track" IsDirectionReversed="False">
                                                    <Track.Thumb>
                                                        <Thumb Background="{DynamicResource PinkBrush}" Cursor="Hand">
                                                            <Thumb.Template>
                                                                <ControlTemplate TargetType="Thumb">
                                                                    <Border Background="{TemplateBinding Background}"
                                                                            CornerRadius="6" Margin="2"/>
                                                                </ControlTemplate>
                                                            </Thumb.Template>
                                                        </Thumb>
                                                    </Track.Thumb>
                                                </Track>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ScrollViewer.Resources>
                        <StackPanel Orientation="Horizontal">
                            <!-- Dynamic Pack Cards -->
                            <ItemsControl x:Name="PackCardsItemsControl">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Width="192" Height="240" Margin="0,0,10,0" CornerRadius="8"
                                                Background="{DynamicResource SurfaceBgBrush}" BorderThickness="1">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="BorderBrush" Value="{DynamicResource PanelAccentBrush}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsExternal}" Value="True">
                                                            <Setter Property="BorderBrush" Value="#FFA500"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="72"/>
                                                    <RowDefinition Height="*"/>
                                                </Grid.RowDefinitions>

                                                <!-- Preview Image (rotating for installed packs, static otherwise) -->
                                                <Border Grid.Row="0" CornerRadius="8,8,0,0" ClipToBounds="True" Background="{DynamicResource DarkerBgBrush}">
                                                    <Grid>
                                                        <!-- Rotating preview image from pack content (for installed packs with loaded previews) -->
                                                        <Image Source="{Binding CurrentPreviewImage}"
                                                               Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"
                                                               Visibility="{Binding HasPreviewImages, Converter={StaticResource BoolToVisibility}}"/>
                                                        <!-- Static preview image from URL (fallback when no dynamic previews) -->
                                                        <Image Source="{Binding PreviewImageUrl, TargetNullValue={x:Null}}"
                                                               Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality"
                                                               Visibility="{Binding HasPreviewImages, Converter={StaticResource InverseBoolToVisibility}}"/>
                                                        <!-- "No Preview" text shows only when no dynamic or static preview exists -->
                                                        <TextBlock Text="{loc:Str label_no_preview}" FontSize="14" Foreground="#666"
                                                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                   Visibility="{Binding HasAnyPreview, Converter={StaticResource InverseBoolToVisibility}}"/>
                                                        <!-- Size badge -->
                                                        <Border Background="{DynamicResource PinkBrush}" CornerRadius="4" Padding="8,3"
                                                                HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="6">
                                                            <TextBlock Text="{Binding SizeDisplay}" FontSize="11" FontWeight="Bold" Foreground="White"/>
                                                        </Border>
                                                        <!-- Installed badge -->
                                                        <Border Background="#4CAF50" CornerRadius="4" Padding="6,2"
                                                                HorizontalAlignment="Right" VerticalAlignment="Top" Margin="6"
                                                                Visibility="{Binding IsDownloaded, Converter={StaticResource BoolToVisibility}}">
                                                            <TextBlock Text="{loc:Str label_installed}" FontSize="8" FontWeight="Bold" Foreground="White"/>
                                                        </Border>
                                                        <!-- Manual download badge for external packs -->
                                                        <Border Background="#FFA500" CornerRadius="4" Padding="6,2"
                                                                HorizontalAlignment="Right" VerticalAlignment="Top" Margin="6"
                                                                Visibility="{Binding ShowExternalButtons, Converter={StaticResource BoolToVisibility}}">
                                                            <TextBlock Text="{loc:Str label_manual_download}" FontSize="8" FontWeight="Bold" Foreground="White"/>
                                                        </Border>
                                                    </Grid>
                                                </Border>

                                                <!-- Info section -->
                                                <Grid Grid.Row="1" Margin="6,3,6,3">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="Auto"/>
                                                    </Grid.RowDefinitions>

                                                    <TextBlock Grid.Row="0" Text="{Binding Name}" FontWeight="Bold"
                                                               Foreground="{StaticResource TextLightBrush}" FontSize="11"
                                                               Margin="0,0,0,1"/>
                                                    <TextBlock Grid.Row="1" Text="{Binding Description}"
                                                               Foreground="{StaticResource TextMutedBrush}" FontSize="9"
                                                               TextWrapping="Wrap" TextTrimming="CharacterEllipsis"
                                                               MaxHeight="60"
                                                               VerticalAlignment="Top"
                                                               ToolTip="{Binding Description}"/>
                                                    <TextBlock Grid.Row="2" Foreground="{DynamicResource PinkBrush}" FontSize="8" Margin="0,1,0,3" FontWeight="SemiBold">
                                                        <TextBlock.Text>
                                                            <MultiBinding StringFormat="{}{0} images, {1} videos">
                                                                <Binding Path="ImageCount"/>
                                                                <Binding Path="VideoCount"/>
                                                            </MultiBinding>
                                                        </TextBlock.Text>
                                                    </TextBlock>

                                                    <!-- Action Buttons -->
                                                    <Grid Grid.Row="3">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="4"/>
                                                            <ColumnDefinition Width="*"/>
                                                        </Grid.ColumnDefinitions>
                                                        <!-- Install/Uninstall Button -->
                                                        <Button Grid.Column="0" Content="{Binding DownloadButtonText}"
                                                                Tag="{Binding}"
                                                                Style="{StaticResource SmallPinkButton}"
                                                                Padding="4,4" Click="BtnPackDownload_Click"
                                                                IsEnabled="{Binding IsNotDownloading}"/>
                                                        <!-- Activate/Deactivate Button (only visible when downloaded) -->
                                                        <Button Grid.Column="2" Content="{Binding ActivateButtonText}"
                                                                Tag="{Binding}"
                                                                Style="{StaticResource SmallPinkButton}"
                                                                Padding="4,4" Click="BtnPackActivate_Click"
                                                                Visibility="{Binding IsDownloaded, Converter={StaticResource BoolToVisibility}}"/>
                                                    </Grid>

                                                    <!-- Progress bar -->
                                                    <ProgressBar Grid.Row="4" Height="4" Margin="0,6,0,0"
                                                                 Minimum="0" Maximum="100" Value="{Binding DownloadProgress}"
                                                                 Foreground="{DynamicResource PinkBrush}" Background="{DynamicResource DarkerBgBrush}"
                                                                 Visibility="{Binding IsDownloading, Converter={StaticResource BoolToVisibility}}"/>
                                                </Grid>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- Creator Card: Join Discord -->
                            <Border Width="176" Height="240" Margin="0,0,10,0" CornerRadius="8"
                                    Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#5865F2" BorderThickness="2">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="72"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <!-- Discord Preview -->
                                    <Border Grid.Row="0" CornerRadius="8,8,0,0" ClipToBounds="True" Background="#5865F2">
                                        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <Image Source="pack://application:,,,/Resources/discord.png"
                                                   Width="48" Height="48" Stretch="Uniform"
                                                   RenderOptions.BitmapScalingMode="HighQuality"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Info section -->
                                    <Grid Grid.Row="1" Margin="8,4,8,4">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Text="{loc:Str label_create_your_own_pack}" FontWeight="Bold"
                                                   Foreground="{StaticResource TextLightBrush}" FontSize="12"
                                                   Margin="0,0,0,2"/>
                                        <TextBlock Grid.Row="1" Text="{loc:Str label_are_you_a_content_creator_join_our_discord_to}"
                                                   Foreground="{StaticResource TextMutedBrush}" FontSize="10"
                                                   TextWrapping="Wrap" MaxHeight="52" VerticalAlignment="Top"/>
                                        <TextBlock Grid.Row="2" Foreground="#5865F2" FontSize="9" Margin="0,2,0,4" FontWeight="SemiBold">
                                            Get featured in the app
                                        </TextBlock>

                                        <!-- Button -->
                                        <Button Grid.Row="3" Content="{loc:Str label_join_discord}"
                                                Background="#5865F2" Foreground="White" BorderThickness="0"
                                                Padding="8,4" FontSize="11" FontWeight="SemiBold" Cursor="Hand"
                                                Click="BtnCreatorDiscord_Click">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </Grid>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Row="2" Height="5" HorizontalAlignment="Stretch"
                          Background="Transparent" Cursor="SizeNS"/>

            <!-- BOTTOM: Interactive File Browser -->
            <Border Grid.Row="3" Style="{StaticResource SectionPanel}" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="{loc:Str section_asset_browser}" Style="{StaticResource SectionHeader}"/>
                            <Button x:Name="HelpBtnAssetBrowser" Style="{StaticResource HelpButtonStyle}" VerticalAlignment="Center" Margin="8,0,0,0" Tag="AssetBrowser"/>
                        </StackPanel>
                        <TextBlock x:Name="TxtAssetCounts" Text="{loc:Str label_0_images_0_videos}"
                                   Foreground="{StaticResource TextMutedBrush}" FontSize="12"
                                   HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>

                    <!-- Action bar with preset selector -->
                    <Grid Grid.Row="1" Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left: Select/Deselect buttons -->
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Button Content="{loc:Str btn_select_all}" Style="{StaticResource OutlineButton}"
                                    Padding="10,4" FontSize="11" Margin="0,0,6,0" Click="BtnSelectAllAssets_Click"/>
                            <Button Content="{loc:Str btn_deselect_all}" Style="{StaticResource OutlineButton}"
                                    Padding="10,4" FontSize="11" Click="BtnDeselectAllAssets_Click"/>
                        </StackPanel>

                        <!-- Right: Preset selector -->
                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <TextBlock Text="{loc:Str label_preset_2}" Foreground="{StaticResource TextMutedBrush}"
                                       FontSize="11" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <ComboBox x:Name="CmbAssetPresets" Width="180" FontSize="11"
                                      DisplayMemberPath="DisplayText"
                                      SelectedValuePath="Id"
                                      SelectionChanged="CmbAssetPresets_SelectionChanged"/>
                            <Button x:Name="BtnSaveAssetPreset" Content="{loc:Str btn_save_as}"
                                    Style="{StaticResource SmallPinkButton}"
                                    Padding="8,4" FontSize="11" Margin="6,0,0,0"
                                    ToolTip="{loc:Str tooltip_save_current_selection_as_a_new_preset}"
                                    Click="BtnSaveAssetPreset_Click"/>
                            <Button x:Name="BtnUpdateAssetPreset" Content="{loc:Str btn_update}"
                                    Style="{StaticResource OutlineButton}"
                                    Padding="8,4" FontSize="11" Margin="4,0,0,0"
                                    ToolTip="{loc:Str tooltip_update_selected_preset_with_current_selection}"
                                    Click="BtnUpdateAssetPreset_Click"/>
                            <Button x:Name="BtnDeleteAssetPreset" Content="{loc:Str btn_delete}"
                                    Style="{StaticResource OutlineButton}"
                                    Padding="8,4" FontSize="11" Margin="4,0,0,0"
                                    ToolTip="{loc:Str tooltip_delete_selected_preset}"
                                    Click="BtnDeleteAssetPreset_Click"/>
                        </StackPanel>
                    </Grid>

                    <!-- Split view: TreeView on left, Thumbnails on right -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="280"/>
                            <ColumnDefinition Width="5"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- LEFT: Folder TreeView -->
                        <Border Grid.Column="0" Background="{DynamicResource DarkerBgBrush}" CornerRadius="6" Padding="4">
                            <TreeView x:Name="AssetTreeView" Background="Transparent" BorderThickness="0"
                                      SelectedItemChanged="AssetTreeView_SelectedItemChanged"
                                      VirtualizingPanel.IsVirtualizing="True"
                                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                      ScrollViewer.VerticalScrollBarVisibility="Visible">
                                <TreeView.Resources>
                                    <!-- Force bigger scrollbars in TreeView -->
                                    <Style TargetType="ScrollBar">
                                        <Setter Property="Width" Value="14"/>
                                        <Setter Property="Height" Value="14"/>
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ScrollBar">
                                                    <Grid Background="Transparent">
                                                        <Border Background="#403949" CornerRadius="7" Opacity="0.5" Margin="2"/>
                                                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                                                            <Track.Thumb>
                                                                <Thumb Background="{DynamicResource PinkBrush}" Cursor="Hand">
                                                                    <Thumb.Template>
                                                                        <ControlTemplate TargetType="Thumb">
                                                                            <Border Background="{TemplateBinding Background}"
                                                                                    CornerRadius="6" Margin="2"/>
                                                                        </ControlTemplate>
                                                                    </Thumb.Template>
                                                                </Thumb>
                                                            </Track.Thumb>
                                                        </Track>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="Orientation" Value="Horizontal">
                                                <Setter Property="Height" Value="14"/>
                                                <Setter Property="Width" Value="Auto"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                    <Style TargetType="TreeViewItem">
                                        <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
                                        <Setter Property="FontSize" Value="12"/>
                                        <Setter Property="Padding" Value="2"/>
                                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                        <Style.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource TransparentPink40Brush}"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="{DynamicResource TransparentPink20Brush}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TreeView.Resources>
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <StackPanel Orientation="Horizontal" Margin="2">
                                            <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                      IsThreeState="False"
                                                      Checked="FolderCheckBox_Changed"
                                                      Unchecked="FolderCheckBox_Changed"
                                                      VerticalAlignment="Center" Margin="0,0,4,0"/>
                                            <TextBlock Text="📁" FontSize="12" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding FileCountDisplay}"
                                                       Foreground="{StaticResource TextMutedBrush}"
                                                       FontSize="10" Margin="4,0,0,0" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Border>

                        <!-- Splitter -->
                        <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch"
                                      Background="Transparent" Cursor="SizeWE"/>

                        <!-- RIGHT: Thumbnail Preview Grid -->
                        <Border Grid.Column="2" Background="{DynamicResource DarkerBgBrush}" CornerRadius="6" Padding="4">
                            <Grid>
                                <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled">
                                    <ItemsControl x:Name="ThumbnailsItemsControl">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel/>
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Width="100" Height="120" Margin="4" CornerRadius="6"
                                                        Background="{DynamicResource SurfaceBgBrush}" BorderBrush="{DynamicResource PanelAccentBrush}" BorderThickness="2" Cursor="Hand"
                                                        MouseLeftButtonDown="ThumbnailItem_Click">
                                                    <Border.ContextMenu>
                                                        <ContextMenu>
                                                            <MenuItem Header="{loc:Str section_preview}" Click="ThumbnailItem_Preview_Click"/>
                                                            <Separator/>
                                                            <MenuItem Header="{loc:Str section_open_in_explorer}" Click="ThumbnailItem_OpenInExplorer_Click"/>
                                                        </ContextMenu>
                                                    </Border.ContextMenu>
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="*"/>
                                                            <RowDefinition Height="Auto"/>
                                                        </Grid.RowDefinitions>

                                                        <!-- Thumbnail image -->
                                                        <Border Grid.Row="0" CornerRadius="6,6,0,0" ClipToBounds="True" Background="{DynamicResource DarkerBgBrush}">
                                                            <Grid>
                                                                <!-- Video placeholder icon (shows when IsVideo and no thumbnail) -->
                                                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                            Visibility="{Binding IsVideo, Converter={StaticResource BoolToVisibility}}">
                                                                    <TextBlock Text="🎬" FontSize="32" HorizontalAlignment="Center"/>
                                                                    <TextBlock Text="{loc:Str label_video}" FontSize="9" Foreground="#888" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                                                </StackPanel>
                                                                <!-- Image thumbnail (for images, or video if we had one) -->
                                                                <Image Source="{Binding Thumbnail}" Stretch="UniformToFill"/>
                                                                <!-- Loading indicator -->
                                                                <TextBlock Text="⏳" FontSize="14"
                                                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                           Visibility="{Binding IsLoadingThumbnail, Converter={StaticResource BoolToVisibility}}"/>
                                                            </Grid>
                                                        </Border>

                                                        <!-- Checkbox overlay -->
                                                        <CheckBox Grid.Row="0" IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                                  HorizontalAlignment="Left" VerticalAlignment="Top"
                                                                  Margin="4" Checked="ThumbnailCheckBox_Changed"
                                                                  Unchecked="ThumbnailCheckBox_Changed"/>

                                                        <!-- Filename -->
                                                        <Border Grid.Row="1" Background="#303050" Padding="4,2">
                                                            <TextBlock Text="{Binding Name}" FontSize="9"
                                                                       Foreground="{StaticResource TextLightBrush}"
                                                                       TextTrimming="CharacterEllipsis"
                                                                       ToolTip="{Binding Name}"/>
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>

                                <!-- Empty state -->
                                <TextBlock x:Name="TxtThumbnailsEmpty"
                                           Text="{loc:Str label_select_a_folder_to_view_its_contents}"
                                           Foreground="{StaticResource TextMutedBrush}" FontSize="13"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </Border>
            </Grid>
        </ContentControl>

        <!-- Enhancements Tab (Skill Tree) -->
        <ContentControl x:Name="EnhancementsTab" Grid.Row="4" Margin="10,5,10,10" Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
            <Grid>
                <!-- Hidden container for named elements (used programmatically) -->
                <StackPanel Visibility="Collapsed">
                    <TextBlock x:Name="TxtSkillPoints" Text="0"/>
                    <TextBlock x:Name="TxtXpMultiplier" Text="1.00x"/>
                    <TextBlock x:Name="TxtPinkRushIndicator" Text="{loc:Str label_rush}"/>
                    <TextBlock x:Name="TxtConditioningTime" Text="{loc:Str label_0h_0m}"/>
                    <WrapPanel x:Name="ActiveBonusesList"/>
                    <Border x:Name="ActiveBonusesPanel"/>
                </StackPanel>

                <Border x:Name="SkillTreeOuterBorder" Background="#151528" CornerRadius="12" Padding="5" Margin="20,5,20,20">
                    <ScrollViewer x:Name="SkillTreeScroller"
                             Style="{StaticResource SkillTreeScrollViewerStyle}"
                             HorizontalScrollBarVisibility="Visible"
                             VerticalScrollBarVisibility="Disabled"
                             PreviewMouseWheel="SkillTreeScroller_PreviewMouseWheel">
                        <Canvas x:Name="SkillTreeCanvas" Width="2400" Height="460" HorizontalAlignment="Left">
                            <!-- Header and skill nodes will be drawn programmatically -->
                        </Canvas>
                    </ScrollViewer>
                </Border>

                <!-- Login Required Overlay -->
                <Border x:Name="EnhancementsLoginOverlay" Background="#E6151528" CornerRadius="12" Visibility="Collapsed">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="40">
                        <TextBlock Text="🔒" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                        <TextBlock Text="{loc:Str label_login_required}" Foreground="{DynamicResource PinkBrush}" FontSize="22" FontWeight="Bold"
                                   HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="{loc:Str label_log_in_with_discord_or_patreon_to_access_enha}"
                                   Foreground="#AA99AA" FontSize="14" HorizontalAlignment="Center"
                                   TextAlignment="Center" TextWrapping="Wrap" MaxWidth="350"/>
                    </StackPanel>
                </Border>
            </Grid>
        </ContentControl>

        <!-- Awareness Engine Tab -->
        <ContentControl x:Name="AwarenessTab" Grid.Row="4" Margin="10,5,10,10" Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
          <Grid>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid Margin="28,20,28,20" MaxWidth="980" HorizontalAlignment="Center">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Header -->
                        <RowDefinition Height="Auto"/> <!-- Pulse -->
                        <RowDefinition Height="Auto"/> <!-- Presets -->
                        <RowDefinition Height="Auto"/> <!-- Timing (two sliders) -->
                        <RowDefinition Height="Auto"/> <!-- Sources + Safety (two columns) -->
                        <RowDefinition Height="Auto"/> <!-- Advanced link -->
                    </Grid.RowDefinitions>

                    <!-- ==================== HEADER ==================== -->
                    <Grid Grid.Row="0" Margin="0,0,0,22">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="👁" FontSize="34" VerticalAlignment="Center" Margin="0,0,14,0"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Awareness Engine" Foreground="White" FontSize="22" FontWeight="Bold"/>
                            <TextBlock Text="She's watching your screen and your typing. React in real time."
                                       Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,2,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <Ellipse x:Name="AwarenessStatusDot" Width="8" Height="8" Fill="#606060" Margin="0,0,8,0"/>
                            <TextBlock x:Name="TxtAwarenessStatus" Text="Off" Foreground="#A0A0A0"
                                       FontSize="12" VerticalAlignment="Center" Margin="0,0,14,0"/>
                            <CheckBox x:Name="ChkAwarenessMaster" Style="{StaticResource ToggleStyle}"
                                      VerticalAlignment="Center"
                                      Checked="ChkAwarenessMaster_Changed" Unchecked="ChkAwarenessMaster_Changed">
                                <CheckBox.ToolTip>
                                    <ToolTip>
                                        <StackPanel MaxWidth="320">
                                            <TextBlock Text="Awareness Engine — Master Switch" Foreground="#FF69B4" FontWeight="Bold" FontSize="12" Margin="0,0,0,4"/>
                                            <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="White">
                                                Turns the whole Awareness system on or off. When ON, enabled signal sources (OCR + keyboard) feed words into the trigger engine and matching keywords fire their actions in real time. When OFF, nothing detects anything — installed presets stay on disk but do nothing.
                                            </TextBlock>
                                        </StackPanel>
                                    </ToolTip>
                                </CheckBox.ToolTip>
                            </CheckBox>
                        </StackPanel>
                    </Grid>

                    <!-- ==================== LIVE PULSE ==================== -->
                    <Border Grid.Row="1" Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#2E2E48"
                            BorderThickness="1" CornerRadius="10" Padding="18,16" Margin="0,0,0,18">
                        <StackPanel>
                            <Grid Margin="0,0,0,10">
                                <TextBlock Text="LAST DETECTED" Foreground="#8A8AA0" FontSize="10" FontWeight="Bold"
                                           VerticalAlignment="Center"/>
                                <TextBlock x:Name="TxtAwarenessFireCount" Text="" HorizontalAlignment="Right"
                                           Foreground="{DynamicResource TextDimBrush}" FontSize="10"/>
                            </Grid>
                            <StackPanel x:Name="AwarenessPulseFeed">
                                <!-- Empty state shown when no fires yet -->
                                <TextBlock x:Name="TxtAwarenessPulseEmpty"
                                           Text="Nothing yet. Enable the engine and she'll start noticing things."
                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="12" FontStyle="Italic"
                                           TextAlignment="Center" Margin="0,18,0,18"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- ==================== PRESETS ==================== -->
                    <StackPanel Grid.Row="2" Margin="0,0,0,18">
                        <Grid Margin="4,0,4,10">
                            <TextBlock Text="PRESETS" Foreground="#8A8AA0" FontSize="10" FontWeight="Bold"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="One-click packs — click any card to preview or install"
                                       HorizontalAlignment="Right"
                                       Foreground="{DynamicResource TextDimBrush}" FontSize="10" FontStyle="Italic"/>
                        </Grid>
                        <!-- Cards are built imperatively in MainWindow.RefreshAwarenessPresetCards
                             so the trailing "+ New Preset" card can share the wrap flow with the
                             real preset cards. -->
                        <WrapPanel x:Name="AwarenessPresetItems" HorizontalAlignment="Stretch"/>
                    </StackPanel>

                    <!-- ==================== TIMING ==================== -->
                    <StackPanel Grid.Row="3" Margin="0,0,0,14">
                        <TextBlock Text="TIMING" Foreground="#8A8AA0" FontSize="10" FontWeight="Bold"
                                   Margin="4,0,4,10"/>
                        <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#2E2E48"
                                BorderThickness="1" CornerRadius="10" Padding="18,14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="14"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Col 0: global effect cooldown -->
                                <StackPanel Grid.Column="0">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="Cooldown between effects" Foreground="White" FontSize="12" FontWeight="SemiBold"
                                                   VerticalAlignment="Center"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Margin="6,0,0,0">
                                            <Button.ToolTip>
                                                <ToolTip>
                                                    <StackPanel MaxWidth="340">
                                                        <TextBlock Text="Cooldown between effects" Foreground="#FF69B4" FontWeight="Bold" FontSize="12" Margin="0,0,0,4"/>
                                                        <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="White">
                                                            Minimum delay between any two Awareness reactions, from ANY trigger. Keeps her from spamming effects when lots of trigger words end up on screen or in your typing at once.
                                                        </TextBlock>
                                                        <TextBlock TextWrapping="Wrap" FontSize="10" Foreground="#A0A0A0" Margin="0,6,0,0">
                                                            Lower = more reactive, higher = calmer pacing. 10s is the default. Max 180s.
                                                        </TextBlock>
                                                    </StackPanel>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                    <Grid>
                                        <Slider x:Name="SliderAwarenessGlobalCooldown" Minimum="1" Maximum="180"
                                                TickFrequency="1" IsSnapToTickEnabled="True"
                                                ValueChanged="SliderAwarenessGlobalCooldown_ValueChanged"/>
                                        <TextBlock x:Name="TxtAwarenessGlobalCooldown" Text="10s"
                                                   Foreground="{DynamicResource PinkBrush}" FontSize="10"
                                                   HorizontalAlignment="Right" IsHitTestVisible="False"/>
                                    </Grid>
                                </StackPanel>

                                <!-- Col 2: same-word cooldown -->
                                <StackPanel Grid.Column="2">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="Same-word cooldown" Foreground="White" FontSize="12" FontWeight="SemiBold"
                                                   VerticalAlignment="Center"/>
                                        <Button Style="{StaticResource HelpButtonStyle}" Margin="6,0,0,0">
                                            <Button.ToolTip>
                                                <ToolTip>
                                                    <StackPanel MaxWidth="340">
                                                        <TextBlock Text="Same-word cooldown" Foreground="#FF69B4" FontWeight="Bold" FontSize="12" Margin="0,0,0,4"/>
                                                        <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="White">
                                                            Minimum gap before the SAME word can fire again, regardless of which trigger caught it. Stops a single word on screen from re-triggering on every OCR scan.
                                                        </TextBlock>
                                                        <TextBlock TextWrapping="Wrap" FontSize="10" Foreground="#A0A0A0" Margin="0,6,0,0">
                                                            Independent of the "cooldown between effects" above — a different word can still fire within this window. 15s default. Max 180s.
                                                        </TextBlock>
                                                    </StackPanel>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                    <Grid>
                                        <Slider x:Name="SliderAwarenessSameWordCooldown" Minimum="1" Maximum="180"
                                                TickFrequency="1" IsSnapToTickEnabled="True"
                                                ValueChanged="SliderAwarenessSameWordCooldown_ValueChanged"/>
                                        <TextBlock x:Name="TxtAwarenessSameWordCooldown" Text="15s"
                                                   Foreground="{DynamicResource PinkBrush}" FontSize="10"
                                                   HorizontalAlignment="Right" IsHitTestVisible="False"/>
                                    </Grid>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <!-- ==================== SOURCES + SAFETY ==================== -->
                    <Grid Grid.Row="4" Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="14"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Signal Sources -->
                        <Border Grid.Column="0" Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#2E2E48"
                                BorderThickness="1" CornerRadius="10" Padding="18,14">
                            <StackPanel>
                                <TextBlock Text="SIGNAL SOURCES" Foreground="#8A8AA0" FontSize="10" FontWeight="Bold"
                                           Margin="0,0,0,10"/>
                                <Grid Margin="0,4,0,8">
                                    <Grid.ToolTip>
                                        <ToolTip>
                                            <StackPanel MaxWidth="340">
                                                <TextBlock Text="Screen OCR" Foreground="#FF69B4" FontWeight="Bold" FontSize="12" Margin="0,0,0,4"/>
                                                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="White">
                                                    Scans your screen every few seconds with optical character recognition and feeds detected words into the trigger engine. This is how she notices text inside other apps — Discord, browsers, image viewers, game UIs, anything visible.
                                                </TextBlock>
                                                <TextBlock TextWrapping="Wrap" FontSize="10" Foreground="#A0A0A0" Margin="0,6,0,0">
                                                    Uses modest CPU/GPU during active sessions. Turn off if you don't want screen-based triggers or want to save power.
                                                </TextBlock>
                                            </StackPanel>
                                        </ToolTip>
                                    </Grid.ToolTip>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="🖵" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="Screen OCR" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                                            <TextBlock Text="Reads words from what's on your screen"
                                                       Foreground="{DynamicResource TextMutedBrush}" FontSize="10"/>
                                        </StackPanel>
                                    </StackPanel>
                                    <CheckBox x:Name="ChkAwarenessOcr" Style="{StaticResource ToggleStyle}"
                                              HorizontalAlignment="Right" VerticalAlignment="Center"
                                              Checked="ChkAwarenessOcr_Changed" Unchecked="ChkAwarenessOcr_Changed"/>
                                </Grid>
                                <Grid Margin="0,4,0,4">
                                    <Grid.ToolTip>
                                        <ToolTip>
                                            <StackPanel MaxWidth="340">
                                                <TextBlock Text="Keyboard Capture" Foreground="#FF69B4" FontWeight="Bold" FontSize="12" Margin="0,0,0,4"/>
                                                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="White">
                                                    Listens to your keystrokes and assembles a rolling buffer of recent typing. When you type a trigger keyword, it fires immediately — no OCR delay.
                                                </TextBlock>
                                                <TextBlock TextWrapping="Wrap" FontSize="10" Foreground="#A0A0A0" Margin="0,6,0,0">
                                                    Only trigger words are acted on; nothing else is stored or logged. Fastest signal source. Turn off if you don't want typing-based triggers.
                                                </TextBlock>
                                            </StackPanel>
                                        </ToolTip>
                                    </Grid.ToolTip>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="⌨" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="Keyboard" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                                            <TextBlock Text="Catches trigger words as you type them"
                                                       Foreground="{DynamicResource TextMutedBrush}" FontSize="10"/>
                                        </StackPanel>
                                    </StackPanel>
                                    <CheckBox x:Name="ChkAwarenessKeyboard" Style="{StaticResource ToggleStyle}"
                                              HorizontalAlignment="Right" VerticalAlignment="Center"
                                              Checked="ChkAwarenessKeyboard_Changed" Unchecked="ChkAwarenessKeyboard_Changed"/>
                                </Grid>
                                <TextBlock Text="More sources coming: browser URL, active window title, process"
                                           Foreground="{DynamicResource TextDimBrush}" FontSize="10" FontStyle="Italic"
                                           Margin="0,10,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Safety -->
                        <Border Grid.Column="2" Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#2E2E48"
                                BorderThickness="1" CornerRadius="10" Padding="18,14">
                            <StackPanel>
                                <TextBlock Text="SAFETY" Foreground="#8A8AA0" FontSize="10" FontWeight="Bold"
                                           Margin="0,0,0,10"/>
                                <CheckBox x:Name="ChkAwarenessIgnoreOwnUi"
                                          Foreground="White" FontSize="12" Margin="0,4,0,2"
                                          Checked="ChkAwarenessIgnoreOwnUi_Changed" Unchecked="ChkAwarenessIgnoreOwnUi_Changed">
                                    <CheckBox.ToolTip>
                                        <ToolTip>
                                            <StackPanel MaxWidth="340">
                                                <TextBlock Text="Ignore CCP's own windows" Foreground="#FF69B4" FontWeight="Bold" FontSize="12" Margin="0,0,0,4"/>
                                                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="White">
                                                    OCR will skip words drawn by Control Panel itself — avatar speech bubbles, subliminal flash overlays, bouncing text, and the main window. Prevents Awareness from reacting to her own voice and creating feedback loops.
                                                </TextBlock>
                                                <TextBlock TextWrapping="Wrap" FontSize="10" Foreground="#A0A0A0" Margin="0,6,0,0">
                                                    Strongly recommended ON. Only turn off if you're debugging why a specific overlay isn't being detected.
                                                </TextBlock>
                                            </StackPanel>
                                        </ToolTip>
                                    </CheckBox.ToolTip>
                                    <TextBlock Text="Ignore CCP's own windows" FontWeight="SemiBold"/>
                                </CheckBox>
                                <TextBlock Text="OCR skips words inside Control Panel, avatar bubbles, and subliminal flashes — so she won't react to her own voice."
                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="10" TextWrapping="Wrap" Margin="22,0,0,10"/>
                                <CheckBox x:Name="ChkAwarenessLoopProtection"
                                          Foreground="White" FontSize="12" Margin="0,4,0,2"
                                          Checked="ChkAwarenessLoopProtection_Changed" Unchecked="ChkAwarenessLoopProtection_Changed">
                                    <CheckBox.ToolTip>
                                        <ToolTip>
                                            <StackPanel MaxWidth="340">
                                                <TextBlock Text="Loop protection" Foreground="#FF69B4" FontWeight="Bold" FontSize="12" Margin="0,0,0,4"/>
                                                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="White">
                                                    When a trigger fires, its keyword is silenced for a few seconds. Stops a single mention on screen from firing multiple times in quick succession if OCR keeps catching it.
                                                </TextBlock>
                                                <TextBlock TextWrapping="Wrap" FontSize="10" Foreground="#A0A0A0" Margin="0,6,0,0">
                                                    Also works across sources — a keyword fired via Keyboard is muted for OCR detection too, and vice versa.
                                                </TextBlock>
                                            </StackPanel>
                                        </ToolTip>
                                    </CheckBox.ToolTip>
                                    <TextBlock Text="Loop protection" FontWeight="SemiBold"/>
                                </CheckBox>
                                <TextBlock Text="After a trigger fires, its keyword is muted for a few seconds so the reaction can't re-arm it."
                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="10" TextWrapping="Wrap" Margin="22,0,0,10"/>

                                <!-- On-screen highlight: toggle + color picker. The highlight overlay
                                     draws a glowing box around OCR-matched words on your screen. -->
                                <CheckBox x:Name="ChkAwarenessHighlight"
                                          Foreground="White" FontSize="12" Margin="0,4,0,2"
                                          Checked="ChkAwarenessHighlight_Changed" Unchecked="ChkAwarenessHighlight_Changed">
                                    <CheckBox.ToolTip>
                                        <ToolTip>
                                            <StackPanel MaxWidth="340">
                                                <TextBlock Text="Highlight matched words on screen" Foreground="#FF69B4" FontWeight="Bold" FontSize="12" Margin="0,0,0,4"/>
                                                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="White">
                                                    Draws a glowing rectangle around every OCR-detected trigger word on your screen, just long enough to see it catch your eye. Gives you a visual confirmation that Awareness is watching.
                                                </TextBlock>
                                                <TextBlock TextWrapping="Wrap" FontSize="10" Foreground="#A0A0A0" Margin="0,6,0,0">
                                                    Only shows for OCR detections, not keyboard triggers. Pick the glow color from the swatches below.
                                                </TextBlock>
                                            </StackPanel>
                                        </ToolTip>
                                    </CheckBox.ToolTip>
                                    <TextBlock Text="Highlight matched words on screen" FontWeight="SemiBold"/>
                                </CheckBox>
                                <TextBlock Text="Draws a glowing box around OCR-detected trigger words. Pick the glow color below."
                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="10" TextWrapping="Wrap" Margin="22,0,0,6"/>
                                <StackPanel x:Name="AwarenessHighlightColorPanel" Orientation="Horizontal" Margin="22,0,0,0"
                                            ToolTip="Click a swatch to pick the highlight glow color, or type a custom hex in the box.">
                                    <TextBlock Text="Color:" Foreground="#A0A0B8" FontSize="10"
                                               VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <Border x:Name="SwatchHighlightPink" Width="22" Height="22" CornerRadius="3"
                                            Background="#FF69B4" BorderBrush="#3A3A5A" BorderThickness="1"
                                            Margin="0,0,4,0" Cursor="Hand" Tag="#FF69B4"
                                            ToolTip="Hot pink — CCP default"
                                            MouseLeftButtonDown="AwarenessHighlightSwatch_Click"/>
                                    <Border x:Name="SwatchHighlightCyan" Width="22" Height="22" CornerRadius="3"
                                            Background="#00E5FF" BorderBrush="#3A3A5A" BorderThickness="1"
                                            Margin="0,0,4,0" Cursor="Hand" Tag="#00E5FF"
                                            ToolTip="Electric cyan — max contrast on dark UIs"
                                            MouseLeftButtonDown="AwarenessHighlightSwatch_Click"/>
                                    <Border x:Name="SwatchHighlightLime" Width="22" Height="22" CornerRadius="3"
                                            Background="#9DFF00" BorderBrush="#3A3A5A" BorderThickness="1"
                                            Margin="0,0,4,0" Cursor="Hand" Tag="#9DFF00"
                                            ToolTip="Lime — visible on pink/purple themes"
                                            MouseLeftButtonDown="AwarenessHighlightSwatch_Click"/>
                                    <Border x:Name="SwatchHighlightOrange" Width="22" Height="22" CornerRadius="3"
                                            Background="#FF8A00" BorderBrush="#3A3A5A" BorderThickness="1"
                                            Margin="0,0,4,0" Cursor="Hand" Tag="#FF8A00"
                                            ToolTip="Amber orange — warm alternative"
                                            MouseLeftButtonDown="AwarenessHighlightSwatch_Click"/>
                                    <Border x:Name="SwatchHighlightViolet" Width="22" Height="22" CornerRadius="3"
                                            Background="#B388FF" BorderBrush="#3A3A5A" BorderThickness="1"
                                            Margin="0,0,4,0" Cursor="Hand" Tag="#B388FF"
                                            ToolTip="Soft violet — matches SissyHypno theme"
                                            MouseLeftButtonDown="AwarenessHighlightSwatch_Click"/>
                                    <Border x:Name="SwatchHighlightWhite" Width="22" Height="22" CornerRadius="3"
                                            Background="#FFFFFF" BorderBrush="#3A3A5A" BorderThickness="1"
                                            Margin="0,0,8,0" Cursor="Hand" Tag="#FFFFFF"
                                            ToolTip="Pure white — most neutral option"
                                            MouseLeftButtonDown="AwarenessHighlightSwatch_Click"/>
                                    <TextBox x:Name="TxtAwarenessHighlightHex" Width="74" FontSize="10"
                                             Background="#1E1E32" Foreground="White" BorderBrush="#3A3A5A"
                                             Padding="4,3" VerticalAlignment="Center"
                                             ToolTip="Custom hex color (e.g. #FF00AA). Type and press Enter or Tab to apply."
                                             LostFocus="TxtAwarenessHighlightHex_LostFocus"
                                             KeyDown="TxtAwarenessHighlightHex_KeyDown"/>
                                </StackPanel>
                                <CheckBox x:Name="ChkAwarenessHighlightVisibleInCapture"
                                          Content="Visible in screen capture / streams"
                                          Foreground="#A0A0A0" FontSize="10" Margin="22,6,0,0"
                                          Checked="ChkAwarenessHighlightVisibleInCapture_Changed"
                                          Unchecked="ChkAwarenessHighlightVisibleInCapture_Changed">
                                    <CheckBox.ToolTip>
                                        <ToolTip>
                                            <StackPanel MaxWidth="340">
                                                <TextBlock Text="Visible in screen capture" Foreground="#FF69B4" FontWeight="Bold" FontSize="12" Margin="0,0,0,4"/>
                                                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="White">
                                                    Off (default): highlights render on your desktop but are excluded from screen capture via WDA_EXCLUDEFROMCAPTURE, so OBS, Discord, and screen recordings don't see them.
                                                </TextBlock>
                                                <TextBlock TextWrapping="Wrap" FontSize="11" Foreground="White" Margin="0,6,0,0">
                                                    On: lets the highlights show up in recordings — useful for streaming or demos. Also a fallback if your GPU driver is one of the few that hides excluded windows from the desktop itself.
                                                </TextBlock>
                                            </StackPanel>
                                        </ToolTip>
                                    </CheckBox.ToolTip>
                                </CheckBox>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- ==================== ADVANCED LINK ==================== -->
                    <Grid Grid.Row="5" Margin="0,0,0,0"
                          ToolTip="Opens a detailed editor for the currently installed preset where you can add/remove keywords, tweak per-trigger actions, and edit phrase pools. If no preset is installed yet, this link opens the App Info &amp; Data popup instead.">
                        <TextBlock HorizontalAlignment="Center">
                            <Hyperlink x:Name="LnkAwarenessAdvanced"
                                       Foreground="{DynamicResource PinkBrush}"
                                       TextDecorations="Underline"
                                       Click="LnkAwarenessAdvanced_Click">
                                <Run x:Name="LnkAwarenessAdvancedText" Text="Customize individual triggers (advanced) →"/>
                            </Hyperlink>
                        </TextBlock>
                    </Grid>
                </Grid>
            </ScrollViewer>

            <!-- Translucent gating overlay (only for free users) -->
            <Border x:Name="AwarenessGate" Visibility="Collapsed"
                    Background="#99000010" BorderBrush="#FF69B4" BorderThickness="2"
                    CornerRadius="10" IsHitTestVisible="True">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="🔒" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock Text="{loc:Str gate_premium_locked}" FontSize="22"
                               FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                    <TextBlock Text="{loc:Str gate_premium_subtitle}" FontSize="14"
                               Foreground="#CCCCCC" HorizontalAlignment="Center" Margin="0,4,0,12"/>
                    <Button Content="{loc:Str gate_unlock_with_patreon}"
                            Click="BtnGateUnlock_Click" Padding="20,8" Background="#FF69B4"
                            Foreground="White" FontWeight="Bold" Cursor="Hand" BorderThickness="0"/>
                </StackPanel>
            </Border>
          </Grid>
        </ContentControl>

        <!-- Remote Control Tab — Patreon premium feature, full redesign with QR pairing -->
        <ContentControl x:Name="RemoteControlTab" Grid.Row="4" Margin="10,5,10,10"
                        Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="36,28,36,28" MaxWidth="1280" HorizontalAlignment="Center">

                    <!-- Header -->
                    <Grid Margin="0,0,0,22">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🎮" FontSize="40" VerticalAlignment="Center" Margin="0,0,16,0"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{loc:Str tab_remote_control}" Foreground="White" FontSize="26" FontWeight="Bold"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="HelpBtnRemoteControl" Style="{StaticResource HelpButtonStyle}"
                                    VerticalAlignment="Center" Margin="0,0,8,0" Tag="RemoteControl"/>
                            <CheckBox x:Name="ChkRemoteControlEnabled" Style="{StaticResource ToggleStyle}"
                                      VerticalAlignment="Center"
                                      Checked="ChkRemoteControlEnabled_Changed" Unchecked="ChkRemoteControlEnabled_Changed"/>
                        </StackPanel>
                    </Grid>

                    <!-- Description card (always visible) -->
                    <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="{DynamicResource PinkBrush}"
                            BorderThickness="1" CornerRadius="12" Padding="24,20" Margin="0,0,0,22">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="56" Height="56" CornerRadius="28"
                                    Background="{DynamicResource PanelBgBrush}" BorderBrush="{DynamicResource PinkBrush}"
                                    BorderThickness="2" VerticalAlignment="Top">
                                <TextBlock Text="🎮" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="{loc:Str label_what_is_this}" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="11" FontWeight="Bold" Margin="0,0,0,4"/>
                                <TextBlock Text="{loc:Str desc_remote_control}" Foreground="White" FontSize="15"
                                           TextWrapping="Wrap" Margin="0,0,0,12" LineHeight="22"/>
                                <TextBlock Text="{loc:Str desc_remote_control_bullets}"
                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="13"
                                           TextWrapping="Wrap" LineHeight="20"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Interactive controls + translucent gating overlay -->
                    <Grid>
                        <StackPanel x:Name="RemoteControlControls">

                            <!-- Tier comparison cards (3 across) -->
                            <Grid Margin="0,0,0,22">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border x:Name="TierCardLight" Grid.Column="0" Background="{DynamicResource SurfaceBgBrush}"
                                        BorderBrush="#FF69B4" BorderThickness="2" CornerRadius="10" Padding="20,16"
                                        MinHeight="140" Cursor="Hand" Tag="0" MouseLeftButtonUp="TierCard_Click">
                                    <StackPanel>
                                        <TextBlock Text="✨" FontSize="26"/>
                                        <TextBlock Text="{loc:Str label_remote_tier_light}" Foreground="White"
                                                   FontSize="17" FontWeight="Bold" Margin="0,6,0,6"/>
                                        <TextBlock Text="Visual triggers only"
                                                   Foreground="{DynamicResource TextMutedBrush}" FontSize="13"
                                                   TextWrapping="Wrap"/>
                                        <TextBlock Text="• Flashes • Subliminals • Overlays" Foreground="#A0A0A0"
                                                   FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>

                                <Border x:Name="TierCardStandard" Grid.Column="2" Background="{DynamicResource SurfaceBgBrush}"
                                        BorderBrush="#2E2E48" BorderThickness="1" CornerRadius="10" Padding="20,16"
                                        MinHeight="140" Cursor="Hand" Tag="1" MouseLeftButtonUp="TierCard_Click">
                                    <StackPanel>
                                        <TextBlock Text="🎯" FontSize="26"/>
                                        <TextBlock Text="{loc:Str label_remote_tier_standard}" Foreground="White"
                                                   FontSize="17" FontWeight="Bold" Margin="0,6,0,6"/>
                                        <TextBlock Text="Light + audio + overlays"
                                                   Foreground="{DynamicResource TextMutedBrush}" FontSize="13"
                                                   TextWrapping="Wrap"/>
                                        <TextBlock Text="• Videos • Haptics • Audio ducking" Foreground="#A0A0A0"
                                                   FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>

                                <Border x:Name="TierCardFull" Grid.Column="4" Background="{DynamicResource SurfaceBgBrush}"
                                        BorderBrush="#2E2E48" BorderThickness="1" CornerRadius="10" Padding="20,16"
                                        MinHeight="140" Cursor="Hand" Tag="2" MouseLeftButtonUp="TierCard_Click">
                                    <StackPanel>
                                        <TextBlock Text="🔥" FontSize="26"/>
                                        <TextBlock Text="{loc:Str label_remote_tier_full}" Foreground="White"
                                                   FontSize="17" FontWeight="Bold" Margin="0,6,0,6"/>
                                        <TextBlock Text="Everything including session control"
                                                   Foreground="{DynamicResource TextMutedBrush}" FontSize="13"
                                                   TextWrapping="Wrap"/>
                                        <TextBlock Text="• Bambi Takeover • Sessions • Lockdown" Foreground="#A0A0A0"
                                                   FontSize="12" Margin="0,8,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Hidden ComboBox keeps existing handler wiring intact -->
                            <ComboBox x:Name="CmbRemoteTier" Visibility="Collapsed"
                                      SelectionChanged="CmbRemoteTier_SelectionChanged">
                                <ComboBoxItem Content="Light" IsSelected="True"/>
                                <ComboBoxItem Content="Standard"/>
                                <ComboBoxItem Content="Full"/>
                            </ComboBox>

                            <!-- Pairing hero panel: 2 columns (code 60% / QR 40%) -->
                            <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="{DynamicResource PinkBrush}"
                                    BorderThickness="1" CornerRadius="12" Padding="28,24" Margin="0,0,0,22">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="3*"/>
                                        <ColumnDefinition Width="20"/>
                                        <ColumnDefinition Width="2*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Left: code + PIN + buttons -->
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="SESSION CODE" Foreground="#8A8AA0" FontSize="12" FontWeight="Bold" Margin="0,0,0,8"/>
                                        <TextBlock x:Name="TxtRemoteCode" Text="- - - - - - - -"
                                                   Foreground="{DynamicResource PinkBrush}" FontSize="52"
                                                   FontWeight="Bold" FontFamily="Consolas"/>
                                        <TextBlock x:Name="TxtRemotePin" Text="" Foreground="{DynamicResource SecondaryBrush}"
                                                   FontSize="24" FontWeight="Bold" FontFamily="Consolas" Margin="0,6,0,0"
                                                   Visibility="Collapsed"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                                            <Button x:Name="BtnCopyRemoteCode" Content="{loc:Str btn_copy}"
                                                    Background="#353550" Foreground="White" FontSize="13"
                                                    Padding="14,8" BorderThickness="0" Cursor="Hand"
                                                    Click="BtnCopyRemoteCode_Click"/>
                                            <Button x:Name="BtnCopyRemoteLink" Content="{loc:Str btn_copy_link}"
                                                    Background="#353550" Foreground="White" FontSize="13"
                                                    Padding="14,8" Margin="10,0,0,0" BorderThickness="0" Cursor="Hand"
                                                    Click="BtnCopyRemoteLink_Click"/>
                                        </StackPanel>
                                        <Border x:Name="RemoteLinkPanel" Visibility="Collapsed"/>
                                        <Border x:Name="RemoteCodePanel" Visibility="Collapsed"/>
                                    </StackPanel>

                                    <!-- Right: QR code with themed frame -->
                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <Border CornerRadius="8" Background="White" Padding="12"
                                                BorderBrush="{DynamicResource PinkBrush}" BorderThickness="2">
                                            <Image x:Name="ImgRemoteQrCode" Width="220" Height="220" Stretch="Uniform"/>
                                        </Border>
                                        <TextBlock Text="{loc:Str label_remote_pairing}"
                                                   Foreground="{DynamicResource TextMutedBrush}" FontSize="13"
                                                   HorizontalAlignment="Center" Margin="0,10,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </Border>

                            <!-- Status row: Controller info (left) + Command log (right) -->
                            <Grid Margin="0,0,0,22">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="14"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Connected controller card -->
                                <Border Grid.Column="0" Background="{DynamicResource SurfaceBgBrush}"
                                        BorderBrush="#2E2E48" BorderThickness="1"
                                        CornerRadius="10" Padding="20,16">
                                    <StackPanel x:Name="ControllerInfoPanel">
                                        <Grid Margin="0,0,0,8">
                                            <TextBlock Text="{loc:Str label_remote_controller_info}"
                                                       Foreground="#8A8AA0" FontSize="12" FontWeight="Bold"/>
                                            <StackPanel x:Name="RemoteStatusPanel" Orientation="Horizontal"
                                                        HorizontalAlignment="Right">
                                                <Ellipse x:Name="RemoteStatusDot" Width="10" Height="10" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                                <TextBlock x:Name="TxtRemoteStatus" Text="{loc:Str label_remote_idle}" Foreground="#A0A0A0" FontSize="13"/>
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <!-- Command log -->
                                <Border Grid.Column="2" Background="{DynamicResource SurfaceBgBrush}"
                                        BorderBrush="#2E2E48" BorderThickness="1"
                                        CornerRadius="10" Padding="20,16">
                                    <StackPanel>
                                        <TextBlock Text="{loc:Str label_remote_command_log}"
                                                   Foreground="#8A8AA0" FontSize="12" FontWeight="Bold" Margin="0,0,0,8"/>
                                        <ListBox x:Name="LstRemoteCommandLog" Background="Transparent" BorderThickness="0"
                                                 Foreground="White" FontSize="13" MaxHeight="260"/>
                                    </StackPanel>
                                </Border>
                            </Grid>

                            <!-- Behavior: stop effects when controller disconnects -->
                            <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#2E2E48"
                                    BorderThickness="1" CornerRadius="10" Padding="20,14" Margin="0,0,0,16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{loc:Str label_remote_stop_on_disconnect}" Foreground="White"
                                                   FontSize="14" FontWeight="Bold"/>
                                        <TextBlock Text="{loc:Str desc_remote_stop_on_disconnect}"
                                                   Foreground="{DynamicResource TextMutedBrush}" FontSize="12"
                                                   TextWrapping="Wrap" Margin="0,4,0,0"/>
                                    </StackPanel>
                                    <CheckBox Grid.Column="1" x:Name="ChkStopEffectsOnRemoteDisconnect"
                                              Style="{StaticResource ToggleStyle}" VerticalAlignment="Center"
                                              Margin="16,0,0,0"
                                              Checked="ChkStopEffectsOnRemoteDisconnect_Changed"
                                              Unchecked="ChkStopEffectsOnRemoteDisconnect_Changed"/>
                                </Grid>
                            </Border>

                            <!-- Stop session button -->
                            <Button x:Name="BtnStopRemote" Content="{loc:Str btn_stop_session}" Background="#FF4444"
                                    Foreground="White" FontWeight="Bold" FontSize="14" Padding="18,10"
                                    BorderThickness="0" Cursor="Hand" HorizontalAlignment="Stretch"
                                    Visibility="Collapsed"
                                    Click="BtnStopRemote_Click"/>

                            <!-- Hidden legacy container — kept so existing handlers don't NRE -->
                            <StackPanel x:Name="RemoteControlPanel" Visibility="Collapsed"/>

                        </StackPanel>

                        <!-- Translucent gating overlay (only for free users) -->
                        <Border x:Name="RemoteControlGate" Visibility="Collapsed"
                                Background="#99000010" BorderBrush="#FF69B4" BorderThickness="2"
                                CornerRadius="10" IsHitTestVisible="True">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="🔒" FontSize="48" HorizontalAlignment="Center"/>
                                <TextBlock Text="{loc:Str gate_premium_locked}" FontSize="22"
                                           FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                <TextBlock Text="{loc:Str gate_premium_subtitle}" FontSize="14"
                                           Foreground="#CCCCCC" HorizontalAlignment="Center" Margin="0,4,0,12"/>
                                <Button Content="{loc:Str gate_unlock_with_patreon}"
                                        Click="BtnGateUnlock_Click" Padding="20,8" Background="#FF69B4"
                                        Foreground="White" FontWeight="Bold" Cursor="Hand" BorderThickness="0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                </StackPanel>
            </ScrollViewer>
        </ContentControl>

        <!-- Bambi Takeover Tab — Patreon premium feature, visible-but-locked for free users -->
        <ContentControl x:Name="BambiTakeoverTab" Grid.Row="4" Margin="10,5,10,10"
                        Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="36,28,36,28" MaxWidth="1180" HorizontalAlignment="Center">
                <!-- Header -->
                <Grid Margin="0,0,0,22">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="🤖" FontSize="40" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock x:Name="TxtBambiTakeoverHeader" Text="{loc:Str tab_bambi_takeover}" Foreground="White" FontSize="26" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>

                <!-- Description card (always visible — even to free users) -->
                <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="{DynamicResource PinkBrush}"
                        BorderThickness="1" CornerRadius="12" Padding="24,20" Margin="0,0,0,22">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="22"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" Width="160" Height="160" CornerRadius="12" ClipToBounds="True"
                                BorderBrush="{DynamicResource PinkBrush}" BorderThickness="2" VerticalAlignment="Top">
                            <Image x:Name="ImgBambiTakeoverDesc"
                                   Source="pack://application:,,,/Resources/features/bambi takeover.png"
                                   Stretch="UniformToFill"/>
                        </Border>
                        <StackPanel Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="{loc:Str label_what_is_this}" Foreground="{DynamicResource PinkBrush}"
                                       FontSize="11" FontWeight="Bold" Margin="0,0,0,4"/>
                            <TextBlock Text="{loc:Str desc_bambi_takeover}" Foreground="White" FontSize="15"
                                       TextWrapping="Wrap" Margin="0,0,0,12" LineHeight="22"/>
                            <TextBlock Text="{loc:Str desc_bambi_takeover_bullets}"
                                       Foreground="{DynamicResource TextMutedBrush}" FontSize="13"
                                       TextWrapping="Wrap" LineHeight="20"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Interactive controls + translucent gating overlay -->
                <Grid>
                    <StackPanel x:Name="BambiTakeoverControls">
                <!-- Lifted from old Bambi Expander — every x:Name preserved -->
                <!-- Locked State (legacy — kept hidden, replaced by gating overlay below) -->
                <StackPanel x:Name="AutonomyLocked" Visibility="Collapsed">
                    <Grid>
                        <TextBlock x:Name="TxtTakeoverLocked" Text="{loc:Str label_bambi_takeover}" Foreground="#666666" FontWeight="Bold" FontSize="13"/>
                        <TextBlock x:Name="TxtAutonomyLockStatus" Text="{loc:Str label_patreon_only}" Foreground="{DynamicResource SecondaryBrush}" HorizontalAlignment="Right" FontSize="11"/>
                    </Grid>
                    <TextBlock x:Name="TxtAutonomyLockMessage" Text="{loc:Str label_support_on_patreon_to_unlock}" Foreground="{DynamicResource PinkBrush}" FontSize="11" Margin="0,4,0,0"/>
                </StackPanel>

                <!-- Unlocked State — always visible now; gating handled by overlay -->
                <StackPanel x:Name="AutonomyUnlocked" Visibility="Visible">
                    <StackPanel x:Name="AutonomySettings">

                        <!-- ============ SUBSECTION 1: CADENCE ============ -->
                        <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#2E2E48"
                                BorderThickness="1" CornerRadius="10" Padding="20,16" Margin="0,0,0,14">
                            <StackPanel>
                                <TextBlock Text="{loc:Str setting_interval}" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="10"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <StackPanel Grid.Row="0" Grid.Column="0" ToolTip="{loc:Str tooltip_autonomy_interval}">
                                        <Grid><TextBlock Text="{loc:Str setting_interval}" Foreground="#AAA" FontSize="12"/><TextBlock x:Name="TxtAutonomyInterval" Text="{loc:Str label_60s}" Foreground="{DynamicResource PinkBrush}" FontSize="12" HorizontalAlignment="Right"/></Grid>
                                        <Slider x:Name="SliderAutonomyInterval" Minimum="30" Maximum="300" Value="60" Height="22" ValueChanged="SliderAutonomyInterval_Changed"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="0" Grid.Column="2" ToolTip="{loc:Str tooltip_autonomy_cooldown}">
                                        <Grid><TextBlock Text="{loc:Str label_cooldown}" Foreground="#AAA" FontSize="12"/><TextBlock x:Name="TxtAutonomyCooldown" Text="{loc:Str label_30s}" Foreground="{DynamicResource PinkBrush}" FontSize="12" HorizontalAlignment="Right"/></Grid>
                                        <Slider x:Name="SliderAutonomyCooldown" Minimum="10" Maximum="300" Value="30" Height="22" ValueChanged="SliderAutonomyCooldown_Changed"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="2" Grid.Column="0" ToolTip="{loc:Str tooltip_autonomy_intensity}">
                                        <Grid><TextBlock Text="{loc:Str setting_intensity}" Foreground="#AAA" FontSize="12"/><TextBlock x:Name="TxtAutonomyIntensity" Text="5" Foreground="{DynamicResource PinkBrush}" FontSize="12" HorizontalAlignment="Right"/></Grid>
                                        <Slider x:Name="SliderAutonomyIntensity" Minimum="1" Maximum="10" Value="5" Height="22" ValueChanged="SliderAutonomyIntensity_Changed"/>
                                    </StackPanel>
                                    <StackPanel Grid.Row="2" Grid.Column="2" ToolTip="{loc:Str tooltip_autonomy_announce}">
                                        <Grid><TextBlock Text="{loc:Str label_announce}" Foreground="#AAA" FontSize="12"/><TextBlock x:Name="TxtAutonomyAnnounce" Text="50%" Foreground="{DynamicResource PinkBrush}" FontSize="12" HorizontalAlignment="Right"/></Grid>
                                        <Slider x:Name="SliderAutonomyAnnounce" Minimum="0" Maximum="100" Value="50" Height="22" ValueChanged="SliderAutonomyAnnounce_Changed"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- ============ SUBSECTION 2: TRIGGERS ============ -->
                        <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#2E2E48"
                                BorderThickness="1" CornerRadius="10" Padding="20,16" Margin="0,0,0,14">
                            <StackPanel>
                                <TextBlock Text="{loc:Str label_triggers}" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="13" FontWeight="Bold" Margin="0,0,0,10"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid Grid.Column="0" Margin="0,2" ToolTip="{loc:Str tooltip_autonomy_idle}"><TextBlock Text="{loc:Str label_idle}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomyIdle" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyIdle_Changed" Unchecked="ChkAutonomyIdle_Changed"/></Grid>
                                    <Grid Grid.Column="2" Margin="0,2" ToolTip="{loc:Str tooltip_autonomy_random}"><TextBlock Text="{loc:Str label_random_2}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomyRandom" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyRandom_Changed" Unchecked="ChkAutonomyRandom_Changed"/></Grid>
                                    <Grid Grid.Column="4" Margin="0,2" ToolTip="{loc:Str tooltip_autonomy_time_aware}"><TextBlock Text="{loc:Str label_time_aware}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomyTimeAware" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyTimeAware_Changed" Unchecked="ChkAutonomyTimeAware_Changed"/></Grid>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- ============ SUBSECTION 3: BEHAVIORS ============ -->
                        <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#2E2E48"
                                BorderThickness="1" CornerRadius="10" Padding="20,16" Margin="0,0,0,14">
                            <StackPanel>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{loc:Str label_basic}" Foreground="{DynamicResource PinkBrush}" FontWeight="Bold" FontSize="13"/>
                                    <TextBlock Grid.Column="2" Text="{loc:Str label_advanced_2}" Foreground="{DynamicResource PinkBrush}" FontWeight="Bold" FontSize="13"/>
                                </Grid>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <!-- Basic column -->
                                    <StackPanel Grid.Column="0">
                                        <Grid Margin="0,2" ToolTip="{loc:Str tooltip_autonomy_flash}"><TextBlock x:Name="TxtAutoFlash" Text="{loc:Str tab_flashes}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomyFlash" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/></Grid>
                                        <Grid Margin="0,2" ToolTip="{loc:Str tooltip_autonomy_video}"><TextBlock x:Name="TxtAutoVideo" Text="{loc:Str tab_videos}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomyVideo" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/></Grid>
                                        <Grid Margin="0,2" ToolTip="{loc:Str tooltip_auto_plays_hypnotube_videos_fullscreen}"><TextBlock Text="{loc:Str label_web_videos}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomyWebVideo" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/></Grid>
                                        <Grid Margin="0,2" ToolTip="{loc:Str tooltip_autonomy_subliminal}"><TextBlock x:Name="TxtAutoSubliminal" Text="{loc:Str tab_subliminals}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomySubliminal" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/></Grid>
                                        <Grid Margin="0,2" ToolTip="{loc:Str tooltip_autonomy_comment}"><TextBlock Text="{loc:Str label_comments}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomyComment" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/></Grid>
                                    </StackPanel>
                                    <!-- Advanced column -->
                                    <StackPanel Grid.Column="2">
                                        <Grid Margin="0,2" ToolTip="{loc:Str tooltip_autonomy_bubbles}"><TextBlock x:Name="TxtAutoBubbles" Text="{loc:Str label_bubbles}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomyBubbles" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/></Grid>
                                        <Grid Margin="0,2" ToolTip="{loc:Str tooltip_autonomy_pink_filter}"><TextBlock x:Name="TxtAutoPinkFilter" Text="{loc:Str label_pink_filter_2}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomyPinkFilter" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/></Grid>
                                        <Grid Margin="0,2" ToolTip="{loc:Str tooltip_autonomy_lock_cards}"><TextBlock x:Name="TxtAutoLockCards" Text="{loc:Str label_lock_cards}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomyLockCard" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/></Grid>
                                        <Grid Margin="0,2" ToolTip="{loc:Str tooltip_autonomy_bouncing_text}"><TextBlock x:Name="TxtAutoBouncing" Text="{loc:Str label_bouncing}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomyBouncingText" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/></Grid>
                                        <Grid Margin="0,2" ToolTip="{loc:Str tooltip_autonomy_mind_wipe}"><TextBlock x:Name="TxtAutoMindwipe" Text="{loc:Str label_mindwipe}" FontSize="12" Foreground="#CCC" VerticalAlignment="Center"/><CheckBox x:Name="ChkAutonomyMindWipe" Style="{StaticResource ToggleStyle}" HorizontalAlignment="Right" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/></Grid>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <!-- ============ SUBSECTION 4: CONTROLS ============ -->
                        <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#2E2E48"
                                BorderThickness="1" CornerRadius="10" Padding="20,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Button x:Name="BtnAutonomyStartStop" Grid.Column="0" Content="{loc:Str btn_start_2}" Background="#2A1A3A" Foreground="#90EE90"
                                        FontWeight="Bold" FontSize="14" Padding="14,8"
                                        Click="BtnAutonomyStartStop_Click" ToolTip="{loc:Str tooltip_start_stop_bambi_takeover}"/>
                                <Button Grid.Column="2" Content="{loc:Str btn_test}" Background="{DynamicResource PinkBrush}" Foreground="White" FontWeight="Bold" FontSize="14"
                                        Padding="14,8" Click="BtnTestAutonomy_Click" ToolTip="{loc:Str tooltip_test_action}"/>
                            </Grid>
                        </Border>

                        <!-- Hidden elements -->
                        <TextBlock x:Name="TxtTakeoverUnlocked" Visibility="Collapsed"/>
                        <CheckBox x:Name="ChkAutonomySpiral" Visibility="Collapsed" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                        <CheckBox x:Name="ChkAutonomyBubbleCount" Visibility="Collapsed" Checked="ChkAutonomyBehavior_Changed" Unchecked="ChkAutonomyBehavior_Changed"/>
                        <Button x:Name="BtnForceStartAutonomy" Visibility="Collapsed" Click="BtnForceStartAutonomy_Click"/>
                        <CheckBox x:Name="ChkAutonomyEnabled" Visibility="Collapsed" Checked="ChkAutonomyEnabled_Changed" Unchecked="ChkAutonomyEnabled_Changed"/>
                    </StackPanel>
                </StackPanel>
                    </StackPanel>

                    <!-- Translucent gating overlay (only for free users) -->
                    <Border x:Name="BambiTakeoverGate" Visibility="Collapsed"
                            Background="#99000010" BorderBrush="#FF69B4" BorderThickness="2"
                            CornerRadius="10" IsHitTestVisible="True">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="🔒" FontSize="48" HorizontalAlignment="Center"/>
                            <TextBlock Text="{loc:Str gate_premium_locked}" FontSize="22"
                                       FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Text="{loc:Str gate_premium_subtitle}" FontSize="14"
                                       Foreground="#CCCCCC" HorizontalAlignment="Center" Margin="0,4,0,12"/>
                            <Button Content="{loc:Str gate_unlock_with_patreon}"
                                    Click="BtnGateUnlock_Click" Padding="20,8" Background="#FF69B4"
                                    Foreground="White" FontWeight="Bold" Cursor="Hand" BorderThickness="0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                </StackPanel>
            </ScrollViewer>
        </ContentControl>

        <!-- Haptics Tab — Patreon premium feature, visible-but-locked for free users -->
        <ContentControl x:Name="HapticsTab" Grid.Row="4" Margin="10,5,10,10"
                        Visibility="Collapsed" Template="{StaticResource AnimatedTabBackground}">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="36,28,36,28" MaxWidth="1280" HorizontalAlignment="Center">

                    <!-- Header -->
                    <Grid Margin="0,0,0,22">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="💜" FontSize="40" VerticalAlignment="Center" Margin="0,0,16,0"/>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{loc:Str tab_haptics}" Foreground="White" FontSize="26" FontWeight="Bold"/>
                        </StackPanel>
                    </Grid>

                    <!-- Description card (always visible) -->
                    <Border Background="{DynamicResource SurfaceBgBrush}" BorderBrush="{DynamicResource PinkBrush}"
                            BorderThickness="1" CornerRadius="12" Padding="24,20" Margin="0,0,0,22">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="22"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Width="160" Height="160" CornerRadius="12" ClipToBounds="True"
                                    BorderBrush="{DynamicResource PinkBrush}" BorderThickness="2" VerticalAlignment="Top">
                                <Image x:Name="ImgHapticsVibeDesc"
                                       Source="pack://application:,,,/Resources/features/vibe.png"
                                       Stretch="UniformToFill"/>
                            </Border>
                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock Text="{loc:Str label_what_is_this}" Foreground="{DynamicResource PinkBrush}"
                                           FontSize="11" FontWeight="Bold" Margin="0,0,0,4"/>
                                <TextBlock Text="{loc:Str desc_haptics}" Foreground="White" FontSize="15"
                                           TextWrapping="Wrap" Margin="0,0,0,12" LineHeight="22"/>
                                <TextBlock Text="{loc:Str desc_haptics_bullets}"
                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="13"
                                           TextWrapping="Wrap" LineHeight="20"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Interactive controls + translucent gating overlay -->
                    <Grid>
                        <Grid>
                    <!-- Haptics Content (enabled for Patreon supporters) -->
                    <Grid x:Name="HapticsContentGrid">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="14"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- ============ SECTION 1: CONNECTION ============ -->
                    <Grid Grid.Row="0">
                        <Border x:Name="HapticsConnectionBox" CornerRadius="10" Padding="22,18"
                                Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#2E2E48" BorderThickness="1">
                            <StackPanel>
                                <!-- Header: title + master toggle -->
                                <Grid Margin="0,0,0,16">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="🔌" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <TextBlock Text="{loc:Str label_haptic_connection}" Foreground="{DynamicResource PinkBrush}"
                                                   FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
                                        <Button x:Name="HelpBtnHaptics" Style="{StaticResource HelpButtonStyle}" VerticalAlignment="Center" Margin="8,0,0,0" Tag="Haptics"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <TextBlock Text="{loc:Str btn_enable}" Foreground="#AAA" FontSize="12"
                                                   VerticalAlignment="Center" Margin="0,0,8,0"/>
                                        <CheckBox x:Name="ChkHapticsEnabled" Style="{StaticResource ToggleStyle}"
                                                  VerticalAlignment="Center"
                                                  Checked="ChkHapticsEnabled_Changed" Unchecked="ChkHapticsEnabled_Changed"/>
                                    </StackPanel>
                                </Grid>

                                <!-- Provider + URL + Connect (single horizontal row) -->
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="200"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="12"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{loc:Str label_provider}" Foreground="White" FontSize="13"
                                               VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <ComboBox x:Name="CmbHapticProvider" Grid.Column="1" Padding="8,4" FontSize="13"
                                              Background="{DynamicResource SurfaceBgBrush}" Foreground="Black" BorderThickness="0"
                                              SelectionChanged="CmbHapticProvider_SelectionChanged">
                                        <ComboBoxItem Content="{loc:Str btn_lovense_connect}" Tag="Lovense" Foreground="Black"/>
                                        <ComboBoxItem Content="{loc:Str btn_buttplug_io_intiface}" Tag="Buttplug" Foreground="Black"/>
                                    </ComboBox>
                                    <TextBox x:Name="TxtHapticUrl" Grid.Column="3"
                                             Background="#FFFFFF" Foreground="Black" BorderThickness="1"
                                             BorderBrush="{DynamicResource PanelAccentBrush}" Padding="8,6" FontSize="13"
                                             VerticalContentAlignment="Center"
                                             TextChanged="TxtHapticUrl_TextChanged">
                                        <TextBox.ToolTip>
                                            <ToolTip Background="{DynamicResource SurfaceBgBrush}" BorderBrush="{DynamicResource SecondaryBrush}" BorderThickness="1" Padding="12">
                                                <StackPanel MaxWidth="280">
                                                    <TextBlock Text="{loc:Str label_connection_guide}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="Bold" Margin="0,0,0,8"/>
                                                    <TextBlock Text="{loc:Str label_lovense_connect}" Foreground="{DynamicResource SecondaryBrush}" FontSize="11" FontWeight="Bold"/>
                                                    <TextBlock Foreground="#CCCCCC" FontSize="10" TextWrapping="Wrap" Margin="0,2,0,6">
                                                        <Run Text="1."/><Run Text="{loc:Str label_open_lovense_remote_app}" Foreground="White"/>
                                                        <LineBreak/><Run Text="2."/><Run Text="{loc:Str label_go_to_settings_game_mode}" Foreground="White"/>
                                                        <LineBreak/><Run Text="3."/><Run Text="{loc:Str label_enable_game_mode_note_the_ip}" Foreground="White"/>
                                                        <LineBreak/><Run Text="4."/><Run Text="{loc:Str label_enter_http_ip_30010}" Foreground="{DynamicResource PinkBrush}"/>
                                                    </TextBlock>
                                                    <TextBlock Text="{loc:Str label_buttplug_io_intiface}" Foreground="{DynamicResource SecondaryBrush}" FontSize="11" FontWeight="Bold"/>
                                                    <TextBlock Foreground="#CCCCCC" FontSize="10" TextWrapping="Wrap" Margin="0,2,0,0">
                                                        <Run Text="1."/><Run Text="{loc:Str label_open_intiface_central}" Foreground="White"/>
                                                        <LineBreak/><Run Text="2."/><Run Text="{loc:Str label_click_start_server}" Foreground="White"/>
                                                        <LineBreak/><Run Text="3."/><Run Text="{loc:Str label_click_start_scanning_for_devices}" Foreground="White"/>
                                                        <LineBreak/><Run Text="4."/><Run Text="{loc:Str label_use_ws_localhost_12345}" Foreground="{DynamicResource PinkBrush}"/>
                                                    </TextBlock>
                                                </StackPanel>
                                            </ToolTip>
                                        </TextBox.ToolTip>
                                    </TextBox>
                                    <Button x:Name="BtnHapticConnect" Grid.Column="5" Content="{loc:Str btn_connect}"
                                            Background="{DynamicResource PinkBrush}" Foreground="White"
                                            BorderThickness="0" Padding="18,8"
                                            FontSize="13" FontWeight="Bold" Cursor="Hand"
                                            Click="BtnHapticConnect_Click"/>
                                </Grid>

                                <!-- URL hint + auto-connect (single row) -->
                                <Grid Margin="0,0,0,14">
                                    <TextBlock x:Name="TxtHapticUrlHint"
                                               Text="{loc:Str label_lovense_enter_ip_from_lovense_remote_settings}"
                                               Foreground="{DynamicResource TextMutedBrush}" FontSize="12"
                                               TextWrapping="Wrap" VerticalAlignment="Center"/>
                                    <CheckBox x:Name="ChkHapticAutoConnect" Content="{loc:Str setting_auto_connect_on_startup}"
                                              Foreground="#CCCCCC" FontSize="12" HorizontalAlignment="Right"
                                              Checked="ChkHapticAutoConnect_Changed" Unchecked="ChkHapticAutoConnect_Changed"
                                              ToolTip="{loc:Str tooltip_automatically_connect_to_your_haptic_device_w}"/>
                                </Grid>

                                <!-- Status + Master Intensity + Test (in inset card) -->
                                <Border Background="{DynamicResource PanelBgBrush}" CornerRadius="8" Padding="16,12">
                                    <StackPanel>
                                        <!-- Status row -->
                                        <Grid Margin="0,0,0,10">
                                            <StackPanel Orientation="Horizontal">
                                                <Ellipse Width="10" Height="10" Margin="0,0,8,0" VerticalAlignment="Center" Fill="#FF6B6B"/>
                                                <TextBlock Text="{loc:Str label_status}" Foreground="{DynamicResource TextMutedBrush}" FontSize="13" VerticalAlignment="Center"/>
                                                <TextBlock x:Name="TxtHapticStatus" Text="{loc:Str label_disconnected}"
                                                           Foreground="#FF6B6B" FontSize="13" Margin="6,0,0,0" VerticalAlignment="Center"/>
                                            </StackPanel>
                                            <TextBlock x:Name="TxtHapticDevices" Text="{loc:Str label_no_devices}"
                                                       Foreground="{DynamicResource TextDimBrush}" FontSize="12"
                                                       HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                        </Grid>
                                        <!-- Intensity slider + test -->
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="50"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{loc:Str label_intensity}" Foreground="White" FontSize="13" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                            <Slider x:Name="SliderHapticIntensity" Grid.Column="1"
                                                    Style="{StaticResource PinkSlider}"
                                                    Minimum="0" Maximum="100" Value="70" VerticalAlignment="Center"
                                                    ValueChanged="SliderHapticIntensity_ValueChanged"/>
                                            <TextBlock x:Name="TxtHapticIntensity" Grid.Column="2" Text="70%"
                                                       Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="Bold"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            <Button x:Name="BtnHapticTest" Grid.Column="3" Content="{loc:Str btn_test_vibration}"
                                                    Background="{DynamicResource SurfaceBgBrush}" Foreground="White"
                                                    BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1"
                                                    Padding="14,8" Margin="12,0,0,0"
                                                    FontSize="13" FontWeight="Bold" Cursor="Hand"
                                                    Click="BtnHapticTest_Click"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>

                                <!-- Help link -->
                                <Button x:Name="BtnHapticsHelp" Content="{loc:Str btn_need_help_setting_up}"
                                        HorizontalAlignment="Left" Margin="0,10,0,0" Background="Transparent"
                                        Foreground="{DynamicResource PinkBrush}" BorderThickness="0" FontSize="12"
                                        Cursor="Hand" Click="BtnHapticsHelp_Click" Padding="0">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <TextBlock Text="{TemplateBinding Content}"
                                                                   Foreground="{TemplateBinding Foreground}"
                                                                   FontSize="{TemplateBinding FontSize}"
                                                                   TextDecorations="Underline"/>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Border>
                        <!-- Lock Overlay (legacy, hidden — gating handled by HapticsGate) -->
                        <Border x:Name="HapticsConnectionLock" Background="#80000000" CornerRadius="10" Visibility="Collapsed">
                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="🔒" FontSize="32" HorizontalAlignment="Center"/>
                                <TextBlock Text="{loc:Str label_patreon_only_2}" Foreground="{DynamicResource SecondaryBrush}" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!-- ============ SECTION 2: PER-FEATURE INTENSITY ============ -->
                    <Grid Grid.Row="2">
                        <Border x:Name="HapticsFeatureBox" CornerRadius="10" Padding="22,18"
                                Background="{DynamicResource SurfaceBgBrush}" BorderBrush="#2E2E48" BorderThickness="1">
                            <StackPanel>
                                <!-- Header -->
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                    <TextBlock Text="✨" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                    <TextBlock Text="{loc:Str section_feature_intensity}" Foreground="{DynamicResource PinkBrush}"
                                               FontSize="15" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="{loc:Str label_adjust_vibration_strength_per_feature}"
                                           Foreground="{DynamicResource TextMutedBrush}" FontSize="12" Margin="0,0,0,14"/>

                                <!-- 2-column feature grid -->
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="22"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- LEFT COLUMN: 5 features -->
                                    <StackPanel Grid.Column="0">

                                        <!-- Bubble Pop -->
                                        <Grid Margin="0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="44"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="38"/>
                                                <ColumnDefinition Width="92"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox x:Name="ChkHapticBubble" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                                      VerticalAlignment="Center" Checked="ChkHapticFeature_Changed" Unchecked="ChkHapticFeature_Changed" Tag="Bubble"/>
                                            <TextBlock Grid.Column="1" Text="{loc:Str label_bubbles}" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                            <Slider x:Name="SliderHapticBubble" Grid.Column="2" Style="{StaticResource PinkSlider}"
                                                    Minimum="0" Maximum="100" Value="50" Margin="5,0" VerticalAlignment="Center"
                                                    ValueChanged="SliderHapticFeature_Changed" Tag="Bubble"/>
                                            <TextBlock x:Name="TxtHapticBubble" Grid.Column="3" Text="50%" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            <ComboBox x:Name="CmbHapticBubbleMode" Grid.Column="4" SelectedIndex="0" FontSize="11" Margin="4,0,0,0" VerticalAlignment="Center"
                                                      Background="{DynamicResource SurfaceBgBrush}" Foreground="{DynamicResource PinkBrush}" BorderBrush="{DynamicResource SecondaryBrush}"
                                                      SelectionChanged="CmbHapticMode_SelectionChanged" Tag="Bubble">
                                                <ComboBoxItem Content="{loc:Str btn_constant}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_pulse}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_wave}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_heartbeat}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_escalate}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_earthquake}" Foreground="{DynamicResource PinkBrush}"/>
                                            </ComboBox>
                                        </Grid>

                                        <!-- Flash Display -->
                                        <Grid Margin="0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="44"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="38"/>
                                                <ColumnDefinition Width="92"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox x:Name="ChkHapticFlashDisplay" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                                      VerticalAlignment="Center" Checked="ChkHapticFeature_Changed" Unchecked="ChkHapticFeature_Changed" Tag="FlashDisplay"/>
                                            <TextBlock Grid.Column="1" Text="{loc:Str label_flash_show}" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                            <Slider x:Name="SliderHapticFlashDisplay" Grid.Column="2" Style="{StaticResource PinkSlider}"
                                                    Minimum="0" Maximum="100" Value="50" Margin="5,0" VerticalAlignment="Center"
                                                    ValueChanged="SliderHapticFeature_Changed" Tag="FlashDisplay"/>
                                            <TextBlock x:Name="TxtHapticFlashDisplay" Grid.Column="3" Text="50%" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            <ComboBox x:Name="CmbHapticFlashDisplayMode" Grid.Column="4" SelectedIndex="0" FontSize="11" Margin="4,0,0,0" VerticalAlignment="Center"
                                                      Background="{DynamicResource SurfaceBgBrush}" Foreground="{DynamicResource PinkBrush}" BorderBrush="{DynamicResource SecondaryBrush}"
                                                      SelectionChanged="CmbHapticMode_SelectionChanged" Tag="FlashDisplay">
                                                <ComboBoxItem Content="{loc:Str btn_constant}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_pulse}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_wave}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_heartbeat}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_escalate}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_earthquake}" Foreground="{DynamicResource PinkBrush}"/>
                                            </ComboBox>
                                        </Grid>

                                        <!-- Flash Click -->
                                        <Grid Margin="0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="44"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="38"/>
                                                <ColumnDefinition Width="92"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox x:Name="ChkHapticFlashClick" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                                      VerticalAlignment="Center" Checked="ChkHapticFeature_Changed" Unchecked="ChkHapticFeature_Changed" Tag="FlashClick"/>
                                            <TextBlock Grid.Column="1" Text="{loc:Str label_flash_click}" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                            <Slider x:Name="SliderHapticFlashClick" Grid.Column="2" Style="{StaticResource PinkSlider}"
                                                    Minimum="0" Maximum="100" Value="50" Margin="5,0" VerticalAlignment="Center"
                                                    ValueChanged="SliderHapticFeature_Changed" Tag="FlashClick"/>
                                            <TextBlock x:Name="TxtHapticFlashClick" Grid.Column="3" Text="50%" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            <ComboBox x:Name="CmbHapticFlashClickMode" Grid.Column="4" SelectedIndex="0" FontSize="11" Margin="4,0,0,0" VerticalAlignment="Center"
                                                      Background="{DynamicResource SurfaceBgBrush}" Foreground="{DynamicResource PinkBrush}" BorderBrush="{DynamicResource SecondaryBrush}"
                                                      SelectionChanged="CmbHapticMode_SelectionChanged" Tag="FlashClick">
                                                <ComboBoxItem Content="{loc:Str btn_constant}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_pulse}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_wave}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_heartbeat}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_escalate}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_earthquake}" Foreground="{DynamicResource PinkBrush}"/>
                                            </ComboBox>
                                        </Grid>

                                        <!-- Video -->
                                        <Grid Margin="0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="44"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="38"/>
                                                <ColumnDefinition Width="92"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox x:Name="ChkHapticVideo" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                                      VerticalAlignment="Center" Checked="ChkHapticFeature_Changed" Unchecked="ChkHapticFeature_Changed" Tag="Video"/>
                                            <TextBlock Grid.Column="1" Text="{loc:Str tab_videos}" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                            <Slider x:Name="SliderHapticVideo" Grid.Column="2" Style="{StaticResource PinkSlider}"
                                                    Minimum="0" Maximum="100" Value="50" Margin="5,0" VerticalAlignment="Center"
                                                    ValueChanged="SliderHapticFeature_Changed" Tag="Video"/>
                                            <TextBlock x:Name="TxtHapticVideo" Grid.Column="3" Text="50%" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            <ComboBox x:Name="CmbHapticVideoMode" Grid.Column="4" SelectedIndex="0" FontSize="11" Margin="4,0,0,0" VerticalAlignment="Center"
                                                      Background="{DynamicResource SurfaceBgBrush}" Foreground="{DynamicResource PinkBrush}" BorderBrush="{DynamicResource SecondaryBrush}"
                                                      SelectionChanged="CmbHapticMode_SelectionChanged" Tag="Video">
                                                <ComboBoxItem Content="{loc:Str btn_constant}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_pulse}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_wave}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_heartbeat}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_escalate}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_earthquake}" Foreground="{DynamicResource PinkBrush}"/>
                                            </ComboBox>
                                        </Grid>

                                        <!-- Target Hit (video attention targets) -->
                                        <Grid Margin="0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="44"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="38"/>
                                                <ColumnDefinition Width="92"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox x:Name="ChkHapticTargetHit" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                                      VerticalAlignment="Center" Checked="ChkHapticFeature_Changed" Unchecked="ChkHapticFeature_Changed" Tag="TargetHit"/>
                                            <TextBlock Grid.Column="1" Text="{loc:Str label_target_hit}" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                            <Slider x:Name="SliderHapticTargetHit" Grid.Column="2" Style="{StaticResource PinkSlider}"
                                                    Minimum="0" Maximum="100" Value="70" Margin="5,0" VerticalAlignment="Center"
                                                    ValueChanged="SliderHapticFeature_Changed" Tag="TargetHit"/>
                                            <TextBlock x:Name="TxtHapticTargetHit" Grid.Column="3" Text="70%" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            <ComboBox x:Name="CmbHapticTargetHitMode" Grid.Column="4" SelectedIndex="1" FontSize="11" Margin="4,0,0,0" VerticalAlignment="Center"
                                                      Background="{DynamicResource SurfaceBgBrush}" Foreground="{DynamicResource PinkBrush}" BorderBrush="{DynamicResource SecondaryBrush}"
                                                      SelectionChanged="CmbHapticMode_SelectionChanged" Tag="TargetHit">
                                                <ComboBoxItem Content="{loc:Str btn_constant}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_pulse}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_wave}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_heartbeat}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_escalate}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_earthquake}" Foreground="{DynamicResource PinkBrush}"/>
                                            </ComboBox>
                                        </Grid>

                                    </StackPanel>

                                    <!-- RIGHT COLUMN: 4 features -->
                                    <StackPanel Grid.Column="2">

                                        <!-- Subliminal -->
                                        <Grid Margin="0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="44"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="38"/>
                                                <ColumnDefinition Width="92"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox x:Name="ChkHapticSubliminal" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                                      VerticalAlignment="Center" Checked="ChkHapticFeature_Changed" Unchecked="ChkHapticFeature_Changed" Tag="Subliminal"/>
                                            <TextBlock Grid.Column="1" Text="{loc:Str tab_subliminals}" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                            <Slider x:Name="SliderHapticSubliminal" Grid.Column="2" Style="{StaticResource PinkSlider}"
                                                    Minimum="0" Maximum="100" Value="50" Margin="5,0" VerticalAlignment="Center"
                                                    ValueChanged="SliderHapticFeature_Changed" Tag="Subliminal"/>
                                            <TextBlock x:Name="TxtHapticSubliminal" Grid.Column="3" Text="50%" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            <ComboBox x:Name="CmbHapticSubliminalMode" Grid.Column="4" SelectedIndex="1" FontSize="11" Margin="4,0,0,0" VerticalAlignment="Center"
                                                      Background="{DynamicResource SurfaceBgBrush}" Foreground="{DynamicResource PinkBrush}" BorderBrush="{DynamicResource SecondaryBrush}"
                                                      SelectionChanged="CmbHapticMode_SelectionChanged" Tag="Subliminal">
                                                <ComboBoxItem Content="{loc:Str btn_constant}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_pulse}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_wave}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_heartbeat}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_escalate}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_earthquake}" Foreground="{DynamicResource PinkBrush}"/>
                                            </ComboBox>
                                        </Grid>

                                        <!-- Level Up -->
                                        <Grid Margin="0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="44"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="38"/>
                                                <ColumnDefinition Width="92"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox x:Name="ChkHapticLevelUp" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                                      VerticalAlignment="Center" Checked="ChkHapticFeature_Changed" Unchecked="ChkHapticFeature_Changed" Tag="LevelUp"/>
                                            <TextBlock Grid.Column="1" Text="{loc:Str label_level_up}" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                            <Slider x:Name="SliderHapticLevelUp" Grid.Column="2" Style="{StaticResource PinkSlider}"
                                                    Minimum="0" Maximum="100" Value="50" Margin="5,0" VerticalAlignment="Center"
                                                    ValueChanged="SliderHapticFeature_Changed" Tag="LevelUp"/>
                                            <TextBlock x:Name="TxtHapticLevelUp" Grid.Column="3" Text="50%" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            <ComboBox x:Name="CmbHapticLevelUpMode" Grid.Column="4" SelectedIndex="4" FontSize="11" Margin="4,0,0,0" VerticalAlignment="Center"
                                                      Background="{DynamicResource SurfaceBgBrush}" Foreground="{DynamicResource PinkBrush}" BorderBrush="{DynamicResource SecondaryBrush}"
                                                      SelectionChanged="CmbHapticMode_SelectionChanged" Tag="LevelUp">
                                                <ComboBoxItem Content="{loc:Str btn_constant}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_pulse}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_wave}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_heartbeat}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_escalate}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_earthquake}" Foreground="{DynamicResource PinkBrush}"/>
                                            </ComboBox>
                                        </Grid>

                                        <!-- Achievement -->
                                        <Grid Margin="0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="44"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="38"/>
                                                <ColumnDefinition Width="92"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox x:Name="ChkHapticAchievement" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                                      VerticalAlignment="Center" Checked="ChkHapticFeature_Changed" Unchecked="ChkHapticFeature_Changed" Tag="Achievement"/>
                                            <TextBlock Grid.Column="1" Text="{loc:Str tab_achievements}" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                            <Slider x:Name="SliderHapticAchievement" Grid.Column="2" Style="{StaticResource PinkSlider}"
                                                    Minimum="0" Maximum="100" Value="50" Margin="5,0" VerticalAlignment="Center"
                                                    ValueChanged="SliderHapticFeature_Changed" Tag="Achievement"/>
                                            <TextBlock x:Name="TxtHapticAchievement" Grid.Column="3" Text="50%" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            <ComboBox x:Name="CmbHapticAchievementMode" Grid.Column="4" SelectedIndex="3" FontSize="11" Margin="4,0,0,0" VerticalAlignment="Center"
                                                      Background="{DynamicResource SurfaceBgBrush}" Foreground="{DynamicResource PinkBrush}" BorderBrush="{DynamicResource SecondaryBrush}"
                                                      SelectionChanged="CmbHapticMode_SelectionChanged" Tag="Achievement">
                                                <ComboBoxItem Content="{loc:Str btn_constant}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_pulse}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_wave}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_heartbeat}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_escalate}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_earthquake}" Foreground="{DynamicResource PinkBrush}"/>
                                            </ComboBox>
                                        </Grid>

                                        <!-- Bouncing Text -->
                                        <Grid Margin="0,5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="44"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="38"/>
                                                <ColumnDefinition Width="92"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox x:Name="ChkHapticBouncingText" Style="{StaticResource ToggleStyle}" IsChecked="True"
                                                      VerticalAlignment="Center" Checked="ChkHapticFeature_Changed" Unchecked="ChkHapticFeature_Changed" Tag="BouncingText"/>
                                            <TextBlock Grid.Column="1" Text="{loc:Str label_bounce_text}" Foreground="White" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
                                            <Slider x:Name="SliderHapticBouncingText" Grid.Column="2" Style="{StaticResource PinkSlider}"
                                                    Minimum="0" Maximum="100" Value="50" Margin="5,0" VerticalAlignment="Center"
                                                    ValueChanged="SliderHapticFeature_Changed" Tag="BouncingText"/>
                                            <TextBlock x:Name="TxtHapticBouncingText" Grid.Column="3" Text="50%" Foreground="{DynamicResource PinkBrush}" FontSize="12" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            <ComboBox x:Name="CmbHapticBouncingTextMode" Grid.Column="4" SelectedIndex="1" FontSize="11" Margin="4,0,0,0" VerticalAlignment="Center"
                                                      Background="{DynamicResource SurfaceBgBrush}" Foreground="{DynamicResource PinkBrush}" BorderBrush="{DynamicResource SecondaryBrush}"
                                                      SelectionChanged="CmbHapticMode_SelectionChanged" Tag="BouncingText">
                                                <ComboBoxItem Content="{loc:Str btn_constant}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_pulse}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_wave}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_heartbeat}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_escalate}" Foreground="{DynamicResource PinkBrush}"/>
                                                <ComboBoxItem Content="{loc:Str btn_earthquake}" Foreground="{DynamicResource PinkBrush}"/>
                                            </ComboBox>
                                        </Grid>

                                    </StackPanel>
                                </Grid>

                        <!-- Video Haptic Sync - Enhanced Featured Card -->
                        <Border x:Name="VideoHapticSyncCard" BorderBrush="{DynamicResource SecondaryBrush}" BorderThickness="1,2,1,1"
                                CornerRadius="8" Margin="0,12,0,0" Background="{DynamicResource SurfaceBgBrush}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Row 0: Header Section -->
                                <Border Grid.Row="0" Background="{DynamicResource PanelBgBrush}" CornerRadius="8,8,0,0" Padding="12,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Title and Description -->
                                        <StackPanel Grid.Column="0">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="🎵" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                                <TextBlock Text="{loc:Str label_video_haptic_sync}" Foreground="{DynamicResource SecondaryBrush}"
                                                           FontSize="14" FontWeight="Bold" VerticalAlignment="Center"/>
                                            </StackPanel>
                                            <TextBlock Text="{loc:Str label_syncs_haptics_to_video_audio_in_real_time}"
                                                       Foreground="{DynamicResource TextMutedBrush}" FontSize="10" Margin="0,2,0,0"/>
                                        </StackPanel>

                                        <!-- Help Button -->
                                        <Button x:Name="HelpBtnVideoHapticSync" Grid.Column="1"
                                                Style="{StaticResource HelpButtonStyle}" Tag="VideoHapticSync"
                                                Margin="0,0,10,0"/>

                                        <!-- Master Enable Toggle -->
                                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                                            <TextBlock Text="{loc:Str btn_enable}" Foreground="#AAAAAA" FontSize="10"
                                                       VerticalAlignment="Center" Margin="0,0,8,0"/>
                                            <CheckBox x:Name="ChkHapticAudioSync" Style="{StaticResource ToggleStyle}"
                                                      IsChecked="False"
                                                      Checked="ChkHapticAudioSync_Changed"
                                                      Unchecked="ChkHapticAudioSync_Changed"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Row 1: Algorithm Cards (left) + Image (right) -->
                                <Grid Grid.Row="1" Margin="12,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Left: Algorithm Selection Cards (stacked vertically) -->
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{loc:Str label_sync_algorithm}" Foreground="#AAAAAA" FontSize="10" Margin="0,0,0,5"/>

                                        <!-- Algorithm 1: Audio Reactive (Enabled) -->
                                        <Border x:Name="AlgorithmCardAudioReactive"
                                                Background="{DynamicResource PanelBgBrush}" CornerRadius="4" Padding="8,5" Cursor="Hand"
                                                BorderThickness="2" BorderBrush="{DynamicResource SecondaryBrush}" Margin="0,0,0,3"
                                                MouseLeftButtonDown="AlgorithmCard_Click" Tag="AudioReactive">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#2A2A4E"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{loc:Str label_audio_reactive}" Foreground="White"
                                                           FontSize="10" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                                <Ellipse x:Name="AlgorithmIndicatorAudioReactive" Grid.Column="1"
                                                         Width="6" Height="6" Fill="{DynamicResource SecondaryBrush}"
                                                         VerticalAlignment="Center" Margin="8,0,0,0"/>
                                            </Grid>
                                        </Border>

                                        <!-- Algorithm 2: Beat Detection (Coming Soon) -->
                                        <Grid Margin="0,0,0,3">
                                            <Border Background="{DynamicResource DarkerBgBrush}" CornerRadius="4" Padding="8,5">
                                                <Border.Effect>
                                                    <BlurEffect Radius="2"/>
                                                </Border.Effect>
                                                <TextBlock Text="{loc:Str label_beat_detection}" Foreground="#666666"
                                                           FontSize="10" FontWeight="SemiBold"/>
                                            </Border>
                                            <Border Background="#CC1A1A2E" CornerRadius="4">
                                                <TextBlock Text="{loc:Str label_coming_soon}" Foreground="{DynamicResource SecondaryBrush}" FontSize="8"
                                                           FontWeight="Bold" HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                            </Border>
                                        </Grid>

                                        <!-- Algorithm 3: AI Enhanced (Coming Soon) -->
                                        <Grid>
                                            <Border Background="{DynamicResource DarkerBgBrush}" CornerRadius="4" Padding="8,5">
                                                <Border.Effect>
                                                    <BlurEffect Radius="2"/>
                                                </Border.Effect>
                                                <TextBlock Text="{loc:Str label_ai_enhanced}" Foreground="#666666"
                                                           FontSize="10" FontWeight="SemiBold"/>
                                            </Border>
                                            <Border Background="#CC1A1A2E" CornerRadius="4">
                                                <TextBlock Text="{loc:Str label_coming_soon}" Foreground="{DynamicResource SecondaryBrush}" FontSize="8"
                                                           FontWeight="Bold" HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"/>
                                            </Border>
                                        </Grid>
                                    </StackPanel>

                                    <!-- Right: Square Image -->
                                    <Border Grid.Column="2" CornerRadius="6" ClipToBounds="True"
                                            BorderBrush="{DynamicResource SecondaryBrush}" BorderThickness="1"
                                            Width="130" Height="130">
                                        <Image x:Name="ImgVideoHapticSync"
                                               Source="pack://application:,,,/Resources/features/vibe.png"
                                               Stretch="UniformToFill"/>
                                    </Border>
                                </Grid>

                                <!-- Row 3: Delay and Power Sliders -->
                                <Border x:Name="VideoHapticSyncSliders" Grid.Row="3" Padding="12,8,12,12"
                                        Visibility="Collapsed">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="12"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Delay Slider -->
                                        <StackPanel Grid.Column="0">
                                            <Grid>
                                                <TextBlock Text="{loc:Str label_delay_2}" Foreground="#AAAAAA" FontSize="10"/>
                                                <TextBlock x:Name="TxtVideoHapticDelay" Text="{loc:Str label_0ms}"
                                                           Foreground="{DynamicResource SecondaryBrush}" FontSize="10"
                                                           HorizontalAlignment="Right"/>
                                            </Grid>
                                            <Slider x:Name="SliderVideoHapticDelay" Minimum="-600" Maximum="600"
                                                    Value="0" Margin="0,4,0,0"
                                                    ValueChanged="SliderVideoHapticDelay_Changed"
                                                    ToolTip="{loc:Str tooltip_adjust_haptic_timing_positive_earlier_negativ}"/>
                                        </StackPanel>

                                        <!-- Power Slider -->
                                        <StackPanel Grid.Column="2">
                                            <Grid>
                                                <TextBlock Text="{loc:Str label_power_2}" Foreground="#AAAAAA" FontSize="10"/>
                                                <TextBlock x:Name="TxtVideoHapticPower" Text="100%"
                                                           Foreground="{DynamicResource SecondaryBrush}" FontSize="10"
                                                           HorizontalAlignment="Right"/>
                                            </Grid>
                                            <Slider x:Name="SliderVideoHapticPower" Minimum="0" Maximum="100"
                                                    Value="100" Margin="0,4,0,0"
                                                    ValueChanged="SliderVideoHapticPower_Changed"
                                                    ToolTip="{loc:Str tooltip_live_intensity_control_reduce_if_vibration_is}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>
                <!-- Lock Overlay for Right Box -->
                <Border x:Name="HapticsFeatureLock" Background="#80000000" CornerRadius="8" Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="🔒" FontSize="32" HorizontalAlignment="Center"/>
                        <TextBlock Text="{loc:Str label_patreon_only_2}" Foreground="{DynamicResource SecondaryBrush}" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
            </Grid>

            <!-- Patreon Only Overlay (hidden for Patreon supporters) -->
            <Border x:Name="HapticsComingSoonOverlay" Background="#CC1A1A2E" CornerRadius="8">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="{loc:Str label_patreon_only_2}" Foreground="{DynamicResource PinkBrush}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <TextBlock Text="{loc:Str label_unlock_haptic_feedback_by_supporting_on_patre}"
                               Foreground="#AAAAAA" FontSize="13" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    <TextBlock Text="{loc:Str label_lovense_buttplug_io_support}"
                               Foreground="{DynamicResource SecondaryBrush}" FontSize="12" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
                </Grid>

                        <!-- Translucent gating overlay (only for free users) -->
                        <Border x:Name="HapticsGate" Visibility="Collapsed"
                                Background="#99000010" BorderBrush="#FF69B4" BorderThickness="2"
                                CornerRadius="10" IsHitTestVisible="True">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="🔒" FontSize="48" HorizontalAlignment="Center"/>
                                <TextBlock Text="{loc:Str gate_premium_locked}" FontSize="22"
                                           FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                <TextBlock Text="{loc:Str gate_premium_subtitle}" FontSize="14"
                                           Foreground="#CCCCCC" HorizontalAlignment="Center" Margin="0,4,0,12"/>
                                <Button Content="{loc:Str gate_unlock_with_patreon}"
                                        Click="BtnGateUnlock_Click" Padding="20,8" Background="#FF69B4"
                                        Foreground="White" FontWeight="Bold" Cursor="Hand" BorderThickness="0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                </StackPanel>
            </ScrollViewer>
        </ContentControl>


        <!-- Global Bottom Bar: Start / Save / Exit (Always visible) -->
        <Border Grid.Row="5" Background="{StaticResource PanelBgBrush}" Margin="10,5,10,10" CornerRadius="10" Padding="15,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Status Text (overlaid on top of buttons, shown when running) -->
                <TextBlock x:Name="TxtPresetsStatus" Grid.ColumnSpan="2"
                           Text="" Foreground="{DynamicResource TextMutedBrush}" FontSize="11"
                           HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,-20,0,0"/>

                <!-- START Button (with session timer display) -->
                <Grid Grid.Column="0" Margin="0,0,8,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="BtnStart" Grid.Column="0" Style="{StaticResource PinkButton}"
                            Height="50" FontSize="18" Click="BtnStart_Click"
                            ToolTip="{loc:Str tooltip_start_stop_the_conditioning_engine}">
                        <StackPanel x:Name="StartButtonContent" Orientation="Horizontal" Height="24" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtStartIcon" Text="▶" FontSize="16" Width="20" Margin="0,0,10,0" VerticalAlignment="Center" TextAlignment="Center"/>
                            <TextBlock x:Name="TxtStartLabel" Text="{loc:Str label_start}" FontSize="18" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <!-- Pause Button (visible only during sessions) -->
                    <Button x:Name="BtnPauseSession" Grid.Column="1" Width="50" Height="50" Margin="5,0,0,0"
                            Visibility="Collapsed" Click="BtnPauseSession_Click"
                            ToolTip="{loc:Str tooltip_pause_session_100_xp_penalty_per_pause}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#FFA500"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FontSize" Value="20"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                        <TextBlock x:Name="TxtPauseIcon" Text="⏸" FontSize="20"/>
                    </Button>
                </Grid>
                
                <!-- Save / Exit (other half) -->
                <Grid Grid.Column="1" Margin="8,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="{loc:Str btn_save_2}" Style="{StaticResource PinkButton}" 
                            Height="50" FontSize="16" Margin="0,0,5,0" Click="BtnSave_Click"
                            ToolTip="{loc:Str tooltip_save_all_settings_to_disk}"/>
                    <Button Grid.Column="1" Content="{loc:Str btn_exit}" Height="50" FontSize="16" Margin="5,0,0,0" Click="BtnExit_Click"
                            ToolTip="{loc:Str tooltip_exit_the_application_completely}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="#FF6B6B"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="20,10">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <!-- Remote Control Overlay (shown when controller is connected) -->
        <Border x:Name="RemoteControlOverlay" Grid.Row="1" Grid.RowSpan="5" Panel.ZIndex="999" Visibility="Collapsed" Background="#E0000000" Opacity="0">
            <Grid>
                <!-- Command notification banner at top -->
                <Border x:Name="RemoteCommandNotification" VerticalAlignment="Top" HorizontalAlignment="Center"
                        Background="#2200FF88" CornerRadius="0,0,10,10" Padding="24,10" Margin="0,0,0,0" Opacity="0">
                    <TextBlock x:Name="TxtRemoteCommand" Text="" Foreground="#00FF88" FontSize="16" FontWeight="SemiBold"
                               HorizontalAlignment="Center"/>
                </Border>

                <!-- Center content -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="🎮" FontSize="56" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <TextBlock Text="{loc:Str label_remote_controlled}" Foreground="#00FF88" FontSize="28" FontWeight="Bold"
                               HorizontalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBlock x:Name="TxtOverlaySessionCode" Text="" Foreground="{DynamicResource TextMutedBrush}" FontSize="18"
                               FontFamily="Consolas" HorizontalAlignment="Center" Margin="0,0,0,16"/>

                    <TextBlock x:Name="TxtRemoteOverlaySubtitle" Text="{loc:Str label_someone_else_is_controlling_your_app}" Foreground="#A0A0A0" FontSize="14"
                               HorizontalAlignment="Center" Margin="0,0,0,24"/>

                    <!-- Session info card — always visible -->
                    <Border HorizontalAlignment="Center" Margin="0,0,0,24"
                            Background="#18FFFFFF" CornerRadius="10" Padding="32,16" MinWidth="320">
                        <Grid>
                            <!-- Idle state: no session running -->
                            <StackPanel x:Name="RemoteSessionIdle" HorizontalAlignment="Center">
                                <TextBlock Text="📋" FontSize="28" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                                <TextBlock Text="{loc:Str label_no_active_session}" Foreground="#606080" FontSize="14"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                            <!-- Active state: session name + timer -->
                            <StackPanel x:Name="RemoteSessionActive" Visibility="Collapsed" HorizontalAlignment="Center">
                                <TextBlock x:Name="TxtRemoteSessionName" Text="" Foreground="{DynamicResource PinkBrush}" FontSize="20" FontWeight="Bold"
                                           HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                <TextBlock x:Name="TxtRemoteSessionTime" Text="" Foreground="#FFFFFF" FontSize="28"
                                           FontFamily="Consolas" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,6"/>
                                <TextBlock x:Name="TxtRemoteSessionPhase" Text="" Foreground="#A0A0B0" FontSize="13"
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- End Session button -->
                    <Button Content="{loc:Str btn_end_session}" Click="BtnEndRemoteSession_Click" Cursor="Hand"
                            Foreground="White" FontSize="15" FontWeight="SemiBold" Padding="32,12"
                            HorizontalAlignment="Center">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="BtnBorder" Background="#CC3333" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="BtnBorder" Property="Background" Value="#FF4444"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="BtnBorder" Property="Background" Value="#AA2222"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Global Drop Overlay (shown when dragging files anywhere) -->
        <Border x:Name="GlobalDropOverlay" Grid.RowSpan="5" Visibility="Collapsed" Background="#D0000000" IsHitTestVisible="False">
            <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="16" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="3"
                    MaxWidth="450" MaxHeight="220" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Padding="40,30">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock x:Name="DropOverlayIcon" Text="📂" FontSize="48" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="DropOverlayTitle" Text="{loc:Str label_drop_to_import}" Foreground="{DynamicResource PinkBrush}" FontSize="20" FontWeight="Bold"
                               HorizontalAlignment="Center" Margin="0,15,0,5"/>
                    <TextBlock x:Name="DropOverlaySubtitle" Text="{loc:Str label_images_videos_zips_folders}" Foreground="#909090" FontSize="14"
                               HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center"/>
                </StackPanel>
            </Border>
        </Border>

        <!-- Main App Tutorial Overlay -->
        <Border x:Name="MainTutorialOverlay" Grid.RowSpan="5" Panel.ZIndex="1000" Visibility="Collapsed" Background="#E8000000">
            <Grid>
                <Border Background="Transparent" MouseLeftButtonDown="MainTutorial_Close"/>
                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="12" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="2"
                        MaxWidth="700" MaxHeight="600" VerticalAlignment="Center" HorizontalAlignment="Center"
                        Margin="40" MouseLeftButtonDown="MainTutorial_ContentClick">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Border Grid.Row="0" Background="{DynamicResource SurfaceBgBrush}" CornerRadius="10,10,0,0" Padding="20,15">
                            <Grid>
                                <TextBlock Text="{loc:Str label_how_to_use_the_app}" Foreground="{DynamicResource PinkBrush}" FontSize="18" FontWeight="Bold"
                                           HorizontalAlignment="Center"/>
                                <Button Content="✕" Click="MainTutorial_Close" HorizontalAlignment="Right" VerticalAlignment="Top"
                                        Background="Transparent" Foreground="#888" FontSize="14" BorderThickness="0" Cursor="Hand"
                                        Margin="0,-5,-5,0"/>
                            </Grid>
                        </Border>

                        <!-- Tutorial Content -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20,15">
                            <StackPanel>
                                <!-- Quick Tip -->
                                <Border Background="#3A2542" CornerRadius="8" Padding="12" Margin="0,0,0,15" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="💡" FontSize="16" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                        <TextBlock Text="{loc:Str label_hover_over_any_control_for_tooltips_click_a_g}"
                                                   Foreground="#D0B0C0" FontSize="12" TextWrapping="Wrap" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Border>

                                <!-- Getting Started -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="🚀" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{loc:Str label_getting_started}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="SemiBold"/>
                                            <TextBlock Text="{loc:Str label_quick_overview_of_the_basics_start_button_add}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="{loc:Str btn_start_guide}" Click="BtnTutorialGettingStarted_Click"
                                                Style="{StaticResource SmallPinkButton}" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- Settings Tab -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="⚙️" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{loc:Str label_settings_tab}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="SemiBold"/>
                                            <TextBlock Text="{loc:Str label_flash_images_videos_subliminals_overlays_audi}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="{loc:Str btn_start_guide}" Click="BtnTutorialSettings_Click"
                                                Style="{StaticResource SmallPinkButton}" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- Presets Tab -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="💾" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{loc:Str label_presets_sessions}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="SemiBold"/>
                                            <TextBlock Text="{loc:Str label_save_configurations_run_timed_sessions_create}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="{loc:Str btn_start_guide}" Click="BtnTutorialPresets_Click"
                                                Style="{StaticResource SmallPinkButton}" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- Progression Tab -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="📊" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{loc:Str tab_progression}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="SemiBold"/>
                                            <TextBlock Text="{loc:Str label_xp_system_level_unlocks_scheduler_and_intensi}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="{loc:Str btn_start_guide}" Click="BtnTutorialProgression_Click"
                                                Style="{StaticResource SmallPinkButton}" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- Achievements Tab -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="🏆" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{loc:Str tab_achievements}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="SemiBold"/>
                                            <TextBlock Text="{loc:Str label_unlock_achievements_by_reaching_milestones_an}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="{loc:Str btn_start_guide}" Click="BtnTutorialAchievements_Click"
                                                Style="{StaticResource SmallPinkButton}" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- Companion Tab -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="💬" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{loc:Str label_companion_settings_2}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="SemiBold"/>
                                            <TextBlock Text="{loc:Str label_speech_bubbles_trigger_messages_and_ai_person}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="{loc:Str btn_start_guide}" Click="BtnTutorialCompanion_Click"
                                                Style="{StaticResource SmallPinkButton}" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- Patreon Tab -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="💎" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{loc:Str label_patreon_exclusives}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="SemiBold"/>
                                            <TextBlock Text="{loc:Str label_ai_chat_window_awareness_slut_mode_and_premiu}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="{loc:Str btn_start_guide}" Click="BtnTutorialPatreon_Click"
                                                Style="{StaticResource SmallPinkButton}" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- Avatar -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="💗" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{loc:Str label_avatar_companion}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="SemiBold"/>
                                            <TextBlock Text="{loc:Str label_interacting_detaching_resizing_evolution_stag}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="{loc:Str btn_start_guide}" Click="BtnTutorialAvatar_Click"
                                                Style="{StaticResource SmallPinkButton}" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- Modding Guide -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{loc:Str label_text_4}" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{loc:Str label_modding_guide}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="SemiBold"/>
                                            <TextBlock Text="{loc:Str label_opens_the_mod_creator_with_a_step_by_step_wal}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="{loc:Str btn_start_guide}" Click="BtnTutorialModding_Click"
                                                Style="{StaticResource SmallPinkButton}" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- Report a Bug -->
                                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,8" Cursor="Hand">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="🐞" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{loc:Str btn_report_bug}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="SemiBold"/>
                                            <TextBlock Text="{loc:Str bug_report_privacy_notice}" Foreground="{DynamicResource TextMutedBrush}" FontSize="11" TextWrapping="Wrap"/>
                                        </StackPanel>
                                        <Button Grid.Column="2" Content="{loc:Str btn_report_bug}" Click="BtnTutorialReportBug_Click"
                                                Style="{StaticResource SmallPinkButton}" VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>

                                <!-- Pro Tips -->
                                <Border Background="#3A2542" CornerRadius="8" Padding="12" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1" Margin="0,8,0,0">
                                    <StackPanel>
                                        <TextBlock Text="{loc:Str label_pro_tips}" Foreground="{DynamicResource PinkBrush}" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,8"/>
                                        <TextBlock Foreground="#D0B0C0" FontSize="11" TextWrapping="Wrap">
                                            <Run Text="•"/><Run Text="{loc:Str label_assets_images}" FontWeight="SemiBold"/><Run Text="{loc:Str label_add_your_flash_images_here}"/>
                                        </TextBlock>
                                        <TextBlock Foreground="#D0B0C0" FontSize="11" TextWrapping="Wrap" Margin="0,2,0,0">
                                            <Run Text="•"/><Run Text="{loc:Str label_assets_videos}" FontWeight="SemiBold"/><Run Text="{loc:Str label_add_your_videos_here}"/>
                                        </TextBlock>
                                        <TextBlock Foreground="#D0B0C0" FontSize="11" TextWrapping="Wrap" Margin="0,2,0,0">
                                            <Run Text="{loc:Str label_use_borderless_windowed_mode_in_games_for_bes}"/>
                                        </TextBlock>
                                        <TextBlock Foreground="#D0B0C0" FontSize="11" TextWrapping="Wrap" Margin="0,2,0,0">
                                            <Run Text="{loc:Str label_right_click_on_features_for_additional_option}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Footer -->
                        <Border Grid.Row="2" Background="{DynamicResource SurfaceBgBrush}" CornerRadius="0,0,10,10" Padding="20,15">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="{loc:Str btn_start_interactive_tutorial}" Click="BtnStartTutorial_Click" Margin="0,0,15,0"
                                        Background="Transparent" Foreground="{DynamicResource PinkBrush}" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="1"
                                        Padding="20,10" FontWeight="SemiBold" Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="{DynamicResource TransparentPink20Brush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <Button Content="{loc:Str btn_got_it}" Style="{StaticResource PinkButton}"
                                        Click="MainTutorial_Close" Padding="40,10"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Border>
        </Grid>
    </Viewbox>

    <!-- Browser fullscreen overlay - sits outside Viewbox so WebView gets native resolution.
         WebView stays within same Window HWND, avoiding DirectX swapchain recreation that causes HDR blinks. -->
    <Grid x:Name="BrowserFullscreenOverlay" Background="Black" Visibility="Collapsed" Panel.ZIndex="9999"/>
    </Grid>
</Window>
