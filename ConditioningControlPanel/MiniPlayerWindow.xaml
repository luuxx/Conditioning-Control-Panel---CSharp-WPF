<Window x:Class="ConditioningControlPanel.MiniPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Preview" Height="350" Width="450"
        MinHeight="200" MinWidth="300"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource DarkerBgBrush}"
        WindowStyle="None" AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        KeyDown="Window_KeyDown">

    <Border CornerRadius="8" Background="{StaticResource DarkerBgBrush}" BorderBrush="{StaticResource PinkBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Background="{StaticResource PanelBgBrush}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="TxtFileName" Text="Preview"
                           Foreground="{StaticResource PinkBrush}" FontSize="12" FontWeight="SemiBold"
                           VerticalAlignment="Center" Margin="12,0,0,0"
                           TextTrimming="CharacterEllipsis"/>
                <Button Grid.Column="1" Content="✕" Width="32" Height="32"
                        BorderThickness="0" FontSize="14" Cursor="Hand"
                        Click="BtnClose_Click"
                        ToolTip="Close (Escape)">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource DarkRedBrush}"/>
                                                <Setter Property="Foreground" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Content Area -->
            <Grid Grid.Row="1" Background="{StaticResource BlackOverlayBrush}">
                <!-- Video Player (LibVLC) -->
                <Border x:Name="VideoContainer" Visibility="Collapsed">
                    <!-- VideoView added programmatically -->
                </Border>

                <!-- Image/GIF Display -->
                <Image x:Name="ImagePreview" Stretch="Uniform" Visibility="Collapsed"
                       RenderOptions.BitmapScalingMode="HighQuality"/>

                <!-- Loading Indicator -->
                <Grid x:Name="LoadingOverlay" Visibility="Collapsed">
                    <TextBlock Text="Loading..." Foreground="{StaticResource TextMutedBrush}" FontSize="14"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Grid>

            <!-- Video Controls (only shown for videos) -->
            <Grid x:Name="VideoControls" Grid.Row="2" Background="{StaticResource PanelBgBrush}" Height="40" Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Play/Pause Button -->
                <Button x:Name="BtnPlayPause" Grid.Column="0"
                        Content="▶" Width="36" Height="28"
                        Background="{StaticResource SessionButtonBackgroundBrush}" Foreground="{StaticResource PinkBrush}"
                        BorderThickness="0" FontSize="12" Cursor="Hand"
                        Margin="8,0,0,0"
                        Click="BtnPlayPause_Click"
                        ToolTip="Play/Pause (Space)"/>

                <!-- Seek Slider -->
                <Slider x:Name="SeekSlider" Grid.Column="1"
                        Minimum="0" Maximum="100" Value="0"
                        VerticalAlignment="Center" Margin="10,0"
                        PreviewMouseDown="SeekSlider_PreviewMouseDown"
                        PreviewMouseUp="SeekSlider_PreviewMouseUp"
                        ValueChanged="SeekSlider_ValueChanged"
                        Style="{StaticResource {x:Type Slider}}"/>

                <!-- Time Display -->
                <TextBlock x:Name="TxtTime" Grid.Column="2"
                           Text="00:00 / 00:00" Foreground="{StaticResource TextMutedBrush}" FontSize="11"
                           VerticalAlignment="Center" Margin="0,0,12,0"
                           FontFamily="Consolas"/>
            </Grid>
        </Grid>
    </Border>
</Window>
