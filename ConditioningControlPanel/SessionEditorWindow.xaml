<Window x:Class="ConditioningControlPanel.SessionEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:ConditioningControlPanel.Localization"
        xmlns:local="clr-namespace:ConditioningControlPanel"
        Title="{loc:Str label_session_editor}"
        Height="765" Width="765"
        MinHeight="680" MinWidth="680"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        PreviewMouseDown="Window_PreviewMouseDown">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"
                      ResizeBorderThickness="8"
                      GlassFrameThickness="0"
                      CornerRadius="12"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
    </Window.Resources>

    <Grid>
        <Border Background="{DynamicResource DarkerBgBrush}" CornerRadius="12" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>  <!-- Title bar -->
                    <RowDefinition Height="*"/>     <!-- Scrollable content -->
                </Grid.RowDefinitions>

                <!-- Title Bar -->
                <Grid Grid.Row="0" Background="{DynamicResource PanelBgBrush}" Margin="2,2,2,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="{loc:Str label_session_editor}" Foreground="#E0E0E0" FontSize="12"
                               VerticalAlignment="Center" Margin="12,8"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,4,8,4">
                        <Button x:Name="BtnHelp" Content="?" Click="BtnHelp_Click" Width="24" Height="24"
                                Background="{DynamicResource PinkBrush}" Foreground="White" FontSize="12" FontWeight="Bold"
                                BorderThickness="0" Cursor="Hand" ToolTip="{loc:Str tooltip_how_to_use_the_session_editor}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="12" Width="24" Height="24">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button Content="−" Click="BtnMinimize_Click" Width="24" Height="24"
                                Background="Transparent" Foreground="#888" FontSize="14"
                                BorderThickness="0" Cursor="Hand" Margin="4,0,0,0"/>
                        <Button Content="✕" Click="BtnClose_Click" Width="24" Height="24"
                                Background="Transparent" Foreground="#888" FontSize="12"
                                BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>
                </Grid>

                <!-- Scrollable Content Area -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>  <!-- Session name + stats -->
                            <RowDefinition Height="Auto"/>  <!-- Duration slider -->
                            <RowDefinition Height="Auto"/>  <!-- Timeline -->
                            <RowDefinition Height="Auto"/>  <!-- Feature icons -->
                            <RowDefinition Height="Auto"/>  <!-- Description + buttons -->
                        </Grid.RowDefinitions>

                        <!-- Session Name + Stats -->
                        <StackPanel Grid.Row="0" Margin="20,12,20,8">
                            <!-- Session Name (editable) -->
                            <TextBox x:Name="TxtSessionName" Text="{loc:Str label_my_new_session}"
                                     FontSize="22" FontWeight="Bold" Foreground="{DynamicResource PinkBrush}"
                                     Background="Transparent" BorderThickness="0"
                                     HorizontalAlignment="Center" TextAlignment="Center"
                                     CaretBrush="{DynamicResource PinkBrush}" SelectionBrush="{DynamicResource PinkBrush}"
                                     ToolTip="{loc:Str tooltip_click_to_edit_session_name}"/>

                            <!-- Auto-calculated stats -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,6,0,0">
                                <TextBlock x:Name="TxtDifficulty" Text="{loc:Str label_easy}"
                                           Foreground="#90EE90" FontSize="11" FontWeight="SemiBold" Margin="0,0,15,0"/>
                                <TextBlock x:Name="TxtXP" Text="{loc:Str label_300_xp}"
                                           Foreground="#FFD700" FontSize="11" FontWeight="SemiBold" Margin="0,0,15,0"/>
                                <TextBlock x:Name="TxtDuration" Text="{loc:Str label_30_min_2}"
                                           Foreground="#E0E0E0" FontSize="11" Margin="0,0,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Duration Slider -->
                        <Grid Grid.Row="1" Margin="20,6,20,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{loc:Str setting_duration}" Foreground="#B0B0B0" FontSize="11"
                                       VerticalAlignment="Center" Margin="0,0,10,0"/>

                            <Slider x:Name="SliderDuration" Grid.Column="1"
                                    Minimum="5" Maximum="181" Value="30"
                                    TickFrequency="5" IsSnapToTickEnabled="True"
                                    ValueChanged="SliderDuration_ValueChanged"/>

                            <TextBlock x:Name="TxtDurationValue" Grid.Column="2" Text="{loc:Str label_30_min_2}"
                                       Foreground="#E0E0E0" FontSize="11" FontWeight="SemiBold"
                                       VerticalAlignment="Center" Margin="10,0,0,0" Width="45"/>
                        </Grid>

                        <!-- Timeline -->
                        <Border Grid.Row="2" Background="{DynamicResource SurfaceBgBrush}" CornerRadius="6" Margin="20,6,20,6" Padding="10" MinHeight="100" MaxHeight="180">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>  <!-- Minute markers -->
                                    <RowDefinition Height="*"/>     <!-- Timeline track (scrollable) -->
                                </Grid.RowDefinitions>

                                <!-- Minute Markers -->
                                <Canvas x:Name="CanvasMarkers" Grid.Row="0" Height="16" Margin="0,0,0,4"/>

                                <!-- Timeline Track (drop zone) -->
                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,15,0">
                                    <Border Background="{DynamicResource PanelBgBrush}" CornerRadius="4"
                                        AllowDrop="True"
                                        DragEnter="Timeline_DragEnter"
                                        DragOver="Timeline_DragOver"
                                        DragLeave="Timeline_DragLeave"
                                        Drop="Timeline_Drop">
                                        <Canvas x:Name="CanvasTimeline" ClipToBounds="True" MinHeight="40" Background="Transparent">
                                            <!-- Timeline events will be added here dynamically -->
                                            <TextBlock Text="{loc:Str label_drag_features_here_to_add_them_to_the_timelin}"
                                                   Foreground="#555" FontSize="10" FontStyle="Italic"
                                                   Canvas.Left="8" Canvas.Top="12"
                                                   x:Name="TxtTimelineHint"/>
                                        </Canvas>
                                    </Border>
                                </ScrollViewer>
                            </Grid>
                        </Border>

                        <!-- Feature Icons Grid - Horizontal Layout -->
                        <Border Grid.Row="3" Margin="20,6,20,6">
                            <Grid x:Name="FeatureIconsPanel">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- AUDIO Category -->
                                <StackPanel Grid.Column="0" Margin="3,0">
                                    <TextBlock Text="{loc:Str label_audio}" Foreground="{DynamicResource PinkBrush}" FontSize="9" FontWeight="SemiBold"
                                               HorizontalAlignment="Center" Margin="0,3,0,5"/>
                                    <StackPanel x:Name="AudioFeatures" HorizontalAlignment="Center"/>
                                </StackPanel>

                                <!-- VIDEO Category -->
                                <StackPanel Grid.Column="1" Margin="3,0">
                                    <TextBlock Text="{loc:Str label_video}" Foreground="{DynamicResource PinkBrush}" FontSize="9" FontWeight="SemiBold"
                                               HorizontalAlignment="Center" Margin="0,3,0,5"/>
                                    <StackPanel x:Name="VideoFeatures" HorizontalAlignment="Center"/>
                                </StackPanel>

                                <!-- OVERLAYS Category -->
                                <StackPanel Grid.Column="2" Margin="3,0">
                                    <TextBlock Text="{loc:Str label_overlays_2}" Foreground="{DynamicResource PinkBrush}" FontSize="9" FontWeight="SemiBold"
                                               HorizontalAlignment="Center" Margin="0,3,0,5"/>
                                    <StackPanel x:Name="OverlayFeatures" HorizontalAlignment="Center"/>
                                </StackPanel>

                                <!-- INTERACTIVE Category -->
                                <StackPanel Grid.Column="3" Margin="3,0">
                                    <TextBlock Text="{loc:Str label_interactive}" Foreground="{DynamicResource PinkBrush}" FontSize="9" FontWeight="SemiBold"
                                               HorizontalAlignment="Center" Margin="0,3,0,5"/>
                                    <StackPanel x:Name="InteractiveFeatures" HorizontalAlignment="Center"/>
                                </StackPanel>

                                <!-- EXTRAS Category -->
                                <StackPanel Grid.Column="4" Margin="3,0">
                                    <TextBlock Text="{loc:Str label_extras_2}" Foreground="{DynamicResource PinkBrush}" FontSize="9" FontWeight="SemiBold"
                                               HorizontalAlignment="Center" Margin="0,3,0,5"/>
                                    <StackPanel x:Name="ExtrasFeatures" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Bottom Section: Description + Buttons -->
                        <Grid Grid.Row="4" Margin="20,6,20,15">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Buttons Row -->
                            <Grid Grid.Row="0" Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Button Content="{loc:Str btn_import}" Style="{StaticResource SecondaryButton}"
                                        Click="BtnImport_Click" Margin="0,0,8,0" FontSize="11" Padding="12,6"/>
                                <Button Grid.Column="1" Content="{loc:Str btn_export}" Style="{StaticResource SecondaryButton}"
                                        Click="BtnExport_Click" FontSize="11" Padding="12,6"/>

                                <Button Grid.Column="3" Content="{loc:Str btn_cancel}" Style="{StaticResource SecondaryButton}"
                                        Click="BtnCancel_Click" Margin="0,0,8,0" FontSize="11" Padding="12,6"/>
                                <Button Grid.Column="4" Content="{loc:Str btn_save_session}" Style="{StaticResource PinkButton}"
                                        Click="BtnSave_Click" FontSize="11" Padding="12,6"/>
                            </Grid>

                            <!-- Vibe Description -->
                            <Grid Grid.Row="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Text="{loc:Str label_vibe_description}" Foreground="#888" FontSize="10" Margin="0,0,0,4"/>
                                <TextBox x:Name="TxtDescription" Grid.Row="1"
                                         Height="55" TextWrapping="Wrap" AcceptsReturn="True"
                                         Background="{DynamicResource PanelBgBrush}" Foreground="#E0E0E0"
                                         BorderBrush="#353555" BorderThickness="1"
                                         Padding="8" FontSize="11" VerticalScrollBarVisibility="Auto"
                                         ToolTip="{loc:Str tooltip_enter_a_description_for_your_session}"/>
                            </Grid>
                        </Grid>
                    </Grid>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Resize Grip -->
        <ResizeGrip HorizontalAlignment="Right" VerticalAlignment="Bottom"
                    Width="16" Height="16" Margin="0,0,4,4" Cursor="SizeNWSE"/>

        <!-- Popup overlay for settings -->
        <Popup x:Name="SettingsPopup" Placement="Mouse" StaysOpen="True"
               AllowsTransparency="True" PopupAnimation="Fade">
            <local:FeatureSettingsPopup x:Name="FeatureSettings"/>
        </Popup>

        <!-- Tutorial Overlay -->
        <Border x:Name="TutorialOverlay" Visibility="Collapsed" Background="#E0000000" CornerRadius="12">
            <Grid>
                <!-- Close on background click -->
                <Border Background="Transparent" MouseLeftButtonDown="TutorialOverlay_Close"/>

                <!-- Tutorial Content -->
                <Border Background="{DynamicResource SurfaceBgBrush}" CornerRadius="12" BorderBrush="{DynamicResource PinkBrush}" BorderThickness="2"
                        MaxWidth="600" MaxHeight="550" VerticalAlignment="Center" HorizontalAlignment="Center"
                        Margin="50" MouseLeftButtonDown="TutorialContent_MouseDown">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Header -->
                        <Border Grid.Row="0" Background="{DynamicResource PanelBgBrush}" CornerRadius="10,10,0,0" Padding="20,15">
                            <Grid>
                                <TextBlock Text="{loc:Str label_how_to_use_the_session_editor}"
                                           Foreground="{DynamicResource PinkBrush}" FontSize="18" FontWeight="Bold"
                                           HorizontalAlignment="Center"/>
                                <Button Content="✕" Click="TutorialOverlay_Close"
                                        HorizontalAlignment="Right" VerticalAlignment="Top"
                                        Background="Transparent" Foreground="#888" FontSize="14"
                                        BorderThickness="0" Cursor="Hand" Margin="0,-5,-5,0"/>
                            </Grid>
                        </Border>

                        <!-- Tutorial Steps -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="25,20">
                            <StackPanel>
                                <!-- Step 1 -->
                                <Border Background="{DynamicResource PanelBgBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,12">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <Border Background="#4CAF50" CornerRadius="12" Width="24" Height="24" Margin="0,0,10,0">
                                                <TextBlock Text="1" Foreground="White" FontWeight="Bold"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="{loc:Str label_hover_to_see_description}" Foreground="#E0E0E0" FontSize="14" FontWeight="SemiBold"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{loc:Str label_hover_over_any_feature_icon_to_see_its_name_a}"
                                                   Foreground="#B0B0B0" FontSize="13" TextWrapping="Wrap" Margin="34,0,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- Step 2 -->
                                <Border Background="{DynamicResource PanelBgBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,12">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <Border Background="#2196F3" CornerRadius="12" Width="24" Height="24" Margin="0,0,10,0">
                                                <TextBlock Text="2" Foreground="White" FontWeight="Bold"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="{loc:Str label_drag_to_create_segments}" Foreground="#E0E0E0" FontSize="14" FontWeight="SemiBold"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{loc:Str label_drag_a_feature_icon_onto_the_timeline_to_crea}"
                                                   Foreground="#B0B0B0" FontSize="13" TextWrapping="Wrap" Margin="34,0,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- Step 3 -->
                                <Border Background="{DynamicResource PanelBgBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,12">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <Border Background="#FF9800" CornerRadius="12" Width="24" Height="24" Margin="0,0,10,0">
                                                <TextBlock Text="3" Foreground="White" FontWeight="Bold"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="{loc:Str label_resize_move_segments}" Foreground="#E0E0E0" FontSize="14" FontWeight="SemiBold"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{loc:Str label_drag_the_left_or_right_edge_handles_to_resize}"
                                                   Foreground="#B0B0B0" FontSize="13" TextWrapping="Wrap" Margin="34,0,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- Step 4 -->
                                <Border Background="{DynamicResource PanelBgBrush}" CornerRadius="8" Padding="15" Margin="0,0,0,12">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <Border Background="#9C27B0" CornerRadius="12" Width="24" Height="24" Margin="0,0,10,0">
                                                <TextBlock Text="4" Foreground="White" FontWeight="Bold"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="{loc:Str label_right_click_to_edit_settings}" Foreground="#E0E0E0" FontSize="14" FontWeight="SemiBold"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{loc:Str label_right_click_any_segment_on_the_timeline_to_op}"
                                                   Foreground="#B0B0B0" FontSize="13" TextWrapping="Wrap" Margin="34,0,0,0"/>
                                    </StackPanel>
                                </Border>

                                <!-- Tip -->
                                <Border Background="#203A20" CornerRadius="8" Padding="15" BorderBrush="#4CAF50" BorderThickness="1">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <Border Background="#4CAF50" CornerRadius="12" Width="24" Height="24" Margin="0,0,10,0">
                                                <TextBlock Text="*" Foreground="White" FontWeight="Bold" FontSize="14"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <TextBlock Text="{loc:Str label_full_session_coverage}" Foreground="#4CAF50" FontSize="14" FontWeight="SemiBold"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{loc:Str label_to_make_an_effect_last_the_entire_session_sim}"
                                                   Foreground="#A5D6A7" FontSize="13" TextWrapping="Wrap" Margin="34,0,0,0"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Footer -->
                        <Border Grid.Row="2" Background="{DynamicResource PanelBgBrush}" CornerRadius="0,0,10,10" Padding="20,15">
                            <Button Content="{loc:Str btn_got_it}" Style="{StaticResource PinkButton}"
                                    HorizontalAlignment="Center" Click="TutorialOverlay_Close"
                                    Padding="40,10"/>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
