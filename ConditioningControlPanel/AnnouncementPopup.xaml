<Window x:Class="ConditioningControlPanel.AnnouncementPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Announcement"
        Width="440" SizeToContent="Height" MaxHeight="600"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="{StaticResource AnnouncementBackgroundBrush}" CornerRadius="12" BorderBrush="{StaticResource PinkBrush}" BorderThickness="2"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="{StaticResource PinkColor}" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <!-- Close Button -->
            <Button Content="✕"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Width="28" Height="28" Margin="0,8,12,0"
                    Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}"
                    BorderThickness="0" Cursor="Hand"
                    Panel.ZIndex="1"
                    Click="BtnClose_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <StackPanel Margin="20">
                <!-- Optional Image -->
                <Border x:Name="ImageContainer" Visibility="Collapsed"
                        CornerRadius="8" ClipToBounds="True" Margin="0,0,0,14"
                        MaxHeight="200" HorizontalAlignment="Center">
                    <Image x:Name="AnnouncementImage" Stretch="Uniform"/>
                </Border>

                <!-- Title -->
                <TextBlock x:Name="TxtTitle"
                           Foreground="{StaticResource PinkBrush}" FontWeight="Bold" FontSize="20"
                           TextWrapping="Wrap" TextAlignment="Center"
                           Margin="0,0,0,10"/>

                <!-- Message -->
                <TextBlock x:Name="TxtMessage"
                           Foreground="White" FontSize="14"
                           TextWrapping="Wrap" TextAlignment="Center"
                           LineHeight="22"
                           Margin="0,0,0,16"/>

                <!-- Got it Button -->
                <Button Content="Got it!" Click="BtnDismiss_Click"
                        HorizontalAlignment="Center"
                        Width="120" Height="36"
                        Cursor="Hand"
                        FontSize="14" FontWeight="SemiBold">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="{StaticResource PinkBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                                CornerRadius="18" Padding="16,6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource AnnouncementMutedPinkBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="{StaticResource AnnouncementDeepPinkBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
