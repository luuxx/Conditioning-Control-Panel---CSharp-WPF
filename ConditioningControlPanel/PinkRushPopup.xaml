<Window x:Class="ConditioningControlPanel.PinkRushPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pink Rush!"
        Width="350" Height="150"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="{StaticResource AchievementBackgroundTransparentBrush}" CornerRadius="12" BorderBrush="{StaticResource DarkPinkBrush}" BorderThickness="2"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="{StaticResource DarkPinkColor}" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Skill Image -->
            <Border Grid.Column="0" Width="80" Height="80" Background="{StaticResource DarkerBgBrush}" CornerRadius="8" Margin="0,0,15,0">
                <Image Source="pack://application:,,,/Resources/skills/pink_rush.png" Stretch="Uniform" Margin="5"/>
            </Border>

            <!-- Text Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="⚡ PINK RUSH!" Foreground="{StaticResource DarkPinkBrush}"
                           FontWeight="Bold" FontSize="18" Margin="0,0,0,4"/>
                <TextBlock Text="3x XP for 60 seconds!" Foreground="{StaticResource LightPinkBrush}"
                           FontSize="13" Margin="0,0,0,6"/>
                <TextBlock x:Name="TxtCountdown" Text="60s remaining" Foreground="{StaticResource TextSilverBrush}"
                           FontSize="12"/>
            </StackPanel>

            <!-- Close Button -->
            <Button Grid.Column="1" Content="✕"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Width="24" Height="24" Margin="0,-5,-5,0"
                    Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}"
                    BorderThickness="0" Cursor="Hand"
                    Click="BtnClose_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
