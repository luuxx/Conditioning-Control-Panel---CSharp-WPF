<Window x:Class="ConditioningControlPanel.QuestCompletePopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quest Complete!"
        Width="350" Height="130"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="{StaticResource AchievementBackgroundTransparentBrush}" CornerRadius="12" BorderBrush="{StaticResource SuccessBrush}" BorderThickness="2"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="{StaticResource SuccessColor}" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>

        <Grid Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Checkmark Icon -->
            <Border Grid.Column="0" Width="60" Height="60" Background="{StaticResource QuestCompleteBannerBackgroundBrush}" CornerRadius="30" Margin="0,0,15,0">
                <TextBlock Text="✓" Foreground="{StaticResource SuccessBrush}" FontSize="32" FontWeight="Bold"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>

            <!-- Text Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="QUEST COMPLETE!" Foreground="{StaticResource SuccessBrush}"
                           FontWeight="Bold" FontSize="16" Margin="0,0,0,4"/>
                <TextBlock x:Name="TxtQuestName" Text="Quest Name"
                           Foreground="White" FontSize="14" FontWeight="SemiBold"
                           TextWrapping="Wrap" Margin="0,0,0,4"/>
                <TextBlock x:Name="TxtXPAwarded" Text="+0 XP"
                           Foreground="{StaticResource GoldBrush}" FontSize="13" FontWeight="Bold"/>
            </StackPanel>

            <!-- Close Button -->
            <Button Grid.Column="1" Content="✕"
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Width="24" Height="24" Margin="0,-5,-5,0"
                    Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}"
                    BorderThickness="0" Cursor="Hand"
                    Click="BtnClose_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
