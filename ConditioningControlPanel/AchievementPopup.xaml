<Window x:Class="ConditioningControlPanel.AchievementPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Achievement Unlocked!"
        Width="400" Height="200"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStartupLocation="Manual"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border Background="{StaticResource AchievementBackgroundTransparentBrush}" CornerRadius="12" BorderBrush="{StaticResource PinkBrush}" BorderThickness="2"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="{StaticResource PinkColor}" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
        </Border.Effect>
        
        <Grid Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Achievement Image -->
            <Border Grid.Column="0" Width="120" Height="120" Background="{StaticResource DarkerBgBrush}" Margin="0,0,15,0">
                <Image x:Name="AchievementImage" Stretch="Uniform" Margin="5"/>
            </Border>
            
            <!-- Text Content -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <!-- Trophy Icon + "Achievement Unlocked" -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock x:Name="TxtHeaderIcon" Text="🏆" FontSize="20" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock x:Name="TxtHeaderText" Text="ACHIEVEMENT UNLOCKED!" Foreground="{StaticResource GoldBrush}"
                               FontWeight="Bold" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Achievement Name -->
                <TextBlock x:Name="TxtName" Text="Achievement Name" 
                           Foreground="{StaticResource PinkBrush}" 
                           FontWeight="Bold" FontSize="18" 
                           TextWrapping="Wrap" Margin="0,0,0,8"/>
                
                <!-- Flavor Text -->
                <TextBlock x:Name="TxtFlavor" Text="Flavor text goes here..." 
                           Foreground="{StaticResource TextSilverBrush}" FontSize="12" 
                           TextWrapping="Wrap" FontStyle="Italic"/>
            </StackPanel>
            
            <!-- Close Button -->
            <Button Grid.Column="1" Content="✕" 
                    HorizontalAlignment="Right" VerticalAlignment="Top"
                    Width="24" Height="24" Margin="0,-5,-5,0"
                    Background="Transparent" Foreground="{StaticResource TextSecondaryBrush}"
                    BorderThickness="0" Cursor="Hand"
                    Click="BtnClose_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>
