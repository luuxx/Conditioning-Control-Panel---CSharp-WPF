<Window x:Class="ConditioningControlPanel.BubbleCountResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:ConditioningControlPanel.Localization"
        Title="{loc:Str dialog_how_many_bubbles}"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowState="Maximized"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        FontFamily="Comic Sans MS, Segoe UI">
    
    <Grid>
        <!-- Semi-transparent background -->
        <Rectangle Fill="#E0000000"/>
        
        <!-- Content Card -->
        <Border Background="#FF2D2D3A"
                CornerRadius="20"
                Padding="50"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MinWidth="500"
                MaxWidth="600">
            <Border.Effect>
                <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="30" ShadowDepth="0" Opacity="0.5"/>
            </Border.Effect>
            
            <StackPanel>
                <!-- Title -->
                <TextBlock Text="{loc:Str label_how_many_bubbles}"
                           FontSize="40"
                           FontWeight="Bold"
                           Foreground="{DynamicResource PinkBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20">
                    <TextBlock.Effect>
                        <DropShadowEffect Color="{DynamicResource PinkColor}" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
                    </TextBlock.Effect>
                </TextBlock>
                
                <!-- Instruction -->
                <TextBlock Text="{loc:Str label_enter_the_number_of_bubbles_you_counted}"
                           FontSize="18"
                           Foreground="{DynamicResource PinkBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>
                
                <!-- Number Input -->
                <TextBox x:Name="TxtAnswer"
                         FontSize="48"
                         FontWeight="Bold"
                         Foreground="White"
                         Background="#40FFFFFF"
                         BorderBrush="{DynamicResource PinkBrush}"
                         BorderThickness="3"
                         Padding="20,10"
                         HorizontalAlignment="Center"
                         MinWidth="200"
                         TextAlignment="Center"
                         MaxLength="3"/>
                
                <!-- Attempts remaining -->
                <TextBlock x:Name="TxtAttempts"
                           Text="{loc:Str label_attempts_remaining_3}"
                           FontSize="16"
                           Foreground="#888888"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,0"/>
                
                <!-- Feedback -->
                <TextBlock x:Name="TxtFeedback"
                           Text=""
                           FontSize="20"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,15,0,0"
                           Visibility="Collapsed"/>
                
                <!-- Submit Button -->
                <Button x:Name="BtnSubmit"
                        Content="{loc:Str btn_submit}"
                        FontSize="24"
                        FontWeight="Bold"
                        Foreground="White"
                        Background="{DynamicResource PinkBrush}"
                        BorderThickness="0"
                        Padding="40,15"
                        Margin="0,30,0,0"
                        Cursor="Hand"
                        HorizontalAlignment="Center"
                        Click="BtnSubmit_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="10"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
                
                <!-- Strict Mode Indicator -->
                <TextBlock x:Name="TxtStrict"
                           Text="{loc:Str label_strict_mode_must_answer_correctly}"
                           FontSize="14"
                           Foreground="#FF6B6B"
                           HorizontalAlignment="Center"
                           Margin="0,20,0,0"
                           Visibility="Collapsed"/>
                
                <!-- ESC Hint -->
                <TextBlock x:Name="TxtEscHint"
                           Text="{loc:Str label_press_esc_to_skip_won_t_earn_xp}"
                           FontSize="12"
                           Foreground="#666666"
                           HorizontalAlignment="Center"
                           Margin="0,15,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
