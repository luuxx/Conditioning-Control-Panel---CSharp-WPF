<Window x:Class="ConditioningControlPanel.AttentionTargetEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:ConditioningControlPanel.Localization"
        Title="{loc:Str dialog_attention_target_style}"
        Height="520" Width="480"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource DarkerBgBrush}"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="{loc:Str dialog_attention_target_style}"
                   Foreground="{StaticResource PinkBrush}"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Preview Section -->
        <Border Grid.Row="1" Background="{DynamicResource PreviewBgBrush}" CornerRadius="8" Padding="20" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="{loc:Str section_preview}" Foreground="#808080" FontSize="11" Margin="0,0,0,10"/>
                <Border x:Name="PreviewBorder" HorizontalAlignment="Center"
                        CornerRadius="20" Padding="20,10,20,10" Cursor="Hand">
                    <Border.Effect>
                        <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="5" Opacity="0.6"/>
                    </Border.Effect>
                    <TextBlock x:Name="PreviewText" Text="{loc:Str label_good_girl}"
                               FontSize="40" FontWeight="Bold"
                               HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.Effect>
                            <DropShadowEffect x:Name="PreviewTextShadow" BlurRadius="3" ShadowDepth="2" Opacity="0.8"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </Border>
            </StackPanel>
        </Border>

        <!-- Settings -->
        <Border Grid.Row="2" Background="{StaticResource PanelBgBrush}" CornerRadius="8" Padding="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Preset Buttons -->
                    <TextBlock Text="{loc:Str label_quick_presets}" Foreground="#808080" FontSize="11" Margin="0,0,0,8"/>
                    <WrapPanel Margin="0,0,0,15">
                        <Button Content="{loc:Str btn_purple_classic}" Style="{StaticResource SecondaryButton}"
                                Margin="0,0,8,8" Click="PresetPurple_Click"/>
                        <Button Content="{loc:Str btn_pink_bubbly}" Style="{StaticResource SecondaryButton}"
                                Margin="0,0,8,8" Click="PresetPink_Click"/>
                        <Button Content="{loc:Str btn_neon_green}" Style="{StaticResource SecondaryButton}"
                                Margin="0,0,8,8" Click="PresetGreen_Click"/>
                        <Button Content="{loc:Str btn_ocean_blue}" Style="{StaticResource SecondaryButton}"
                                Margin="0,0,8,8" Click="PresetBlue_Click"/>
                    </WrapPanel>

                    <!-- Color 1 -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{loc:Str label_primary_color}" Foreground="White" VerticalAlignment="Center"/>
                        <Button x:Name="BtnColor1" Grid.Column="1" Style="{StaticResource ColorButton}"
                                Click="BtnColor1_Click"/>
                        <TextBlock x:Name="TxtColor1" Grid.Column="2" Foreground="#808080"
                                   VerticalAlignment="Center" Margin="10,0,0,0" FontSize="11"/>
                    </Grid>

                    <!-- Color 2 -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{loc:Str label_secondary_color}" Foreground="White" VerticalAlignment="Center"/>
                        <Button x:Name="BtnColor2" Grid.Column="1" Style="{StaticResource ColorButton}"
                                Click="BtnColor2_Click"/>
                        <TextBlock x:Name="TxtColor2" Grid.Column="2" Foreground="#808080"
                                   VerticalAlignment="Center" Margin="10,0,0,0" FontSize="11"/>
                    </Grid>

                    <!-- Text Color -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{loc:Str label_text_color}" Foreground="White" VerticalAlignment="Center"/>
                        <Button x:Name="BtnTextColor" Grid.Column="1" Style="{StaticResource ColorButton}"
                                Click="BtnTextColor_Click"/>
                        <TextBlock x:Name="TxtTextColor" Grid.Column="2" Foreground="#808080"
                                   VerticalAlignment="Center" Margin="10,0,0,0" FontSize="11"/>
                    </Grid>

                    <!-- Floating Text Toggle -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{loc:Str label_floating_text}" Foreground="White" VerticalAlignment="Center"
                                   ToolTip="{loc:Str tooltip_remove_background_just_show_text_transparent}"/>
                        <CheckBox x:Name="ChkFloatingText" Grid.Column="1" VerticalAlignment="Center"
                                  Checked="ChkFloatingText_Changed" Unchecked="ChkFloatingText_Changed"/>
                    </Grid>

                    <!-- Border Toggle -->
                    <Grid x:Name="BorderToggleRow" Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{loc:Str label_show_border}" Foreground="White" VerticalAlignment="Center"/>
                        <CheckBox x:Name="ChkShowBorder" Grid.Column="1" VerticalAlignment="Center"
                                  Checked="ChkShowBorder_Changed" Unchecked="ChkShowBorder_Changed"/>
                    </Grid>

                    <!-- Border Color (only visible when border enabled) -->
                    <Grid x:Name="BorderColorRow" Margin="0,0,0,12" Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{loc:Str label_border_color}" Foreground="White" VerticalAlignment="Center"/>
                        <Button x:Name="BtnBorderColor" Grid.Column="1" Style="{StaticResource ColorButton}"
                                Click="BtnBorderColor_Click"/>
                        <TextBlock x:Name="TxtBorderColor" Grid.Column="2" Foreground="#808080"
                                   VerticalAlignment="Center" Margin="10,0,0,0" FontSize="11"/>
                    </Grid>

                    <!-- Font -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{loc:Str label_font}" Foreground="White" VerticalAlignment="Center"/>
                        <ComboBox x:Name="CmbFont" Grid.Column="1" Background="{DynamicResource SurfaceBgBrush}"
                                  Foreground="White" Padding="8,6"
                                  SelectionChanged="CmbFont_SelectionChanged">
                            <ComboBoxItem Content="{loc:Str btn_segoe_ui}" Tag="Segoe UI"/>
                            <ComboBoxItem Content="{loc:Str btn_comic_sans_ms}" Tag="Comic Sans MS"/>
                            <ComboBoxItem Content="{loc:Str btn_impact}" Tag="Impact"/>
                            <ComboBoxItem Content="{loc:Str btn_arial_black}" Tag="Arial Black"/>
                            <ComboBoxItem Content="{loc:Str btn_verdana}" Tag="Verdana"/>
                        </ComboBox>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Footer Buttons -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="{loc:Str btn_test_target}" Style="{StaticResource SecondaryButton}"
                    Click="BtnTest_Click" ToolTip="{loc:Str tooltip_spawn_a_test_target_to_see_how_it_looks}"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="{loc:Str btn_cancel}" Style="{StaticResource SecondaryButton}"
                        Margin="0,0,8,0" Click="BtnCancel_Click"/>
                <Button Content="{loc:Str btn_save}" Style="{StaticResource ActionButton}"
                        Click="BtnSave_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
